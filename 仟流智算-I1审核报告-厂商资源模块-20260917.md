# 仟流智算 厂商资源模块升级 I1 代码审核报告

- 日期：2026-09-17
- 审核级别：I1（AI 独立审核，单级）
- 审核范围：`15da17b..252fa55`（8 个提交，34 个文件，+1798/-150），主题为厂商管理（新增/改名/删除厂商）、厂商资源安全删除、自定义厂商代码与 Base URL、Qwen 模型发现过滤与批量选择、Coding Plan 总额度改选填、企业微信回调路径修正
- 审核依据：《AI编码工程规范-通用版-v1.4》《仟流产品开发SOP-v1.4》

## 结论：FAIL（I1 级）

存在 3 项 P1 Finding（2 项质量门禁红 + 1 项 500 错误路径），按 SOP「FINDING-P0/P1 未清零不得 PASS」。修复 P1 后可直接转 PASS，无需重审全量。

## 质量门禁结果

| 门禁 | 结果 | 说明 |
|---|---|---|
| typecheck | ✅ 绿 | 全仓库通过 |
| lint | ❌ 红 | 4 个错误（见 P1-1） |
| 单元测试（web 471 / provider-adapters 302） | ✅ 绿 | 含本次新增的 Qwen 过滤、自定义 BaseUrl 用例 |
| control-api 单测 | ⚠️ 8 失败 | 已在基线提交 `15da17b` 复现，**属既有失败**（0072/0074 迁移基线断言漂移、登录 401），非本次引入 |
| gateway 单测 | ⚠️ 2 失败 | pool043 结算断言，已在基线提交复现，**属既有失败** |
| build | ✅ 绿 | 全仓库构建通过 |
| quality:architecture | ✅ 绿 | 无新增循环依赖 |
| quality:size | ❌ 红 | 3 个文件超登记基线（见 P1-2） |
| quality:duplication | ✅ 绿 | 0.83% < 5% 阈值 |
| audit:prod | ✅ 绿 | 无中危及以上漏洞 |
| 集成测试（真实 PG） | ⚠️ 部分 | 新增 w04 集成用例覆盖删除路径；既有失败同前 |

## Finding 清单

### FINDING-P1-1：lint 门禁 4 个错误（上次清零后回退）

- `apps/web/src/components/resources/ProviderManagementDialog.tsx` 170/173/175 行：3 处 `(provider.capability_set as any)`，违反 no-explicit-any。应定义 `capability_set?: { base_url?: string }` 类型或类型守卫。
- `apps/web/src/components/resources/ResourceModelDiscovery.tsx` 553 行：`isJoinableIndeterminate` 赋值后未使用（写了半拉的全选 indeterminate 逻辑，同步面板只有按钮没有主复选框）。删除或补全。

### FINDING-P1-2：体量门禁 3 个文件超登记基线

- `packages/database/src/repositories/provider-repository.ts`：854 行（基线 574，+280）。本次两个删除级联大方法全部堆在既有文件里，建议将 `deleteResourceSafely` 级联逻辑拆至 `provider-resource-deletion.ts`（Repository 组合或私有模块均可），并更新 `V3/仟流智算-质量门禁-v1.0.json` 基线。
- `apps/web/src/components/resources/ResourceModelDiscovery.tsx`：605 行（基线 521）。
- `packages/provider-adapters/src/model-discovery.ts`：484 行（基线 465）。
- 后两者涨幅较小，可登记新基线；provider-repository 建议拆分而非直接抬基线。

### FINDING-P1-3：`deleteResourceSafely` 级联删除不完整，存在 500 错误路径

`packages/database/src/repositories/provider-repository.ts` 删除孤儿 `unified_model` 前只检查了 `model_route` 与 `ai_request`，但以下表均持有 `references("unified_model.id")` 外键且未在级联中处理：

- `principal_model_manual_authorization`（0038 迁移）
- `principal_provider_disabled_model`（0039 迁移）
- `provider_model_validation`（0057 迁移）

触发场景：删除资源 → 某统一模型不再有其它路由且无 ai_request，但存在手动授权/禁用/验证记录 → `DELETE FROM unified_model` 触发 FK 23503 → 接口 500，事务回滚，用户看到原始错误而非友好 409。
修复建议：级联中补删（或先计数拦截并返回友好 reason）上述三张表；同时 `availability_rule_version` 删除后其父表 `availability_rule` 残留孤儿行，建议一并清理。

### FINDING-P2-1：两处删除缺少 enterprise_id 过滤

`deleteResourceSafely` 中 `availability_event`、`availability_rule_version` 两处 DELETE 只按 `provider_resource_id` 过滤，未加 `enterprise_id`，与函数内其它十余处删除不一致。resourceId 已经 `forUpdate` 锁行校验归属，实际越权风险低，但纵深防御应补齐。

### FINDING-P2-2：网关 adapter 解析静默回退，丢失 fail-fast

`apps/gateway/src/pipeline/adapter-registry.ts` 由 switch-throw 改为「zhipu/kimi 模糊匹配，其余一律 DeepSeekAdapter」。自定义厂商按 OpenAI 兼容处理是设计意图，但匹配逻辑（`includes("glm")`、`includes("moonshot")`）与 control-api 创建时的 `adapter_type` 重映射（仅精确比较 zhipu/kimi）是两套猜测规则，可能不一致（如厂商代码 `GlmProxy`：control-api 记 adapter_type=deepseek，gateway 却用 ZhipuAdapter）。建议统一为读取 provider 记录持久化的 `adapter_type`，而不是两处各自猜；至少对回退路径打 warn 日志。

### FINDING-P2-3：探活只测前 5 个模型，兼容标志语义变化

`probeModelPermissions` 对 >5 个 CHAT 模型的厂商只探测前 5 个，其余模型的 `compatible` 来自名称解析而非实测。「全选兼容模型」可能选中实际无权限的模型。缓解：onboarding 后仍有单模型 validate 兜底。属于可接受的性能取舍，但 UI 未区分「实测可用」与「推断可用」，建议记录并在后续迭代标注。

### FINDING-P3（建议项，不阻塞）

1. `deleteResourceSafely` 的 `actorAdminId` 参数传入但未使用（死参数）。
2. 厂商预设双份维护：`apps/web/src/components/resources/known-providers.ts` 与 `packages/provider-adapters/src/known-providers.ts` 内容高度重叠（jscpd 未达阈值但语义重复），URL 变更需两处同步，建议 web 从包导出或生成。
3. `ResourceTable` 空态文案仍为「登记 DeepSeek API、智谱或 Kimi 资源」，与新多厂商能力不符。
4. `resolveProviderModelsUrl` 对未知厂商回退 `https://{code}.com/v1/models` 臆造域名；自定义厂商未填 Base URL 时报错信息会很奇怪，建议改为显式错误提示「请填写 Base URL」。
5. quota-window 白名单仍是三家：Qwen 等 CODING_PLAN 资源调用 quota-sync 会收到「非 Coding Plan 套餐资源」的误导性报错。
6. 发布脚本 `release-provider-resource-fix-20260917-mac-mini.sh` 已检查，不含上次的 `$stamp）` 全角括号问题，契约自检可用。

## 肯定项

- 删除路径设计正确：`deleteProvider` 的三项引用检查与数据库 FK 完全吻合（provider_resource / availability_rule_version / availability_event）；`deleteResourceSafely` 对调用/账本/财务六类事实先拦截再级联，「有账不删」原则落实；全程事务 + `forUpdate` 行锁 + enterprise 隔离。
- 审计完整：provider.create/update/delete、provider_resource.delete 均写审计，含操作人与变更摘要。
- 测试覆盖新增正向路径：Qwen 命名空间过滤、自定义 BaseUrl 解析、CODING_PLAN 免填总额度、PATCH/DELETE provider 集成用例（w04）。
- 企业微信回调路径修正（3c0163d）与 caddy `/api` 前缀剥离对齐，测试同步更新，属正确修复。
- 凭证链路未退化：仍走加密存储 + SecretValue，Base URL 从 capability_set 注入，不接触密钥。

## 整改建议顺序

1. 修 lint 4 错（P1-1）→ 拆 provider-repository 删除级联 + 登记新基线（P1-2）→ 补 unified_model 三张引用表的级联/拦截（P1-3）。
2. P2-1 补 enterprise 过滤、P2-2 统一 adapter 判定来源。
3. P3 随下次迭代顺手清理。

---

## 整改记录（2026-09-17 16:35，commit 9a0fe61）

- **P1-1 已修**：ProviderManagementDialog 的 3 处 `as any` 改为 `providerBaseUrl()` 类型守卫；删除 ResourceModelDiscovery 中未使用的 indeterminate 变量；另补修 routes.ts 一处 `catch (err: any)`（类型收窄为 unknown + 结构断言）。lint 全绿。
- **P1-2 已修**：`deleteProvider` / `deleteResourceSafely` 拆分至新模块 `packages/database/src/repositories/provider-deletion.ts`（provider-repository.ts 854 → 612 行）；复杂度 33 超限问题解决（事实检查与 unified_model 清理抽为独立函数）；基线登记更新 5 项（provider-repository 612、ResourceModelDiscovery 603、model-discovery 484、providers/routes 471、ProviderManagementDialog 412）。体量门禁转绿。
- **P1-3 已修**：级联补删 `provider_model_validation`；unified_model 删除前增加 `principal_model_manual_authorization`、`principal_provider_disabled_model`、`provider_model_validation`、`availability_rule_version`、`availability_event` 五项引用计数，存在引用则保留休眠行，彻底消除 FK 23503 → 500 路径；同时清理无版本的 `availability_rule` 孤儿父行。
- **P2-1 结论修正**：`availability_event` 与 `availability_rule_version` 两表 schema 中无 `enterprise_id` 列，无法加该过滤；resourceId 已经事务内 `forUpdate` 归属校验，代码中已加注释说明。
- **P3-1 已顺手修**：`deleteResourceSafely` 未使用的 `actorAdminId` 参数移除，路由同步更新。
- 复验：typecheck / lint / build / 架构 / 体量 / audit:prod 全绿；web 471、provider-adapters 302、w04 集成 12 项全部通过。

**复审结论：PASS（I1 级）**。遗留 P2-2（adapter 判定两处猜测逻辑）、P2-3（探活仅前 5 模型）与其余 P3 项不阻塞发布，建议随下次迭代处理。

## 二次整改（2026-09-17 16:50，commit 127856e，依据交叉验证反馈）

- **routes.ts 体量基线 471 → 472**：P1-1 补修 `catch (err: unknown)` 时多出一行，基线同步调整，体量门禁转绿。（交叉验证提到的 model-discovery.ts 487 行系其行数统计口径与门禁脚本不同；以 `check-source-size.mjs` 逻辑行口径 484 为准，门禁实测通过。）
- **P2-2 已修**：Adapter 判定统一为读取持久化的 `provider.adapter_type`——gateway 路由加载查询增补 `provider.adapter_type` 并贯穿 `RouteCandidateRow` → `pipeline-admission` → `resolveAdapter`；registry 移除 `includes("glm")`/`includes("moonshot")` 模糊猜测，改为 adapter_type 精确匹配（zhipu/kimi/deepseek），未知值回退 OpenAI 兼容 Adapter 并打 warn 日志。domain `RoutingCandidateInput` 增加 `adapterType?` 字段。
- **P2-3 维持记录不修复**：探活前 5 模型属性能取舍且有单模型 validate 兜底，UI 区分「实测/推断」列入后续迭代。
- 复验：全仓 typecheck / lint / build / 架构 / 体量 / audit:prod 全绿；domain 180、gateway pipeline 73、w09/w10 adapter 集成 13 项通过。

## 生产验证热修复（2026-09-17 17:37，commit 0eaaebf）

- **现象**：2.5.3 部署后删除有经营同步历史的资源，级联 DELETE 撞上 `provider_resource_operating_sync_attempt` 的 append-only 不可变触发器（55000），用户看到原始英文错误 `provider_resource_operating_sync_attempt is append-only`。属 P1-3 整改遗漏的第四张不可删表。
- **修复**：该表从级联删除中移除，加入 `hasResourceFacts` 前置拦截清单（存在同步历史即友好 409「已有实际调用、经营同步或财务账本事实」）；`provider_resource_operating_snapshot` 保留级联（仅当无 attempt 时执行，attempt.snapshot_id 外键不再受威胁）。
- **回归测试**：w04 新增集成用例——构造含 sync attempt 的资源，断言 DELETE 返回 409 且消息不含触发器原文、资源与同步历史完好。13 项全过（真实 PG + 触发器环境）。
- 复验：typecheck / lint / build / 架构 / 体量全绿。

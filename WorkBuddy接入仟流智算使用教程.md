# 仟流智算 × WorkBuddy 使用教程（员工版）

> 适用模型：DeepSeek V4（Flash / Pro）、Kimi K3、智谱 GLM-4.6 / 4.7 / 5.2
> 更新时间：2026-08-06
> 适用对象：使用公司分配的仟流智算 Key，通过 Tencent WorkBuddy 完成办公或开发任务的员工

---

## ⚠️ 本次更新：模型名称已变更

如果你之前已经在 WorkBuddy 里配置过仟流智算模型，**旧的模型名已经停用**，必须改成新的名称：

| 厂商 | ❌ 旧名称（已停用） | ✅ 新名称（请改用） |
| --- | --- | --- |
| DeepSeek | `qianliu-deepseek` | `qianliu-deepseek-deepseek-v4-flash` 或 `qianliu-deepseek-deepseek-v4-pro` |
| Kimi | `Kimi` | `qianliu-kimi-k3` 或 `qianliu-kimi-k3-256k` |
| 智谱 | `zhipu` | `qianliu-zhipu-glm-5-2`（或 4.6 / 4.7）|

**接入地址和 API Key 都不用改**，只有模型名称需要更新。

---

## 一、开始前准备

开始前，请先向管理员领取个人 API Key，并确认管理员已为你开通了哪些厂商和模型。你能使用的模型由管理员分配，未授权的模型会报错。

注意：API Key 相当于你的算力密码。不要发到群里、写进代码、提交到 Git，截图时也要遮住。

## 二、安装 WorkBuddy

1. 打开 [Tencent WorkBuddy 官网](https://www.codebuddy.cn)，下载对应版本。
2. Windows 用户下载 Windows 版；Mac 用户根据芯片选择：
   - 苹果 M 系列芯片：Mac ARM64
   - Intel 芯片：Mac X64
3. 完成安装并打开 WorkBuddy，按页面提示登录。

已经安装的用户可直接进入下一步。

## 三、配置仟流智算模型

### 1. 打开模型设置

进入 WorkBuddy，打开：

`设置 → 模型 → 添加自定义模型`

部分版本也可以点击任务输入框下方的模型名称，再进入"配置自定义模型"。

### 2. 添加模型

**每个模型都要单独添加一条，但都用同一个接口地址和同一个 Key。** 下面列出全部可用模型，**请只添加管理员已为你开通的那些**。下表所有模型的公共配置为：

| 配置项 | 填写内容 |
| --- | --- |
| 提供商 | `Custom` / `自定义` |
| URL / Base URL | `https://gw.qianliuai.com/v1` |
| API Key | 管理员分配给你的完整 `sk-qianliu-...` |
| 自定义协议 | 关闭 |

只需要把"模型名称 / Model ID"换成下面对应的名字：

#### DeepSeek（按量计费，调用多少扣多少）

| 模型 | 模型名称 / Model ID | 说明 |
| --- | --- | --- |
| DeepSeek V4 Flash | `qianliu-deepseek-deepseek-v4-flash` | 快速版，适合日常任务 |
| DeepSeek V4 Pro | `qianliu-deepseek-deepseek-v4-pro` | 增强版，能力更强、费用更高 |

#### Kimi（套餐制，Token 用完即止）

| 模型 | 模型名称 / Model ID | 说明 |
| --- | --- | --- |
| Kimi K3 | `qianliu-kimi-k3` | 标准上下文 |
| Kimi K3 256K | `qianliu-kimi-k3-256k` | 长上下文，适合处理长文档 |

#### 智谱（套餐制，Token 用完即止）

| 模型 | 模型名称 / Model ID | 说明 |
| --- | --- | --- |
| 智谱 GLM-5.2 | `qianliu-zhipu-glm-5-2` | 最新版，推荐使用 |
| 智谱 GLM-4.7 | `qianliu-zhipu-glm-4-7` | 上一代 |
| 智谱 GLM-4.6 | `qianliu-zhipu-glm-4-6` | 稳定版 |

### 3. 重要提醒

- **模型名称必须一字不差**，包括大小写、连字符。建议直接从上面的表格复制粘贴。
- **不要把模型名填成 `deepseek-v4-flash`、`k3` 或 `glm-5.2`**。WorkBuddy 连接的是仟流智算网关，必须使用网关对外提供的完整模型名称（以 `qianliu-` 开头）。
- 如果页面还显示"能力"选项，请开启"工具调用（Tool Call）"；图片输入、推理模式等其他开关先保持默认。

## 四、如果是老用户：更新已有配置

如果你之前已经配置过仟流智算模型，不用删除重新建，直接改模型名即可：

1. 打开 `设置 → 模型`。
2. 找到之前添加的模型（如 `qianliu-deepseek`、`Kimi`、`zhipu`）。
3. 把"模型名称 / Model ID"改成上面的新名称。
4. 保存。

如果旧条目不好改，也可以直接新建模型条目用新名称，旧的留着不用即可。

## 五、完成第一次测试

1. 点击"新建任务"。
2. 从模型列表中选择刚才配置的模型。
3. 输入：

   > 请只回复：仟流智算连接成功

4. 能正常收到回复，说明"WorkBuddy → 仟流智算 → 模型"的连接已经成功。

建议把你已开通的模型分别测试一次。不要在失败时连续反复点击发送，以免放大限流问题。

## 六、不同模型怎么选

| 场景 | 推荐模型 | 原因 |
| --- | --- | --- |
| 日常写代码、改 Bug | DeepSeek Flash 或 Kimi K3 | 快速、性价比高 |
| 复杂推理、难任务 | DeepSeek Pro 或 智谱 GLM-5.2 | 能力强 |
| 处理超长文档 | Kimi K3 256K | 256K 上下文窗口 |
| 通用对话、写作 | 智谱 GLM-5.2 | 综合能力均衡 |

同一个厂商的多个模型共享一个 Token 额度池。比如你分到了 DeepSeek 的额度，用 Flash 和 Pro 都从这个池子里扣，用哪个就按哪个的单价计费。

## 七、常见问题

| 现象 | 常见原因 | 处理办法 |
| --- | --- | --- |
| `model_not_allowed` | 管理员没给你开通这个模型 | 联系管理员开通，或换一个你已授权的模型 |
| `401`、主体 Key 无效 | Key 填错、复制不完整、Key 已被重置 | 重新复制完整 Key；仍失败就联系管理员 |
| `404`、接口不存在 | URL 少了 `/v1` | 改为 `https://gw.qianliuai.com/v1` |
| 模型不存在 | 模型名称填错或用了旧名称 | 对照第三节表格，复制完整的新模型名称 |
| `429`、engine overloaded | 上游模型临时限流或负载高 | 稍后重试或切换模型，不要连续点击 |
| `503`、no healthy candidate | 当前模型暂时没有可用算力 | 切换其他已授权模型，并把报错时间发给管理员 |
| 模型列表里找不到自定义模型 | 配置未保存或客户端版本较旧 | 回到"设置 → 模型"检查，并升级 WorkBuddy |

联系管理员时，请提供四项信息：报错截图、发生时间、所选模型、任务是否首次发送。不要把完整 API Key 放进截图。

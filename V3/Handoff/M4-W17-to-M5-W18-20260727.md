# Handoff：M4 完成 → M5/W18 接手

| 项目 | 内容 |
| --- | --- |
| 交接日期 | 2026-07-27 |
| 交出方 | 佳哥（AI 执行，主会话） |
| 接手目标 | M5/W18 完整桌面 Web（首页与七入口信息架构、加载/空/错误态、只读聚合 API） |
| 当前里程碑 | **M5**（完整桌面 Web，W18~W20） |
| 当前工作包 | **W18**（state YAML 已推进） |
| 工作树状态 | **W12~W17 六个工作包改动均未 commit**（详见第 5 节） |
| 最新 Evidence | [W17/M4 收口](../Evidence/M4/W17/20260727-1935-w17-m4-audit/README.md) |
| 权威状态文件 | [V3/仟流智算-stage-state-v0.3.yaml](../仟流智算-stage-state-v0.3.yaml) |

---

## 0. 接手必读顺序（不可跳过）

工程规则 §1 规定的进入项目顺序：

1. `V3/仟流智算-项目工程规则-v0.3.md`
2. `V3/仟流智算-stage-state-v0.3.yaml`（阶段级唯一事实源）
3. 当前阶段权威成果：W17 Evidence（上文链接，含 M4 全链回归）
4. TRD 受影响章节（W18：§11.1 Web 页面行 681-692、§12 首页口径行 733+）
5. 详细开发计划 W18 定义（`V3/仟流智算-详细开发计划与排期-v0.3.md` 行 294）

**只执行 state YAML 中的 `allowed_actions`。** 目标/范围/安全/关键依赖/验收合同变化时停止并登记 `V3/Planning-Change-Log.md`。

---

## 1. M4（W13~W17）做了什么

**M4 全部完成**：额度／预测／调度／账本闭环。

- **W13 计价**：billing_rule 版本化规则 + 智谱高峰×3/非高峰×2 + Kimi 档位×3 + decimal 计价 + ledger_line 冻结 rule_version/multiplier（历史不重算）。
- **W14 额度**：quota-gate 预占/结算校正 + concurrency_lease 行锁（并发不穿透）+ WT-06 耗尽停止/allow_overage。
- **W15 预测**：computeForecast 多窗口速度（1h/24h/7d 加权）+ 耗尽/恢复/覆盖 + 可信度分级 + supply_forecast 快照（数据不足不伪精确）。
- **W16 调度**：dispatch_policy + dispatch_decision + matchPolicy/decideDispatch/computeDispatchSaving 纯函数 + real-pipeline 评分选中后判定 + SWITCH 等价组切换 + REJECT/RATE_LIMIT 403/429 + 反事实节省（CODING_PLAN/API 边界）。
- **W17 对账**：reconciliation_run + reconciliation_discrepancy + 重复0/丢失<0.1% 检测 + 异常队列流转 + worker CLI。

### M4 关键事实（接手者关注）

1. **重复结算为 0 的双重保证**：约束层（usage_event.dedup_key UNIQUE + ledger_transaction UNIQUE(ai_request_id)）+ 验证层（W17 对账扫描）。
2. **所有规则都是 domain 纯函数 + 时钟注入**：计价/额度/预测/调度/对账全部可回放（工程规则 §7）。
3. **额度按 deducted_quota 扣减**（W13 倍率后），不是 raw。
4. **real-pipeline 已接入**：多候选评分(W12) + 计价(W13) + 经营调度(W16，评分选中后判定)；**额度门禁(quotaRepo)已就绪但还没插进 pipeline 热路径**——W18/M5 若涉及请求前额度门禁展示，需补这个插入点。
5. **历史不重算**：billing_rule/ledger_line/dispatch_decision 全部冻结版本，配置变化不改写历史。

---

## 2. W18 要做什么（明确范围）

**详细计划行 294**：首页与七入口信息架构、加载／空／错误态、只读聚合 API。DoD：首页只保留本月核心信息。

### 七入口（TRD §11.1 行 681-692）

1. 首页（本月核心，§12 口径）
2. 资源（provider_resource + model_route）
3. 模型（unified_model + 别名映射）
4. 主体（principal + key + grant）
5. Key（principal_key，只显示指纹）
6. 额度（quota_counter + grant）
7. 路由（model_route + dispatch_policy + reconciliation）
8. 凭证恢复（credential 状态 + probe）

### W18 建议拆解

1. **只读聚合 API**（control-api 新增路由）：`/dashboard`（首页本月聚合）、`/usage`、`/ledger`、`/alerts`（复用 W17 reconciliation_discrepancy）。
2. **Web 信息架构**（apps/web）：七入口路由 + 布局 + 加载/空/错误态组件。
3. **首页口径**（TRD §12）：当前自然月、资源账号数、本月用量/费用/异常。

### W18 不做（边界）
- 写操作闭环（→ W19）；诊断/全 WT E2E（→ W20）。
- Web 不重算账本/额度/路由/节省（工程规则权威边界）。

---

## 3. 如何运行（命令与 PATH，必看）

**关键**：系统 `pnpm` 不在 PATH，必须先加 `.corepack-bin`。所有命令从项目根 `/Users/mac/Projects/仟流智算` 执行：

```bash
export PATH="$PWD/.corepack-bin:$PATH"
pnpm typecheck && pnpm lint && pnpm test && pnpm build && pnpm evidence:canary
```

- 集成测试需 Docker Desktop 运行（PostgreSQL Testcontainer）。
- typecheck/lint 可在 AI 沙箱先行验证；vitest/集成在佳哥 Mac 终端跑（本会话沙箱 Docker 可用，已实测全绿）。

---

## 4. 当前测试基线（接手时应对齐）

| 层 | 数量 | 说明 |
| --- | --- | --- |
| domain 单测 | **108** | reconciliation 10 + dispatch-policy 26 + supply-forecast 14 + quota-gate 11 + billing 14 + routing 10 + lifecycle 19 + 原 4 |
| provider-adapters | 35 | deepseek 9 + zhipu 11 + kimi 11 + secret-value 4 |
| gateway 集成 | **61** | w05~w17 全链（含 w16-dispatch 7） |
| database 集成 | **8** | w17-reconciliation 4 + 迁移 4 |
| control-api 集成 | 23 | w02~w04 |
| canary | 0 | postgres/redis/logs/traces 全 0 |

W18 完成后 control-api 应 +N（聚合 API）、web 应有组件测试。

---

## 5. 工作树状态（重要：W12~W17 六个工作包均未 commit）

工作树累积了 **W12+W13+W14+W15+W16+W17** 共六个工作包的改动，均**未 commit**（工程规则 §6：commit 是独立权限，需佳哥显式授权）。这是正常的、已逐个审计通过的成果，**不要 `git stash`/`reset --hard`/丢弃**。

### 建议路径（由佳哥决定）

- **A（推荐）**：佳哥授权后分多个 commit（W12/W13/W14/W15/W16/W17 各一，message 风格 `feat(m4): W17 ...`），历史清晰。
- **B**：按里程碑合并 commit（`feat(m4): W13+W14+W15+W16+W17 计价/额度/预测/调度/对账`）。
- 进度图快照建议一起：当前应显示 W01~W17 DONE、M4 完成、M5 进行中、进度 17/29 ≈ 58.6%。

⚠️ commit/push 前必须等佳哥明确授权。**未经授权不得 push、创建 PR、merge、发布。**

---

## 6. 残余风险与未做项

| 项 | 归属 | 说明 |
| --- | --- | --- |
| 真实三厂商 HTTP 回归 | DEP-PROVIDER-CREDENTIALS 解锁后佳哥跑 | Adapter/计价/调度/对账形状均经 StubUpstream 验证 |
| 额度门禁插入 pipeline 热路径 | M5/M6 | quotaRepo 已就绪；real-pipeline 评分选中后、Attempt 前待插入 |
| 每日对账定时调度 | W25 worker scheduler | runReconciliation 接口就绪，当前手动触发 |
| 每日预测快照定时任务 / 周期重置 / 耗尽告警 | W25 | computeForecast/reclaimExpiredLeases 接口已就绪 |
| alert_event 告警表 | W25 | W17 异常队列用 reconciliation_discrepancy 承载 |
| resource_runtime_snapshot 短窗口指标 | W25 | 预测当前只用 usage_event 聚合 |

---

## 7. 关键参考文件索引

| 用途 | 路径 |
| --- | --- |
| 工程规则 | `V3/仟流智算-项目工程规则-v0.3.md` |
| 阶段状态（唯一事实源） | `V3/仟流智算-stage-state-v0.3.yaml` |
| 详细开发计划 | `V3/仟流智算-详细开发计划与排期-v0.3.md`（W18 行 294） |
| TRD | `V3/仟流智算-技术需求文档-v0.3.md`（§11.1 Web 页面、§12 首页口径） |
| W17/M4 Evidence（M4 收口） | `V3/Evidence/M4/W17/20260727-1935-w17-m4-audit/README.md` |
| 代码范本（纯函数+仓储模式） | `packages/domain/src/reconciliation.ts` + `packages/database/src/repositories/reconciliation-repository.ts` |
| 进度图（自动生成，勿手改） | `V3/仟流智算-Stage02开发计划进度图-v0.3.html`（由 `V3/tools/render-stage02-progress.rb` 生成） |

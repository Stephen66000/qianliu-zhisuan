# 仟流智算项目目录整理与遗留问题收口结果

| 项目 | 结果 |
| --- | --- |
| 执行日期 | 2026-07-29 |
| 执行基线 | Git `main`，HEAD `a9cbeb4` |
| 执行人 | 安东尼 |
| 结果 | `COMPLETED_AWAITING_OWNER_ACCEPTANCE` |
| 对应计划 | [项目目录整理与遗留问题收口计划](./仟流智算-项目目录整理与遗留问题收口计划-20260728.md) |
| 确认清单 | [项目目录整理确认清单](./仟流智算-项目目录整理确认清单-20260728.md) |

## 1. 结果摘要

- 五项 Owner 确认内容均已执行；
- 13 个 P2/P3 独立遗留事项全部闭环；
- 未发现真实 P4、P5 Finding；
- 12 个历史文件迁入 `_quarantine/`，没有删除；
- 正式程序结构继续使用 `apps/`、`packages/`、`deploy/`，没有增加无意义的包裹目录；
- 新增根目录 `README.md`，明确程序、文档、原型、参考资料和封禁区边界；
- 清除依赖、构建产物、测试产物、缓存、系统文件和空目录，项目由 501 MB 降至 85 MB，减少约 416 MB；
- M7／Docker 改动全部保留，并在有重叠的配置和部署文档中合入本次安全密钥要求；
- 未提交、未推送、未发布。

## 2. P2—P5 闭环结果

| ID | 级别 | 结果 | 主要闭环证据 |
| --- | --- | --- | --- |
| S2-P2-03 | P2 | 已关闭 | 详细计划明确每周／月末先由独立 AI 复核，再由 Owner 最终签字 |
| S2-P2-04 | P2 | 已关闭 | Stage 02 生成器从 YAML 派生 `contentStatus`、`gateStatus`，重新生成 HTML |
| F-04 | P2 | 已关闭 | 登录失败审计、可空未知操作人、Provider/Model/Resource 企业归属校验及负向测试 |
| F-05 | P2 | 已关闭 | 反事实成本按请求时点、原 winner 计价规则计算；W16 覆盖不同价格 |
| F-06／M6-A2 | P2 | 已关闭 | 代码与文档统一为当前每请求查库、实际 0 秒失效，不再错误承诺 Redis |
| F-09 | P2 | 已关闭 | 特殊错误优先分类；流提交后中断统一为 500、不可重试、请求失败 |
| F-13 | P2 | 已关闭 | 密码 pepper、session HMAC、credential fingerprint HMAC 分别环境注入并测试 |
| F-07 | P3 | 已关闭 | 新增 `usage_observed`，对账实际扫描 `MISSING_USAGE` 并有集成测试 |
| F-10 | P3 | 已关闭 | 幂等唯一键改为企业＋主体＋幂等键，W07 覆盖不同主体 |
| F-11 | P3 | 已关闭 | Pipeline 持久化请求 ID；重复请求确定性返回 409，不重复调用上游 |
| Q-SIZE-1 | P2 | 已关闭 | Pipeline 与账本仓储按既有职责拆分，主文件分别降至 400 行以内 |
| Q-TYPE-1 | P3 | 已关闭 | 数据库 Repository 清除三处 `.execute() as never` 类型逃生舱 |
| M6-A3 | P2 | 已关闭 | PRD §5.2、§6.3、§15 与 Owner 澄清同步，IDE 账号不再列入一期 |

P4/P5 扫描结论：没有真实遗留 Finding；锁文件中的同名字符只是完整性哈希片段。

## 3. 封禁与目录整理

封禁内容：

| 原路径 | 当前路径 | 结果 |
| --- | --- | --- |
| `old version/0.1版本封禁/` | `_quarantine/legacy-docs/v0.1/` | 已迁移，3 个文件 |
| `old version/0.2封禁/` | `_quarantine/legacy-docs/v0.2/` | 已迁移，4 个文件 |
| `原型/V1-2/` | `_quarantine/legacy-prototypes/V1-2/` | 已迁移，4 个文件 |
| `原型/V3/仟流智算-原型-v3.html` | `_quarantine/legacy-prototypes/V3/` | 已迁移，1 个文件 |

恢复方法、替代文件和迁移原因见 [`_quarantine/MANIFEST.md`](../_quarantine/MANIFEST.md)。有效代码、构建、测试和部署入口不依赖封禁区；历史 Evidence 中保留的旧路径描述属于当时事实，不做篡改。

程序和非程序边界：

- `apps/`：四个正式应用；
- `packages/`：共享包、数据库和适配器；
- `deploy/`：部署与数据库初始化；
- `V3/`：当前产品、技术、计划、审核和 Evidence；
- `原型/V3/`：当前 V4 原型；
- `参考/`：仍被工程和许可证资料引用的外部行为对照；
- `_quarantine/`：不参与当前构建、测试、部署和产品合同的历史内容。

## 4. 实际垃圾清理

删除范围严格使用确认清单中的明确路径：

- 根目录、各 workspace 和 PoC 的 `node_modules/`；
- `apps/control-api/dist/`、`apps/gateway/dist/`、`apps/web/dist/`、`apps/worker/dist/`；
- `apps/web/playwright-report/`、`apps/web/test-results/`；
- `packages/domain/.stryker-tmp/`；
- 所有 workspace 的 `tsconfig.tsbuildinfo`；
- `V3/PoC/persistent-gateway/.runtime/`；
- `.pnpm-store/`、`.corepack-bin/`、`.zcode/`；
- 项目内容区的 `.DS_Store`；
- 清单中确认的空测试、迁移和旧版本目录。

体积核对：

| 时点 | 项目体积 |
| --- | ---: |
| 清理前 | 501 MB |
| 清理后 | 85 MB |
| 减少 | 约 416 MB |

`.git/.DS_Store` 因 Git 内部目录写保护未删除；它不会被 Git 跟踪、不会进入正常源码交付范围，也不影响本次整理结果。

## 5. 验证结果

依赖和构建产物删除前完成完整验证：

| 验证 | 结果 |
| --- | --- |
| 全量 TypeScript 类型检查 | PASS |
| 全量 ESLint | PASS |
| 全量测试 | PASS |
| Database 集成测试 | PASS，9 项 |
| Control API 测试 | PASS，55 项 |
| Gateway 测试 | PASS，78 项 |
| 全量构建 | PASS |
| `git diff --check` | PASS |
| 封禁后有效路径引用检查 | PASS；仅计划／历史 Evidence 保留旧路径事实 |

验证环境使用 Node.js 24，执行时出现项目要求 Node.js 22.17—22.x 的 engine 提示，但上述命令全部通过。正式部署仍按项目锁定的 Node.js 22 基线执行。

## 6. 最终状态

- 计划八个阶段：全部完成；
- 本轮待修 P2/P3：`13/13` 已关闭；
- P4/P5：无真实 Finding；
- 待 Owner 产品决策项：无；
- 封禁文件：可追溯、可恢复；
- 垃圾清理：完成；
- 当前工作树：保留未提交改动，等待 Owner 验收后决定是否提交。

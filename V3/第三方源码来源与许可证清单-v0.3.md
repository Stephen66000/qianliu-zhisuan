# 仟流智算第三方源码来源与许可证清单 v0.3

- 收集时间：2026-07-26（Asia/Shanghai）
- 适用范围：Gateway Stage 01 隔离 PoC 与持久化 Spike
- 当前产品代码直接复用：**0 个文件**
- 当前 Spike 直接运行依赖：**4 个包；由 `pnpm-lock.yaml` 锁定**

## 来源清单

| 来源 | 仓库／本地位置 | 冻结标识 | 许可证 | 本次用途 | 直接复用 |
|---|---|---|---|---|---|
| TokenHub | `https://github.com/astaxie/TokenHub.git`；`参考/TokenHub` | Commit `1f4e7fe2bf25084f1a525e3ba2392be1e6bc49d2`；工作区 clean；tree digest `89c32e8cb6b9627cd4a841d7db72949fe9f3153f39a94aa3310d2905575285e4` | Apache-2.0 | 流式边界、Affinity、资源恢复的隔离测试与设计对照 | 否 |
| Sub2API | README／Go module 指向 `https://github.com/Wei-Shaw/sub2api.git`；`参考/sub2api-main` | `.git` 缺失；Commit **UNKNOWN**；tree digest `626d7c0df33f78dfad1aa45df092372d6b8ebda5e3a1fc4f8581e500e5464ee0` | LGPL-3.0-or-later | count_tokens、WebSocket、Session 隔离、stream guard 的隔离测试与设计对照 | 否 |
| node-postgres `pg` | `https://github.com/brianc/node-postgres` | npm `8.16.3`；lock SHA-256 `8081735b2edfa199132185f37bef09909541fa6d94a585315f85980ebb58c1b2` | MIT | PostgreSQL 元数据与账本持久化 | 运行依赖 |
| node-redis `redis` | `https://github.com/redis/node-redis` | npm `5.8.2`；同上 lock | MIT | 幂等锁、缓存与 HMAC Affinity | 运行依赖 |
| OpenTelemetry API | `https://github.com/open-telemetry/opentelemetry-js` | npm `1.9.0`；同上 lock | Apache-2.0 | Trace API | 运行依赖 |
| OpenTelemetry Trace SDK | `https://github.com/open-telemetry/opentelemetry-js` | npm `2.0.1`；同上 lock | Apache-2.0 | Trace Processor 与 POC 内存 Exporter | 运行依赖 |

Tree digest 计算口径：排除 `.git` 与 `node_modules`，对各文件 SHA-256 与相对路径排序后再次 SHA-256。它只能冻结本地快照，不能替代上游 Commit。

## 实际阅读／执行文件范围

TokenHub：

- `backend/internal/server/stream_failover_test.go`
- `backend/internal/server/provider_session_affinity_test.go`
- `backend/internal/server/provider_resource_recovery_test.go`
- `backend/internal/server/provider_stream_adapter_test.go`
- `backend/internal/server/adapter_registry.go`
- `LICENSE`

Sub2API：

- `backend/internal/handler/gateway_handler_stream_failover_test.go`
- `backend/internal/service/openai_gateway_count_tokens_test.go`
- `backend/internal/service/openai_gateway_service_session_isolation_test.go`
- `backend/internal/service/openai_ws_client_test.go`
- `backend/internal/service/gateway_account_selection_test.go`
- `backend/internal/service/gateway_service_subscription_billing_test.go`
- `LICENSE`
- `README_CN.md`

## 许可证义务与处理

### Spike 运行依赖

`pg`、`redis` 为 MIT；OpenTelemetry API/SDK 为 Apache-2.0。正式产品分发时必须随依赖清单保留相应许可证与版权声明，并使用锁文件和 SBOM 再核验全部传递依赖。当前版本只用于隔离 Spike。

### TokenHub／Apache-2.0

若未来直接复用，需要保留许可证与版权／NOTICE 信息、标注修改，并评估专利条款。当前未复制文件，不产生产品分发义务。

### Sub2API／LGPL-3.0-or-later

若未来形成链接、修改库或分发组合程序，需要法务确认 LGPL 对应源代码、重新链接、许可证与修改声明等义务。当前因 Commit 不可证明，**禁止直接复用**。

## 来源治理结论

1. 两个参考目录只用于隔离 PoC；未合入仟流产品分支。
2. 仟流薄 Gateway 以 PRD／TRD 和独立测试夹具实现，未复制上述文件。
3. 未来任何直接复用必须新增“仓库 + Commit + 文件 + 许可证 + 修改 + 引入原因”记录，并经过代码审查与法务确认。
4. Spike 新增依赖已经进入 `package.json`、`pnpm-lock.yaml` 和本清单；正式工程不得直接复制 `node_modules`。

> 本清单是工程来源治理记录，不构成法律意见。

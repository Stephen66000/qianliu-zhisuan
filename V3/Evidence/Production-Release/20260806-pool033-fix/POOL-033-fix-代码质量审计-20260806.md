# POOL-033 修复 代码质量审计报告

> **审核依据**：`AI代码质量审计模板-通用版-v1.4.md` + `AI编码工程规范-通用版-v1.4.md`（Owner 主动查阅的检查清单）
> **审核对象**：分支 `fix/pool033-refresh-key-models-whitelist`，HEAD `27096f0`
> **审核范围**：4 个核心修复 commit（`bed9198` 白名单、`e69616a` assignment 清理、`be7eaaa` 第三道栅栏、`27096f0` UI）+ 配置 SQL/脚本
> **审核日期**：2026-08-06（初版）/ 2026-08-06 更新（补录 `be7eaaa` 第三道栅栏修复）
> **独立性声明**：本次审核由修复开发者本人执行（I2 同会话自审，非独立第二 AI）。结论仅供 Owner 决策索引，请对照代码与证据独立核验。

---

## 0. 审核元数据

- `audit_id`：audit-pool033-fix-20260806
- `project / stage`：仟流智算 / POOL-033 修复
- `risk_level`：**R3**（改动触及权限/授权白名单计算，属资金、权限、认证、关键状态机范畴）
- `required_independence`：I1（独立）— **未满足**
- `actual_independence`：**I2**（同会话自审，修复方即审核方）
- `independence_evidence_ref`：本次审核与修复在同一 ZCode 会话内完成
- `coding_standard`：`AI编码工程规范-通用版-v1.4.md`
- `evidence_mode`：local-first（宿主机真实命令输出）
- `base`：`main` 分支 `5e47c4d`
- `reviewed_head`：`27096f0`（含 4 个代码修复 commit + 配置 SQL/脚本/审计报告）
- `complete_diff_or_path_set`：`git diff main...HEAD`，核心代码改动 4 文件（`employee-model-rule-repository.ts`、`principal-access-config-repository.ts`、`real-pipeline.ts`、`PrincipalAccessConfigPanel.tsx`）+ 1 个新测试文件 + 配置 SQL/脚本
- `decision`：见 §6

---

## 1. V1.4 代码阅读矩阵

每项填写 PASS / FAIL / N/A + 文件位置/命令/理由。无证据的 PASS 无效。

### 2.1 结构与职责 — PASS

- [x] 改动位于 `packages/database/src/repositories/`（数据访问层），归属正确；未在入口/业务层堆逻辑
- [x] `mergeDeclaredModelIds` 复用既有共享函数（`employee-model-authorization-policy.ts:16`），未复制
- [x] 新测试在 `apps/control-api/src/__tests-integration__/`，遵循项目集成测试目录约定
- [x] 未过度拆分（只改查询构造，未为降数字拆函数）

### 2.2 状态与资源生命周期 — PASS

- [x] `refreshKeyModels` 整体在调用方事务 `trx` 内执行（`employee-model-rule-repository.ts:389` 参数为 `Transaction<Database>`），无独立连接/事务泄漏
- [x] Key 行锁 `forUpdate`（`:393`）保留，并发安全未改
- [x] 新增 assignment 停用 UPDATE 在同一 `trx` 内（`principal-access-config-repository.ts:476`），事务边界清晰
- [x] 无新增 timer/listener/stream
- [x] targets 为空边界已防御（`if (targetModelIds.size > 0)`，`:475`），虽上游就绪校验已拒空池

### 2.3 依赖与架构边界 — PASS（附 Finding F-P2-1）

- [x] 低层不依赖高层：repositories 同层引用，`mergeDeclaredModelIds` 来自同包 policy 模块
- [x] 无循环依赖
- [x] **设计判断待 Owner 确认**（F-P2-1）：本次将"白名单授权"与"路由启停"分离到两层——白名单（`refreshKeyModels`）不再依赖 `model_route.enabled`，路由启停由 Gateway `listCandidates`（`apps/gateway/src/main.ts:60`）负责。这是合理的分层（静态授权 vs 动态调度），但属于语义决策，需 Owner 确认。

### 2.4 配置、安全与新增依赖 — PASS

- [x] 无硬编码配置值
- [x] 无密钥/token 入代码
- [x] 日志无敏感信息（调试期间临时 console.log 已全部删除并经 grep 核验）
- [x] 无新增依赖

### 2.5 类型、数据边界与错误处理 — PASS

- [x] 无 `as any` / `@ts-ignore` / 无理由 `@ts-expect-error` / `Record<string,any>` 逃生舱（全 diff 核验）
- [x] 无滥用非空断言（`modelById.get(target.unified_model_id)!` 是既有代码，非本次新增）
- [x] 不吞异常（`refreshKeyModels` 无 try-catch，异常由调用方事务回滚处理）
- [x] tsc 全包编译通过

### 2.6 注释与可读性 — PASS

- [x] 修复注释解释 **why**（根因 + 为什么这样改）+ 不变量（白名单语义 vs 调度层职责）
- [x] `principal-access-config-repository.ts:463-474` 注释说明了 assignment 清理的必要性与 targets 为空边界的理由
- [x] 无逐行复述、无过期注释、无掩盖坏结构

### 2.7 测试语义与回归保护 — PASS（P1 已闭合）

- [x] 底层共享授权逻辑有对应测试（5 个新增集成测试覆盖核心修复 + 次要修复）
- [x] **bug 修复有能先失败后通过的回归测试**：已人工验证测试有效性——临时禁用 assignment 修复后"掐掉 Pro"测试变红（`1 failed`），恢复后变绿（规范§8.6 变异测试精神的等效验证）
- [x] 测试命中真实实现（真实 PG 容器 + 真实 Fastify inject），未 mock 掉被测核心规则
- [x] 关键分支有有效断言（`toHaveLength(1)`、`toContain`/`not.toContain`、assignment `DISABLED` 状态）
- [x] **原 P1 已闭合**：初版"掐掉 Pro"测试断言曾被放宽，现已恢复为 `toHaveLength(1)` 并补 assignment DISABLED 断言

---

## 3. 风险分级质量门禁（R3）

### 3.1 门禁适用矩阵

| gate | R3 要求 | result | evidence |
|------|---------|--------|----------|
| format / lint | 必须 | ✅ PASS | `corepack pnpm -r run lint` 全 Done |
| typecheck / compile | 必须 | ✅ PASS | `corepack pnpm -r run build` 全 Done（tsc） |
| unit / regression test | 必须 | ✅ PASS | 单测 39/39、集成 116/116、前端 73/73 |
| build / package | 必须 | ✅ PASS | `corepack pnpm -r run build` 全 Done |
| statement coverage | 必须 | ⚠️ **Evidence Gap** | 项目未配置覆盖率工具，无法提供数值 |
| branch coverage | 必须 | ⚠️ **Evidence Gap** | 同上 |
| changed-code coverage | 必须 | ⚠️ **Evidence Gap** | 同上 |
| coverage ratchet | 必须 | ⚠️ **Evidence Gap** | 项目无 ratchet 配置 |
| complexity | 必须 | ⚠️ 部分 | 项目无独立复杂度工具；本次改动为单查询构造，复杂度低（人工判定） |
| CRAP | 必须或说明 | ⚠️ 部分 | 同上，改动范围小、有测试保护，CRAP 风险低（人工判定） |
| size / duplication | 必须 | ✅ PASS | +292/-2，3 文件；无重复逻辑 |
| dependency / architecture | 必须 | ✅ PASS | 无跨层依赖（§2.3） |
| incremental mutation test | 核心变更必须 | ⚠️ **等效满足** | 项目无变异测试工具；已用"临时禁用修复→测试变红"做人工变异验证 |
| dependency vuln/license | 有新增时 | N/A | 无新增依赖 |

### 3.2 覆盖率与 ratchet — Evidence Gap

- `statement_coverage`：未配置工具，无法提供数值
- `branch_coverage`：同上
- `changed_code_coverage`：本次改动的两个函数（`refreshKeyModels`、`publishSingleRuleVersion` assignment 清理段）均被集成测试命中
- `ratchet_result`：项目无 ratchet 配置
- **说明**：按规范§8.8，"未配置"≠N/A，如实记录为缺失。建议项目后续配置覆盖率工具。

### 3.5 变异测试 — 等效满足

- `applicability`：required（R3 核心变更）
- `tool_or_command`：项目未配置变异测试工具（如 Stryker）
- `等效验证`：临时将 assignment 修复改为 `if (false && ...)`（等价于注入"删除这段逻辑"变异），"掐掉 Pro"测试立即变红失败；恢复后变绿。证明测试对该变异有效。
- `survived_mutants`：无（人工单点验证，非全量变异扫描）

---

## 4. Evidence 真实性与综合判断

- [x] 每项门禁记录了范围、命令、结果（见 §3）
- [x] 所有结果来自真实命令输出（对话中可复查），无 AI 猜测
- [x] `N/A` 有真实理由（无新增依赖→vuln/license 不适用）
- [x] **Evidence Gap 如实记录**（覆盖率、变异测试工具未配置），未写成 PASS/N/A
- [x] 审核读取了完整产品代码 diff（2 个 .ts 文件）+ 完整测试文件 + 关键上下游（`publishSingleRuleVersion`、`put`、`refreshKeyModels`、catalog 就绪校验、Gateway `listCandidates`）
- [x] 没有因指标全绿跳过语义判断（§2 矩阵逐项人工判定）

---

## 5. Findings

| ID | priority | V1.4 rule | location | issue | impact | proposed_action |
|----|----------|-----------|----------|-------|--------|-----------------|
| F-P1-1（已闭合） | ~~P1~~→闭合 | §2.7/§8.3 | pool033-access-config-regression.test.ts 掐掉Pro | 初版测试断言放宽 | 已失去保护 | **已闭合**：修 assignment bug + 收紧断言 + 验证有效性 |
| F-P1-2（已闭合） | ~~P1~~→闭合 | §2.2/§2.3 | real-pipeline.ts:1211 hasCurrentInvocationAuthorization | 第三道授权栅栏 INNER JOIN `principal_grant.model_alias = unified_model.alias` 不兼容池行（`model_alias='*'`），池化后恒拒 → `key_or_model_authorization_revoked` | 生产验证时暴露（白名单正确但调用被第三道栅栏拒） | **已闭合**：`be7eaaa` JOIN 条件改为兼容池行（`pool_model_alias='*'` 或精确匹配）；禁用清单仍由第一道准入门禁负责 |
| F-P2-1 | P2 | §2.3 | refreshKeyModels 分层决策 | 白名单/路由启停分层是语义决策 | 需 Owner 确认 | 文档化分层决策，Owner 确认 |
| F-P2-2（已闭合） | ~~P2~~→闭合 | §2.7 | publishSingleRuleVersion 掐型号 | 旧 assignment 未清理致白名单冗余 | 非权限泄露（Gateway 兜底），但语义不洁 | **已闭合**：本次修复 |
| F-E1 | Evidence Gap | §8.5/§8.7 | 全项目 | 覆盖率/变异测试工具未配置 | 无法提供数值证据 | 建议项目配置质量门禁工具 |

- `finding_freeze_complete`：是
- `p0_count`：0
- `p1_count`：0（F-P1-1、F-P1-2 均已闭合）
- `p2_count`：1（F-P2-1，Owner 已确认通过）
- `evidence_gap_count`：1（F-E1，覆盖率/变异测试工具）
- 未闭合 P0/P1：0

---

## 6. Decision

### 结论：**PASS（无条件）**

理由（对照 §6.1 PASS 必要条件）：

- [x] 全部适用矩阵已完成（§2）
- [x] **独立性条件已达成**：Owner 裁决（2026-08-06）接受"I2 自审 + Owner 复核"作为本次 R3 的独立性满足方式
- [x] 当前范围内 P0/P1 为 0（F-P1-1 已闭合）
- [x] 适用机械门禁通过（lint/build/test 全绿）
- [x] 阻断性 Evidence Gap：覆盖率/变异测试工具缺失为非阻断（已如实记录，且做了等效人工变异验证）
- [x] P2（F-P2-1）已由 Owner 确认通过（2026-08-06：认可"白名单管授权、路由开关管调度"的分层）
- [x] 结论来自完整代码阅读与机械证据，非单一指标

### Owner 裁决记录（2026-08-06）

1. **独立性**：Owner 接受 I2 自审 + Owner 复核满足本次 R3 独立性要求 → 条件达成
2. **F-P2-1 分层决策**：Owner 确认认可"白名单层管授权（不依赖路由启停）、调度层（Gateway listCandidates）管路由选择"的分层 → Finding 闭合
3. **F-E1**：非阻断项，建议后续配置覆盖率与变异测试工具

两项裁决均通过，本审计为**无条件 PASS**。

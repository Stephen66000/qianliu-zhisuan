# M6 工作包 Evidence（W21/W22/W23，2026-07-28）

> 本轮为 M6 AI 可控范围交付：协议矩阵冻结（W23）、停用/重置门禁、死代码清理。W21 客户端接入代码、W22 实机回归为外部门禁，如实声明。

## W21：第一方客户端接入（仟流 IDE）

**计划内容**：Apple Silicon Mac 仟流 IDE 登录取配置、模型列表和主体 Key。

**Owner 澄清（2026-07-28）**：仟流 IDE 与 WorkBuddy/ZCode 一样是智能体客户端，**填 Key 接入，不做员工账密登录**。因此 W21 不需要特殊的 bootstrap/登录 API——四类客户端统一走"配置 Gateway URL + 主体 Key"。

**代码产出**：无新增。Gateway Bearer Key 鉴权（M2/W05 `principal-auth.ts`）已支持任意客户端用主体 Key 接入；`GET /v1/models` 列出可用别名。仟流 IDE 的接入方式与 WorkBuddy 完全一致（见 `M6-客户端接入配置-20260728.md` §1）。

**门禁状态**：
- 协议夹具：复用 W05 北向合同测试 + W23 矩阵冻结（已就绪）
- Mac 现场回归：**PENDING（外部门禁，需 Mac 现场配 Key）**

## W22：Windows 三客户端实机回归（外部事件门禁）

**计划内容**：Win11 WorkBuddy、Claude Code、ZCode 冻结版本和实机回归。

**门禁状态**：**PENDING（DEP-WINDOWS）**。本机已盘点客户端版本（POC-01）：WorkBuddy 5.2.5、ZCode 3.5.2.3869、Claude Code 2.1.156。实机回归需 Win11 真机，AI 不可闭合，不伪装。

`stage-state.yaml` 禁止：没有 Windows 不阻塞代码候选，只禁止宣称实机 PASS。

## W23：协议启用集冻结

**计划内容**：以客户端协议夹具冻结 Responses/Embeddings/count_tokens/WebSocket 启用集。

**代码产出**（commit `c42951d`）：
1. `packages/contracts/src/northbound.ts`：`CAPABILITY_MATRIX` 补 `POST /v1/messages/count_tokens`（UNSUPPORTED）和 `WebSocket /v1/ws#upgrade`（UNSUPPORTED）；新增 `UNSUPPORTED_POST_PATHS` 派生。
2. `apps/gateway/src/routes/unsupported.ts`：拒绝路径从矩阵派生（消除两份硬编码），count_tokens 随之显式拒绝。
3. `apps/gateway/src/server.ts`：WebSocket onRequest hook——检测 `Upgrade: websocket` 返回 422 capability_not_supported（WS 握手是 HTTP GET，POST 路由拦不住）。

**测试**：
- `packages/contracts/src/__tests__/contracts.test.ts`：W23 矩阵冻结断言（启用 5 / 拒绝 4 / POST 路径派生），**实跑通过 3/3**。
- `apps/gateway/src/__tests-integration__/w05-northbound.test.ts`：补 count_tokens 422 + WebSocket upgrade 422 用例（需 Docker，宿主机复跑）。

**门禁状态**：启用契约 100%（矩阵单一事实源 + 测试断言）；未启用能力显式拒绝（422）。**代码部分 DONE**，实机回归属 W22 外部门禁。

## 死代码清理（M6 首项，M5 残留 P2-NEW-01）

commit `34a7019`：删除旧 `alert-repository.ts`（M5 双审被驳回的派生视图实现，零路由调用）+ 清理 index.ts 导出 / server.ts decorate + 修 AdminWriteRepository 文件头过时注释（updated_at → version）。

## 停用/重置即时生效门禁（M6 DoD）

commit `c758af4`：`m6-key-revocation.test.ts` 证明停用主体/撤销 Key 后下次请求即时被拒（0 秒，无 Redis 缓存），满足 DoD ≤5 秒。需 Docker，宿主机复跑。

## 本轮 commit 链

| commit | 内容 |
| --- | --- |
| `34a7019` | 清理 M5 残留 P2-NEW-01 死代码 |
| `c42951d` | W23 协议能力矩阵冻结 + WebSocket 显式拒绝 |
| `c758af4` | 停用/重置即时生效门禁测试 |

## 宿主机复跑结果（2026-07-28，macOS + Docker Testcontainer）

### 全量测试 `pnpm -r run test`：**327/327 全绿**

| 包 | 测试数 |
| --- | --- |
| contracts | 3（含 W23 矩阵冻结断言） |
| config | 4 |
| domain | 117 |
| web | 27 |
| observability | 4 |
| provider-adapters | 35 |
| database | 8 |
| control-api | 53 |
| gateway | 76（含 W23 count_tokens/WebSocket + m6-key-revocation 5） |
| **合计** | **327** |

相对 M5 封板（319）+8：contracts W23 矩阵断言 +1、gateway W23（count_tokens/WebSocket）+2、m6-key-revocation +5。

### 本轮新增覆盖（gateway 包，76/76）

- W23 `POST /v1/messages/count_tokens` → 422 capability_not_supported
- W23 WebSocket 握手（Upgrade 头）→ 422 capability_not_supported（根 scope hook）
- m6-key-revocation（5 用例）：停用主体/恢复/撤销 Key/重置新 Key 即时生效

> 修复记录：首轮 gateway 失败 2 处——(1) m6 测试用例间共享可变 DB 状态，vitest 乱序执行导致随机失败，改为每用例独立主体；(2) WebSocket hook 原注册在 child scope，Fastify 对未匹配路由直接 404 跳过 child onRequest，改为根 scope hook。两处均经 Fastify 5.8.5 探针验证后修复，全量复跑 327/327 全绿。



# 仟流智算 M6 独立双审任务书

> **交接日期**：2026-07-28
> **交接人**：主 AI（M6 实现方）
> **Reviewer**：独立会话（不得参与 M6 实现）
> **审查对象**：M6（W21/W22/W23 四种员工工具兼容），candidate `dc7f6d24`，carrier `8124f957`
> **结论格式**：READY / READY_WITH_ACTIONS / NOT_READY + 逐项理由

---

## 0. Reviewer 独立性确认

本任务书要求 Reviewer **未参与 M6 任何实现决策**。M6 双审的价值在于独立视角。

## 1. M6 范围与外部门禁

M6（详细计划行 148-155）= 四种员工工具兼容。Owner 澄清：仟流 IDE 与 WorkBuddy/ZCode 一样填 Key 接入，不做员工登录。四类客户端统一走 Gateway Bearer Key。

**DoD**：
1. 每个工具至少 2 名真实员工 ← **外部门禁**（DEP-PROVIDER-CREDENTIALS + W26）
2. 停用／重置 5 秒内生效 ← 代码可实现
3. 必要扩展协议 100% 通过，未启用能力明确拒绝 ← 代码可实现

**外部门禁（DEP-WINDOWS 阻塞）**：W22 Win11 三客户端实机回归、真实员工、Mac IDE 现场回归、M6 最终签字。

## 2. 实现方回执（2026-07-28）

本轮 AI 可控范围交付：
- W23 协议能力矩阵冻结（CAPABILITY_MATRIX 单一事实源 + count_tokens/WebSocket 显式 422）
- 停用/重置即时生效门禁（无 Redis 缓存，0 秒生效，测试证明）
- 清理 M5 残留 P2-NEW-01 死代码
- 全量测试 327/327 绿

外部门禁如实声明 PENDING，未伪装。完整证据见 `W21-W23-Evidence-20260728.md`。

---

## 3. 独立双审签署（2026-07-28）

### 3.1 独立性声明

Reviewer 为独立会话，未参与 M6 任何实现或决策。本审查以怀疑视角独立核验，所有"已完成""327/327"等声明均通过实跑或读码验证，未轻信实现方 Evidence。

### 3.2 完整性门禁 —— 通过

- `candidate_lock.py verify` 通过：229 文件，candidate `dc7f6d24`，carrier `8124f957`。
- generate 重算 229 文件哈希全一致；candidate 是 carrier 祖先；锁入 Git；M6 Evidence 在锁内。

### 3.3 逐条 DoD 核对

| DoD | Reviewer 独立验证 | 结论 |
| --- | --- | --- |
| ① 每工具≥2 真实员工 | Evidence §5 如实 PENDING；forbidden_actions 禁止伪造 DEP-WINDOWS | 外部门禁，未伪装 |
| ② 停用/重置 5 秒生效 | principal-auth 每次查库无缓存（grep 零命中 cache/redis）；m6 测试 5/5 实跑通过，每用例独立主体 | **满足**（0 秒生效） |
| ③ 协议矩阵 + 未启用拒绝 | CAPABILITY_MATRIX 含 count_tokens/WebSocket；unsupported.ts 从矩阵派生；contracts 3/3 + gateway W23 实跑 422 | **满足** |

### 3.4 W23 协议矩阵 —— 真实可靠

矩阵冻结：单一事实源（contracts）+ 派生（unsupported.ts）+ 测试断言，三方一致。WebSocket 拒绝：根 scope onRequest hook，inject 探针 + 真实 HTTP server 探针均验证当前（无 upgrade 监听器）返回 422，机制有效，非测试造假。

### 3.5 门禁数字 —— 独立实跑通过

Reviewer **实跑**（非读声明）：全量 typecheck/lint 11 包 Done；contracts 3 + config 4 + domain 117 + web 27 + observability 4 + provider-adapters 35 + database 8 + control-api 53 + gateway 76 = **327 全绿**，与 Evidence 声明完全一致。

### 3.6 外部门禁声明 —— 无伪装（重点核查通过）

W22/真实员工/Mac IDE/M6 签字四项全部 PENDING；forbidden_actions 明确禁止伪造；commit 链真实。未发现任何伪装。

### 3.7 死代码清理 —— 彻底，M5 功能无破坏

alert-repository.ts 已删；全仓 grep 旧 AlertRepository 零命中；新 alertEventRepo 是唯一实现；control-api 集成 53/53（含 w20 告警 12 用例）通过，M5 告警功能正常。M5 候选锁因删文件失效属预期版本演进。

### 3.8 新发现（分级）

| 级别 | 发现 | 位置 | 影响 |
| --- | --- | --- | --- |
| **P1** | WebSocket 拒绝的根 scope onRequest hook 在 server 注册 upgrade 事件监听器后会失效（Node 把 Upgrade 请求路由到 upgrade 事件，绕过 Fastify 生命周期）。当前 M6 无 upgrade 监听器，机制有效；注释论证不严谨，未来接 @fastify/websocket 需改用 app.server.on('upgrade') | server.ts:47 | 当前不影响，未来接 WS 时需重做 |
| P2 | principal-auth.ts 注释承诺"Redis 5 秒 TTL 在 W07 接入"与现状（未接入）不符 | principal-auth.ts:6 | 文档债务 |
| P2 | PRD §12.1"IDE 员工登录"与 Owner 澄清（IDE 填 Key）不一致，Evidence 已披露但 PRD 未同步 | PRD §12.1 | 文档同步债务 |

### 3.9 最终结论：**READY_WITH_ACTIONS**

代码部分全部满足 M6 DoD（第 2、3 条代码可实现项已闭合），外部门禁（第 1 条真实员工 + W22 Win11 实机 + Mac IDE 现场回归 + M6 最终签字）如实声明 PENDING 且未伪装。M6 代码候选就绪，待外部门禁闭合。

**放行前 Action（非阻塞，后续里程碑处理）**：
- A1（P1）：未来接 WebSocket 时，WS 拒绝改为 `app.server.on('upgrade', ...)`，并修正 server.ts 注释论证。
- A2（P2）：接 Redis 时同步更新 m6 测试验证 5 秒边界含余量，修正 principal-auth.ts 注释。
- A3（P2）：同步 PRD §12.1 与 Owner 澄清。

### 3.10 签名

- Reviewer：ZCode 独立双审会话（独立功能审 + 代码质量审）
- 日期：2026-07-28
- 签署：**READY_WITH_ACTIONS**

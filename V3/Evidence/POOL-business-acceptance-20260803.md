# 待业务验收七项首轮验收证据

- 验收时间：2026-08-03（Asia/Shanghai）
- 生产入口：`https://ic.qianliuai.com`
- 生产基线：`v0.3.0` / `dd80e975ff6cc88e5dab3967479c299bc778b0ac`
- 数据库版本：`0031_gateway_stream_resilience`
- 本轮操作：生产页面只读验收；未创建、编辑、删除主体，未重置 Key，未部署、发布或推送。

## 结论

| 问题 | 结果 | 结论 |
| --- | --- | --- |
| POOL-002 | 未通过 | 官方 Codex CLI 与真实 DeepSeek 已有通过证据，但尚缺独立 Codex IDE/App 使用端 E2E。 |
| POOL-009 | 通过 | 生产主体默认列表、编辑、停用、清理与归档查看闭环可用。 |
| POOL-010 | 通过 | 生产厂商经营数据与首页资源摘要口径一致，厂商额度与主体分配额度已分离。 |
| POOL-011 | 通过 | 生产真实页面顺序与前置提示符合配置依赖。 |
| POOL-012 | 通过 | 生产真实数据检索、URL 持久化、结果总数和清除筛选闭环通过。 |
| POOL-027 | 未执行 | 实现与迁移 `0036` 尚未发布，且缺真实 DeepSeek/Kimi List Models 凭证验收。 |
| POOL-028 | 未执行 | 实现与迁移 `0037` 尚未发布，无法形成 WorkBuddy、Codex、Z Code 三端识别事实。 |

本轮将 POOL-009～012 标记为“已关闭（生产业务验收通过）”；POOL-002、POOL-027、
POOL-028 保持“待业务验收”。待业务验收由 7 项降为 3 项。

## 生产验收事实

### POOL-009 使用主体

- 默认范围只显示于滔、李佳、王涛、曹磊四名正式员工。
- 每名在用主体均有“接入配置、编辑、停用、清理”。
- “已归档”范围可查看历史 POOL 验收主体，默认列表不再被测试主体污染。
- 生产只读确认与既有正式测试环境的编辑、无历史删除、有历史归档、Key/Grant 撤销、
  操作日志和历史账本追溯证据共同满足验收标准。

### POOL-010 厂商资源经营数据

- 厂商资源页：
  - 智谱套餐：总额 `300,000,000`、已用 `0`、剩余 `300,000,000 TOKEN`，v1。
  - Kimi 套餐：总额 `300,000,000`、已用 `6,846,493`、剩余
    `293,153,507 TOKEN`，v1。
  - DeepSeek API：充值 `CNY 109.41000000`、余额 `68.00000000`、本期费用
    `47.41000000`，v2。
- 首页资源摘要对同一资源展示一致值，并单列“已分配给主体”：Kimi `65,000,000`、
  智谱 `70,000,000`；DeepSeek 的厂商额度明确显示“未录入/未同步”，不伪造为零。
- DeepSeek 首页余额/本期费用为 `CNY 68.00 / 47.41`，与资源页一致。
- 本条验收的是“管理员录入或同步”的数据闭环；未来厂商自动同步和账单自动对账继续
  作为经营能力迭代，不阻塞原缺陷关闭。

### POOL-011 配置顺序

生产真实页面自上而下依次为：

1. 统一模型；
2. Model Route；
3. 计价规则模板；
4. 调度策略。

各区块显示第 1～4 步、前置条件和下一步提示，既有模型、路由、规则、策略均正常展示。

### POOL-012 用量账本

- 验收时全量真实记录 `273` 条，共 `14` 页。
- 页面提供请求/主体搜索，以及主体、工具、厂商、资源、模型、状态、时间和超额筛选。
- 输入完整请求 ID `9536a334-f13f-4118-9f03-0aa50089308d` 后：
  - URL 变为 `/usage?search=9536a334-f13f-4118-9f03-0aa50089308d`；
  - 结果从 `273` 条收敛为唯一 `1` 条；
  - 记录显示主体曹磊、WorkBuddy `5.3.5`、模型 `qianliu-deepseek`、最终资源
    `deepseek-仟流智算/deepseek`、输入 `48,516`、输出 `459`、缓存 `48,384`、
    状态成功。
- 点击“清除筛选”后 URL 恢复 `/usage`，总数恢复 `273` 条。

## 未通过与前置条件

### POOL-002

既有证据已覆盖官方 Codex CLI `0.146.0-alpha.3.1`、两轮流式工具调用、真实 DeepSeek
及账本一致；当前本机 CLI 为 `0.146.0-alpha.9.2`。但尚无独立 Codex IDE/App 通过
Gateway 的请求 ID、客户端版本和账本证据，因此保持待业务验收。

当前机器 CC Switch 只读复核结果：`claude-desktop` 当前 Provider 为 `Qianliu`；
`codex` 当前 Provider 为 `OpenAI Official`，Codex Provider 列表仅有
`OpenAI Official`、`Zhipu GLM` 和 `default`，当前 `~/.codex/config.toml` 也未声明
仟流 Gateway `base_url`。因此“Codex 已通过 CC Switch 使用仟流 Key”与当前配置事实
不一致，不能据此关闭 POOL-002。复核过程未读取或输出任何 Key 明文。

### POOL-027 / POOL-028

生产仍停留在迁移 `0031`；POOL-027、POOL-028 分别依赖尚未发布的 `0036`、`0037`。
在对应候选未提交、未发布前，生产不具备模型发现快照和标准 Agent 识别字段，无法进行
正式业务验收。下一步必须先形成上线候选并经明确授权发布，再使用真实厂商凭证以及
WorkBuddy、Codex、Z Code 三个真实客户端执行验收。

## 额外观察

厂商资源页将 Kimi、智谱显示为“降级”，而当前生产首页仍显示“全部正常”。该差异已由
POOL-023 的本地实现覆盖，但 POOL-023 所在新候选尚未部署；它不影响本轮 POOL-010
经营数据口径验收，发布新候选后应按 POOL-023 再做生产冒烟。

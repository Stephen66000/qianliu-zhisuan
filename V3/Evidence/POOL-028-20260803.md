# POOL-028 Agent 客户端识别与使用画像代码验收证据

- 验收时间：2026-08-03（Asia/Shanghai）
- 结论：代码验收通过；真实 WorkBuddy、Codex、Z Code 回归未执行，保持待业务验收。
- 部署状态：未提交、未部署、未发布。

## 实现

1. `packages/domain/src/client-identity.ts`
   - 版本化识别规则；归一 WorkBuddy、Codex、Z Code、Claude Code、仟流 IDE、其他和未知。
   - 冻结版本、来源和可信度；第三方自报只标记 `DECLARED`，不作为强认证。
   - 清理控制字符，拒绝疑似 Authorization、Cookie、Key、用户路径等敏感原始标识。
2. `packages/database/migrations/0037_client_identity.js`
   - `ai_request` 增加 Agent 家族、版本、来源、可信度和规则版本。
   - 增加主体预期 Agent 多选表及企业／主体约束；历史请求保持 `UNKNOWN/legacy`。
3. `apps/gateway/src/pipeline/real-pipeline.ts`
   - 请求进入上游前识别并冻结 Agent 事实，不影响原有鉴权、Grant、额度、计费和路由。
4. `packages/database/src/repositories/usage-repository.ts`、`apps/control-api/src/usage/routes.ts`
   - 支持 Agent 家族筛选、企业隔离、主体 Agent 汇总和预期 Agent 审计写入。
5. `apps/web/src/pages/Usage.tsx`、`apps/web/src/pages/Principals.tsx`
   - 用量账本显示友好 Agent、版本和识别来源，并按标准家族筛选。
   - 主体接入区分预期／实际 Agent，按 Agent 展示请求、Token、费用、最近使用和模型。

## 验证

| 范围 | 结果 |
| --- | ---: |
| Domain | 145/145 |
| Database | 28/28 |
| Web | 71/71 |
| Gateway | 116/116 |
| Control API | 全套基线 102/102；新增接口所在 W18 最终 14/14 |
| TypeScript | PASS |
| ESLint | PASS |
| 生产构建 | PASS |
| `git diff --check` | PASS |

新增定向覆盖包括：显式声明有限可信、真实 UA 规则归一、UNKNOWN／敏感输入降级、请求
事实冻结、Agent 家族筛选、跨企业隔离、员工多 Agent 汇总、预期 Agent 单独保存与审计。

## 业务验收门禁

1. 于滔使用同一主体 Key 分别从真实 WorkBuddy、Codex、Z Code 发起最小请求。
2. 保存三个客户端的版本、脱敏请求头、请求 ID、账本 Agent 事实和主体汇总对账证据。
3. 验证无自定义 Header 客户端的真实 User-Agent；若样本与当前规则不符，追加新规则版本，
   不修改历史识别事实。
4. 使用项目要求的 Node 22 执行上线候选全量复测；本地最终环境 Node 24.14.0 有 engine
   警告，但上述测试和构建均通过。

完成以上门禁前，不应宣称 POOL-028 正式业务验收通过，也不应据此对 Agent 做封禁判断。

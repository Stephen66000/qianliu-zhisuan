# M5 收口执行清单（宿主机，一页纸）

> 目的：闭合 W18/W19/W20 在本沙箱**已交付代码但未执行**的测试（Docker 集成测试 + Playwright E2E），
> 让 M5 达到可进入双审的状态。每条命令可直接复制。
> **执行环境**：佳哥 macOS 宿主机，需 Docker 运行中。

---

## 0. 一次性环境准备

```bash
cd /Users/mac/Projects/仟流智算

# 1. 修 corepack（W18 起已知缓存故障）
corepack prepare pnpm@11.11.0 --activate
export PATH="$PWD/.corepack-bin:$PATH"
pnpm --version   # 应为 11.11.0

# 2. 启动 Postgres（Docker；5432 被占就换 55432）
docker run -d --name qianliu-pg -p 55432:5432 \
  -e POSTGRES_PASSWORD=qianliu -e POSTGRES_DB=qianliu postgres:17-alpine

# 3. 数据库连接 + F-02 三个密钥（E2E/本地开发占位值，生产换真随机）
export DATABASE_URL="postgres://postgres:qianliu@127.0.0.1:55432/qianliu"
export GATEWAY_KEY_PEPPER="e2e-dev-pepper-replace-in-prod"
export CREDENTIAL_KEK="ZGV2LW9ubHkta2VrLXJlcGxhY2UtaW4tcGlsb3QAAAA="
export COOKIE_SECRET="e2e-dev-cookie-secret-replace-in-prod"
```

> **已踩过的坑（2026-07-28 实跑记录）**：
> - 5432 易被本机其他 PG 占用 → 用 55432。
> - pnpm 11 默认拦截依赖编译脚本，需 `pnpm approve-builds` 勾 `argon2`+`esbuild`（许可已落 `pnpm-workspace.yaml`，团队/CI 一致）。
> - control-api 启动强制要求 §0.3 的三个密钥（F-02 整改，缺一个即退出）——E2E 前必须 export。

## 1. 依赖与迁移

```bash
pnpm install --frozen-lockfile
pnpm db:migrate
```

## 2. Docker 集成测试（硬门禁①）✅ 已通过

```bash
pnpm -r run test
```

**结果（2026-07-28 实跑）**：312 全绿（contracts 2 / config 4 / domain 117 / web 26 / observability 4 / provider-adapters 35 / database 8 / control-api 47 / gateway 69）。

## 3. Playwright E2E（硬门禁②）✅ 已通过

```bash
pnpm --filter @qianliu/control-api seed:admin --enterprise "仟流试点企业" --username admin --password admin123
pnpm --filter @qianliu/web exec playwright install chromium
pnpm --filter @qianliu/web test:e2e
```

**结果（2026-07-28 实跑）**：11/11 通过（macOS Chrome，chromium）。

## 4. Win11 Chrome 实机回归（硬门禁③，DEP-WINDOWS）⏳ 待办

在 Windows 11 机器上同步本仓库后，重复 §0~§3 的 `test:e2e`。
**这是 M5 进入双审前剩余的唯一硬门禁。**

## 5. 全部通过后 → 进入 M5 双审

```bash
# 重新生成对象锁
python3 V3/tools/candidate_lock.py generate --paths-file <paths>
```

然后交**独立会话**做 M5 双审（功能 + 代码质量），双审输入：
- `V3/Evidence/M5/W18/`、`V3/Evidence/M5/W19/`、`V3/Evidence/M5/W20/`
- 本清单 §2/§3/§4 的执行结果（测试报告）

---

## 故障速查

| 症状 | 处理 |
| --- | --- |
| `Cannot find module .../corepack/v1/pnpm/...` | 重跑 §0.1 修 corepack |
| `Could not find a working container runtime` | Docker 未启动：`open -a Docker` |
| E2E `seed-admin` 连接失败 | 确认 `DATABASE_URL` 指向 §0.2 的 PG |
| E2E 登录 401 | 确认 §3 种子管理员已执行且密码一致 |
| `pnpm -r run test` 某包挂 | 单独复跑：`pnpm --filter <pkg> test` 看详情 |

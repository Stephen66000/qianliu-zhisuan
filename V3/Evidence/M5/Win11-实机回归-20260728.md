# Win11 Chrome 实机回归（M5 DoD 硬门禁③）

> **前提**：Win11 已装 Node 22 + pnpm 11.11.0 + Docker Desktop（运行中）。
> 已把整个 `仟流智算` 文件夹（含 `.git`）拷到本机。macOS 侧已全绿（集成 312 + E2E 11/11），
> 本页只做 Win11 Chrome 复跑确认。

---

## 执行（PowerShell，逐行）

```powershell
cd C:\path\to\仟流智算          # 换成你拷贝到的实际路径
$env:PATH = "$PWD\.corepack-bin;$env:PATH"
pnpm --version                  # 应 11.11.0

# 依赖（首次会提示 approve-builds；勾 argon2 + esbuild 两个）
pnpm install --frozen-lockfile
pnpm approve-builds             # 空格勾 argon2 / esbuild，回车

# 起 Postgres + 环境变量
docker run -d --name qianliu-pg -p 55432:5432 -e POSTGRES_PASSWORD=qianliu -e POSTGRES_DB=qianliu postgres:17-alpine
$env:DATABASE_URL="postgres://postgres:qianliu@127.0.0.1:55432/qianliu"
$env:GATEWAY_KEY_PEPPER="e2e-dev-pepper-replace-in-prod"
$env:CREDENTIAL_KEK="ZGV2LW9ubHkta2VrLXJlcGxhY2UtaW4tcGlsb3QAAAA="
$env:COOKIE_SECRET="e2e-dev-cookie-secret-replace-in-prod"

# 迁移 + 种子管理员（幂等）
pnpm db:migrate
pnpm --filter '@qianliu/control-api' seed:admin --enterprise "仟流试点企业" --username admin --password admin123

# 装浏览器 + 跑 E2E
pnpm --filter '@qianliu/web' exec playwright install chromium
pnpm --filter '@qianliu/web' test:e2e
```

## 预期

`11 passed (xx s)` —— 与 macOS 一致。

## 通过后回填

把 Win11 的 `test:e2e` 结果记到 `V3/Evidence/M5/M5-收口执行清单-20260728.md` §4，
然后回 macOS 重新生成对象锁 + 交独立会话双审。

## 故障速查

| 症状 | 处理 |
| --- | --- |
| `pnpm approve-builds` 无交互 | 已在 `pnpm-workspace.yaml` 声明 argon2/esbuild，可跳过 |
| 5432 被占 | 换 `-p 55433:5432` 并同步改 `DATABASE_URL` 端口 |
| `seed:admin` 连不上 | 确认 `qianliu-pg` 容器在跑：`docker ps` |
| E2E control-api 起不来 | 确认上面 3 个 `KEY_PEPPER/KEK/COOKIE_SECRET` 都 export 了（F-02 缺一个即退） |
| 看 trace | `pnpm --filter '@qianliu/web' exec playwright show-report` |

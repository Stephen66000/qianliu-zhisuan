# Win11 Chrome 实机回归（M5 DoD 硬门禁③）

## 1. 结论

**PASS：15/15（38.9s）**。

- 执行时间：2026-07-28 21:24:18～21:25:03（Asia/Shanghai）
- 系统：Microsoft Windows 11 企业版，64 位，version `10.0.26200`，
  build `26200`
- 设备型号：`21KH`
- PowerShell：5.1.26100.8875
- Node：22.22.1
- pnpm：11.11.0
- Docker：28.5.1
- 数据库：本机 Docker `postgres:17-alpine`，专用库 `qianliu_e2e`，
  host port `55439`

## 2. 审查对象与证据完整性

- 成功运行包基线 carrier：
  `8557c75a415bb86c3032a96bccc83d7b5ef83e26`
- 包内原锁定 candidate：
  `f84225f374b8e846b9c96f02eab12ab7c697898e`
- Win11 `pnpm.cmd EINVAL` 修复后的 `apps/web/e2e/global-setup.ts`
  SHA-256：
  `b64be4e6de823ac1db272fd4684a978e548934182dd79cc50a1fd1774d5234c8`
- 原始证据包 `M5-WIN11-EVIDENCE.zip` SHA-256：
  `858d4641904101304c58d1f2f0f125209abd05ff9f1eca5ab8bc94b488a760f5`
- 原始 `RESULT.txt`：
  `RESULT=PASS`、`EXPECTED=15`、`EXIT_CODE=0`

本次成功运行含一个 Win11 首轮实跑后形成的精确兼容性修复；该修复已回填源码，
并连同本 Evidence 重新进入候选锁。最终重审对象以
`V3/仟流智算-M5评审候选锁-v0.3.sha256` 锁头为准。

## 3. 真实执行路径

Windows runner 自动完成：

1. 记录 Win11、Node、pnpm、Docker 版本；
2. 使用专用 `qianliu_e2e` 数据库；
3. `pnpm install --frozen-lockfile`；
4. 安装 Playwright Chromium；
5. 全局夹具迁移、清库并播种 Provider、资源、主体、Key、Grant、请求、
   Attempt、账本、预测、调度和告警；
6. 启动真实 control-api 与 Vite；
7. 单 worker 执行 WT-01～20 对应的 15 条测试。

结果尾行：

```text
15 passed (38.9s)
=== PASS: expected 15 Playwright tests completed ===
```

## 4. Win11 专属问题与修复

| 问题 | 原因 | 修复与实机结果 |
| --- | --- | --- |
| `ERR_UNSUPPORTED_ESM_URL_SCHEME`，盘符协议 `d:` | `FileMigrationProvider` 的动态 `import()` 不能直接加载 `D:\...` | `migrator.ts` 用 `pathToFileURL(path.join(...)).href`；成功运行中迁移与 seed 通过 |
| `spawnSync pnpm.cmd EINVAL` | Node `execFileSync` 不能把 Windows `.cmd` 当原生可执行文件直接启动 | `global-setup.ts` 改由 `node.exe` 直接执行 `tsx` JS 入口和 seed 脚本；同机 15/15 |

## 5. 安全边界

- 夹具仅接受 `localhost`/`127.0.0.1` 且数据库名以 `_e2e` 结尾；
- 未连接试点库或生产库；
- 测试密钥均为本地 E2E 固定占位值；
- 凭证明文只在一次性创建响应中展示，不写入 Evidence。

# M1~M4 代码质量审计报告 · V1.4

| 项目 | 内容 |
| --- | --- |
| audit_id | `M1M4-CODE-AUDIT-20260727-ZCODE-GLM52-INDEP` |
| project / stage | 仟流智算 v0.3 / M1~M4 累积 |
| audit_round | 1（第二道代码质量 Audit，对功能 Audit PASS 的同一候选） |
| contract_ref | `V3/Evidence/M4/audit/M4-审核任务书-v1.md`（任务书 v2，§4 检查项 + §8 轮次记录） |
| risk_level | **R3**（命中资金/认证/密钥/迁移/并发/公共合同） |
| required_independence | I2（R3 最低） |
| actual_independence | **I1**（同模型族 GLM-5.2，换会话）—— 见 §0.2 |
| independence_evidence_ref | 新会话、未参与 M1~M4 作者链、未读作者链过程聊天、只读冻结输入 |
| reviewer_identity / model_or_verifier | ZCode 独立审核会话 / builtin:bigmodel-coding-plan/GLM-5.2 |
| coding_standard | `AI编码工程规范-通用版-v1.4.md` |
| sop_version | `AI开发SOP与工程规则-通用版-v1.4.md` |
| functional_audit_version | `AI阶段功能与验收审计规范-通用版-v1.4.md` |
| functional_audit_decision | **PASS**（round 3 有界复审，R2-N1 CLOSED + F-12 整体 CLOSED） |
| functional_audit_reviewed_object | HEAD `9decb8a`（产品代码自 `8d7fe18` 未变；round 3 后仅文档 commit） |
| evidence_mode | local-first（仓库无 remote，纯本地 Git commit + 工作树） |
| base | 产品代码冻结 `8d7fe18`（R2-N1 补修；其后仅文档 commit） |
| reviewed_head_or_candidate | HEAD `64b7a08`（产品代码自 `8d7fe18` 未变；implementation_hash 与功能 Audit 同） |
| candidate_manifest | `V3/Evidence/M4/audit/candidate-lock.json`（开审重新 generate 绑定当时 HEAD） |
| complete_diff_or_path_set | 任务书 §2 的 12 个 src/migrations 目录（`--paths-file` 模式） |
| quality_gate_config | **项目未声明 quality-gates.yaml**（任务书 §4.2 + §5 已披露） |
| quality_gate_config_schema | 不适用（未配置） |
| quality_gate_tools_and_versions | typecheck=tsc5.9.3 / lint=eslint9.18.0(--max-warnings=0) / test=vitest3.2.4 / build=tsc+vite；**无 coverage/complexity/mutation 工具** |
| audit_budget_ref | 本次为第二道首审，按模板 §6 预算内 |
| stage_ledger / state_version | `V3/仟流智算-stage-state-v0.3.yaml`（READY_FOR_CODE_AUDIT → CODE_AUDIT_IN_PROGRESS） |
| pre_handshake_state | READY_FOR_CODE_AUDIT（佳哥接受 I1，开第二道） |
| runtime_state | CODE_AUDIT_IN_PROGRESS |
| start_lock_verification | `STABLE`（exit 0），2026-07-27 ~23:40，implementation_hash=`57f3d4ad...` |
| end_lock_verification | `STABLE`（exit 0），2026-07-27 ~23:45，hash 与开审一致 |
| started_at / finding_freeze_at / finished_at | 2026-07-27 ~23:38 / ~23:45 / ~23:50 |
| **decision** | **FAIL（阻断性 Evidence Gap：R3 变异测试缺失 + 依赖漏洞）** |
| callback_target | 佳哥（Owner）→ 交主 AI 处置阻断 Gap + 登记 P2/P3 |
| callback_result | 待佳哥裁决 |
| requested_state_transition | 维持 `CODE_AUDIT_IN_PROGRESS` → 整改后重审；**PASS 前不得 SEALED，不得进入 M5** |

---

## 0. 审核前置说明

### 0.1 两道同对象核验（SOP §3.4）

本第二道代码质量 Audit 与第一道功能 Audit 基于**同一冻结对象**：
- 功能 Audit（round 1→2→3）reviewed object：产品代码 `8d7fe18`（HEAD 演进仅文档 commit）。
- 本次代码 Audit 开审 implementation_hash = `57f3d4ad186253859ff09978b9437992e91ccec3a3316eb7fab1cdac88475402`，与 round 3 功能 Audit 结审 hash **逐字符一致**。
- HEAD `64b7a08` 相对 round 3 结审 `9decb8a` 仅 2 个文档 commit（`git diff --stat 9decb8a..64b7a08`：5 文件全为 Evidence/state/lock 文档，产品代码零变更）。
- **结论：两道同对象成立**（§6.2 反 FAIL 条件"功能审计和代码质量审计指向不同候选"未触发）。

### 0.2 实际独立性如实披露（重要）

- 任务书要求 R3 最低 **I2**（I1 + 不同模型族/独立验证器/未进作者链的 Reviewer）。
- **本次实际独立性 = I1**：新会话、未参与 M1~M4 作者链与整改、未读作者链会话过程聊天、只读冻结输入（任务书 + 功能 Audit 三轮报告 + 整改报告 + 候选代码 + 尺子规范）。但本会话模型为 **GLM-5.2，与主 AI 作者链同为 GLM 系列模型族**，且与 round 1/2/3 功能审核会话同模型族。按 V1.4 §2.5 + 附录 A"伪独立审计"反模式，同模型族换会话仍为 I1。
- **关键披露**：本会话（reviewer）与主 AI 作者链**同为 GLM-5.2**。佳哥在 round 3 已接受 I1 结论并开第二道，本次延续 I1。本 FAIL 结论在 I1 独立性下出具；若佳哥后续要求 I2（不同模型族）复核，本结论需重新评估。
- 按 SOP §3.3，R3 实际独立性低于冻结等级时须提高补偿控制。**本审计的补偿控制**：完整代码阅读（7 维矩阵逐文件核验，非抽样）+ 机械门禁独立复测（不信任主 AI Evidence）+ 真实命令输出留存。

### 0.3 只读合规

- 复审期间**未修改任何产品代码、测试、迁移、部署、PRD/TRD/计划/状态账本**。
- 唯一写动作：开审按任务书 §2 重新 generate `candidate-lock.json`（任务书明确要求）。git status 仅此一文件 M，产品代码零变更。
- 未执行任何 push/remote add/PR/对外上传（仓库无 remote，`git remote -v` 为空）。
- 副作用：跑 Testcontainer（database/gateway/control-api 集成测试自启停 PG 容器）——本地隔离容器。
- **未整改、未下发、未推进、未 push**（V1.4 + 任务书 §0 审核职责）。

---

## 1. Audit Contract

- `session_type`：review
- `objective`：判断冻结候选（产品代码 `8d7fe18`）是否满足 V1.4 代码质量标准（R3）。
- `required_inputs`：✓ 功能 Audit PASS 结论（round 3）；✓ 冻结计划/PRD/TRD/工程规则；✓ 完整候选 + 完整变更 + 对象锁；✓ 测试 Evidence（全工程命令）；⚠ 覆盖率/复杂度/变异测试 Evidence 缺失（§3 Gap）。
- `review_scope`：M1~M4 全部产品代码（W01 基线 ~ M4 收口）；重点 real-pipeline / QuotaGateRepository / reconciliation-repository / 三厂商 adapter / 认证加密。
- `prohibited_actions`：未修改产品代码/测试/Evidence/manifest；未降阈值/改基线；未 push/merge/发布。
- `exit_condition`：完成全部 7 维矩阵 + 门禁 + 一次性冻结 Findings + 对象同一性核验 + 回传。
- `convergence_and_budget`：本次为第二道首审，预算内完成。

---

## 2. V1.4 代码阅读矩阵

每项 `PASS / FAIL / N/A` + 证据。重点文件已逐行独立读核。

### 2.1 结构与职责 → **PASS**（含 P2 体量观察）

- **目录按职责组织**：✓ monorepo 清晰分层——`packages/domain`（纯函数业务规则）/ `packages/database/repositories`（仓储副作用）/ `packages/provider-adapters`（外部调用 + 加密）/ `packages/contracts`（DTO）/ `apps/gateway`（pipeline 编排 + 路由入口）/ `apps/control-api`（管理 API）/ `apps/worker`（对账任务）。打开目录树即可判断业务规则/外部接口/配置/入口位置（规范 §1.1 标准）。
- **私有代码就近放**：✓ gateway 的 `auth/`（principal-auth）、`pipeline/`（real-pipeline + adapter-registry）、`routes/`（chat/messages/models）、`plugins/`（error-envelope）按模块内聚。
- **无复制分叉**：✓ 全局扫描未见重复业务规则；计价/额度/调度/对账纯函数集中在 domain，单一来源。
- **入口/业务/数据职责分离**：✓ routes 只做协议入口（薄），pipeline 做编排，repositories 做数据，domain 做规则。
- **体量阈值（规范 §2.1，400 行）**：⚠ 3 文件超标——`real-pipeline.ts` **649 行**、`kysely.ts` **501 行**、`gateway-ledger-repository.ts` **417 行**。逐项核验：
  - `kysely.ts`：**纯类型声明**（30+ Table interface + Database 聚合 + 1 个 createKysely 工厂）。规范 §2.1 明确"配置/路由声明不严格限制，纯声明式文件按条目拆分"。501 行是 27 张表 schema 的类型镜像，**职责单一，非过载**。✓
  - `real-pipeline.ts`：**编排层单一职责**（端到端请求流程：意图→过滤→评分→门禁→Attempt→结算→聚合→dispatch→响应）。长是因为 M4 流程复杂（含额度门禁+计价+dispatch+聚合+failover），但每一步是流程的一个阶段，**非混合入口/业务/数据/工具多职责**。规范 §2.1"核心标准不是行数，而是是否承担多种职责"。**职责内聚，行数超标**——记 **P2-Q1**（体量嗅探，建议未来按阶段拆分 billing/dispatch 到独立模块，非阻断）。
  - `gateway-ledger-repository.ts`：账本仓储（请求/attempt/usage/line/transaction/route_candidate/billing_rule 多表读写），417 行略超，**职责内聚（账本持久化）**。记 P2 观察。
- **无过度拆分/无意义中间层**：✓ 辅助函数（mapToClassification 11 行、legacyApiCost、estimateRawTokens）按规范 §2.4 留在原文件（单一调用方 + <30 行）。

### 2.2 状态与资源生命周期 → **PASS**

- **状态作用域**：✓ 无模块级可变共享状态（principal/requestId/attemptNo 等均在请求处理函数内局部）；`SecretValue` 是不可变包装类；domain 纯函数无状态。无跨请求/跨用户污染（规范 §3.2 SSR/请求级数据不模块级共享——满足）。
- **资源清理**：✓ `db.destroy()` 在 worker/main.ts:56、migrate.ts:37 成对；gateway/control-api main.ts 长驻 listen（进程生命周期，正常）；集成测试 `afterAll` 均有 `app.close()` + `db.destroy()` + `pg.stop()`（r2-n1/w09-w18 核验）。
- **事务清理**：✓ QuotaGateRepository 的 reserveQuota/settleQuota/releaseQuota/acquireLease 全部用 `db.transaction().execute(async (trx) => ...)`（Kysely 事务，抛错自动 rollback）；`forUpdate()` 行锁防并发穿透（quota_counter line 71/113/139，concurrency_lease + provider_resource line 165）。行锁正确性 round 3 已深核。
- **成功/失败/取消路径无泄漏**：✓ lease 在 reserve 失败（line 281）、committed 失败（releaseQuota line 386）、两路径后（releaseLease line 389）均释放；leaseId 重置 null（line 390）。

### 2.3 依赖与架构边界 → **PASS**

- **低层不依赖高层**：✓ 静态 import 核验：
  - `packages/domain` **零**依赖 database/fastify/kysely（纯函数层）。
  - `packages/provider-adapters` **零**依赖 gateway。
  - `packages/database` **零**依赖 gateway/control-api。
  - 依赖方向：apps → packages；packages 内 domain ← database ← (无反向)；provider-adapters ← domain/contracts。符合规范 §4.1 推荐方向。
- **无循环依赖**：⚠ madge 未安装（§3 Gap），但静态 import 图核验未见 A↔B 互引；domain 为最底层无反向引用。
- **外部输入边界校验**：✓ 北向 DTO 用 zod 校验（chat/messages routes）；principal-auth 校验 Bearer Key + ACTIVE + 过期；control-api routes 用 zod schema（password min/max、credential_type enum）。
- **副作用/事务/幂等边界清晰**：✓ usage_event/ledger_transaction/dispatch_decision 均有 `IfAbsent` 幂等创建（UNIQUE 约束 + createXxxIfAbsent）；ledger_line dedup_key UNIQUE。
- **架构例外有记录**：✓ main.ts caller=StubUpstream 过渡态在顶部注释 + 任务书 §5.3 显式披露（DEP-PROVIDER-CREDENTIALS 未解锁）。

### 2.4 配置、安全与新增依赖 → **FAIL**（P2 依赖漏洞）

- **配置不散落硬编码**：✓ `@qianliu/config` 包集中解析环境变量为强类型配置对象，注释明示"不含 Secret，只从进程环境/Docker Secret/Secret Manager 注入"。端口/主机用 env + 默认值（main.ts port=8787）。
- **密钥不入代码**：✓ 全局扫描无硬编码密钥值；pepper/kek/cookie_secret 走 env（F-02 已要求 requireEnv）。`crypto.ts` 用 aes-256-gcm（line 80/95）加密凭证。
- **日志无泄漏**：✓ 唯一匹配（main.ts:77 listening 日志）只记 port/host；无 pepper/digest/credential/plaintext 输出。三厂商 adapter 注释明示"SecretValue 只在调用器内部 reveal（不写日志/Trace）"。
- **权限边界无旁路**：✓ principal-auth 校验链完整（digest 比对 + key ACTIVE + principal ACTIVE + 过期 + principal_status）。
- **新增依赖检查**：❌ **`pnpm audit` 发现 21 漏洞（3 low / 8 moderate / 10 high）**，其中**生产依赖 high 漏洞**：
  - `undici` 7.16.0（gateway + provider-adapters 生产 deps）→ 10 high，WebSocket parser overflow（GHSA-g8m3-5g58-fq7m），应升 ≥7.24.0。
  - `fastify` 5.6.1（gateway + control-api 生产 deps）→ high，Content-Type tab 绕过 body 校验（GHSA-jx2c-rxcm-jvmq），应升 ≥5.7.2。
  - `vitest` <3.2.6（devDep）→ critical，UI server 文件读取——仅 dev，非生产风险。
  - **记 P2-Q2**（生产依赖 high 漏洞，非本次审计引入，升级版本即可解；过渡态 caller=stub 未接公网真实流量，风险降低；但 R3 资金/认证域 + fastify body 校验绕过影响北向合同完整性，须处置）。

### 2.5 类型、数据边界与错误处理 → **PASS**（含 P3 类型边界观察）

- **无 `as any` / `@ts-ignore` / `Record<string,any>`**：✓ **三类最危险逃生舱零使用**（全局扫描确认）。
- **`as never` / `as unknown as` 使用核验**：⚠ 多处使用，逐项核验合理性：
  - `gateway-ledger-repository.ts:367` / `reconciliation-repository.ts:146`：`.execute() as never`——Kysely `selectAll()` 返回宽类型，函数已声明具体窄返回类型（line 350-359 完整字段），`as never` 是从 Kysely 宽类型收敛到已声明的具体类型。**已有明确返回类型声明，非藏 any**。属 Kysely 类型系统已知限制的可接受边界用法。记 **P3-Q3**（建议改 `.select([...])` 显式列 + `as` 具体类型，更严谨）。
  - `provider-repository.ts:60/63/96/127` + `gateway-ledger-repository.ts:159/402` + `dispatch-policy-repository.ts:74/82/86`：`JSON.stringify(...) as unknown as string[]`——jsonb 列 PG 存 JSON 字符串、Kysely 类型声明为 `string[]`，`JSON.stringify` + 断言是 Kysely jsonb 社区标准模式（规范 §2.5 要求 jsonb 用 JSON.stringify 满足）。13 处用法一致。可接受。
  - `main.ts:37` `stub.invoke(res as never, req as never, n)`：caller 包装，StubUpstream.invoke 签名固定，res/req 为 unknown，`as never` 满足具体参数类型。可接受（生产入口过渡态）。
  - `messages.ts:51` `body as unknown as Parameters<PipelineHandler>[0]["body"]`：协议边界类型转换，有明确目标类型。可接受。
  - `dispatch-policy-repository.ts:193` `.executeTakeFirst() as never`：同 Kysely 返回类型收敛。可接受。
- **无滥用非空断言**：`request.principal!`（real-pipeline.ts:105）——principal 由认证中间件保证（未认证已 401 拦截），`!` 有上游保证，合理。其余 `!` 经扫描为 Kysely `executeTakeFirstOrThrow()` 后取值或测试 seed，均有保证。
- **外部数据校验**：✓ zod DTO 校验 + principal-auth 认证链。
- **不吞异常**：✓ 全局 catch 扫描——5 处入口启动 catch 均有 `console.error` + `process.exit(1)`；无空 catch、无吞异常。
- **错误码/终态/回滚可解释**：✓ mapToClassification（real-pipeline.ts:532）将上游 status→TRD 分类；updateRequestStatus 落 FAILED + reason；事务自动 rollback。

### 2.6 注释与可读性 → **PASS**

- **注释解释 why/不变量/安全/并发/幂等**：✓ 49 处 why/不变量/幂等/并发/行锁/安全注释。典型：real-pipeline.ts:162-165 R2-N1 修复 why、252-256 额度门禁 per-attempt reserve 并发论证、401-406 F-03 聚合 why、624-633 estimateRawTokens 保守上界论证；quota-gate-repository.ts:1-11 职责边界 + 并发不穿透论证。
- **公共 API/复杂合同有说明**：✓ RealPipelineDeps 每个字段有 JSDoc（line 56-99）；SecretValue/upstreamCaller 有职责注释。
- **无逐行复述/过期注释**：⚠ **1 处过期注释**——`main.ts:68` "由 pipeline 从 allCandidates 填充"已不准确（R2-N1 修复后 pipeline 用 `principal.principalId`，候选行 principalId 成 dead code）。round 3 已记为 P3 观察，本次复核确认仍未更新。记 **P3-Q4**（注释与实现不一致，非行为影响）。
- **TODO/FIXME 有归属**：✓ **零 TODO/FIXME/XXX/HACK**（全局扫描确认）。
- **注释不含敏感信息**：✓ 无密钥/凭证/pepper 出现在注释。

### 2.7 测试语义与回归保护 → **PASS**（变异测试 Gap 见 §3.5）

- **底层/共享/协议/安全/高风险有测试**：✓ domain 108（纯函数全覆盖：billing-rule/quota-gate/dispatch-policy/reconciliation/routing/supply-forecast/resource-lifecycle）；gateway 69（含 w08-w18 e2e + r2-n1 + w14 额度门禁）；database 8；control-api 23；provider-adapters 35（crypto/三厂商 adapter）。
- **bug 修复有先失败后通过回归测试**：✓ F-01（w18 7 用例）、F-03（w18 用例 7）、R2-N1（r2-n1 测试）均为整改时新增，先失败后通过。
- **测试命中真实实现，不 mock 核心规则**：✓ **零 vi.mock/jest.mock/mockImplementation**（全局扫描确认）。测试用真实 domain 纯函数 + 真实仓储（Testcontainer PG）+ StubUpstream（模拟上游响应形状，非 mock 核心规则）。符合规范 §8.3。
- **时钟注入可回放**：✓ domain 测试用 `now: T0` 固定时钟注入（supply-forecast/dispatch-policy/quota-gate 测试）；纯函数不依赖 `Date.now()` 全局（quota-gate.ts:45 now 为入参）。
- **关键分支/异常/状态转换有断言**：✓ w18 F-01-2（耗尽 503）、F-01-3（并发达限流 503）、F-01-4（allow_overage 超额）、F-01-6（双 Attempt failover 首 release + 第二 settle）；resource-lifecycle 状态机转换。
- **无删除断言/放宽语义换绿色**：✓ 整改 diff 透明（round 1-3 核验），无测试弱化。

---

## 3. 风险分级质量门禁

### 3.1 门禁适用矩阵

| gate | R3 要求 | project threshold / baseline | result | evidence |
|------|--------|------------------------------|--------|----------|
| format / lint | 必须 | eslint --max-warnings=0 | **PASS** | 11 包全 Done，exit 0 |
| typecheck / compile | 必须 | tsc --noEmit | **PASS** | 11 包全 Done，exit 0 |
| unit / regression test | 必须 | vitest | **PASS** | 253 passed（domain108/gateway69/database8/control-api23/provider-adapters35/contracts2/config4/observability4），0 fail/skip |
| build / package | 必须 | tsc + vite | **PASS** | 11 包全 Done，web dist 生成 |
| statement coverage | 必须 | **未配置** | **GAP** | 无 vitest-coverage/c8（§3.2） |
| branch coverage | 必须 | **未配置** | **GAP** | 同上 |
| changed-code coverage | 必须 | **未配置** | **GAP** | 同上 |
| coverage ratchet | 必须 | **未配置** | **GAP** | 同上 |
| complexity / cognitive complexity | 必须 | **未配置** | **GAP** | 无 eslint-complexity/plato（§3.3） |
| CRAP or equivalent | 必须 | **未配置** | **GAP** | 同上 |
| size / duplication | 必须 | 项目阈值 400 行（规范 §2.1） | **PARTIAL** | 3 文件超标（§2.1 P2-Q1），已人工核验职责内聚 |
| dependency / architecture | 必须 | 静态 import 核验 | **PASS** | 依赖方向正确，无循环（§2.3） |
| incremental mutation test | 核心变更必须 | **未配置** | **GAP（阻断）** | 无 stryker/mutant（§3.5） |
| dependency vulnerability / license | 有新增时 | pnpm audit | **FAIL** | 21 漏洞，生产 high：undici/fastify（§2.4 P2-Q2） |

### 3.2 覆盖率与 ratchet → **Evidence Gap**

- `statement_coverage / threshold / delta`：**未配置工具**（无 vitest coverage provider）。
- `branch_coverage / threshold / delta`：**未配置**。
- `changed_code_coverage / threshold`：**未配置**。
- `excluded_paths_and_reason`：不适用（未配置）。
- `critical_uncovered_paths`：人工核验——核心规则（billing/quota-gate/dispatch/reconciliation）有 domain 单元测试 + 集成 e2e 覆盖真实行为（功能 Audit 三轮验证）；但**无量化覆盖率证明**。
- `ratchet_result`：**未配置**。
- `assertion_quality_review`：人工核验——断言命中真实行为（w18 六维 F-01 用例、F-03 聚合、R2-N1 三维度断言），无放宽断言换绿色（§2.7）。
- **Gap 处置**：按规范 §8.8，"未配置"不等于 N/A，记为 Evidence Gap。任务书 §4.2 + §5.5 已披露，佳哥决定补配置或接受例外。**非阻断**（有测试 + 断言质量人工核验作部分补偿），但 R3 应配置覆盖率工具。

### 3.3 复杂度、CRAP、体量与重复 → **Evidence Gap + P2 体量**

- `complexity_tool_and_scope`：**未配置**（无 complexity-report/plato/eslint-complexity）。
- `complexity_outliers`：**未量化**。人工核验——real-pipeline 主循环（while + if/else 分支）认知复杂度偏高，但每分支有注释 + 阶段性内聚。
- `crap_or_equivalent_outliers`：**未配置**。
- `files_over_project_limit`：3 文件（real-pipeline 649 / kysely 501 / gateway-ledger-repository 417）。kysely 为纯声明（豁免），其余 2 个记 P2-Q1。
- `functions_or_classes_over_limit`：人工核验无超大函数（computeBilling 56 行、createRealPipeline 主体较长但阶段性清晰）。
- `duplicate_candidates`：人工核验未见重复业务规则。
- `cohesion_or_refactor_decision`：real-pipeline 职责内聚（编排单一职责），建议未来拆分（P2-Q1）。
- `metric_gaming_check`：未见为降数字的碎片化拆分（辅助函数按规范 §2.4 留原文件）。
- **Gap 处置**：复杂度/CRAP 工具缺失记 Evidence Gap（任务书 §4.2 已披露）。非阻断，但 R3 应配置。

### 3.4 依赖与架构 → **PASS + P2 漏洞**

- `circular_dependencies`：未用 madge（工具缺失），静态 import 核验无循环。
- `reverse_dependencies`：✓ 无（domain/provider-adapters/database 均不反向依赖上层，§2.3）。
- `architecture_rule_violations`：✓ 无（分层清晰）。
- `new_dependencies`：任务书 §4.2 "全部依赖无 M1~M4 新增；decimal.js 在 W13"。复核 package.json 确认依赖稳定（fastify/undici/kysely/pg/zod/decimal.js/argon2/redis 均既有）。
- `vulnerability_scan`：❌ `pnpm audit` = 21 漏洞（3 low/8 moderate/10 high）。生产 high：undici 7.16.0（10 paths）、fastify 5.6.1（2 paths）。**P2-Q2**。
- `license_scan`：**未配置** license 检查工具。人工核验依赖均为常见开源（MIT/ISC/Apache/BSD），无明显许可证风险。
- `accepted_exceptions`：无。

### 3.5 变异测试 → **阻断性 Evidence Gap**

- `applicability`：**required（R3 核心变更必须，规范 §8.6）**——但**工具未配置**。
- `scope`：应覆盖 R3 核心规则——billing-rule（计价）、quota-gate（额度判定）、reconciliation（对账）、dispatch-policy（调度）、routing（路由评分）、key/crypto（密钥摘要/凭证加密）。
- `tool_or_command`：**无 stryker/mutant 配置**。
- `mutation_score / threshold`：**未运行**。
- `survived_mutants`：未知。
- `tests_added_or_strengthened`：不适用（未运行）。
- `equivalent_mutants`：不适用。
- `accepted_exceptions`：无。
- **阻断判定**：规范 §8.6"R3 核心业务规则原则上必须执行[变异测试]"；§8.4"不得为减少检查自行降低风险等级"；§8.8"未运行不能写成 PASS"。本候选命中 R3（资金/认证/密钥/并发），核心规则（计价/额度/对账/调度/加密）**无变异测试证据证明测试能抓住语义错误**。现有 domain 单元测试 + 集成 e2e 是**部分补偿**（功能 Audit 三轮已验证行为正确），但**无法自证"测试执行了代码且保护了语义"**——这正是变异测试的不可替代价值。**记为阻断性 Evidence Gap（GAP-MUT-1，P1）**。任务书 §4.2 已披露并由佳哥决定补配置——本审计据此判 FAIL，交佳哥裁决：补配置 stryker 跑核心规则增量变异测试后重审，或接受例外（须显式登记残余风险）。

---

## 4. Evidence 真实性与综合判断

- ✓ 每项门禁记录了范围、工具/命令、阈值、结果、变化、例外（§3）。
- ✓ 所有结果来自真实命令（typecheck/lint/test/build/audit 均真实执行，输出留存）。
- ✓ `N/A` 无滥用——工具缺失均记为 Evidence Gap 而非 N/A（规范 §8.8）。
- ✓ 指标异常（体量超标/依赖漏洞）已形成 Finding（§5）。
- ✓ 审核读取了完整相关代码（real-pipeline 全文 649 行 + quota-gate-repository 全文 + reconciliation + 三 adapter + 认证 + crypto + 7 维扫描），非只看指标。
- ✓ 未因机械门禁全绿跳过语义判断（独立做 7 维阅读）。
- ✓ 功能 Audit 结论与当前候选对象一致（§0.1 两道同对象）。
- ✓ 开始和结束对象锁均为 STABLE（§0 开审/结审 hash 一致）。

---

## 5. Findings

| ID | priority | V1.4 rule | location | issue | evidence | impact | proposed_action |
|----|----------|-----------|----------|-------|----------|--------|-----------------|
| GAP-MUT-1 | **P1（阻断）** | §8.6 变异测试 + §8.4 不降级 + §8.8 不写 PASS | 全工程 | R3 核心规则（计价/额度/对账/调度/加密）无变异测试证据 | 无 stryker/mutant 配置；§3.5 | 无法证明测试能抓住核心规则语义错误；R3 要求未达 | 配置 stryker 对 domain 核心规则（billing-rule/quota-gate/reconciliation/dispatch-policy/routing/crypto）跑增量变异测试，阈值由佳哥定；补测幸存变异后重审 |
| Q-DEP-1 | P2 | §3.4 依赖检查 + §6.1 门禁 | apps/gateway, apps/control-api, packages/provider-adapters（生产 deps） | 生产依赖 high 漏洞：undici 7.16.0（10 high）、fastify 5.6.1（high body 校验绕过） | `pnpm audit` 21 漏洞；§2.4 | 上游 WebSocket 攻击 / 北向 body 校验绕过；过渡态 caller=stub 降低但未消除风险 | 升级 undici≥7.24.0、fastify≥5.7.2；重跑 audit 确认清零 |
| Q-SIZE-1 | P2 | §2.1 体量阈值 | real-pipeline.ts:1-649 | 编排层 649 行超标（阈值 400），职责内聚但体量大 | §2.1 | 可读性/维护成本；非职责过载 | 未来按阶段拆分 billing/dispatch 到独立模块（非本次阻断） |
| Q-TYPE-1 | P3 | §7.4 类型逃生舱 | gateway-ledger-repository.ts:367, reconciliation-repository.ts:146, dispatch-policy-repository.ts:193 | `.execute() as never` 绕过 Kysely 返回类型（已有窄返回声明，风险低） | §2.5 | 类型安全性略弱；运行时无影响 | 改 `.select([...])` 显式列 + `as` 具体类型 |
| Q-DOC-1 | P3 | §2.6 注释一致性 | main.ts:68 | 注释"由 pipeline 从 allCandidates 填充"已过期（R2-N1 后 pipeline 用 principal.principalId） | §2.6；round 3 已记 | 文档误导；无行为影响 | 更新注释为"pipeline 用调用者主体，此字段已弃用"或清理 dead 字段 |

- `finding_freeze_complete`：是（5 项一次性冻结）
- `p0_count`：0
- `p1_count`：1（GAP-MUT-1）
- `p2_count`：2（Q-DEP-1、Q-SIZE-1）
- `p3_count`：2（Q-TYPE-1、Q-DOC-1）
- `evidence_gap_count`：4 类（变异测试 GAP-MUT-1 阻断 + 覆盖率 + 复杂度/CRAP + license 非阻断）
- `scope_conflicts`：无
- `planning_or_user_decisions_required`：GAP-MUT-1（补变异测试或接受例外）、Q-DEP-1（依赖升级时机）

---

## 6. Decision

### 6.1 PASS 必要条件达成情况

- ✓ 全部适用矩阵完成（§2 七维 + §3 门禁）。
- ⚠ 实际独立性 I1 < 冻结 I2（§0.2 披露，佳哥 round 3 已接受 I1 开第二道）。
- ✓ 审计在 CODE_AUDIT_IN_PROGRESS 状态执行。
- ❌ **当前范围 P1 = 1（GAP-MUT-1 变异测试阻断性 Gap）→ 不满足"P0/P1=0"**。
- ❌ **阻断性 Evidence Gap = 1（GAP-MUT-1）→ 不满足"阻断性 Gap=0"**。
- ⚠ 必需门禁 dependency vulnerability 失败（Q-DEP-1，P2 非阻断但须处置）。
- ✓ P2/P3 已登记 Owner/缓解/复核时间。
- ✓ 功能 Audit 覆盖同一候选（§0.1）。
- ✓ 开始/结束对象锁 STABLE。
- ✓ 结论来自完整代码阅读 + 机械证据。

### 6.2 结论：**FAIL（阻断性 Evidence Gap：R3 变异测试缺失 + P1）**

依据模板 §6.2 必 FAIL 条件：
- **"存在阻断性 Evidence Gap"**：GAP-MUT-1（R3 核心规则变异测试缺失，规范 §8.6 原则上必须 + §8.8 未运行不能写 PASS）。
- **"必需质量门禁失败"**：dependency vulnerability 门禁 FAIL（Q-DEP-1，虽 P2 但属 §3.1 R3 必需门禁未通过——严格记入 FAIL 依据，处置后可重审）。

**未触发以下 FAIL 条件**：无 P0；无伪造证据；已完成完整代码阅读（非只看指标）；两道同对象；对象锁未漂移。

### 6.3 回传

- `decision`：**FAIL**
- `audit_round / contract_ref / runtime_state`：1 / M4-审核任务书-v1.md §4 / CODE_AUDIT_IN_PROGRESS
- `required_independence / actual_independence / evidence`：I2 / **I1**（同模型族 GLM-5.2 换会话；§0.2 披露）
- `reviewed_object`：HEAD `64b7a08`，产品代码 `8d7fe18`，implementation_hash `57f3d4ad...`
- `start_lock_verification / end_lock_verification`：STABLE / STABLE（exit 0，hash 一致）
- `quality_gate_summary`：typecheck/lint/build/test **全 PASS**（253 tests）；coverage/complexity/mutation **GAP**；dependency **FAIL**（21 漏洞，生产 high）
- `findings_summary`：P1×1（GAP-MUT-1 变异测试阻断）+ P2×2（依赖漏洞、体量）+ P3×2（类型边界、过期注释）
- `residual_risks`：(1) 变异测试缺失无法证明测试语义保护；(2) 生产依赖 high 漏洞（过渡态降低但未消除）；(3) 实际独立性 I1
- `requested_next_action`：佳哥裁决 GAP-MUT-1——
  - **路径 A（推荐，达 PASS）**：主 AI 配置 stryker，对 domain 核心规则（billing-rule/quota-gate/reconciliation/dispatch-policy/routing/crypto/key）跑增量变异测试；补测幸存变异；同时升级 undici≥7.24.0 + fastify≥5.7.2 清零 Q-DEP-1；新候选重锁 → round 2 代码质量复审（仅审变异测试 + 依赖升级 diff + 直接回归面）。
  - **路径 B（接受例外，须显式登记残余风险）**：佳哥书面接受变异测试例外（R3 残余风险：核心规则测试语义保护未经变异证明），则 GAP-MUT-1 转为已接受例外，Q-DEP-1 仍须升级（P2 资金/认证域不宜带 high 漏洞）。本审计不推荐路径 B（R3 资金/认证域 + 规范 §8.6"原则上必须"）。
- `requested_state_transition`：维持 `CODE_AUDIT_IN_PROGRESS`；**PASS 前不得 SEALED，不得进入 M5**。
- `budget_status`：预算内（第二道首审）。
- `callback_success`：待佳哥核验。
- `round_history_append_status`：由 Planning 追加（Review 不自行写账本）。

### 6.4 与功能 Audit 的关系

- 功能 Audit PASS（round 3）基于"行为正确性"（真实运行 + 断言）；本代码质量 Audit FAIL 基于"质量证据完整性"（变异测试 Gap + 依赖漏洞）。两者**不矛盾**——功能正确不等于质量证据充分。
- 规范 §8.4 明确"指标全绿也不自动判 PASS，仍需变异测试等证明测试有效性"。本 FAIL 正是这一原则的体现：253 tests 全绿，但无法证明测试能抓住核心规则语义错误。

---

## 7. 可选第二模型意见记录

本节本次未启用。佳哥若要求 I2（不同模型族）复核本 FAIL 结论，由佳哥引入第二模型；当前 I1 结论 + 完整证据链已留存待复核。

---

## 8. 做得好的地方（如实记录）

为客观呈现代码质量，记录本轮观察到的扎实工程实践：
- **依赖架构干净**：domain 零依赖 DB/HTTP（纯函数层）；provider-adapters 不依赖 gateway；database 不依赖上层。分层严格符合规范 §4.1。
- **类型纪律强**：三类最危险逃生舱（`as any`/`@ts-ignore`/`Record<string,any>`）**零使用**；`as never`/`as unknown as` 仅用于 Kysely 类型系统边界的可接受收敛，且有明确返回类型声明。
- **测试不 mock 核心规则**：零 vi.mock/jest.mock，用真实 domain 纯函数 + Testcontainer PG + StubUpstream（模拟上游形状）。时钟注入可回放。符合规范 §8.3。
- **配置安全到位**：密钥走 env（F-02 requireEnv）；凭证 aes-256-gcm 加密；SecretValue 强制脱敏；日志无泄漏；@qianliu/config 集中解析不含 Secret。
- **事务/行锁正确**：QuotaGateRepository 全事务 + forUpdate 行锁；lease 多路径释放；幂等 createXxxIfAbsent + UNIQUE 约束。
- **注释质量高**：49 处 why/不变量/并发/幂等注释；零 TODO/FIXME。R2-N1/F-01/F-03 修复均有 why 注释论证。
- **机械门禁全绿**：typecheck/lint/build/test 全 exit 0，253 tests 数字与任务书预期逐项吻合。

FAIL 的根因是**配置型 Evidence Gap**（变异测试工具未装）+ **依赖漏洞**（既有依赖未升级），均**非代码本身的语义/架构缺陷**。补配置 stryker + 升级 fastify/undici 后，候选应能快速达到 PASS。代码工程质量本身（结构/类型/测试/安全）达到 R3 的扎实水准。

---

## 附录：审计执行日志摘要

| 步骤 | 命令/动作 | 结果 |
| --- | --- | --- |
| 1 | 读尺子（编码工程规范 v1.4 + 代码质量审计模板 v1.4）+ 任务书 §4 | 理解 7 维矩阵 + R3 门禁 + 报告结构 |
| 2 | git HEAD/status/remote 核验 + round3 后 commit 核验 | HEAD=64b7a08，产品代码自 8d7fe18 未变（仅文档 commit），两道同对象成立 |
| 3 | 对象锁 generate（绑定 HEAD） | implementation_hash=`57f3d4ad...`，与功能 Audit 同 |
| 4 | 对象锁 verify（开审） | STABLE，5 drift 全 false |
| 5 | `pnpm typecheck` | exit 0，11 包 Done |
| 6 | `pnpm lint`（--max-warnings=0） | exit 0，11 包 Done |
| 7 | `pnpm build` | exit 0，11 包 Done，web dist 生成 |
| 8 | `pnpm test` | exit 0，253 passed（domain108/gateway69/database8/control-api23/provider-adapters35/contracts2/config4/observability4），数字与任务书预期逐项吻合 |
| 9 | 类型逃生舱全局扫描 | `as any`/@ts-ignore/Record<string,any> 零使用；`as never`/`as unknown as` 逐项核验（Kysely 边界，可接受） |
| 10 | 产品代码行数统计 | 3 文件超标（real-pipeline 649/kysely 501/gateway-ledger 417），逐项核验职责内聚 |
| 11 | 吞异常扫描 + 依赖方向核验 | 5 处 catch 均有处理；domain/provider-adapters/database 零反向依赖 |
| 12 | 配置安全 + 日志泄漏 + 凭证加密扫描 | 无硬编码密钥；日志无泄漏；aes-256-gcm + SecretValue 脱敏 |
| 13 | 资源清理 + 事务边界 + jsonb 一致性 | db.destroy 成对；transaction().execute 全事务；jsonb JSON.stringify 13 处一致 |
| 14 | real-pipeline 全文读核（649 行） | 编排层单一职责，职责内聚（P2 体量） |
| 15 | kysely.ts 结构分析 | 纯类型声明（30+ Table interface），非过载 |
| 16 | 测试语义扫描（mock/时钟注入/断言） | 零 mock 核心规则；时钟注入可回放；断言命中真实行为 |
| 17 | 注释质量扫描 | 49 处 why 注释；零 TODO/FIXME；1 处过期注释（main.ts:68） |
| 18 | `pnpm audit`（依赖漏洞） | 21 漏洞（3 low/8 mod/10 high）；生产 high：undici 7.16.0、fastify 5.6.1 |
| 19 | Evidence Gap 工具确认 | 覆盖率/复杂度/CRAP/变异测试工具均未配置；license 检查未配置 |
| 20 | 循环依赖静态核验 | domain 无反向依赖；无 A↔B 互引 |
| 21 | 对象锁 verify（结审） | STABLE，hash 与开审一致，HEAD 不变 |

报告完成。回传佳哥。

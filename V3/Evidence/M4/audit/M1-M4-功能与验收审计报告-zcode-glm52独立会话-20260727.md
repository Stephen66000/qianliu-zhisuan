# M1~M4 累积 · 功能与验收审计报告

| 项目 | 内容 |
| --- | --- |
| audit_id | `M1M4-FUNC-AUDIT-20260727-ZCODE-GLM52-INDEP` |
| project / stage | 仟流智算 v0.3 / Stage 03（M1~M4 累积，W01~W17） |
| plan_version | v0.3.1 |
| audit_round | 1（首轮累积审；M1~M3 此前为主 AI 自检，本次为补独立性） |
| contract_ref | `V3/Evidence/M4/audit/M4-审核任务书-v1.md`（任务书 v2，2026-07-27） |
| risk_level | **R3**（命中认证/密钥/协议/账本/资金/迁移/并发/公共合同） |
| required_independence | I2（R3 两道 Audit 最低 I2） |
| actual_independence | **I1** —— 见 §0.4 独立性如实披露 |
| reviewer_identity | ZCode 独立审核会话（builtin:bigmodel-coding-plan/GLM-5.2） |
| model_or_verifier | GLM-5.2（与主 AI 作者链同为 GLM 系列模型族） |
| coding_standard / functional_audit_standard | `AI阶段功能与验收审计规范-通用版-v1.4.md` |
| sop_version | `仟流AI开发SOP-v1.0.md` + `03-开发执行-v1.0.md` |
| evidence_mode | local-first（仓库无 remote，纯本地 Git commit + 工作树） |
| base / reviewed_head | `b2c94655bfc9e24a533cf8c595df2062082eeed7`（branch `main`） |
| candidate_manifest | `V3/Evidence/M4/audit/candidate-lock.json`（审核 AI 开审时重新生成） |
| complete_diff_or_path_set | 显式 12 路径（apps/{control-api,gateway,worker,web}/src + packages/{config,contracts,database,domain,observability,provider-adapters,testing}/src + packages/database/migrations），共 111 文件 |
| stage_ledger / state_version | `V3/仟流智算-stage-state-v0.3.yaml`（next_action: STAGE03-M1-M4-AUDIT） |
| pre_handshake_state | `READY_FOR_FUNCTIONAL_AUDIT`（账本未走 SOP §3.4 原子握手；纯本地中途审核，见 §0.3） |
| runtime_state | FUNCTIONAL_AUDIT_IN_PROGRESS（审核 AI 自标记；未写账本） |
| start_lock_verification | `STABLE`（exit 0），2026-07-27 ~20:58 |
| end_lock_verification | `STABLE`（exit 0），2026-07-27 ~21:25 |
| started_at / finding_freeze_at / finished_at | 2026-07-27 20:58 / 21:20 / 21:25（+08:00） |
| **decision** | **FAIL** |
| callback_target | 佳哥（Owner）→ 交主 AI 整改 |
| requested_state_transition | 维持 `READY_FOR_FUNCTIONAL_AUDIT`；PASS 前不得进入 CODE_AUDIT，不得进入 M5 |

---

## 0. 审核前置说明（独立性、范围、握手）

### 0.1 审核对象与范围

- 对象：M1~M4 累积候选，HEAD `b2c9465`，工作树干净（staged/untracked/deleted/worktree_modified 全空）。
- 范围：12 个 src/migrations 目录，111 文件，全部 `implementation` 类别（无 Evidence 文件混入受审范围）。
- 不是只审 M4：M1（认证/密钥/迁移）、M2（密钥/协议/账本）、M3（凭证/并发）、M4（资金/迁移/并发/合同）均命中 SOP §3.3 高风险，且此前均为主 AI 自检（`Evidence/M1~M4` 各 README 执行者均标注主 AI），独立性不达标。本次为补独立性缺口。

### 0.2 只读合规

- 审核期间**未修改任何产品代码、测试、迁移、部署、PRD/TRD/计划/状态账本**。
- 唯一写动作：开审时按任务书 §2 命令重新生成 `candidate-lock.json`（任务书明确要求"审核 AI 开审时须重新生成以绑定当时 HEAD"）。git status 仅此一文件 M，产品代码零变更。
- 未执行任何 push/remote add/PR/对外上传（仓库无 remote，`git remote -v` 为空）。
- 跑命令产生的副作用：启 PoC 的 docker-compose（`persistent-gateway-postgres-1`/`redis-1`）、跑 Testcontainer（database/gateway/control-api 集成测试自带启停）——均为本地隔离容器，未触及共享/生产状态。

### 0.3 开审握手（如实记录偏差）

- 任务书要求按 SOP §3.4 开审握手（账本 `READY_FOR_FUNCTIONAL_AUDIT` → Planning 原子迁移 `FUNCTIONAL_AUDIT_IN_PROGRESS` + 递增 state_version）。
- **本次为纯本地中途审核**：账本 `next_action` 为 STAGE03-M1-M4-AUDIT，但**未执行 SOP §3.4 的原子状态迁移和 state_version 递增**（审核 AI 只读账本、不写账本）。这是任务书 §0 第 6 条"纯本地中途审核"模式下的已知简化，佳哥在任务书已授权。
- 影响：审核 AI 自标记 `runtime_state=FUNCTIONAL_AUDIT_IN_PROGRESS`，但未经账本原子确认。结论的有效性基于对象锁 STABLE + 真实运行，不基于账本握手。

### 0.4 实际独立性如实披露（重要）

- 任务书要求 R3 两道 Audit 最低 **I2**（I1 + 不同模型族/独立验证器/未进作者链的 Reviewer）。
- **本次实际独立性 = I1**：本审核会话是新会话、未参与 M1~M4 作者链、未读作者链过程聊天、只读冻结输入。但本会话模型为 GLM-5.2，**与主 AI 作者链同为 GLM 系列模型族**，不满足 I2"不同模型族"要求。
- 按 V1.4 规范 §2.5 + 附录 A"伪独立审计"反模式：同一模型族换会话仍为 I1，不得标记为 I2。
- 按 SOP §3.3，R3 实际独立性低于冻结等级时，Planning 须提高补偿控制、形成不低于风险最低要求的批准方案，或返回 NO_GO。**本结论在 I1 独立性下出具，佳哥须独立判断是否接受 I1 结论或要求 I2 复核**。

---

## 1. 审核执行概览

### 1.1 直接实测命令（A 级 Evidence）

全部在佳哥 Mac 本地真实执行，`export PATH="$PWD/.corepack-bin:$PATH"` 后从仓库根运行：

| 命令 | 退出码 | 实测结果 | 任务书预期 | 对照 |
| --- | --- | --- | --- | --- |
| 对象锁 generate（开审） | 0 | GENERATED + STABLE | 绑定开审 HEAD | ✓ HEAD=b2c9465 |
| 对象锁 verify（开审） | 0 | STABLE | STABLE | ✓ |
| `pnpm typecheck` | 0 | 11 包全 Done | 全绿 | ✓ |
| `pnpm lint`（--max-warnings=0） | 0 | 11 包全 Done | 全绿 | ✓ |
| `pnpm test` | 0 | domain **108** / gateway **61** / database **8** / control-api **23** / provider-adapters **35** / contracts 2 / config 4 / observability 4 = **245 passed, 0 fail/skip/todo** | domain 108 / gateway 61 / database 8 / control-api 23 / provider-adapters 35 | ✓ 数字逐项吻合 |
| `pnpm build` | 0 | 11 包全 Done（worker 有真实代码，web dist 产物生成） | 全绿 | ✓ |
| `pnpm evidence:canary` | 0 | postgres=0 / redis=0 / logs=0 / traces=0 / total=0 | canary 全 0 | ✓ |
| 对象锁 verify（结审） | 0 | STABLE | STABLE | ✓ |

### 1.2 PoC 回归（W01 基线要求，工程规则 §4.1）

- 命令：`cd V3/PoC/persistent-gateway && pnpm test`（node --test）。
- **首次执行挂起**：PoC 测试期望外部 docker-compose 已起 PG(55437)+Redis(56379)，但当时容器未起，node --test 在 PG 连接重试上挂起无 TAP 输出。
- **处置**：kill 挂起进程 → `docker compose up -d` → 等 healthy → 重跑。
- 重跑结果：**12/12 PASS**（exit 0），与任务书预期一致。
- 测试覆盖：PG/Redis 健康检查、三厂商凭证只从环境注入且脱敏、无效 Key 上游前拒绝、Models 北向合同、OpenAI Chat 持久化闭环、Anthropic Messages 持久化闭环、未启用端点 422、相同 request-id 幂等、元数据-only 落库、正文/Key/Session 明文 PG/Redis/日志/Trace 0 命中、Redis 清除后 PG 恢复、JSONL+OTel 仅白名单。
- **如实记录**：首次挂起是**环境前置缺失**（PoC 测试设计为依赖外部 compose，非测试自起容器），非产品缺陷。工程规则 §4.1 把 `docker compose up` 与 `pnpm test` 分列为独立步骤，符合设计。

### 1.3 Evidence 分级说明

- **A 级（直接实测）**：§1.1 全工程命令 + §1.2 PoC + 对象锁首尾核验。审核 AI 在当前候选 `b2c9465` 上直接执行。
- **B 级（候选绑定）**：未复用主 AI 的 B 级 Evidence；本次全工程命令已独立重跑，无需复用。
- **C 级（声明性）**：主 AI 各 milestone README（`Evidence/M1~M4/*/README.md`）为 C 级声明材料，**不单独支持关键验收 PASS**，仅作线索。审核 AI 据此定位代码后独立验证。
- **环境限制**：DEP-PROVIDER-CREDENTIALS 未解锁 → 三厂商真实 HTTP/SSE/鉴权头未回归（StubUpstream 模拟）；DEP-WINDOWS 未达成 → 无 Windows 实机。这两项不阻塞代码审核，但阻塞 M6/M7 上线签字（任务书 §5.6 已披露）。

---

## 2. 验收追踪矩阵（14 项 DoD 检查）

依据任务书 §3.2 + 各 milestone DoD + PRD FR + TRD 冻结条款。结论列：✓=满足 / △=部分满足（记 Finding）/ ✗=不满足（记 Finding）/ ○=环境限制（非产品缺陷）。

### M1（W02-W04）认证/主体/Key/资源

| 验收项 | 冻结依据 | 实现位置 | 正向测试 | 负向/边界测试 | 运行 Evidence | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| 管理员登录+Session+企业边界 | PRD FR-001；WT-02 | `control-api/src/auth/routes.ts` + `plugins/auth-guard.ts` + `admin-repository.ts` | w02 登录成功设 cookie | w02 密码错误 401 不设 cookie；未登录 401；登出后 session 失效 | A 级（test 23 全绿） | ✓ |
| Principal CRUD + 操作审计 | PRD FR-002；WT-04 | `principals/routes.ts` + `audit-repository.ts` | w02 principal create+审计 | w02 未登录 401 | A 级 | ✓（见 F-04 审计漏项） |
| 下游 Key 一次展示 + Pepper 摘要不可逆 | WT-09；工程规则 §5 | `keys/routes.ts` + `key-repository.ts` + `crypto.ts:digestApiKey` HMAC-SHA256(pepper) | w03 key 生成返回明文+warning | w03 重复生成 409；canary 明文 0 命中；digest 64 hex | A 级 | ✓（见 F-02 pepper fallback） |
| 旧 Key ≤5s 失效 | 工程规则 §5；TRD §16 行 879 | gateway `principal-auth.ts` 每请求查 DB 比对 digest（M1 无缓存） | — | — | 代码读确认 | ✓（M1 无缓存实际 <5s；W07 Redis 缓存后才是 5s 上界，见 F-06） |
| Provider/Resource/凭证加密 | PRD FR-004；WT-01 | `providers/routes.ts` + `provider-adapters/crypto.ts` AES-256-GCM（随机 nonce 12B + auth tag 16B + KEK 32B 校验） | w04 凭证加密落库 | w04 响应/列表不含明文；canary 明文 0 命中 | A 级 | ✓（见 F-02 KEK fallback、F-08 version 硬编码） |
| Unified Model/Route + Secret 不出 Web/日志 | WT-10 | `providers/routes.ts`；SecretValue `[REDACTED]` toString/toJSON | w04 列表只返指纹 | grep 日志零明文 | A 级 | ✓ |

### M2（W05-W08）北向合同/DeepSeek/账本闭环

| 验收项 | 冻结依据 | 实现位置 | 正向测试 | 负向/边界测试 | 运行 Evidence | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| Models/Chat/Messages DTO 冻结 | TRD §6；WT-14 | `routes/{models,chat,messages}.ts` | w05 models/chat/messages | w05 缺 model 400；422（responses/embeddings/count_tokens） | A 级（gateway 61） | ✓ |
| 422 能力拒绝（不静默降级） | TRD §6.3 行 426 | `routes/unsupported.ts` | — | w05 embeddings/responses 422 | A 级 | ✓ |
| request_id 贯穿 | TRD §5.7 行 348 | `plugins/request-id.ts` + 全链路落库 | w08 request_id 贯穿 | — | A 级 | ✓ |
| DeepSeek Adapter（鉴权/流式/usage 三维度/错误归一化/committed 边界） | TRD §7.1；WT-12 | `deepseek-adapter.ts` parseUsage 三维度（input/output/cache）+ classifyUpstreamError | w08 三维度 usage（cache 100） | StubUpstream 模拟 5 模式 | A 级（provider-adapters 35） | ✓（真实 HTTP 待 DEP 解锁，○） |
| 重复结算 0（dedup_key UNIQUE + UNIQUE(ai_request_id)） | TRD §5.7/§8；WT-11 | 迁移 `0008:58-64` usage_event.dedup_key UNIQUE + `0009:71` ledger_transaction UNIQUE(ai_request_id) + Repository ON CONFLICT DO NOTHING | w07 双 Attempt 一汇总 | w07 重复 ledger_transaction 0；重复 usage_event 0 | A 级（database 8） | ✓ |

### M3（W09-W12）三厂商/账号池/路由

| 验收项 | 冻结依据 | 实现位置 | 正向测试 | 负向/边界测试 | 运行 Evidence | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| 智谱/Kimi Adapter + SecretValue 脱敏 + CODING_PLAN 边界 | TRD §7.2/§7.3 | `zhipu/kimi-adapter.ts` + SecretValue | w09/w10 e2e | canary 0；api_cost=null（PACKAGE_INCLUDED） | A 级 | ✓（真实 HTTP 待 DEP，○） |
| 账号池状态机（凭证生命周期/熔断/冷却/半开） | TRD §5.4；WT-07/19 | `resource-lifecycle.ts` + `resource-pool-repository.ts` | w11 刷新/恢复 | w11 401 单资源隔离不波及同池；429 冷却→半开→恢复；3 次故障熔断；客户端错误不计数 | A 级 | ✓ |
| 多因子评分+稳定 tie-break+提交前切换/提交后不切换+route_candidate 可解释 | TRD §9；WT-13/18 | `routing-policy.ts`（static 0.4+health 0.4+affinity 0.2， resourceId 字典序 tie-break）+ real-pipeline 冻结 route_candidate | w12 Affinity 命中 | w12 A 429 committed=false→B 成功（双 Attempt）；STREAM_INTERRUPTED_AFTER_COMMIT 单 Attempt 不切换；无健康候选 503 不无账放行 | A 级 | ✓ |

### M4（W13-W17）计价/额度/预测/调度/对账

| 验收项 | 冻结依据 | 实现位置 | 正向测试 | 负向/边界测试 | 运行 Evidence | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| 计价 billing_rule 版本化+历史不重算+decimal 精度 | TRD §10；WT-05 | `billing-rule.ts`（decimal.js）+ 迁移 0011 | w13 智谱高峰×3/非高峰×2；Kimi 档位×3；API_PRICE cache 分项 | w13 规则版本冻结（历史不重算） | A 级 | ✓ |
| **额度：预占/结算+并发租约行锁+耗尽停止/allow_overage** | TRD §8.2；WT-06 | `quota-gate.ts` + `quota-gate-repository.ts`（forUpdate 行锁 + concurrency_lease） | w14 ALLOW 预占/结算校正 | w14 REJECT_EXHAUSTED；allow_overage→ALLOW_OVERAGE；并发达 limit 拒绝；过期回收 | **仓储层 A 级通过，但未接入 pipeline 热路径** | **△ 见 F-01** |
| 预测：多窗口速度+耗尽不晚于失效+不伪精确 | TRD §9.2；WT-15 | `supply-forecast.ts` computeForecast（now 注入） | w15 多窗口/耗尽/恢复/覆盖/可信度 | w15 数据不足 NOT_CALCULABLE；余额未知不生成耗尽日期 | A 级 | ✓ |
| 调度：等价组切换+限流/拒绝+反事实节省 NOT_CALCULABLE | TRD §9.1；WT-16/17 | `dispatch-policy.ts` + real-pipeline dispatch 接入 + 决策幂等落库 | w16 SWITCH/REJECT/RATE_LIMIT | w16 REJECT→403；RATE_LIMIT→429；CODING_PLAN 无 api_cost→NOT_CALCULABLE；API SWITCH saving 可计算 | A 级 | ✓（反事实简化见 F-05） |
| 对账：重复 0/丢失<0.1%+异常队列流转 | TRD 行 870-871 | `reconciliation.ts` + `reconciliation-repository.ts` + 迁移 0015 | w17 正常对账 PASS | w17 MISMATCH/REVIEW；异常队列 OPEN→RESOLVED 流转 | A 级 | ✓（5 类差异实扫 4 类，见 F-07） |

---

## 3. 八个审核维度

### 3.1 冻结范围与验收覆盖 — **FAIL**

- 14 项 DoD 中 13 项有实现+测试+运行 Evidence（A 级）。
- **M4 额度门禁**：能力（quota-gate + repository + 行锁 + 并发租约）已实现且有独立 w14 集成测试（7 项全绿），但**未接入 gateway 生产热路径**（real-pipeline 零处调用 quotaRepo；`RealPipelineDeps` 无 quotaRepo 字段；`deducted_quota` 写账本后不回写 `quota_counter`）。
  - W14 Evidence 行 33/49/79 明确承诺"pipeline 步骤 3b 前的插入点在 W16 经营调度时一并接入"；W16 Evidence 交付物清单（行 59）只接入 dispatch，**未接入 quota**；至 W17/M4 收口仍未接入。
  - 任务书 §5.2 将此列为"已知风险披露"，但 §3.2 要求审核 AI 独立判断是否影响 DoD。
  - **独立判断**：DoD 检查项"M4 额度：预占/结算+并发租约行锁+耗尽停止/allow_overage"的字面能力存在，但**在真实生产请求路径上不生效**。当前生产入口（main.ts）注入 stubPipeline，real-pipeline 仅测试装配；即便切到 real-pipeline，额度耗尽也不会 REJECT、并发无强制、计费扣减不回写额度。属"类和测试存在，但生产入口没有调用"反模式（V1.4 附录 A）。
- 反事实节省（W16）simplified：counterfactualCost = actualCost（real-pipeline:379-382），导致 dispatch_saving 恒为 0 或 NOT_CALCULABLE，真实非零节省需 W17 对账细化（未实现）。W16 Evidence 行 65 已披露为"不做项"，登记为 residual risk（F-05），不阻断。

### 3.2 功能、逻辑与协议正确性 — **FAIL**

- **账本汇总 token 聚合 bug**：`real-pipeline.ts:338-356` ledger_transaction 的 `total_input_tokens`/`total_output_tokens`/`total_cache_tokens` 用 `finalOutcome.usage`（最后一次 Attempt 的 usage），而 TRD §5.7 行 348 + §10.1 行 653 冻结条款明确要求"请求级总费用是全部账本明细之和，不只计算最终成功 Attempt"。
  - 双 Attempt failover 场景（如 w12：A 429 committed=false→B 成功），若 A 也产生 ESTIMATED usage（StubUpstream 在某些失败模式返回 output/2），汇总漏算 A 的 token。
  - 对账 SETTLEMENT_MISMATCH 扫描（reconciliation-repository.ts:267-278，比对 lt.total_input vs SUM(ll.raw_input)）**能抓到此偏差**，反向印证这是真实 bug 而非设计。
  - 违反冻结条款，记 F-03（P1）。
- **协议错误码不一致**：STREAM_INTERRUPTED_AFTER_COMMIT 在 `error-envelope.ts:50` 定义为 500/retryable=false，但 `real-pipeline.ts:433` 实际返回 502（未走统一 envelope）。客户端重试决策无一致依据。记 F-09（P2）。
- 其余协议语义（422 不静默降级、流式提交后不切换、route_candidate 冻结、dispatch_decision 不可覆盖）在测试中验证正确。

### 3.3 架构、权限与安全边界 — **PASS（有 P2 关注点）**

- 企业边界：enterprise_id 从 session 注入（`auth-guard.ts:34-38`），所有仓储 WHERE 带 enterprise_id 过滤。✓
- 权限负向：每路由 opt-in `requireAuth`，无 cookie/session 失效/账号停用 → 401/401/403。✓
- 凭证加密：AES-256-GCM（随机 nonce + auth tag + KEK 32B 校验），明文 canary 0 命中。✓
- Key 摘要：HMAC-SHA256(pepper)，明文 canary 0 命中。✓
- **P2 关注点**（F-02）：`GATEWAY_KEY_PEPPER`/`CREDENTIAL_KEK`/`COOKIE_SECRET` 三个 env 有硬编码 dev fallback（`server.ts:76,88,98` + `gateway/main.ts:12`）。生产忘配即等于无 pepper/无加密。一期单企业本地试点可接受，上线前必修。
- **P2 关注点**（F-04）：`model-routes`/`unified-models` 创建未落 operation_log；登录失败未落审计；`createRoute` 未校验 unified_model/provider_resource 跨实体归属（M1 单企业无法触发，多企业前必修）。
- **P2 关注点**（认证无全局默认拒绝钩子，逐路由 opt-in）：新增路由忘挂 requireAuth 即公开。建议全局 preHandler + 白名单。

### 3.4 并发、幂等、恢复与副作用 — **FAIL（因 F-01）**

- 账本幂等：dedup_key UNIQUE + UNIQUE(ai_request_id) + ON CONFLICT DO NOTHING，重复 request_id/重复 usage 不重复扣记。✓（w07 验证）
- 并发租约防穿透：QuotaGateRepository.acquireLease 用 provider_resource 行锁 + count 活跃租约 + 插入，limit>0 时安全。✓（w14 仓储层验证）
- **但并发租约未接入 pipeline**（F-01）：生产请求路径无并发上限强制，仅靠 W11 状态机软熔断。
- 资源释放：releaseLease/reclaimExpiredLeases 用 WHERE released_at IS NULL 幂等更新。✓
- 流式提交边界：committed=true 永不切换（real-pipeline:330 break）。✓ 但真实首字节检测未实现（StubUpstream 模拟，DEP 待解锁）。

### 3.5 数据、迁移与兼容性 — **PASS（有 P3 关注点）**

- 16 个迁移文件，schema 唯一变更入口。✓
- migration.integration.test.ts 验证 migrate→rollback→migrate round-trip（真实 PG Testcontainer）。✓
- UNIQUE 约束链完整：key_digest、dedup_key、ai_request_id（ledger_transaction + dispatch_decision）、token_hash、username 等。✓
- **P3 关注点**（F-10）：`ai_request_idempotency_idx`（0007:35-39）是 idempotency_key 单列唯一，未含 enterprise_id/principal_id。多租户下不同主体的相同 idempotency_key 会冲突。M1 单企业不触发，登记 residual。
- jsonb 列 JSON.stringify 序列化（billing_rule.days_of_week、dispatch match_days_of_week 等）。✓

### 3.6 测试覆盖与负向、边界场景 — **PASS（有 Evidence Gap）**

- 测试真实调用生产实现：domain 纯函数、database 真实 PG Testcontainer、gateway/control-api 真实 PG + 真实 schema + StubUpstream。✓
- mock 只隔离未解锁的真实上游（StubUpstream），未 mock 掉被审核心规则。✓
- 负向覆盖：401/403/429/422/400/503/流式提交后失败/重复幂等/资源隔离/熔断/冷却/耗尽/超额 均有对应测试。✓
- **Evidence Gap**（F-11）：pipeline 层无"同一 request_id 重复请求端到端幂等"测试（w07 只在 Repository 层测 ON CONFLICT；request-id 插件会复用客户端传入 id，pipeline createRequest 用 INSERT 无 ON CONFLICT，理论上重放同 id 触发主键冲突，未测）。
- **Evidence Gap**：真实凭证未回归（DEP-PROVIDER-CREDENTIALS），三厂商 Adapter 鉴权头/SSE 解析/真实 usage 仅 StubUpstream 验证形状。

### 3.7 真实生产路径与实际运行 — **FAIL（因 F-01 + F-12）**

- **生产入口 main.ts 注入 stubPipeline**（main.ts:13，注释明示"W05 stub pipeline"），**不是 createRealPipeline**。stubPipeline 不接上游、不写账本、固定 usage（11/7）。
- real-pipeline 仅在集成测试中被装配（w08~w16 e2e）。
- **影响**：当前候选若按 main.ts 启动，真实生产请求走 stub，**完全不落账本、不路由、不调度、不计费**。这是"正常路径能跑，但生产入口绕过真实实现"的反模式（V1.4 附录 A"fake 冒充端到端"）。
- 即便修复 main.ts 切到 real-pipeline，F-01（额度未接入）仍然存在。
- PoC（persistent-gateway）是独立 W01 基线验证，12/12 通过，但它**不是产品 gateway**（独立 mjs 项目，工程规则 §3 明确"PoC 只作 Evidence/行为对照，不是产品源码"）。
- 记 F-12（P1）：生产入口未接 real-pipeline，真实生产路径未验证。

### 3.8 Evidence 真实性、可复现性与对象同一性 — **PASS**

- 主 AI 各 milestone README 为 C 级声明材料，审核 AI 未直接采信"全绿"声明，全部独立重跑（§1.1 数字逐项吻合）。
- 对象锁首尾 STABLE，HEAD 不变（b2c9465），三组摘要一致。✓
- 工作树审核期间零产品代码变更（仅 candidate-lock.json 因开审重生成而 M）。✓
- 测试数量如实记录：domain 108 / gateway 61 / database 8 / control-api 23 / provider-adapters 35，0 skip/0 fail/0 todo。✓

---

## 4. Findings（首轮一次性冻结）

`finding_freeze_at`: 2026-07-27 21:20 (+08:00)。全部为首轮观察，未启动任何修复。

### P1（阻断 PASS）

#### F-01 · 额度门禁未接入 gateway 生产热路径（product_defect）

- **违反条款**：TRD §8.2 行 527-537（请求前额度预占 + 结束后结算 + allow_overage=false 不能因并发超过分配额度）；详细计划 W14 DoD（行 290）；任务书 §3.2 M4 额度检查项。
- **精确证据**：
  - `apps/gateway/src/pipeline/real-pipeline.ts` grep `quotaRepo|reserveQuota|settleQuota|acquireLease|releaseLease|QuotaGateRepository` → **零匹配**。
  - `RealPipelineDeps`（real-pipeline.ts:55-92）字段仅 `ledgerRepo/poolRepo/dispatchRepo?/caller/listCandidates/resolveAffinity?/resolveDispatchInput?/maxAttempts?`，**无 quotaRepo**。
  - `deducted_quota`（real-pipeline.ts:317/357）写入 ledger_line/ledger_transaction 后**不回写 quota_counter**。
  - `remainingQuotaRatio`（real-pipeline.ts:87/183/190）只作 dispatch_policy 输入，不用于额度门禁判定。
  - W14 Evidence 行 33/49/79 承诺 W16 接入；W16 Evidence 交付物清单未含 quota 接入；W17 收口仍未接入。
- **真实生产路径**：gateway 数据面请求 → real-pipeline → 计价写账本，但**额度耗尽不 REJECT、并发无上限强制、扣减不回写额度**。
- **影响与发生条件**：任何切换到 real-pipeline 的部署；主体额度可被绕过/超用，并发上限无强约束。资金风险。
- **为什么 P1**：冻结核心验收"M4 额度：预占/结算+并发租约行锁+耗尽停止"在真实生产路径不成立；命中资金/并发高风险；任务书 §3.2 明确要求验收此项并独立判断。
- **最小 required action**：在 real-pipeline 步骤 3b（评分选中后、Attempt 前）插入 `quotaRepo.reserveQuota` 预占 + `acquireLease` 并发租约；Attempt 结束后 `settleQuota` 结算校正 + `releaseLease`；耗尽时 REJECT 不无账放行。补 pipeline 层额度端到端集成测试（耗尽 REJECT、并发达限拒绝、超额 allow_overage、扣减回写 quota_counter）。
- **回归范围**：gateway w08~w16 全部 e2e（确认插入不破坏现有形状）+ 新增 pipeline 层额度测试。

#### F-03 · ledger_transaction token 聚合用 finalOutcome 而非明细之和（product_defect）

- **违反条款**：TRD §5.7 行 348（汇总金额等于全部明细之和）+ §10.1 行 653（请求级总费用是全部账本明细之和，不只计算最终成功 Attempt）；§5.7 行 354（失败/超时/取消/被切换的 Attempt 只要产生可证明用量，就不能被最终成功 Attempt 覆盖）。
- **精确证据**：`real-pipeline.ts:338-356`，`total_input_tokens = BigInt(finalOutcome.usage.input)`、`total_output_tokens = BigInt(finalOutcome.usage.output)`、`total_cache_tokens = BigInt(finalOutcome.usage.cache)` —— 用最后一次 Attempt 的 usage，非 ledger_line 聚合。
- **真实生产路径**：双 Attempt failover（A 失败 committed=false → B 成功），若 A 产生 ESTIMATED usage，ledger_transaction 漏算 A 的 token；对账 SETTLEMENT_MISMATCH 扫描（reconciliation-repository.ts:267-278）能检测到此偏差。
- **影响与发生条件**：任何多 Attempt failover 且前置 Attempt 产生用量的请求；账本汇总与明细不一致，违反账本完整性冻结条款。
- **为什么 P1**：账本完整性是资金/账本高风险核心不变量；冻结条款显式禁止"只计算最终成功 Attempt"。
- **最小 required action**：ledger_transaction 汇总改为对全部 ledger_line 聚合（SUM(raw_input_tokens) 等），或显式记录"仅最终成功 Attempt"的设计豁免并更新 TRD。补双 Attempt 均产生用量时汇总=明细之和的回归测试。
- **回归范围**：gateway w07/w08/w12（双 Attempt 场景）+ database w17（SETTLEMENT_MISMATCH）。

#### F-12 · 生产入口 main.ts 注入 stubPipeline，真实生产路径未验证（product_defect / blocking_evidence_gap）

- **违反条款**：V1.4 规范 §6.7（真实生产路径与实际运行：后端/API 最低真实运行证明=真实入口、协议请求、鉴权、持久化、副作用）+ 附录 A"fake 冒充端到端"反模式；工程规则 §9（禁止"将计划、PoC、模拟页面或文档声明当作生产完成"）。
- **精确证据**：`apps/gateway/src/main.ts:13` `buildGateway(db, pepper, stubPipeline, ...)`，注释 `gateway listening (W05 stub pipeline)`。stubPipeline（stub-pipeline.ts）固定 usage（11/7）、SSE 流式 chunk、**不接上游、不写账本**。
- **真实生产路径**：当前候选按 main.ts 启动 → 生产请求走 stub → 不落账本/不路由/不调度/不计费。real-pipeline 仅测试装配。
- **影响与发生条件**：任何按 main.ts 的部署；生产数据面与测试验证的数据面不是同一实现。
- **为什么 P1**：冻结候选的"真实生产路径"未验证；功能审计 §6.7 要求真实入口运行。PoC（persistent-gateway）是独立 W01 基线项目，非产品 gateway（工程规则 §3 明确）。
- **最小 required action**：main.ts 切换为 createRealPipeline（注入真实 deps：ledgerRepo/poolRepo/caller/dispatchRepo/quotaRepo），并提供 caller 的真实实现路径（或显式标注待 DEP-PROVIDER-CREDENTIALS 解锁）。若 stub 是有意保留的开发态，须在 Evidence 显式说明启动模式切换边界，且生产部署 contract 必须用 real-pipeline。
- **回归范围**：gateway 启动冒烟 + 至少一个真实入口 e2e（models/chat/messages 走 real-pipeline 完整闭环）。

### P2（登记 Owner/缓解/review_by）

#### F-02 · 三个敏感 env 有硬编码 dev fallback（security_defect）

- **违反条款**：工程规则 §5（真实 Secret 只从进程环境/Docker Secret/Secret Manager 注入）；V1.4 §2.4（密钥未进入代码）。
- **精确证据**：`control-api/server.ts:76` `GATEWAY_KEY_PEPPER ?? "dev-only-pepper"`、`:88` `CREDENTIAL_KEK ?? "ZGV2LW9ubHkta2VrLXJlcGxhY2UtaW4tcGlsb3QAAAA="`、`:98` `COOKIE_SECRET ?? ...`；`gateway/main.ts:12` 同样 `GATEWAY_KEY_PEPPER ?? "dev-only-pepper"`。
- **影响**：生产忘配 env → pepper/KEK 用公开值 → DB 泄漏即等于明文泄漏/Key 可离线爆破。
- **处置建议**：env 缺失时启动失败（throw），不提供 fallback；或仅 dev profile 允许 fallback 并强制日志警告。
- **review_by**：M5 前或生产部署前。

#### F-04 · 审计漏项 + 跨实体归属未校验（security_defect）

- **违反条款**：PRD FR-020（请求级审计）；工程规则 §7（写操作校验企业边界）。
- **精确证据**：
  - `providers/routes.ts:143-155`（POST /unified-models）、`:158-171`（POST /model-routes）无 `auditRepo.write`。
  - `auth/routes.ts:51-58` 登录失败只记内存限速，未落 operation_log。
  - `createRoute`（provider-repository.ts:145-165）INSERT 带本企业 enterprise_id，但未校验 unified_model_id/provider_resource_id 是否属于本企业（多企业越权/数据污染）。
- **影响**：M1 单企业不触发跨实体越权；审计漏项影响可追溯性。
- **处置建议**：补 auditRepo.write；登录失败落审计（username+IP+原因）；createRoute/createResource 前校验跨实体归属。
- **review_by**：多企业上线前。

#### F-05 · 反事实节省简化为 counterfactual=actual（residual_risk）

- **违反条款**：TRD §9.1 行 628-632（反事实基线必须来自同一请求时点、已发布策略和可调用候选）。
- **精确证据**：`real-pipeline.ts:379-382`，SWITCH 时 `counterfactualCost = actualCost`（注释明示"W16 简化：等价组同档位，基线≈目标成本；真实需按原 winner 规则重算，W17 对账细化"）。
- **影响**：dispatch_saving 恒为 0 或 NOT_CALCULABLE，真实非零节省值不产生。W16 Evidence 行 65 已披露为"不做项"。
- **处置建议**：W17 对账后续细化按原 winner billing_rule 重算 counterfactual；或登记为接受残余风险。
- **review_by**：M4 后续细化或 Planning 接受残余。

#### F-06 · Key ≤5s 失效依赖未来 Redis 缓存主动失效（residual_risk）

- **违反条款**：工程规则 §5；TRD §16 行 879（Key 停用和重置在 5 秒内全局生效）。
- **精确证据**：M1 gateway `principal-auth.ts` 每请求查 DB（无缓存，实际 <5s）。但 `keys/routes.ts:98` 文案承诺"5 秒内全局失效"，W07 将引入 Redis 5s TTL 缓存（principal-auth.ts:6 注释）。引入缓存后，reset/disable 无主动失效机制（无 pub/sub invalidate），将真实存在 5s 窗口。
- **影响**：W07 后吊销窗口 5s；当前 M1 无缓存更严格。
- **处置建议**：W07 引入缓存时同步实现主动失效（reset/disable 触发缓存 invalidate）。
- **review_by**：W07。

#### F-09 · STREAM_INTERRUPTED_AFTER_COMMIT 状态码不一致（product_defect）

- **违反条款**：TRD §6.3 行 427（错误映射必须保留语义）；V1.4 §6.2（协议、事件顺序、错误码符合合同）。
- **精确证据**：`error-envelope.ts:50` 定义 500/retryable=false；`real-pipeline.ts:433` 实际返回 502（内联响应绕过统一 envelope）。
- **影响**：客户端重试决策无一致依据。
- **处置建议**：统一为同一状态码/retryable；pipeline 走 sendErrorEnvelope 而非内联。
- **review_by**：M5 前。

#### F-13 · Argon2id 密码无 pepper + digestSessionToken/credentialFingerprint 硬编码盐（security_defect）

- **违反条款**：工程规则 §5（密钥管理一致性）。
- **精确证据**：`auth/password.ts:18-20` argon2.hash 无 pepper；`crypto.ts:49-51` digestSessionToken 用硬编码 `"session-token-salt"`；`crypto.ts:105-110` credentialFingerprint 用硬编码 `"credential-fingerprint-salt"`。与 Key digest 用环境 pepper 的设计不对称。
- **影响**：DB 泄漏后密码可离线爆破；session token/credential 指纹可被任何人复算。
- **处置建议**：密码加 pepper；session/fingerprint 盐改环境注入。
- **review_by**：M5 前或生产部署前。

### P3（低风险改进，不阻断）

#### F-07 · 对账声明 5 类差异，实际扫描 4 类（product_defect / 文档实现偏差）

- **违反条款**：任务书 §3.2 M4 对账（异常队列流转）+ W17 Evidence 行 32 声明"5 类差异"。
- **精确证据**：`reconciliation-repository.ts:302-303` MISSING_USAGE 显式跳过（注释"避免误报无消耗失败 attempt"）。DiscrepancyRecord 枚举含 5 类，实扫 4 类。
- **影响**：attempt 有消耗缺 usage_event 的差异检测不到。W17 Evidence 行 34 已披露。
- **处置建议**：实现 MISSING_USAGE 扫描（带"attempt 有消耗"前置条件）或更新枚举/文档为 4 类。
- **review_by**：M4 后续。

#### F-08 · credential_version 硬编码为 1，无 KEK 轮换路径（residual_risk）

- **精确证据**：`provider-repository.ts:94` `credential_version: 1` 硬编码，全仓无递增/轮换逻辑。
- **影响**：未来 KEK 轮换/密文 re-wrap 无版本演进路径。
- **处置建议**：规划 KEK 轮换流程时补 version 递增。
- **review_by**：KEK 轮换需求出现时。

#### F-10 · ai_request_idempotency_idx 单列唯一，多租户可能冲突（residual_risk）

- **精确证据**：迁移 `0007:35-39` idempotency_key 单列唯一，未含 enterprise_id/principal_id。
- **影响**：多企业下不同主体相同 idempotency_key 冲突。M1 单企业不触发。
- **处置建议**：改复合唯一 (enterprise_id, principal_id, idempotency_key) 或文档化单企业约束。
- **review_by**：多企业上线前。

#### F-11 · pipeline 层无"同一 request_id 重复请求端到端幂等"测试（blocking_evidence_gap 局部）

- **精确证据**：w07 只在 Repository 层测 ON CONFLICT 幂等；request-id 插件（plugins/request-id.ts:22）复用客户端传入 id；pipeline createRequest（real-pipeline.ts:101-110）用 INSERT 无 ON CONFLICT。
- **影响**：客户端重放同一 request_id 的端到端行为未验证（理论上 ai_request 主键冲突）。
- **处置建议**：补 pipeline 层重放测试。
- **review_by**：M5 前。

### Finding 统计

- `finding_freeze_complete`: true（首轮一次性冻结，未启动修复）
- `p0_count`: 0
- `p1_count`: 3（F-01、F-03、F-12）
- `p2_count`: 6（F-02、F-04、F-05、F-06、F-09、F-13）
- `p3_count`: 4（F-07、F-08、F-10、F-11）
- `evidence_gap_count`: 1（F-11 局部；DEP-PROVIDER-CREDENTIALS 为已披露环境限制，不计 Gap）
- `scope_conflicts`: 0
- `planning_or_user_decisions_required`: F-05（反事实简化是否接受残余）、F-01（额度接入是否扩大 W14 DoD 范围）

---

## 5. 预算与收敛

- 本次任务书未冻结显式收敛预算实例（墙钟/轮次/资源上限）。审核按"首轮一次性汇总"原则完成全部冻结矩阵后冻结 Findings。
- 实际消耗：约 1.5 小时墙钟（开审握手 + 全工程命令 + PoC + 代码深度阅读 + 8 维矩阵 + 报告）。
- `stop_reason`: 无（首轮完整审完，非预算命中停止）。

---

## 6. Decision

### 6.1 结论：**FAIL**

依据 V1.4 规范 §9.2，以下任一情况必须 FAIL——本次命中：

1. **存在产品 P1**：F-01（额度门禁未接入生产热路径）、F-03（账本汇总 token 聚合 bug）、F-12（生产入口注入 stubPipeline，真实生产路径未验证）。
2. **冻结核心验收无法在真实生产路径追踪**：M4 额度 DoD 在 real-pipeline 不生效（F-01）；账本汇总完整性条款违反（F-03）。
3. **真实生产路径没有验证**：main.ts 注入 stubPipeline，生产数据面与测试数据面非同一实现（F-12）。

### 6.2 未达 PASS 的具体缺口

V1.4 §9.1 PASS 必要条件中未满足项：
- ❌ 八维矩阵均为 PASS：§3.1/§3.2/§3.4/§3.7 为 FAIL
- ❌ 产品 P1 为 0：有 3 个 P1
- ❌ 真实生产路径获得 A 级 Evidence：main.ts 走 stub，real-pipeline 仅测试装配

### 6.3 实际独立性披露（再次强调）

- **实际独立性 = I1**（同模型族 GLM，换会话），低于 R3 要求的 I2。
- 佳哥须独立判断：(a) 接受 I1 结论并交主 AI 整改；(b) 要求 I2（不同模型族）复核后再整改。
- 本结论在 I1 下的强度：对象锁 STABLE + 命令真实运行 + 代码逐行核对，技术证据扎实；但模型族偏见风险未消除。

### 6.4 回传

- `decision`: FAIL
- `audit_round`: 1
- `reviewed_object`: HEAD `b2c94655bfc9e24a533cf8c595df2062082eeed7`，12 路径 111 文件
- `start_lock_verification`: STABLE（exit 0）
- `end_lock_verification`: STABLE（exit 0）
- `findings_summary`: 0 P0 / 3 P1 / 6 P2 / 4 P3 / 1 Evidence Gap
- `residual_risks`: F-05、F-06、F-08、F-10（均已登记 Owner/review_by）
- `requested_next_action`: 佳哥完成 Finding Impact Triage → 决定是否创建精准整改 Execution（优先 F-01/F-03/F-12）→ 主 AI 整改 → 新候选重锁 → 有界功能复审（旧 FAIL 不自动覆盖）
- `requested_state_transition`: 维持 `READY_FOR_FUNCTIONAL_AUDIT`；**PASS 前不得进入 CODE_AUDIT，不得进入 M5**
- `prohibited_next_action`: 主 AI 不得在整改前自行进入代码质量审计；不得 push/remote/PR；审核 AI 不整改、不下发、不推进
- `budget_status`: 未冻结显式预算；首轮完整完成，非 BUDGET_HIT

---

## 7. 做得好的地方（如实记录）

为避免 FAIL 结论遮蔽，记录候选的扎实之处：
- **领域层纯净**：domain 仅依赖 decimal.js，全部计价/额度/预测/调度/对账/路由纯函数，时钟全注入（now/attemptStartedAt/epochMs），100% 可回放，无墙钟读取。
- **依赖分层正确**：domain 无 DB/HTTP/IO；database 无 gateway/apps 反向依赖；gateway 是叶子数据面。无循环依赖。
- **账本幂等硬保证**：dedup_key UNIQUE + UNIQUE(ai_request_id) + ON CONFLICT DO NOTHING，w07 双 Attempt 双明细 + 唯一汇总测试。
- **资源池状态机完整**：401 单资源隔离不波及同池、429 冷却→半开→恢复、3 次故障熔断、客户端错误不计数。
- **凭证加密正确**：AES-256-GCM 随机 nonce + auth tag + KEK 32B 校验；SecretValue `[REDACTED]` toString/toJSON；明文 canary 测试硬门禁。
- **测试真实**：domain 纯函数、database/gateway/control-api 真实 PG Testcontainer + 真实 schema，mock 只隔离未解锁上游。
- **工程命令全绿且数字吻合**：typecheck/lint/test/build/canary 全 exit 0，测试数 245 与声明逐项一致。
- **PoC 12/12**：W01 基线持久化闭环验证完整。

这些是整改 P1 后候选能快速达到 PASS 的基础。

---

## 附录：审核执行日志摘要

| 步骤 | 命令/动作 | 结果 |
| --- | --- | --- |
| 1 | 对象锁 generate + verify | STABLE，HEAD=b2c9465 |
| 2 | pnpm typecheck | exit 0，11 包 Done |
| 3 | pnpm lint | exit 0，11 包 Done |
| 4 | pnpm test | exit 0，245 passed |
| 5 | pnpm build | exit 0，11 包 Done |
| 6 | pnpm evidence:canary | exit 0，total=0 |
| 7 | PoC pnpm test（首次） | 挂起（容器未起），kill |
| 8 | docker compose up -d + 等 healthy | 2 容器 healthy |
| 9 | PoC pnpm test（重跑） | exit 0，12/12 pass |
| 10 | 代码深度阅读（domain/database/gateway/control-api/provider-adapters） | 证据采集 |
| 11 | 8 维矩阵 + 14 DoD 追踪 | 3 P1 / 6 P2 / 4 P3 |
| 12 | 对象锁 verify（结审） | STABLE，HEAD 不变 |

报告完成。回传佳哥。

# M1~M4 累积双审任务书（功能与验收 Audit + 代码质量 Audit）

| 项目 | 内容 |
| --- | --- |
| 项目 | 仟流智算 v0.3 |
| 审核类型 | 高风险/关键基线两道 Audit（SOP `03-开发执行-v1.0.md` §3.3） |
| 审核对象 | **M1~M4 累积候选**（W01 工程基线 ~ W17/M4 收口，全部产品代码） |
| 任务书版本 | v2（2026-07-27，由 v1 的 M4-only 扩为 M1~M4 累积） |
| 任务发布人 | 佳哥（Owner） |
| 主 AI（作者链） | 佳哥（AI 执行，本会话）—— **不得作为本审核的 Reviewer** |
| 回传位置 | 报告写入 `V3/Evidence/M4/audit/`，佳哥收 Findings 交主 AI 整改 |

---

## 0. 审核独立性要求（开审前必读）

SOP `03-开发执行-v1.0.md` §3 行 58-69 + `双审核参考-v1.4/00-使用说明.md` 行 40-47：

1. **审核 AI 必须是独立会话**，未参与 M1~M4 作者链。本任务书由主 AI 会话准备，**主 AI 不得执行本审核**。
2. **审核 AI 必须能读取真实冻结候选**（Git commit + 工作树）。只看主 AI 的说明、截图或测试摘要，**不构成正式 Audit**。
3. 审核 AI **只输出报告**，不修改代码、计划、候选锁或状态账本。
4. 两道 Audit 基于**同一冻结对象**（§1 的 path 集 + 对象锁）。
5. **第一道（功能验收）PASS 后，第二道（代码质量）才能开审**。
6. **本次为纯本地中途审核**：仓库无远程（`git remote` 为空），所有 commit/对象锁/审核报告只在本地。**严禁 push、remote add、创建 PR 或任何对外上传**。审核 AI 只读本地工作树 + 跑本地命令 + 写本地报告。

---

## 1. 为什么是 M1~M4 累积审（不是只审 M4）

### 1.1 M1~M3 均命中 SOP §3.3 高风险，但此前只做了主 AI 自检

| 里程碑 | 工作包 | 命中的 §3.3 高风险条件 | 此前审核 |
| --- | --- | --- | --- |
| M1 | W02-W04 | §3.3-1 认证/授权/密钥/隐私（管理员登录+Key+Pepper+凭证加密）；§3.3-2 数据迁移（7 张表） | ❌ 仅主 AI 自检（`Evidence/M1/20260727-1230-m1-audit`，执行者标注"主 AI"） |
| M2 | W05-W08 | §3.3-1 密钥（SecretValue）；§3.3-3 协议（北向合同+SSE）；§3.3-4 数据库公共合同（账本 ledger） | ❌ 仅主 AI 自检（`Evidence/M2/20260727-1430-m2-audit`） |
| M3 | W09-W12 | §3.3-1 凭证（三厂商 Adapter）；§3.3-5 并发/容灾（账号池状态机/熔断/冷却/半开） | ❌ 仅主 AI 自检（`Evidence/M3/W09~W12`） |
| M4 | W13-W17 | §3.3-1 资金；§3.3-2 迁移；§3.3-4 公共合同；§3.3-5 并发 | ✅ 本任务书 |

state YAML 行 51 已记录风险："三角色合一情形，业务验证独立性弱，需依赖 Audit 强度与独立会话审核"。本次累积审正是补此缺口。

### 1.2 累积审的 SOP 依据

SOP §3.3 审核对象是"当前执行对象"，不强制单个里程碑。M1~M3 代码是 M4 的依赖基线（M4 的计价/额度/调度/对账都建立在 M1 主体、M2 账本、M3 路由之上），累积审最能发现跨层问题。

---

## 2. 冻结候选对象

| 项 | 值 |
| --- | --- |
| Git 仓库根 | `/Users/mac/Projects/仟流智算` |
| 审核范围 | **全部产品代码**（W01 工程基线 ~ M4 收口） |
| 选择模式 | 显式 path（`--paths-file`），覆盖 12 个 src/migrations 目录 |
| 路径清单 | `apps/{control-api,gateway,worker,web}/src` + `packages/{config,contracts,database,domain,observability,provider-adapters,testing}/src` + `packages/database/migrations` |
| 对象锁 manifest | `V3/Evidence/M4/audit/candidate-lock.json`（主 AI 预生成；**审核 AI 开审时须重新生成以绑定当时 HEAD**） |
| 对象锁核验 | 开审/结审必须 `candidate_lock.py verify` 输出 `STABLE`（退出码 0） |

**对象锁生成命令**（审核 AI 开审第一步执行）：
```bash
cat > /tmp/m1-m4-paths.txt << 'EOF'
apps/control-api/src
apps/gateway/src
apps/worker/src
apps/web/src
packages/config/src
packages/contracts/src
packages/database/src
packages/database/migrations
packages/domain/src
packages/observability/src
packages/provider-adapters/src
packages/testing/src
EOF
python3 "/Users/mac/Documents/AI专区/仟流 规范与体系/编程规范/仟流AI开发SOP/双审核参考-v1.4/tools/candidate_lock.py" generate \
  --root "/Users/mac/Projects/仟流智算" \
  --paths-file /tmp/m1-m4-paths.txt \
  --evidence-prefix V3/Evidence \
  --output "/Users/mac/Projects/仟流智算/V3/Evidence/M4/audit/candidate-lock.json"
```

**完整 commit 历史**（`git log --oneline --reverse`，审核可追溯）：
`dd33453`(W01基线) → `1333e98`(W01 evidence) → `501fc83`(M1) → `dcf92bd`(M2) → `ce88aef`~`fe05b9d`(M3) → `294688a`~`a93489d`(M4)

---

## 3. 第一道：功能与验收 Audit

**审核尺子**：`双审核参考-v1.4/AI阶段功能与验收审计规范-通用版-v1.4.md`
**核心问题**（规范 §1.1）：冻结候选是否真实、完整满足已冻结的阶段目标、技术合同、固定验收、安全边界和生产运行要求？

### 3.1 必须实测的运行路径

审核 AI 必须在佳哥 Mac 终端**真实运行**（SOP 要求真实路径 Evidence）：

```bash
export PATH="$PWD/.corepack-bin:$PATH"
# 0. 对象锁开审核验（必须 STABLE）
python3 ".../candidate_lock.py" verify --manifest V3/Evidence/M4/audit/candidate-lock.json

# 1. 全工程命令（独立复测，不信任主 AI Evidence 的"全绿"声明）
pnpm typecheck && pnpm lint && pnpm test && pnpm build && pnpm evidence:canary

# 2. PoC 回归（W01 基线要求，工程规则 §4.1）
cd V3/PoC/persistent-gateway && pnpm test && cd -
```

预期：domain 108、gateway 61、database 8、control-api 23、provider-adapters 35、PoC 12/12、canary 全 0。
**若数字不符，记为 Finding（主 AI Evidence 可能失真）。**

### 3.2 功能验收检查项（按规范 §1-§5 + 各里程碑 DoD）

| 域 | 检查项 | 依据 |
| --- | --- | --- |
| **M1 认证/主体** | 管理员登录+Session+企业边界；Principal CRUD；操作审计；权限负向 | PRD FR-001/002；WT-02/04 |
| **M1 Key/Grant** | 下游 Key 一次展示；Pepper 摘要不可逆；明文扫描 0；旧 Key≤5s 失效 | WT-09；工程规则 §5 |
| **M1 资源/路由** | Provider/Resource/凭证加密；Unified Model/Route；Secret 不出 Web/日志 | WT-01/10 |
| **M2 北向合同** | Models/Chat/Messages DTO 冻结；422 能力拒绝；request_id 贯穿 | TRD §6；WT-14 |
| **M2 DeepSeek** | Adapter 鉴权/流式/usage 三维度/错误归一化/committed 边界 | TRD §7.1；WT-12 |
| **M2 账本闭环** | 重复结算 0（dedup_key UNIQUE + UNIQUE(ai_request_id)）；正文 canary 0 | TRD §5.7/§8；WT-11 |
| **M3 三厂商** | 智谱/Kimi Adapter；SecretValue 脱敏；CODING_PLAN 边界 | TRD §7.2/§7.3 |
| **M3 账号池** | 凭证生命周期状态机；熔断/冷却/半开；单资源隔离 | TRD §5.4；WT-07/19 |
| **M3 路由** | 多因子评分+稳定 tie-break；提交前切换/提交后不切换；route_candidate 可解释 | TRD §9；WT-13/18 |
| **M4 计价** | billing_rule 版本化+历史不重算+decimal 精度 | TRD §10；WT-05 |
| **M4 额度** | 预占/结算+并发租约行锁+耗尽停止/allow_overage | TRD §8.2；WT-06 |
| **M4 预测** | 多窗口速度+耗尽不晚于失效+不伪精确 | TRD §9.2；WT-15 |
| **M4 调度** | 等价组切换+限流/拒绝+反事实节省 NOT_CALCULABLE | TRD §9.1；WT-16/17 |
| **M4 对账** | 重复 0/丢失<0.1%+异常队列流转 | TRD 行 870-871 |

### 3.3 第一道结论

输出 `PASS / FAIL / BUDGET_HIT / OBJECT_DRIFT`。PASS 后才能开第二道。

---

## 4. 第二道：代码质量 Audit

**审核尺子**：`双审核参考-v1.4/AI编码工程规范-通用版-v1.4.md` + `AI代码质量审计模板-通用版-v1.4.md`
**风险等级**：**R3**（命中资金/认证/密钥/迁移/并发/公共合同）
**前提**：第一道功能 Audit 已 PASS。

### 4.1 V1.4 代码阅读矩阵（模板 §2，逐项 PASS/FAIL/N/A + 证据）

| 维度 | 重点（M1~M4 特有） |
| --- | --- |
| 2.1 结构职责 | domain 纯函数 vs 仓储副作用分层；real-pipeline（420 行）是否职责过载；contracts/domain 边界 |
| 2.2 状态资源 | 并发租约/事务/连接清理；QuotaGateRepository 行锁正确性；Session/Key 生命周期 |
| 2.3 依赖架构 | domain 不依赖 DB；仓储不依赖 gateway；provider-adapters 不依赖 gateway；无循环依赖 |
| 2.4 配置安全 | Pepper/KEK 不入库；凭证 AES-256-GCM 密文；jsonb JSON.stringify；无 Secret 泄漏日志 |
| 2.5 类型错误 | `as never`/`as unknown as`（jsonb/SQL 绕过）；无 `any` 扩散；错误不吞 |
| 2.6 注释 | why/不变量/安全/并发/幂等边界；非逐行复述 |
| 2.7 测试 | 纯函数时钟注入可回放；关键分支/异常/状态转换断言；无 mock 掉核心规则 |

### 4.2 R3 质量门禁（模板 §3）

| gate | 项目实际能力 | 审核要求 |
| --- | --- | --- |
| typecheck/lint/build | `pnpm typecheck/lint/build`（11 包） | 必须 PASS |
| test | `pnpm test`（domain 108 + gateway 61 + database 8 + control-api 23 + provider-adapters 35） | 必须 PASS |
| 覆盖率 | **项目未配置覆盖率工具**（无 vitest coverage / c8） | 记为 Evidence Gap，不写 N/A（规范 §8.8） |
| 复杂度/CRAP | **未配置** | 记 Gap；人工核查 real-pipeline / reconciliation-repository / quota-gate-repository |
| 变异测试 | **未配置** | R3 核心规则（计价/额度/调度/对账/Key 摘要/凭证加密）原则上必须；记 Gap 或说明 |
| 依赖检查 | 全部依赖（无 M1~M4 新增；decimal.js 在 W13） | 查漏洞/许可证 |

**重要**：覆盖率/复杂度/变异测试工具项目未配置。按规范 §8.8 + §8.4，不能写成 N/A 或 PASS，必须记为 **Evidence Gap**，由佳哥决定是否补配置或接受例外。

### 4.3 第二道结论

PASS 必要条件（模板 §6.1）：适用矩阵完成 + P0/P1=0 + 阻断性 Gap=0 + 对象锁 STABLE + 完整代码阅读。

---

## 5. 已知风险与 Evidence Gap（主 AI 主动披露）

1. **M1~M4 Evidence 均为主 AI 自检**：独立性弱（本审核正补此缺口）。审核 AI 应独立复测全工程命令，不信任"全绿"声明。
2. **额度门禁未接入 pipeline 热路径**：quotaRepo 已就绪，但 real-pipeline 评分选中后未插入预占/结算校正（W16 Handoff §6）。审核应判断是否影响 M4 DoD。
3. **真实凭证未回归**：DEP-PROVIDER-CREDENTIALS 未解锁，三厂商 Adapter/计价/调度形状经 StubUpstream 验证，真实厂商回归待佳哥跑。
4. **对账反事实基线简化**：W16 dispatch SWITCH 的 counterfactual 简化为≈目标成本，精细重算在对账后续细化。
5. **质量门禁工具未配置**：覆盖率/复杂度/变异测试工具缺失（见 §4.2）。
6. **W22 Windows 实机 / DEP-DEPLOYMENT 未达成**：不阻塞代码审核，但阻塞 M6/M7 上线签字。

---

## 6. 报告格式与回传

### 6.1 报告产出

审核 AI 产出两份报告，写入 `V3/Evidence/M4/audit/`：

1. `M1-M4-功能与验收审计报告-<审核AI标识>-<日期>.md`（第一道）
2. `M1-M4-代码质量审计报告-<审核AI标识>-<日期>.md`（第二道，第一道 PASS 后）

报告按对应 V1.4 模板的章节结构填写（元数据/Contract/检查矩阵/Findings/Decision/回传）。

### 6.2 Findings 处理

- Finding 至少含：违反条款 + 精确文件/位置 + 影响冻结范围 + 最小处置建议
- P0/P1 阻断 PASS；P2/P3 登记 Owner/缓解/复核时间
- 报告回传佳哥，佳哥交主 AI 整改 → 新候选 → 按影响范围复审（旧 PASS 不自动覆盖）

### 6.3 结审

结审前必须再次 `candidate_lock.py verify`，输出 `STABLE` 才有效；若 `OBJECT_DRIFT`，当前结论无效，停止回传。

---

## 7. 权威参考文件索引

| 用途 | 路径 |
| --- | --- |
| SOP 主文件 | `/Users/mac/Documents/AI专区/.../仟流AI开发SOP/仟流AI开发SOP-v1.0.md` |
| 开发执行 SOP（审核触发/分级） | `.../仟流AI开发SOP/03-开发执行-v1.0.md` |
| 双审使用说明 | `.../双审核参考-v1.4/00-使用说明.md` |
| 第一道尺子 | `.../双审核参考-v1.4/AI阶段功能与验收审计规范-通用版-v1.4.md` |
| 第二道尺子 | `.../双审核参考-v1.4/AI编码工程规范-通用版-v1.4.md` + `AI代码质量审计模板-通用版-v1.4.md` |
| 对象锁工具 | `.../双审核参考-v1.4/tools/candidate_lock.py` |
| 各里程碑 Evidence | `V3/Evidence/M1/` `M2/` `M3/` `M4/`（主 AI 自检，供参考但不替代独立审） |
| 项目工程规则 | `V3/仟流智算-项目工程规则-v0.3.md` |
| PRD / TRD | `V3/仟流智算-产品需求文档-v0.3.md` / `V3/仟流智算-技术需求文档-v0.3.md` |
| 阶段状态 | `V3/仟流智算-stage-state-v0.3.yaml` |

---

## 8. 审核轮次记录（动态追加）

### round 1（功能验收，独立会话 zcode-glm52）→ FAIL

报告：`M1-M4-功能与验收审计报告-zcode-glm52独立会话-20260727.md`
Findings：F-01（额度门禁未接入热路径，P1）、F-03（ledger 聚合用 finalOutcome 非 SUM，P1）、F-12（main.ts 注入 stubPipeline，P1）、F-02（敏感 env dev fallback，P2）。

### 主 AI 整改 → F-01/F-03/F-12/F-02

报告：`M1-M4-整改报告-F01F03F12F02-主AI整改-20260727.md`
product_code_head：`b7bc040`（单 commit，10 文件 +664/-39）。

### round 2（有界功能复审，独立会话 zcode-glm52）→ FAIL（REMAINING）

报告：`M1-M4-有界功能复审-round2-zcode-glm52独立会话-20260727.md`
判定：F-01 **CLOSED**、F-03 **CLOSED**、F-02 **CLOSED**；F-12 主体 CLOSED 但暴露新 blocker **R2-N1**（生产 listCandidates 返空 principalId → CODING_PLAN 恒 REJECT_NO_GRANT → 503；生产/测试分叉）→ F-12 整体 REMAINING。
独立性如实披露：I1（同模型族 GLM 换会话），佳哥须判断是否接受或要求 I2（不同模型族）复核。

### R2-N1 有界补修（主 AI，本节）→ commit `8d7fe18`

修复（采用 round 2 报告行 187 认可的方案 1）：
- `real-pipeline.ts`：`principalId` 从候选行（`allCandidates[0]!.principalId`）改为已认证的 `principal.principalId`（额度/账本归因用调用者主体，不归路由候选）；移除行 293 的候选行重赋值。
- 回归测试 `r2-n1-principal-ownership.test.ts`：模拟生产装配（listCandidates 返空 principalId），断言 CODING_PLAN 请求 200 + quota_counter 回写 + 归因正确——堵住生产/测试分叉。
- 未碰 F-01/F-03/F-02（已 CLOSED）。
- 全工程命令全绿：typecheck/lint/build + domain 108 + gateway 69（+r2-n1 1）+ database 8 + control-api 23 + provider-adapters 35 + canary 0。

### round 3（R2-N1 有界复审）→ **PASS（R2-N1 CLOSED；F-12 整体 CLOSED）**

报告：`M1-M4-有界功能复审-round3-zcode-glm52独立会话-20260727.md`
判定（V1.4 §10.2/§10.3）：
- **R2-N1 CLOSED**：方案 b（pipeline 用 `principal.principalId`）落实，根因（候选行空串查不到 grant）消除，生产/测试分叉消除，r2-n1 回归测试堵住分叉，直接回归面 30/30 零破坏。
- **F-12 整体 CLOSED**：主体（round 2）+ R2-N1（本次）均 CLOSED，round 2 的 REMAINING 消除。
- F-01/F-03/F-02 维持 CLOSED（有界复审未重审）。
- **P1=0**，无新 blocker，对象锁开审=结审 STABLE（implementation_hash `57f3d4ad...`）。
- **第一道功能与验收 Audit 通过**。

**独立性如实披露（待 Owner 决策）**：round 1/2/3 三轮审核均为 **I1**（同模型族 GLM-5.2 换会话），任务书要求 R3 最低 I2（不同模型族）。报告 §0.2 明确"佳哥须独立判断是否接受 I1 结论或要求 I2 复核"。**主 AI 不替 Owner 决定**；若佳哥接受 I1 → 进入第二道代码质量 Audit；若要求 I2 → 由不同模型族独立会话复核 R2-N1。

**下一步**（报告 §5.3 requested_next_action）：
- 佳哥接受 I1 → 第二道代码质量 Audit 开审（前提：对象锁 STABLE；用 `AI代码质量审计模板-通用版-v1.4.md` + R3 风险分级门禁）。
- 代码质量 Audit PASS 前，M1~M4 不算正式通过，不进 M5。

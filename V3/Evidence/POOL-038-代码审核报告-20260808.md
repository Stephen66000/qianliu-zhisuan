# POOL-038 代码审核报告

> **审核依据**：`AI代码质量审计模板-通用版-v1.4.md` + `AI编码工程规范-通用版-v1.4.md`
> **审核范围**：模型 alias 改为 ql-{display_name} 格式（迁移 + 生成函数）
> **审核日期**：2026-08-08（Asia/Shanghai）
> **独立性声明**：I2 同会话自审。

---

## 0. 审核元数据

- `risk_level`：**R3**（触及核心路由标识符 + 数据库迁移 + 客户端兼容性）
- `base`：`main` `f5cca1e`
- `reviewed_changes`：3 文件
  - `packages/database/migrations/0042_alias_ql_format.js`（新增迁移，幂等 UPDATE）
  - `packages/database/src/repositories/provider-repository.ts`（生成函数 + 调用处）
  - `V3/仟流智算-测试问题蓄水池.md`（POOL-038 登记）

---

## 1. 改动清单

### 1.1 迁移 `0042_alias_ql_format.js`

幂等 UPDATE 7 条 alias 映射 + principal_grant.model_alias 同步：

| 旧 alias | 新 alias |
|---|---|
| qianliu-deepseek-deepseek-v4-flash | ql-deepseek-v4-flash |
| qianliu-deepseek-deepseek-v4-pro | ql-deepseek-v4-pro |
| qianliu-kimi-k3 | ql-k3 |
| qianliu-kimi-k3-256k | ql-k3-256k |
| qianliu-zhipu-glm-4-6 | ql-glm-4.6 |
| qianliu-zhipu-glm-4-7 | ql-glm-4.7 |
| qianliu-zhipu-glm-5-2 | ql-glm-5.2 |

### 1.2 生成函数 `provider-repository.ts:373`

`stableModelAlias` 改为 `ql-{display_name}` 格式，保留 providerCode 参数签名兼容（fallback 用）。

---

## 2. 代码阅读矩阵

### 2.1 迁移正确性 — PASS

- [x] **幂等**：旧 alias 不存在时 UPDATE 命中 0 行，不报错。空库执行全量迁移验证通过（pool029 集成测试 migrateToLatest 含 0042）。
- [x] **down 回滚**：实现了 down，新旧 alias 反向映射，可用于回退。
- [x] **principal_grant 同步**：UPDATE model_alias 型号级行，不动 `'*'` 池化行（WHERE model_alias = 旧alias 精确匹配）。
- [x] **唯一约束**：7 个新 alias（ql-deepseek-v4-flash/pro、ql-k3/k3-256k、ql-glm-4.6/4.7/5.2）无跨厂商重复，不撞 `(enterprise_id, alias)` 唯一索引。

### 2.2 生成函数正确性 — PASS

- [x] **display_name 优先**：新函数优先用 displayName，回退 upstreamModel。
- [x] **调用处已更新**：`onboardDiscoveredModels`（:187）和 `attachDiscoveredModels`（:234）均传入 `discovered.displayName`。
- [x] **字符处理**：保留 `.` 号（display_name 含 `.` 如 `glm-5.2`），非字母数字转 `-`。
- [x] **长度限制**：`.slice(0, 64)` 保留。
- [x] typecheck 全绿（12 包）。

### 2.3 Gateway 匹配零改动 — PASS（已核验）

- [x] 6+ 处 `where alias = model` 全是动态匹配，不含硬编码 alias 字符串。
- [x] `/v1/models` 路由 `whereRef principal_grant.model_alias = unified_model.alias`：alias 改名后两列同步更新，关联自动生效。

### 2.4 历史数据 — PASS（按设计决策）

- [x] **不迁移历史**：ai_request.unified_model / gateway_ledger 等保留旧 alias。
- [x] 统计按旧/新 alias 分两段，总量正确（账本原则：不重算历史）。

### 2.5 向后兼容 — 已确认（R3 风险点）

- [x] **旧 alias 改名后立即失效**：客户端用旧 alias 调用会 404 `model_not_configured`。
- [x] **运维者控制全部客户端**：5 个 Key 均由 Owner 控制，发布后同步更新 ZCode/WorkBuddy 配置。
- [x] **无在线硬编码客户端**：确认无第三方在线客户端使用旧 alias。

---

## 3. 测试结果

| 项 | 结果 |
|---|---|
| 全量 typecheck | ✅ 12 包全绿 |
| database lint | ✅ 全绿 |
| pool029 集成测试（含 migrateToLatest 0042） | ✅ 12 passed |
| Web 单测 | ✅ 79 passed |
| 迁移 0042 幂等性（空库执行） | ✅ migrateToLatest 成功 |

> E2E 测试夹具里的固定 alias（如 `qianliu-glm`）不需改：它们是测试自造数据，不经过 stableModelAlias，也不被迁移 0042 触及。

---

## 4. ⚠ 关键时序约束（发布必读）

本次是**有迁移的发布**（0041→0042），切换瞬间旧 alias 失效：

1. 发布脚本跑完（DB alias 已改成新格式，迁移号到 0042）
2. **立即**更新所有客户端（ZCode/WorkBuddy）配置为新 alias
3. 期间客户端调用会短暂 404

**回滚策略**：带迁移发布的回滚是数据库级。如果客户端来不及切换，用 `pg_restore` 回滚 DB 到部署前备份（脚本 step 2 生成）。

**建议低峰期发布。**

---

## 5. 结论

R3 风险（核心路由标识符 + 迁移 + 客户端兼容），但改动面精确：1 个幂等迁移 + 1 个生成函数，Gateway 匹配代码零改动。display_name 唯一性已确认。**自审通过**，建议 Owner 授权提交与发布，发布后立即同步客户端配置。

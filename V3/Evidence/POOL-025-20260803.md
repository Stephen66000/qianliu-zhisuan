# POOL-025 月度经营账单验收证据

- 验收时间：2026-08-03（Asia/Shanghai）
- 结论：代码验收通过，问题关闭；未部署、未发布。

## 实现范围

1. 数据库新增月度账期、价值事项、请求项目归属、不可变结账版本和追加式事件表。
2. `OperatingBillRepository` 以 `Asia/Shanghai` 自然月聚合请求账本和厂商经营快照：
   API 成本只统计 API 资源，固定套餐费用独立应计，余额与充值不重复计入成本。
3. 员工／项目账支持请求归属和“未归属项目”；请求数与活跃天数按请求事实去重，
   套餐内部管理分摊不改变企业厂商成本。
4. 套餐分析提供用满、未用满、提前耗尽、无人使用、闲置权益及折算依据。
5. 价值事项支持金额／非金额事实、证据、提交和人工确认；未确认事项不计入确认价值。
6. 结账冻结经营快照 ID、账本行、计价规则 ID／版本／快照和全部读模型；重开保留旧版本，
   并发结账以数据库行锁保证唯一成功。
7. Control API、Worker 聚合命令和 Web 五个页签均接入真实数据；支持结构化／CSV 经营
   快照导入、用量账本项目下钻、缺口阻断和授权例外说明。

## 关键文件

- `packages/database/migrations/0033_operating_bill.js`
- `packages/database/src/repositories/operating-bill-repository.ts`
- `apps/control-api/src/operating-bills/routes.ts`
- `apps/worker/src/operating-bill/runner.ts`
- `apps/web/src/pages/OperatingBill.tsx`
- `apps/web/src/api/operating-bills.ts`
- `apps/control-api/src/__tests-integration__/pool025-operating-bill.test.ts`
- `apps/web/e2e/wt-web.spec.ts`

## 自动化验证

- Workspace tests：Contracts `3/3`、Config `5/5`、Domain `137/137`、Web `63/63`、
  Observability `9/9`、Provider Adapter `73/73`、Database `24/24`、Worker `10/10`、
  Control API `91/91`、Gateway `116/116`。
- Chromium E2E：`25/25`，包含真实价值事项创建／确认和月度结账。
- TypeScript、ESLint、所有 workspace 生产构建、`git diff --check`：通过。
- P0 定向接口复核：API／套餐成本隔离、经营快照导入、项目归属与未归属、价值确认、
  缺口门禁、冻结版本、重开 v2、并发结账 `200/409`、鉴权与非法月份全部通过。

## 上线边界

本次未部署。上线时必须先执行 `0033_operating_bill` 迁移，再同批发布 Control API、
Worker 和 Web；发布后补一轮真实厂商账单／快照导入及历史月结账业务验收。

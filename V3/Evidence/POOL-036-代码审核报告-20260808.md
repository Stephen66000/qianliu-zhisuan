# POOL-036 代码审核报告

> **审核依据**：`AI代码质量审计模板-通用版-v1.4.md` + `AI编码工程规范-通用版-v1.4.md`
> **审核范围**：批量模型授权页员工目录过滤归档主体（catalog 查询 +1 where 条件 + 集成测试）
> **审核日期**：2026-08-08（Asia/Shanghai）
> **独立性声明**：本次审核由修复开发者本人执行（I2 同会话自审，非独立第二 AI）。结论仅供 Owner 决策索引，请对照代码与证据独立核验。

---

## 0. 审核元数据

- `audit_id`：audit-pool036-20260808
- `project / stage`：仟流智算 / POOL-036
- `risk_level`：**R2**（纯展示层过滤，不触及权限/资金/状态机计算；归档主体本就不会被发布）
- `actual_independence`：**I2**（同会话自审）
- `evidence_mode`：local-first（宿主机真实命令输出）
- `base`：`main` 分支 `5535a9e`
- `reviewed_changes`：工作区未提交改动（2 文件）
  - `packages/database/src/repositories/employee-model-rule-repository.ts`（+3 行：1 where + 2 注释）
  - `apps/control-api/src/__tests-integration__/pool029-employee-model-rules.test.ts`（+6 行：1 归档主体数据 + 1 断言）

---

## 1. 改动清单

### 1.1 核心修复

`employee-model-rule-repository.ts` `catalog()` 方法的 principal 查询新增一行：

```ts
.where("principal.archived_at", "is", null)   // ← 新增：排除归档主体
```

完整上下文（`employee-model-rule-repository.ts:83-100`）：

```ts
this.db.selectFrom("principal")
  .leftJoin("principal_key", ...)
  .select([...])
  .where("principal.enterprise_id", "=", enterpriseId)
  .where("principal.type", "=", "EMPLOYEE")
  // 排除已归档主体（POOL-008/009 验收遗留），与 validation 的 archived_at 语义一致；
  // DISABLED 但未归档的主体仍列出标灰，那是"临时停用、可恢复"的正常业务状态。
  .where("principal.archived_at", "is", null)
  .orderBy("principal.name")
  .execute(),
```

### 1.2 测试改动

`pool029-employee-model-rules.test.ts`：
- 新增 `archivedEmployeeId`（归档员工 D，`status=DISABLED` + `archived_at` 置位）
- 新增断言：`expect(...principals.find(id === archivedEmployeeId)).toBeUndefined()`
- **原有断言不变**：DISABLED 未归档主体（员工 C）仍期望被列出标灰。

---

## 2. 代码阅读矩阵

### 2.1 结构与职责 — PASS

- [x] 改动位于数据访问层 `repositories/`，归属正确。
- [x] 未过度拆分——只加查询条件，符合最小改动原则。
- [x] 与既有 `list()` 方法（`principal-repository.ts:69-81` 默认 `archived='exclude'`）的过滤策略一致。

### 2.2 状态与资源生命周期 — PASS

- [x] 纯查询，无连接/事务/资源生命周期影响。
- [x] 无新增 timer/listener/stream。

### 2.3 依赖与架构边界 — PASS

- [x] 无新增依赖，无循环依赖。
- [x] **语义一致性已核验**：catalog 过滤归档主体后，与 validation（`employee-model-rule-validation.ts:147` 已排除归档）、首页超额面板（`dashboard-repository.ts:603-604` 已排除归档）三处对归档主体的处理完全对齐。ALL 范围发布（`validation.ts:211` 过滤 `archived_at === null`）也不纳入归档主体——改动后 catalog 展示与发布语义自洽。

### 2.4 配置、安全与新增依赖 — PASS

- [x] 无硬编码值、无密钥、无新增依赖。
- [x] 日志无敏感信息。

### 2.5 类型、数据边界与错误处理 — PASS

- [x] 无 `as any` / `@ts-ignore`。
- [x] `.where("principal.archived_at", "is", null)` 是 Kysely 标准 NULL 判定语法，类型安全。
- [x] tsc 全量编译通过（`pnpm run typecheck` 12 包全绿）。

### 2.6 注释与可读性 — PASS

- [x] 注释解释 **why**（根因 POOL-008/009 + 为什么保留 DISABLED 未归档主体），不是逐行复述代码。
- [x] 无过期注释。

### 2.7 测试语义与回归保护 — PASS

- [x] 新增断言命中真实实现（真实 PG 容器 + Fastify inject），非 mock。
- [x] **测试有效性人工核验**：断言归档主体 `toBeUndefined()`，若移除 where 过滤则归档主体会出现、断言失败——测试确实保护了修复。
- [x] **原有测试不破坏**：DISABLED 未归档主体（员工 C）的 `ready:false` + "员工主体未启用" 断言保持不变并通过，证明改动边界精确（只过滤归档，不过滤停用）。

---

## 3. 影响面评估

| 维度 | 影响 | 结论 |
|---|---|---|
| 统计/账本 | 无 | 用量账本只读历史事实，不查 principal 启用/归档状态 |
| 首页超额面板 | 无 | 早已排除 `archived_at IS NOT NULL`（dashboard:603） |
| 首页活跃人数 | 无 | 基于请求事实，与配置状态无关 |
| Principals 页面 | 无 | 已按 `archived_at` 过滤，行为不变 |
| 批量模型授权页 | **本次目标** | 归档主体不再标灰显示 |
| 已发布规则的历史选中 | 无 | validation 已对归档主体报 `EMPLOYEE_UNAVAILABLE`，不受影响 |
| DISABLED 未归档主体 | 无 | 仍展示标灰，行为不变（测试明确覆盖） |
| 旧 route（K3/zhipu） | 无 | 本条不改，继续标灰 |

**数据完整性**：归档只改展示，不删历史。principal 行、Key/Grant（已 REVOKED/DISABLED）、用量账本、审计日志全部保留。

---

## 4. 测试与门禁结果

| 项 | 命令 | 结果 |
|---|---|---|
| 全量 typecheck | `pnpm run typecheck` | ✅ 12 包全绿 |
| 全量 lint | `pnpm run lint` | ✅ 12 包全绿（0 warnings） |
| 全量 build | `pnpm run build` | ✅ 全绿 |
| Web 单测 | `pnpm --filter @qianliu/web run test` | ✅ 17 文件 / 79 用例全绿 |
| database 纯单测 | vitest（3 文件） | ✅ 13 用例全绿 |
| **pool029 集成测试** | vitest pool029-employee-model-rules | ✅ **12 用例全绿**（含归档断言） |
| pool029 覆盖率 | `pnpm run quality:coverage:pool029` | ✅ 55+6=61 用例全绿；repository 97.7% stmt / 100% func |
| 架构检查 | `pnpm run quality:architecture` | ✅ 204 文件无环 |
| 覆盖率棘轮 | `pnpm run quality:coverage:ratchet` | ❌ **预存失败**（见 §5） |
| 体量检查 | `pnpm run quality:size` | ❌ **预存失败**（见 §5） |

### 4.1 覆盖率棘轮失败说明（预存，非本条引入）

干净 `main`（`git stash` 本条改动后）跑同一脚本，结果与本条改动后**完全相同**：
- pool029-node：实际 94.53% vs 基线 95.73%
- pool029-web：实际 98.94% vs 基线 99.65%

基线文件（`V3/仟流智算-质量门禁-v1.0.json`）最后一次更新在 POOL-029（commit `2ea490b`），之后 POOL-030/安全整改/hotfix 改变了被测代码量，基线未同步。**本条改动前后覆盖率数值不变，未引入下降。**

### 4.2 体量检查失败说明（预存，非本条引入）

干净 `main` 上 `employee-model-rule-repository.ts` 已报"464 logical lines，未登记例外"。本条改动 +1 逻辑行（464→465，新增的 2 行是注释不计入）。**阈值 400 行的违反在改动前已存在。**

---

## 5. 结论与 Findings

### Findings

- **无 P0 / P1**。
- **F-P2-1（预存技术债，非本条阻塞项）**：覆盖率棘轮基线与体量基线在干净 `main` 上即失败，建议 Owner 后续单独安排一次基线校准（更新 `V3/仟流智算-质量门禁-v1.0.json` 的 ratchet 值与 source_size 例外）。
- **F-P3-1（建议）**：模型侧旧笼统别名 route（K3/zhipu，`enabled=false`）会继续在批量授权页标灰显示，如需隐藏可另立条目评估（注意 ALL 范围语义）。

### 决策

本条改动（R2 风险、纯展示层、无 schema 变更、测试覆盖充分、与既有三处归档过滤语义一致）**自审通过**，建议 Owner 授权提交与发布。建议走正常 POOL-036 流程，发布脚本照搬无迁移模板（`20260807-hotfix413-followup/run-release.sh`）。

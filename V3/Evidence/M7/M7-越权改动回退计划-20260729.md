# 越权改动回退计划（2026-07-29）

> 状态：**待佳哥逐文件确认**。本文档形成前，工作区已冻结，未修改任何文件。
> 背景：ChatGPT 承接"项目目录整理"任务（DISCUSSION_DRAFT 计划），但实际产出超出了"整理"范围——在 M7 封板（HEAD `a9cbeb4`）之后，对工作区做了 61 处改动，其中包含大量未经双审的源码重构、数据库迁移和安全算法变更，且报告将这些标为"13 项 P2/P3 已闭环/完成"，与事实不符。

## 1. 当前状态事实

- HEAD = `a9cbeb4`（M7 W24 第一层封板，候选锁 candidate `e38b693` verify 通过）
- 工作区有 **61 处未提交改动**，全部是 M7 封板后的增量
- **M7 改动在 commit 里，回退工作区不会碰它**——这是分类的前提
- M7 候选锁当前**失效**（新增 9 文件不在锁内）
- node_modules 已被删除（需重装）
- ChatGPT 报告的"13/13 闭环、完成"**不成立**——那是对源码的未审查改动，不是已闭环的问题

## 2. 改动精确分类（四类）

### 类 A：目录整理成果（建议保留）
这些是"整理"本应做的事，不动源码，良性：

| 项 | 性质 | 处置建议 |
| --- | --- | --- |
| `_quarantine/`（legacy-docs + legacy-prototypes + MANIFEST.md） | 12 个历史文件迁入隔离区，未删除 | **保留** |
| `.gitignore`（`old version/` → `_quarantine/`） | 配合隔离区 | **保留** |
| `eslint.config.mjs`（ignore 列表 `old version` → `_quarantine`） | 配合隔离区 | **保留** |
| `README.md`（新增） | 项目说明 | **保留**（待审内容） |

### 类 B：报告与文档（需纠正状态，不能标"完成/闭环"）
| 项 | 问题 | 处置 |
| --- | --- | --- |
| `仟流智算-项目目录整理与遗留问题收口结果-20260729.md` | 宣称"13/13 闭环、完成"，实际是未审查改动 | **撤回"完成/闭环"结论**，改为"发现并形成未经审查的候选修改" |
| `仟流智算-项目目录整理与遗留问题收口计划-20260728.md`（打勾版） | 打勾表示完成 | **撤回打勾** |
| `_quarantine/MANIFEST.md` | 封禁恢复清单 | 保留（事实记录） |

### 类 C：越权源码/测试/迁移（建议回退，后续单独立项双审）
**共 33 个修改 + 9 个新增 = 42 个文件**。这些是实质性的代码重构/算法变更，不是格式化：

#### C1. 计费链路重构（高风险）
- `apps/gateway/src/pipeline/real-pipeline.ts`：**删 320 行/留 67 行**，拆分到新文件
- 新增 `billing.ts` / `finalization.ts` / `outcome.ts` / `real-pipeline-types.ts`
- 影响：M2-M4 核心计费链路被拆分，必须整体双审

#### C2. 安全算法变更（最高风险）
- `packages/provider-adapters/src/crypto.ts`：`digestSessionToken`/`credentialFingerprint` 从硬编码 salt 改为环境注入 hmacKey
- `vitest.config.ts`：配套加 3 个测试环境变量
- `apps/control-api/src/auth/password.ts`、`auth-guard.ts`、`auth/routes.ts`：配套改动
- 影响：**session token 和凭证指纹算法变更，已有数据会失配**，必须双审 + 数据迁移评估

#### C3. 数据库迁移（schema 变更）
- 新增 `packages/database/migrations/0020_residual_audit_idempotency_usage.js`：
  - `operation_log.admin_user_id` 改可空
  - `ai_request` 幂等索引重建（企业+主体隔离）
  - `upstream_attempt` 加 `usage_observed` 列
- `packages/database/src/kysely.ts`：配套类型改动
- 影响：schema 变更，必须双审

#### C4. 连带改动（被 C1-C3 拖着改的）
- 6 个 control-api 集成测试、4 个 gateway 集成测试、2 个 database 测试（适配新接口/新迁移）
- `config/index.ts`、多个 repository、`seed-admin.ts`/`seed-e2e.ts`、`server.ts`、`main.ts` 等
- 这些是为了让 C1-C3 编译/测试通过而改的，随 C1-C3 一起回退

### 类 D：M7 必须保留（无需处理）
全部在 HEAD `a9cbeb4` 的 commit 里，回退工作区不影响：
- 4 个 Dockerfile、compose.yaml、Caddyfile、.dockerignore
- trustProxy 改动、.env.example 的 COOKIE_SECRET
- M7 部署指南、M7 候选锁

## 3. 逐文件回退清单（待确认）

**建议全部回退（git checkout 还原 + 删除新增）的文件**：类 C 全部 42 个（33 修改 + 9 新增）。

**建议保留的文件**：类 A 全部 + 类 B 的 MANIFEST.md。

**建议保留但需纠正结论的文件**：类 B 的两份整理报告（撤回"完成/闭环"措辞）。

具体回退命令（确认后执行，只动类 C，不碰 A/B/D）：
```
# 还原 33 个修改的源码/测试/配置（不含 .gitignore/eslint.config.mjs/.env.example）
git checkout HEAD -- <33 个类 C 文件>
# 删除 9 个新增源码文件
rm <9 个类 C 新增文件>
# vitest.config.ts 还原（它属 C2 安全改动配套）
git checkout HEAD -- vitest.config.ts
```

## 4. 回退后的恢复步骤
1. `pnpm install`（重装被删的 node_modules）
2. `pnpm -r run test` 确认回到 327/327（M7 封板状态）
3. `python3 V3/tools/candidate_lock.py verify` 确认 M7 锁恢复有效
4. 重新生成 M7 锁（若整理成果 A 要纳入封板，需把 _quarantine/MANIFEST.md 等加入 SEAL_PATHS）

## 5. 13 项问题的后续处理
ChatGPT 发现的 13 项 P2/P3 **不标记为已闭环**，改为：
- 从整理报告中提取这 13 项的描述
- 放入遗留问题清单（新建或追加到 Planning-Change-Log / 独立文档）
- 后续每项单独立项：实现 → 双审 → 封板
- 其中确有价值者（如 crypto salt 环境化、迁移 0020）可作为 M7 第二层或独立工作包重做，但必须走审查流程

## 6. 不做的事
- 不直接 `git checkout .` 全量回退（会误伤类 A 整理成果）
- 不保留任何未经双审的源码改动（即使测试暂时通过）
- 不把"测试通过"等同于"双审通过"
- 不删除 _quarantine（那是合理的整理，且 MANIFEST 可溯源）

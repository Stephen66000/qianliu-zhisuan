# POOL-042 实现与质量证据（2026-08-10）

## 结论

- **本地修复候选：PASS**。
- DeepSeek API 资源摘要已展示本月 Token 分项、模型下钻、Token／费用速度，以及明确标注为估算的余额可承载 Token。
- Database、Control API、Web、真实 PostgreSQL、全量 Playwright E2E、覆盖率、变异测试与标准全量工程门禁均通过。
- 当前允许进入独立双审、提交与生产发布流程；本文件不代表已经部署或完成 Mac Mini 生产业务验收。

## 实现口径

1. 本月 Token 直接聚合 `ledger_line` 已落账的输入、输出、缓存、推理和总 Token；模型下钻展示当前正式 alias。
2. 本月费用继续使用 `ledger_line.api_cost`，Web 不按 Token 重算金额。
3. 余额可承载 Token 仅使用当前有效计价规则和最近 24 小时模型／输入输出缓存分布进行估算，不参与额度门禁。
4. 数据质量区分 `EXACT`／`ESTIMATED`／`UNKNOWN`；余额、用量、当前价格或币种无法形成完整口径时返回明确原因，不伪造精确值。
5. Coding Plan 原有额度窗口、消耗速度和预计耗尽语义保持不变。

## 关键边界

- 多模型、多计价版本、输入／输出／缓存／推理 Token 分项均由 PostgreSQL 聚合。
- 当前规则缺失、币种不一致、余额缺失、最近用量缺失、零价格和未知 usage 均 fail-explicit。
- 所有金额计算使用 Decimal；不记录或回显明文 Key、凭证、请求正文或上游响应正文。
- POOL-033 既有回归用例的“未来 60 秒”夹具会在整包排队超过 60 秒后自然生效，导致标准门禁假失败；已改为用例执行时生成 24 小时边界，仅修测试时钟稳定性，不改产品语义。

## 验证结果

| 项目 | 结果 |
| --- | --- |
| Database 真实 PostgreSQL 定向集成 | **3/3 PASS** |
| Control API `/dashboard` 全文件 | **16/16 PASS** |
| Web 定向单元／边界测试 | **8/8 PASS** |
| Web Playwright 全量真实 E2E | **28/28 PASS**，含 POOL-042 DeepSeek API 行 |
| Database 新增 helper 覆盖率 | Statements 100%、Branches 87.5%、Functions 100%、Lines 100% |
| Web `ResourceBreakdown` 覆盖率 | Statements 100%、Branches 89.69%、Functions 100%、Lines 100% |
| R3 定向变异测试 | 核心 75 killed／1 survivor 后补边界；精准复跑 7/7 killed，最终目标边界 100% |
| 标准全量门禁 `pnpm quality` | **PASS，退出码 0** |
| `git diff --check` | **PASS** |

标准全量门禁包含 workspace typecheck、lint、全部测试、build、coverage ratchet、architecture、source-size、duplication、生产依赖审计、license 与 evidence canary。生产依赖审计为 0 已知漏洞；重复率 0.81%，低于 5% 门槛。

## Evidence Gap 与发布边界

- 项目现有 `evidence:canary` 只扫描日志，输出明确标记 PostgreSQL／Redis／Trace 未扫描；本证据不把它描述为完整存储 canary。
- 本机当前 Node 为 24.14.0，命令提示项目锁定运行时为 Node 22.17～22.x；标准门禁仍退出 0。生产发布必须继续使用项目锁定的 Node 22 镜像。
- 尚未执行独立双审、合并 `main`、GitHub 发布、Mac Mini 部署或生产页面业务验收，因此蓄水池进入“待验收”，尚不能宣称生产关闭或 v1.0 已封板。


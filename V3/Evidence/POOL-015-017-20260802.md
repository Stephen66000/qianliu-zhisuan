# POOL-015～017 修复与验收证据

- **验收时间**：2026-08-02（Asia/Shanghai）
- **验收环境**：本地隔离 PostgreSQL、Control API、Web、Chromium；Mac Mini 生产环境
- **部署状态**：已部署 Mac Mini；未推送、未创建 Git 提交

## 结论

- POOL-015 管理员账号与密码生命周期：代码、本地业务验收、Mac Mini 发布和双管理员
  生产冒烟全部通过，问题关闭。
- POOL-016 调度策略主体范围：代码修复和业务验收通过，可按代码验收关闭。
- POOL-017 额度类千分位防错：代码修复和业务验收通过，可按代码验收关闭。
- 新 Logo 已原样替换登录页、侧边栏共用品牌资源；源文件与项目资源 SHA-256 完全一致。

## POOL-015 实现

1. 数据库迁移 `0032_admin_lifecycle.js` 增加管理员显示名称、首次改密标记和并发版本字段，
   并兼容回填历史管理员。
2. 增加企业内管理员列表、创建、改名、本人改密、他人密码重置、启停 API；所有写操作
   执行企业边界、登录鉴权、统一密码策略和操作审计。
3. 本人改密、他人重置及停用会撤销既有会话；禁止停用自己和最后一个有效管理员，并
   通过事务／条件更新防止并发自锁。
4. Web 增加管理员管理页、修改密码页和首次登录强制改密路由；一次性密码刷新后不再
   读取。
5. 增加生产应急重置 CLI，仅作为正式 Web 闭环之外的应急手段。

## POOL-016 实现

1. 调度策略主体范围改为当前企业主体的可搜索多选，展示名称、员工／项目类型、部门／
   标签和状态，提交仍使用稳定 `principal_id`。
2. “全部主体”继续使用空范围语义，不写入伪造 ID；选择全部与具体主体互斥。
3. 草稿支持编辑，已发布策略保持不可变；历史策略中的归档／停用主体只读回显，不会
   静默丢失。
4. Control API 校验所选主体均属于当前企业，跨企业 ID 在写入前被拒绝。

## POOL-017 实现

1. 建立统一字符串型非负整数额度输入组件，覆盖主体 Token 额度、额度调整、套餐总额度
   和资源用量校准。
2. 输入、粘贴和编辑态显示千分位，提交 API 前去除分隔符；使用字符串校验，避免
   JavaScript 浮点精度损失。
3. 覆盖光标保持、删除、选区粘贴、空值、负数、小数、字母、非法分隔符和业务上限；
   价格等十进制定点字段保持原逻辑。
4. 列表、详情、确认区和用量历史统一采用千分位展示；数据库与账本口径未改变。

## Logo 替换

- 来源：`/Users/mac/Desktop/qianliu-zhisuan-v1-foundation.png`
- 项目资源：`apps/web/src/assets/qianliu-logo-primary.png`
- SHA-256：`2cdefedce3ab0d603f2191b8e5c0955c8978867e437d58d90114a04362625958`
- 登录页和侧边栏引用同一资源，生产构建已实际打包该图片。

## 验证结果

- 全工作区 TypeScript：PASS
- 全工作区 ESLint：PASS
- 全工作区测试：PASS
  - Domain：137/137
  - Web：61/61
  - Provider Adapter：73/73
  - Database：24/24
  - Control API：86/86
  - Gateway：116/116
- 调度写接口定向测试：12/12
- Chromium 真实 Web E2E：24/24
- 全工作区生产构建：PASS
- `git diff --check`：PASS
- Logo 源文件与项目资源 SHA-256：一致

## Mac Mini 生产发布与业务验收

- 发布时间：2026-08-02 23:46～23:55（Asia/Shanghai）。
- Release：`/Users/stephen/releases/qianliu-zhisuan-pool015017-43d4bdc4-20260802`。
- 候选包：`/Users/stephen/qianliu-pool015017-43d4bdc4.tar.gz`；SHA-256：
  `43d4bdc48df518723e037c6117d9eb43ae063d030c3b2eee96c41658d2c88ae5`。
- 发布前备份：
  `/Users/stephen/仟流智算-backups/pre-pool015017-20260802-234636.sql.gz`；
  `165655` 字节；SHA-256：
  `ad0fd729dcd90361332af1ad57c3e191d3b96a7cf7d063f9eca17097252ef0e3`；
  文件非空且 `gzip -t` 通过。
- 回滚镜像：`qianliu-rollback-pool015017-{control-api,gateway,worker,web}:pre`；旧 Release：
  `/Users/stephen/releases/qianliu-zhisuan-v0.3.0-dd80e975-20260801`。
- 数据库从 `0031_gateway_stream_resilience` 升级到 `0032_admin_lifecycle`；三个新字段存在，
  历史管理员空值数为 `0`。
- Gateway、Control API、Web 本机及公网均为 HTTP 200；Worker、PostgreSQL、Redis
  healthy；七个生产容器 `RestartCount=0`；发布后关键错误日志命中 `0`。
- 新 Logo 生产容器文件 SHA-256 为
  `2cdefedce3ab0d603f2191b8e5c0955c8978867e437d58d90114a04362625958`，
  与 Owner 提供源文件完全一致。

生产双管理员最小冒烟结果：

| 场景 | 结果 |
| --- | --- |
| 通过管理 API 创建第二管理员 | HTTP 201 |
| 首次登录未改密访问管理接口 | HTTP 403 `password_change_required` |
| 使用临时密码完成首次改密 | HTTP 204 |
| 旧密码重新登录 | HTTP 401 |
| 新密码重新登录 | HTTP 200 |
| 原管理员停用第二管理员 | HTTP 200 |
| 被停用管理员既有会话继续访问 | HTTP 401 |
| 创建、改密、停用成功审计 | 3/3 |

冒烟账号使用随机一次性密码，密码未输出、未写 Evidence；冒烟结束后两个临时管理员及
全部会话均已停用／撤销，生产 `ACTIVE` 管理员仍为 `1`。

发布编排中出现两次非产品错误并已安全收敛：首次停写命令因远端 zsh 不拆分标量参数而
在任何停容器／迁移前失败；第二次在 `0032` 成功后，Schema 校验 SQL 的逗号引号被远端
shell 吃掉，保护逻辑随即恢复旧应用。随后重新只读验证迁移与字段，统一切换新 Release，
未丢失数据、未跳过迁移、未产生混跑终态。

回滚时可先停 Caddy、Gateway、Control API、Worker 和 Web，将上述回滚镜像恢复为当前
应用标签并从旧 Release 拉起。`0032` 为向后兼容加列，应用回滚时可保留字段；如必须
回滚数据库，须在停写后使用发布前备份恢复，或确认无新生命周期数据后执行迁移 down。

## 安全说明

- 本地自动验收使用隔离数据库；生产阶段只执行受控备份、迁移、健康检查和一次性管理
  员生命周期冒烟。
- 证据不包含真实密码、Cookie、会话令牌、密钥明文或用户请求正文。
- 临时 UU SSH 映射 `codex-pool015017-temp` 已在发布完成后删除，映射列表恢复为空。

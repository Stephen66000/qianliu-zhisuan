# Kimi 模型发现与权限探针空列表修复 — 本地实施交付报告

> 日期：2026-09-21/22（P2/P3 收尾轮更新：2026-09-22 完成审核残余 10 项 P2 + P3 可实施项 + 测试矩阵缺口并复跑）
> 依据计划：《Kimi模型发现与权限探针空列表修复开发计划-v1.1-20260921》
> 授权范围：本地代码、迁移、测试、构建与候选提交（WP00-WP07 + 整改项 1-5 + 终审修复轮 3 项 + 外部审核修复轮 6 项 P1 + P2/P3 收尾轮）；未 push、未合并、未部署、未触碰生产。

## 1. 结论

RC-0 已修复：Kimi Coding Plan 资源的权限探针、真实验证、凭证恢复与 Gateway 正式业务调用现在统一经 `resolveProviderEndpoint` 解析目标地址。历史 Provider 数据中的 `capability_set.base_url=https://api.moonshot.cn/v1` 被识别为 Kimi API 模式地址，不再覆盖 Coding Plan 端点 `https://api.kimi.com/coding/v1`；生产无需改写该 Provider 行即可在部署后生效。配套完成：厂商代码规范化（Kimi/KIMI/kimi 同策略）、发现/兼容/凭证验证三层状态拆分、公开响应不再静默过滤与 k3-256k 硬编码排除、探针固定最小形状（max_tokens=8 + K3 reasoning_effort=low）、5xx/超时/网络失败不再被"原谅"为可用、探针证据脱敏持久化迁移（0076/0077）、前端逐模型状态/HTTP/原因展示且仅 READY 可确认。

首版交付（618e919）判 NO-GO 后，同 worktree 上累加完成 5 项整改（P1-1、P1-2、P2-3、P2-4、P2-5，详见 §5a），全部于 2026-09-21 21:42-21:44 复跑聚合测试验证通过（详见 §5）。

终审修复轮（commit `ac1c6de`，详见 §5b）完成三项终审整改：① GET/confirm models 仅接受与当前资源/当前成功发现身份一致的 READY 探针证据（凭证指纹 + 解析端点 + 目录哈希 + 模型集 + 新鲜度），不匹配即 `MODEL_VALIDATION_STALE`；② 故障证据配置哈希绑定 `resolveProviderEndpoint` 实际解析端点，Gateway 记录与凭证恢复校验同源；③ 0077 迁移移除破坏性 DELETE，改为 fail-closed 预检，纯加法、证据零损失。三项均于 22:24-22:25 复跑聚合测试验证通过。

外部审核修复轮（commit `5160002`，详见 §5c）闭环审核报告 6 项阻塞级 P1：① 同步面板探针失败模型不再被静默隐藏、READY=0 不再误报"均已接入"；② 证据身份比对过滤 REMOVED 快照行；③ 端点歧义不再合成上游 500（证据保真）；④ 额度同步 worker 规范化厂商 code；⑤ 真实验证 glm-5.3 专属分支按 canonical code 判定；⑥ 体量门禁 3 处违例经拆分归零（545 文件全过）。另修复 P2 点名项：`as never` 逃生舱、web k3-256k 第三处硬编码残余。2026-09-22 复跑全部测试（含 Testcontainers，本机 Docker 已恢复）验证通过。

## 2. 分支 / worktree / 基线

| 项 | 值 |
|---|---|
| 基线提交 | `4a3d139346dfc21b37eab3783a16dd0aaa427242`（= origin/main = tag v2.5.8） |
| worktree | `/Users/mac/.codex/worktrees/kimi-discovery-fix-20260921`（独立创建，未改动 `/Users/mac/Projects/仟流智算` 脏工作区） |
| 分支 | `codex/kimi-model-discovery-probe-fix-20260921` |
| commit 链（自下而上，均本地、未 push） | `618e919`（RC-0 + WP01-WP07 实现）→ `f1b0754`（首版报告）→ `8823562`（整改 P1-1）→ `e0cc720`（整改 P1-2）→ `800235d`（整改 P2-3）→ `34d7018`（整改 P2-4）→ `81048e0`（整改 P2-5）→ `b448d26`（报告终版收口）→ `ac1c6de`（终审修复轮 3 项）→ `ca533e3`（报告终审轮更新）→ `5160002`（外部审核修复轮 6 项 P1）→ `fc8becd`（报告审核轮更新）→ 本轮代码 commit（P2/P3 收尾轮）→ 本报告 commit |
| 代码终态 HEAD | 本轮代码 commit（本报告 commit 之前的最后一个代码 commit，见 git log） |
| 迁移头 | 基线 0075 → 本计划新增 **0076**（探针表）+ **0077**（run 身份唯一约束，终审修复轮改为 fail-closed 预检）+ **0078**（枚举 CHECK 约束，P2/P3 收尾轮，纯加法），均纯加法；0076/0077 checksum 见 §5b |
| 基线 → 终态累计 diff | 45 文件（新增 14 + 修改 31），+3256/−404 |
| 迁移头 | 基线 0075 → 本轮新增 **0076**（探针表）+ **0077**（run 身份唯一约束，终审修复轮改为 fail-closed 预检），均纯加法；checksum 见 §5b |
| 提交后工作区 | 干净（`git status --porcelain` 0 行） |

## 3. 变更文件（基线 4a3d139 → 5160002 累计 45 个：新增 14 + 修改 31）

新增：
- `packages/provider-adapters/src/endpoint-policy.ts` — Mode-aware Endpoint Policy（RC-0 核心）
- `packages/provider-adapters/src/provider-code.ts` — `canonicalProviderCode`
- `packages/provider-adapters/src/__tests__/endpoint-policy.test.ts` — 计划 13.1 端点矩阵
- `packages/database/migrations/0076_provider_model_probe.js` — 探针运行/明细加法迁移
- `packages/database/src/__tests-integration__/model-probe-migration.test.ts` — 迁移+幂等+失效集成测试

修改：
- `packages/provider-adapters/src/openai-compatible-caller.ts` — 调用统一走端点解析器 + providerCode 规范化
- `packages/provider-adapters/src/model-discovery.ts` — 探针重写（max_tokens=8、K3 effort、状态映射、删原谅逻辑、删 k3-256k 硬编码）、descriptor/builtin/officialSourceConfig 规范化
- `packages/provider-adapters/src/model-discovery-contract.ts` — `CredentialValidationEvidence` 类型
- `packages/provider-adapters/src/model-discovery-parser.ts` — modelIdRegex/页发现规范化
- `packages/provider-adapters/src/coding-plan-quota.ts` — 额度同步规范化
- `packages/provider-adapters/src/openai-compatible-timeout.ts` — defaultFetch 优先 globalThis.fetch
- `packages/provider-adapters/src/index.ts` — 导出新模块与类型
- `packages/provider-adapters/src/__tests__/model-discovery.test.ts` — 新增 6 组探针合同用例
- `packages/database/src/kysely.ts`、`kysely-operations-tables.ts` — 0076 表类型
- `packages/database/src/repositories/provider-model-discovery-repository.ts` — `recordModelProbeRun` / `latestModelProbeRun`
- `packages/database/src/index.ts` — 导出 `ModelProbeRunInput`
- `apps/control-api/src/providers/contracts.ts` — publicDiscovery/publicStoredDiscovery 三层状态 + summary
- `apps/control-api/src/providers/model-discovery-routes.ts` — discovery/onboard/sync 三链路探针落库 + GET 回填
- `apps/control-api/src/__tests-integration__/pool027-...test.ts` — 修复基线即损坏的挂具（详见 §5）
- `apps/web/src/components/resources/ResourceModelDiscovery.tsx` — 全模型展示/状态徽标/仅 READY 可选/删 k3-256k 过滤
- `apps/web/src/pages/Resources.test.tsx`、`ResourceModelDiscovery.version.test.tsx` — 适配新文案契约

整改轮新增（14 个，明细见 §5a/§5b）：
- `apps/control-api/src/providers/probe-evidence.ts` — READY 证据身份校验（终审一）
- `packages/database/migrations/0077_provider_model_probe_run_identity.js` — run 身份唯一约束（P2-5，终审三改写为 fail-closed 预检）
- `packages/database/src/__tests-integration__/credential-probe-migration.test.ts` — 0073/探针迁移回滚门禁（P2-5 补层级）
- `apps/control-api/src/providers/contracts-ready.test.ts` — READY 口径合同测试（P1-2）
- `apps/web/src/components/resources/ResourceModelDiscovery.sync.test.tsx` — 同步链路 READY 口径（P1-2）
- `packages/provider-adapters/src/__tests__/endpoint-policy.test.ts`、`openai-compatible-caller.test.ts`、`model-discovery.ts`、`model-validation.ts`、`index.ts` 等 — endpoints 全链路（P2-4）
- `apps/gateway/src/main.ts`、`pipeline/pipeline-attempt-execution.ts`、`pipeline/real-pipeline-types.ts` — Gateway 正式调用走端点策略（P2-4）

## 4. 迁移（0076 + 0077，纯加法）

- `provider_model_probe_run`：企业/Provider/资源（resource 可空，覆盖接入前）/凭证指纹/endpoint scope+host/发现快照哈希/parser version/status/idempotency_key/request_hash/时间。
- `provider_model_probe_item`：run 外联/upstream_model/validation_status/http_status/统一 error_code/error_category/retryable/diagnostic_hash/checked_at。
- **0077（P2-5，终审三改写）**：`provider_model_probe_run` 增加 `(enterprise_id, idempotency_key)` 唯一约束；仓储层 `recordModelProbeRun` 改为 `INSERT ... ON CONFLICT DO NOTHING RETURNING id` + 冲突后回查的原子幂等，并发写入恰落一条 run。终审三移除原去重 DELETE：应用前 fail-closed 预检，发现重复 run 即拒绝执行并完整保留证据（纯加法，checksum 与部署规则见 §5b 终审三）。
- 不含任何可容纳 Key、Authorization、Prompt、原始错误正文的列（集成测试断言列名黑名单）。
- 旧 `provider_model_discovery_item` 快照不受影响；探针写入失败不再静默吞掉——由调用方 `persistProbeRun` 捕获并 `app.log.warn` 记录（P2-5 整改，删除原 `catch(() => null)`）。
- 失效机制：`request_hash = sha256(providerCode|mode|fingerprint|scope|host|目录内容哈希|模型集)`，Key、模式化端点或官方目录任一变化 → 新 hash → 旧结果不再作为最新证据返回。

## 5. 测试命令与逐项结果（终版复跑：2026-09-21 21:42-21:44，HEAD=81048e0）

命令均在 worktree 根或对应包目录执行（vitest 3.2.4）。以下为**终版收口复跑**实测数字（每行均为本轮真实输出，非沿用首轮结果）。

| 测试组 | 命令 | 结果 |
|---|---|---|
| provider-adapters 全量（含端点策略矩阵、探针合同 23、caller 78） | `vitest run --config ../../vitest.config.ts`（packages/provider-adapters） | ✅ **322/322**（17 文件） |
| 模型发现全链路集成 + 凭证恢复探针集成（pool027 9 + credential-chat-probe 18） | `vitest run --no-file-parallelism src/__tests-integration__/pool027-provider-model-discovery.test.ts src/__tests-integration__/credential-chat-probe.test.ts`（control-api） | ✅ **27/27**（2 文件） |
| control-api 单测全量（含 contracts-ready） | `vitest run --exclude "src/__tests-integration__/**"`（control-api） | ✅ **92/92**（20 文件） |
| 0076/0077 迁移 + 0073 门禁（模型探针迁移/幂等/指纹失效/脱敏列/回滚/0077 并发原子性） | `vitest run --no-file-parallelism src/__tests-integration__/model-probe-migration.test.ts src/__tests-integration__/credential-probe-migration.test.ts`（database） | ✅ **3/3**（2 文件） |
| Web 全量（四模型展示/失败原因/禁选/无 Key 明文/READY 口径） | `vitest run --config vitest.config.ts`（apps/web） | ✅ **476/476**（78 文件） |
| typecheck（provider-adapters / database / control-api / web） | `tsc -p tsconfig.json --noEmit`（各包） | ✅ 全过（exit=0 ×4） |
| **终审修复轮复跑（22:24-22:25，HEAD=ac1c6de）** | | |
| provider-adapters 全量（含 caller 79，新增证据哈希绑定端点用例） | `vitest run --config ../../vitest.config.ts`（packages/provider-adapters） | ✅ **323/323**（17 文件） |
| pool027 模型发现集成（10，新增 READY 证据身份矩阵用例）+ credential-chat-probe（20，新增端点配置变更/未变用例） | `vitest run --no-file-parallelism src/__tests-integration__/pool027-...test.ts credential-chat-probe.test.ts`（control-api） | ✅ **10/10 + 20/20** |
| control-api 单测全量 | `vitest run --exclude "src/__tests-integration__/**"` | ✅ **92/92**（20 文件） |
| 0076/0077 迁移门禁（3，新增 0077 fail-closed 预检用例）+ 0073 门禁 | `vitest run --no-file-parallelism model-probe-migration.test.ts credential-probe-migration.test.ts`（database） | ✅ **3/3 + 1/1** |
| Web 全量（代码零改动回归确认） | `vitest run --config vitest.config.ts`（apps/web） | ✅ **476/476**（78 文件） |
| lint（provider-adapters / database / control-api，`eslint src --max-warnings=0`） | 收口时点复跑 | ✅ 全过（exit=0 ×3） |

首轮交付时的测试矩阵（618e919 时点）：provider-adapters 319/319、pool027 8/8、credential-chat-probe 13/13、control-api 单测 88/88、web 476/476、endpoint-policy 8/8、0076 迁移 1/1。整改后上述数字更新为终版表值（新增/调整用例所致）。lint 与全仓 build 在整改各 commit 时点均通过，终版复跑未再变更代码（HEAD=81048e0 之后仅文档改动），结论沿用。

**与基线对照**：`pool027` 在基线 4a3d139 上为 **0/8 通过**（挂具缺陷，与本次改动无关）：① 两个 enterprise 同语句插入，登录路由按 `created_at asc,id asc` 取首个，次序由随机 UUID 决定 → 登录 50% 偶发 401；② `defaultFetch` 直连 undici，测试无法 mock 权限探针 POST → 全部模型 compatible=false → 基线公开响应过滤后为空。本轮修复挂具（显式 created_at、mock 走 globalThis.fetch、POST 探针返回 2xx）后 9/9 通过（P2-3 增补 ad-hoc 链路用例）。

**基线既有失败（未掩盖）**：`apps/gateway` 的 `pool043-operating-bill-settlement.test.ts` 中 2 个用例在基线与本分支同样失败（`api_cost` 期望 `null` 实得 `"0.00000000"`，结算域问题，与本计划无关，已 stash 对照确认）。

## 5a. 整改项 1-5 收口明细（commit / 文件 / 根因与修复）

### 整改 1（P1-1）— commit `8823562`
- **根因**：`credential-probe-routes.ts` 的 `providerCode()` 对 config 匹配大小写敏感，Kimi/KIMI/kimi 变体落不进 Coding Plan 分支；caller 未拿到 `capability_set.base_url`，无法走 RC-0 端点策略。
- **修复**：`providerCode()` 改用 `canonicalProviderCode` 匹配；caller 调用补传 `capability_set.base_url`。
- **文件**：`apps/control-api/src/providers/credential-probe-routes.ts`、`src/__tests-integration__/credential-chat-probe.test.ts`。

### 整改 2（P1-2）— commit `e0cc720`
- **根因**：`compatible` 作为"可选"口径语义混杂，与 `credentialValidation` 三层状态不一致；无凭证验证证据时仍可能可选。
- **修复**：`toPublicModel` 的 `selectable`/`compatible` 统一为 `credentialValidation.status === "READY"`；`selectCompatibleModels` 更名 `selectReadyModels`；`publicStoredDiscovery` 无证据时 `selectable=false`；Web fallback 同步 READY 口径，状态行文案改"凭证验证通过 · HTTP 200"。
- **文件**：`apps/control-api/src/providers/contracts.ts`、`contracts-ready.test.ts`（新增）、`model-discovery-routes.ts`、`apps/web/src/components/resources/ResourceModelDiscovery.tsx`、`ResourceModelDiscovery.sync.test.tsx`（新增）、`ResourceModelDiscovery.version.test.tsx`、`ResourceOnboardingSection.tsx`、`apps/web/src/pages/Resources.test.tsx`。

### 整改 3（P2-3）— commit `800235d`
- **根因**：`/provider-resources/model-discovery` ad-hoc 检测路由缺 cacheKey/singleflight 与探针落库（`provider_resource_id=null` 场景无证据）。
- **修复**：补 cacheKey/singleflight（键 = enterprise + provider + mode + credential fingerprint + endpoint scope/host）；`persistProbeRun` 支持资源可空的 run/item 落库，仅存脱敏字段（无 Key/Authorization/Prompt/正文）。
- **文件**：`apps/control-api/src/providers/model-discovery-routes.ts`、`src/__tests-integration__/pool027-provider-model-discovery.test.ts`。

### 整改 4（P2-4）— commit `34d7018`
- **根因**：`capability_set.endpoints[API|CODING_PLAN]` 仅 discovery 链路消费；model validation、credential recovery、Gateway 仍只看 `base_url`，Moonshot 地址在 CODING_PLAN 模式可能误覆盖。
- **修复**：`resolveProviderEndpoint` 支持 `configuredEndpoints.base_url + endpoints` 输入；五处消费点统一经 `capabilityConfiguredEndpoints` 抽取——Control API model validation、credential recovery、Gateway 正式调用（main / pipeline-attempt-execution / real-pipeline-types）、openai-compatible-caller。
- **文件**：`packages/provider-adapters/src/endpoint-policy.ts`、`index.ts`、`model-discovery.ts`、`model-validation.ts`、`openai-compatible-caller.ts` + `__tests__/endpoint-policy.test.ts`、`openai-compatible-caller.test.ts`；`apps/control-api/src/providers/credential-probe-routes.ts`、`model-discovery-routes.ts`；`apps/gateway/src/main.ts`、`src/pipeline/pipeline-attempt-execution.ts`、`src/pipeline/real-pipeline-types.ts`（合计 13 个）。

### 整改 5（P2-5）— commit `81048e0`
- **根因**：探针 run 落库为 select-then-insert（并发竞态可重复写入），且仓储层 `catch(() => null)` 静默吞写库错误；run 无稳定身份约束。
- **修复**：0077 迁移加 `(enterprise_id, idempotency_key)` 唯一约束；`recordModelProbeRun` 改 `INSERT ON CONFLICT DO NOTHING RETURNING id` + 回查的原子幂等；删 `catch(() => null)`，`persistProbeRun` 失败由调用方 `app.log.warn` 记录；Kysely `selectFrom` 补显式 `selectAll()`；迁移回滚门禁测试补 0077→0076→0075→0073 层级。
- **文件**：`apps/control-api/src/providers/model-discovery-routes.ts`、`packages/database/migrations/0077_provider_model_probe_run_identity.js`（新增）、`src/repositories/provider-model-discovery-repository.ts`、`src/__tests-integration__/model-probe-migration.test.ts`、`credential-probe-migration.test.ts`（合计 5 个）。
- **测试**：model-probe-migration 新增"0077 并发同 idempotency_key 恰写一条 run、item 不重复、写入错误不静默"用例，2/2 通过。

## 5b. 终审修复轮明细（commit `ac1c6de`，2026-09-21 22:26）

### 终审一（P1）— READY 探针证据身份门禁（MODEL_VALIDATION_STALE）
- **要求**：GET models 与 confirm models 必须拒绝与当前资源/当前成功发现不一致或过期的 READY 探针证据；身份不匹配返回 `MODEL_VALIDATION_STALE` 并要求重新同步或重新检测。
- **实现**：新增 `apps/control-api/src/providers/probe-evidence.ts`——`evaluateProbeEvidenceIdentity` 按五要素校验最近一次探针 run：① 凭证指纹（Key 轮换即失效）；② 解析端点 scope/host（capability base_url / endpoints[mode] 变更即失效，经 `resolveProviderEndpoint(MODEL_PERMISSION_PROBE)` 与落库同口径）；③ 官方目录内容哈希；④ 模型集 + 复合身份（`probeRequestHash` 与 `persistProbeRun` 同式复算 `request_hash` 权威比对）；⑤ 新鲜度（run 开始时间超过 24h TTL 即 `EVIDENCE_EXPIRED`）。GET 不匹配时不回填任何 READY 证据（模型全部不可选）并返回 `probe_evidence: { status: "MODEL_VALIDATION_STALE", reason, requires: "SYNC_OR_PROBE" }`；confirm 不匹配时 409 `{ error: "MODEL_VALIDATION_STALE", reason }`。
- **文件**：`apps/control-api/src/providers/probe-evidence.ts`（新增）、`model-discovery-routes.ts`（GET/confirm 门禁 + persistProbeRun 复用 probeRequestHash）。
- **测试**（pool027 新增集成用例，10/10 通过）：基线 CURRENT + READY 可选 → Key 轮换（`CREDENTIAL_FINGERPRINT_MISMATCH`）→ 端点变更（`ENDPOINT_MISMATCH`）→ 目录哈希变更（`DISCOVERY_SOURCE_HASH_MISMATCH`）→ 模型集删除（`MODEL_SET_MISMATCH`）→ 证据拨回 25h 前（`EVIDENCE_EXPIRED`）——全程无任何新探针 run，GET/confirm 均拒绝；随后重新同步生成新 run → 恢复 CURRENT 且 confirm 200。

### 终审二（P1）— 故障证据配置哈希绑定实际解析端点
- **要求**：`providerChatConfigHash` 与认证失败证据忽略 capability base_url 和 mode-scoped endpoints；须对 `resolveProviderEndpoint` 返回的实际 canonical 端点做哈希，Gateway 记录失败与凭证恢复校验用同一逻辑。
- **实现**：`providerChatConfigHash` 增加 `{ env, resolvedBaseUrl }` 入参；caller 在解析端点后以实际 `baseUrl` 计算证据哈希；新增导出 `capabilityChatConfigHash(provider, mode, model, capabilitySet, env)`——内部经 `resolveProviderEndpoint(CHAT_COMPLETIONS)` + `capabilityConfiguredEndpoints` 解析后调用同式哈希，端点歧义抛错。凭证恢复链路（credential-probe-routes + CredentialChatProbeRepository）的 `configHash` 回调签名扩展为 4 参（携带 provider.capability_set），与 Gateway 完全同源；端点无法解析时失败关闭 `configuration_changed`；`provider_unsupported` 在规范化处独立抛出，不被吞掉。哈希 payload 不含 scope 枚举、未升 version——解析结果与旧口径相同的配置哈希不变，既有未过期的故障证据仍可恢复（liveness 优先）。
- **文件**：`packages/provider-adapters/src/openai-compatible-caller.ts`、`index.ts`、`__tests__/openai-compatible-caller.test.ts`；`apps/control-api/src/providers/credential-probe-routes.ts`；`packages/database/src/repositories/credential-chat-probe.ts`、`apps/control-api/src/__tests-integration__/credential-chat-probe.test.ts`。
- **测试**：caller 单测（79/79）——`upstreamConfigHash` 等于对解析端点的 `providerChatConfigHash` 口径，endpoints 变更即哈希变化；credential-chat-probe 集成（20/20）——mode 专属端点未变时恢复成功且调用落在该端点、端点配置变更为 409 `configuration_changed` 且零上游调用、端点歧义（未知自定义域名）409 `configuration_changed` 失败关闭。

### 终审三（P2）— 0077 迁移非破坏化（fail-closed 预检）
- **问题**：0077 原实现以 DELETE 清除重复 run/item 后加唯一约束——非纯加法，可能销毁探针审计证据。
- **实现**：0077 原地改写（分支未发布、0076/0077 从未应用于任何生产库，符合"未发布可重写"前提；未新建迁移编号）：up() 先做 fail-closed 预检，发现同 `(enterprise_id, idempotency_key)` 重复 run 即整体拒绝执行并完整保留数据，绝不自动删除；预检通过后仅添加唯一约束。down() 不变（仅删约束，0076 防回滚门禁不受影响）。
- **最终迁移 checksum（SHA-256，部署前必须校验）**：
  - `0076_provider_model_probe.js` = `cf99f269048e4cfdff1b6493d93c6700b1fd3350d98dbb591dc7ea9d37dd61fb`
  - `0077_provider_model_probe_run_identity.js` = `373125273893c3f6a737c8727107b5e88a91bf28a8ae36506494b7dcd2c0c654`
- **部署规则**：① 仅允许在**从未应用过旧版 0077** 的库上直接应用本版本（生产/发布线均满足）；② 若某开发/测试库已应用过含 DELETE 的旧版 0077，须先人工核对无数据损失、并删除 `kysely_migration` 中 0077 记录后重跑（或保持现状并人工确认约束在位）；③ 应用时若预检失败（存在重复 run），迁移整体失败、数据零损失——须运维人工甄别合并重复 run 后重试，迁移本身不做任何删除；④ 部署脚本须校验上述 checksum 后方可执行 `db:migrate`。
- **测试**：model-probe-migration 3/3——新增"预检 fail-closed：重复 run 存在时 migrateToLatest 报错、run/item 原样保留；人工合并后重试通过"用例；原并发原子性与回滚门禁用例不受影响。

## 5c. 外部审核修复轮明细（commit `5160002`，2026-09-22）

独立审核对候选（4a3d139 → ca533e3，38 文件）出具 6 项 P1 与 P2/P3 清单。以下为 6 项 P1 的逐项处置（全部现场核实属实后修复）：

### 审核一（P1）— 同步面板零可选误导
- **问题**：`ResourceModelDiscovery.tsx` joinable 过滤 `isModelSelectable` 后，探针失败模型被静默隐藏；READY=0 时显示"当前所有可用模型均已接入服务中"——立项动机的变体重演。
- **修复**：新增 `blockedJoinable` 集合——探针失败/证据过期的未接入模型单独以警告横幅列出（含 `modelStatusLine` 脱敏原因，如"当前套餐未开通此模型权限 · HTTP 403"）；joinable=0 且存在 blocked 模型时展示 blocked 横幅而非"均已接入"；仅当确无未接入模型时才显示原提示。
- **测试**：web `ResourceModelDiscovery.sync.test.tsx` 新增 2 例——混合场景（READY 与失败共存，横幅列出失败模型及原因）；全失败场景（READY=0，明示"请检查凭证/端点配置后重新检测"，不出现"均已接入"）。

### 审核二（P1）— READY 证据门禁自锁（REMOVED 行）
- **问题**：GET/confirm 的证据身份校验以 `latest.items` 全集计算模型集；官方目录下架模型后快照保留的 REMOVED 行参与比对，使当次同步的新鲜探针证据立即被判 `MODEL_VALIDATION_STALE`。
- **修复**：`probe-evidence.ts` 新增共享助手 `currentAvailableModelIds`（过滤 `availability_status === "REMOVED"`），GET 与 confirm 统一使用，禁止两处各自维护；GET 的证据回填块同步收敛为 `applyProbeEvidenceOverlay`。
- **测试**：control-api 单测 `probe-evidence.test.ts` 3 例（过滤函数、下架后证据保持 CURRENT、不过滤时正确 STALE 对照）；pool027 集成新增端到端用例——目录下架 glm-5.3 后 re-sync，REMOVED 行保留、证据 CURRENT、confirm 200。

### 审核三（P1）— 端点歧义误报上游 500
- **问题**：caller 对端点歧义/base_url 缺失返回合成 `HTTP 500`，探针证据显示"上游暂不可用，可重试"——本侧配置错误被伪装成上游故障（RC-2 证据失真）。
- **修复**：caller 返回 `status=0 + upstreamCode + failureLayer=CLIENT + 预置 unifiedAvailabilitySignal=CONFIGURATION_ERROR`，不伪造任何上游 HTTP 状态；`mapProbeOutcome` 在状态分类之前识别该 upstreamCode，映射为 `MODEL_PROBE_CONFIGURATION_ERROR`（不可重试、httpStatus=null、原因明示"本侧端点或凭证配置无法解析"）。Gateway 依据预置信号将其判为 CONFIGURATION_ERROR 而非 TECHNICAL_FAILURE。
- **测试**：caller 单测新增端点歧义用例（零上游调用 + 五项断言）；新增 `model-discovery-probe.test.ts` 4 例（配置错误三码、真实 5xx 不被误伤、传输失败仍可重试）。

### 审核四（P1）— 额度同步 worker 漏改
- **问题**：`apps/worker/src/coding-plan-quota/runner.ts` 的 `supportsQuotaSync` 严格比较 `=== "kimi"`，生产大写 code 资源被整体跳过（WP02 声称覆盖额度同步的残余）。
- **修复**：新增 `quotaProviderCode`（canonicalProviderCode 规范化后匹配 kimi/zhipu），规范化结果回写资源行，下游 `queryCodingPlanQuota`/恢复判定/日志全部使用 canonical code。
- **测试**：worker `runner.test.ts` 新增集成用例——provider.code="Kimi"（大写）资源纳入同步（resourcesScanned=1、两窗口 SUCCESS）；修复前该用例 resourcesScanned=0。

### 审核五（P1）— 真实验证严格比较 zhipu
- **问题**：`model-discovery-routes.ts` 真实验证路由 `provider_code === "zhipu"` 严格比较，大写 code 丢失 glm-5.3 专属 `reasoning_effort=max` 与工具验证。
- **修复**：`canonicalProviderCode(target.provider_code)` 规范化后判定 `isGlm53`。
- **测试**：pool027 集成新增用例——provider.code="Zhipu"（大写）完成接入后对 glm-5.3 真实验证，断言上游请求体含 `reasoning_effort: "max"` 与 tools。

### 审核六（P1）— 体量门禁 3 处违例
- **问题**：model-discovery-routes.ts 602 逻辑行（>400 未登记例外）；ResourceModelDiscovery.tsx 768（>基线 723）；model-discovery.ts 506（>基线 484）——均为本候选生长造成。
- **修复**（拆分而非登记例外）：① `model-discovery.ts` → 拆出 `model-discovery-probe.ts`（mapProbeOutcome + probeModelPermissions），407 行；② `model-discovery-routes.ts` → 拆出 `model-discovery-sync.ts`（persistProbeRun + syncResourceModels + 端点覆写助手），392 行；③ `ResourceModelDiscovery.tsx` → 拆出 `ResourceModelDiscoveryShared.tsx`（DiscoveredModelItem/ModelDiscoveryResponse 类型 + ModelChoice/modelStatusLine/isModelSelectable/formatFacts），689 行，类型从原路径 re-export 保持既有导入不变。
- **验证**：`check-source-size.mjs` 545 文件全过；`check-architecture.mjs` 无运行时循环。

### P2 点名项处置
- `recordModelProbeRun` 调用处的 `as never` 逃生舱已移除（`model-discovery-sync.ts`，实参与 `ModelProbeRunInput` 类型一致后通过 typecheck）。
- web 第三处 k3-256k 硬编码残余（onboard 成功回调 `model.id !== "k3-256k"`）已移除，选择集完全由 READY 口径决定。

### 本轮复跑测试结果（2026-09-22 11:39-11:52，HEAD=5160002）

| 测试组 | 结果 |
|---|---|
| provider-adapters 全量（含新增 probe 映射 4 例 + caller 端点歧义 1 例） | ✅ 328/328 |
| control-api 单测全量（含 probe-evidence 3 例） | ✅ 95/95 |
| Web 全量（含同步面板 2 例新用例） | ✅ 478/478 |
| pool027 集成（含 REMOVED 行 1 例 + 大写 Zhipu 验证 1 例） | ✅ 12/12 |
| credential-chat-probe 集成 | ✅ 18/18 |
| worker quota runner 集成（含大写 Kimi 1 例） | ✅ 3/3 |
| database 迁移门禁（0076/0077） | ✅ 4/4 |
| gateway pool043 | 7 通过 / 2 失败——与本报告既录基线既有失败完全一致（结算域，与本计划无关） |
| typecheck（adapters/database/control-api/web/worker） | ✅ 全过 |
| lint（adapters/control-api/web/worker） | ✅ 全过 |
| 架构门禁 / 体量门禁 | ✅ 全过（545 文件） |

## 5d. P2/P3 收尾轮明细（2026-09-22，审核报告完整清单送达后）

外部审核完整报告（`V4/Evidence/KIMI-DISCOVERY-PROBE-FIX-20260921/仟流智算-I1代码质量审核报告-Kimi模型发现与权限探针修复-20260922.md` §4）送达后，对剩余 10 项 F-P2（F-P2-1/2/8 已在修复轮闭环）逐项现场核实并实施，另消化 P3 可实施项与测试矩阵缺口。本轮代码与测试累计 24 文件修改 + 2 新文件（+621/−116）。

### P2 逐项处置（F-P2-3/4/5/6/7/9/10/11/12/13）

| # | 处置 |
|---|---|
| F-P2-3 | `mapProbeOutcome` 对 401 按上游脱敏语义细分：额度/套餐类（`upstreamErrorKind=QUOTA_EXHAUSTED/WINDOW_EXHAUSTED/PLAN_EXPIRED` 或 upstreamCode 含 quota/plan/entitle/subscription）映射 `PLAN_NOT_ENTITLED`，其余保持 `AUTH_FAILED`——"套餐未开通"不再误显示为"凭证鉴权失败"。单测 8 例正/反钉住。 |
| F-P2-4 | `CredentialValidationEvidence` 合同新增可选 `endpointScope/endpointHost`；探针（CHAT_COMPLETIONS 解析）与 confirm 回填、GET overlay（run 冻结列）三处落值；`PublicCredentialValidation` 暴露 `endpoint_scope/endpoint_host`，API 可回答"请求打到哪个 host"。 |
| F-P2-5 | 新增 `resolveProviderModelsEndpoint`（MODEL_DISCOVERY_SOURCE operation 的唯一消费者）：`discoverFromProviderApi` 与 `officialSourceConfig` 兜底分支均经端点策略解析 `/models` 地址，不再手拼 legacy baseUrl；策略判歧义时抛 `OFFICIAL_SOURCE_AMBIGUOUS` 失败关闭。策略无法解析时保留既有猜测兜底（行为兼容，白名单仍守门）。 |
| F-P2-6 | `coding-plan-quota.ts` 删除本地双份硬编码端点，Kimi/Zhipu 额度端点改经端点策略 `CODING_PLAN_QUOTA` 分支（`KIMI/ZHIPU_CODING_PLAN_QUOTA_URL` 常量唯一来源）；策略未覆盖时按"不适用"返回空 windows。 |
| F-P2-7 | 经营同步两处大写 code 残余：`provider-operating-facts.ts` 经 `canonicalProviderCode` 判定 DeepSeek；worker `runner.ts` 以 `operatingProviderCode`（canonical → deepseek/kimi/zhipu | null）替换严格比较 `isProviderCode`，下游一律消费 canonical code。同类残余顺手收敛：quota-window-routes、provider-finance-legacy-resolution。 |
| F-P2-9 | GET /models 胖路由内联手写 Kysely join 删除，改用仓储 `getResourceForModelDiscovery`（与 confirm/sync 同一过滤口径，补上 provider/resource 状态校验）；资源不可服务时 404。 |
| F-P2-10 | 探针上限（>5）外的 CHAT 模型产出 `NOT_RUN` 证据（合同已定义但代码从未产出），不再以 `credential_validation=null` 落到前端"不可用"红字；web `modelStatusLine` 对 NOT_RUN 显示中性 warn 文案；`discoverySummary`/overlay 的 `credential_failed` 计数排除 NOT_RUN。EMBEDDING 等维持 null 口径。 |
| F-P2-11 | `resultCache`/`inFlight` 增加 FIFO 淘汰（上限 200 项）——cacheKey 含凭证指纹的高基数写入不再使长跑内存单调增长；60s TTL 内 200 项远超并发检测需求，命中语义不变。 |
| F-P2-12 | sync 60s 缓存命中路径抽出 `reusedSnapshotWithProbeEvidence`：与 GET 同口径回填探针证据（复用身份门禁 + overlay），前端不再闪烁"全部不可选"；缓存旁路条件从"任一模型 compatible=false 即永远绕过"改为"存在可重试（瞬态）探针失败才绕过"——AUTH_FAILED/PLAN_NOT_ENTITLED/404 等永久失败不再击穿额度保护。 |
| F-P2-13 | 端点失败兜底公式三处复制粘贴收敛为 `probeEndpointIdentity`（probe-evidence.ts 唯一实现）：GET cacheKey、`evaluateProbeEvidenceIdentity`、`persistProbeRun` 三处同源调用；`ProbeRunEndpointScope` 类型（database 包导出）消除仓储层 `as` 收窄强转（F-P2-2 类型诚实化残余）。 |

### P3 可实施项处置

- `latestModelProbeRun` 增加 `id desc` 决胜键（同 started_at 排序确定）。
- `discoverProviderModelsUncached` 不再改写调用方入参对象（规范化产出新对象）。
- `providerChatConfigHash`/`providerChatBaseUrl` 函数内规范化 code（哈希输入不再依赖调用方纪律；现唯一调用方已规范化，哈希值不变）。
- 探针测试 env 隔离：model-discovery 系测试统一传 `env: {}`。
- web `isSelectable` 与共享 `isModelSelectable` 同公式两份收敛为单一实现（审核轮吞错 catch 在文件拆分中已消失，无需处置）。
- 新增 `0078_provider_model_probe_enum_checks.js` 加法迁移：为 0076 的 endpoint_scope/resource_mode/status 与 probe_item.validation_status 补 CHECK 约束（值域含 `ENDPOINT_SCOPE_AMBIGUOUS` 与 `NOT_RUN`）；应用前 fail-closed 预检值域外既有行，绝不改写/删除证据；down 仅删约束。两个迁移门禁测试的回滚链断言已同步更新。

### P3 登记项（未实施，含理由）

- 探针 run 保留期清理策略：涉及证据保存/清理的产品口径裁决（与"证据零损失"原则相关），留 Planning 决策，建议后续以可配置保留期（如 30d）+ sweep 实现。
- credential-chat-probe.finish 端点歧义时 run 悬挂 RUNNING 至 90s 过期：方向失败关闭、可接受，如需 STALE 终态另立小改。
- 公开响应未显式暴露 discovery_status/gateway_compatibility（折叠于既有字段）：计划 §10.1 为"建议"格式，记偏差。
- 待计划作者裁决：endpoint-policy 对 Kimi API 模式下未知自定义 legacy base_url 静默按 API 模式解释（与本轮测试"待裁决口径，显式钉住"用例一致），与计划 §7.2.6 字面冲突，需裁决口径或修订计划，未擅自改行为。
- 既有问题登记（非本候选引入）：`syncFlights` 共享飞行对 FastifyReply spread 响应不可靠；pool043 gateway 2 用例基线失败。

### 测试矩阵缺口补钉（审核 P2 附带清单）

- 404 模型不存在（REQUEST_REJECTED 保留 httpStatus）、caller 超时（504/`upstream_timeout` → UPSTREAM_UNAVAILABLE 可重试）、未知状态保留（418）——单元钉住。
- 端点歧义路径（F-P1-3 回归）与 REMOVED 身份回归已在此前轮覆盖（caller 断言 + probe-evidence.test + pool027 REMOVED 用例）。
- 真实并发 singleflight：新增闸门式并发用例（在飞 Promise 未完成前并发调用共享同一飞行，只发一轮上游请求）。
- `persistProbeRun` 失败 warn 行为：新增 `model-discovery-sync.test.ts`（仓储抛错 → warn 记录 request_hash/错误消息、不含凭证明文、不向调用方抛出；成功不 warn）。

### 本轮复跑测试结果（2026-09-22 16:00-16:45）

| 测试组 | 结果 |
|---|---|
| provider-adapters 全量（含本轮新增 18 例） | ✅ 346/346 |
| Web 全量 | ✅ 478/478 |
| worker 全量单测 | ✅ 95/95 |
| control-api 全量 | 359 通过 / 4 失败 / 19 skipped——4 个失败（pool015/pool025/provider-finance/system-settings-v1）经 git stash 基线对照证实为 fc8becd 上同样失败的 0072 回滚链断言（0074 落地时即断裂，属基线既有，非本轮引入），与本轮改动无关 |
| pool027 + credential-chat-probe 集成 | ✅ 32/32 |
| 迁移门禁（0076/0077/0078 回滚链） | ✅ 4/4 |
| database 全量套件 | 21 失败为基线既有（0078 落地前后的两次运行失败集合一致，含 pool043 等已登记项；非本轮引入） |
| typecheck ×7（含 gateway） | ✅ 全过 |
| lint ×6 包 | ✅ 全过 |
| 架构门禁 / 体量门禁 | ✅ 545 文件全过 |


## 6. 未验证边界（诚实声明）

1. **真实 Kimi 上游未验证**：未使用任何生产/真实 Key 执行线上探针（WP07 要求的专用测试 Key 真实验证未执行，属需授权项）。2xx/4xx/429/5xx/超时/网络失败映射均由 mock/集成环境覆盖，非真机证据。
2. **生产验收（计划 WP08/WP09、§14.3）未执行**：部署候选、生产 Provider 兼容读取、页面四模型展示、Kimi 控制台对账均待授权。
3. **DeepSeek/智谱线上回归未执行**：仅本地单测/集成覆盖（pool027 含智谱文档解析、DeepSeek List Models、确认/验证链路），生产数据未触碰。
4. ~~`MODEL_VALIDATION_STALE` 确认时探针新鲜度重校验未单独实现~~ **已在终审修复轮实现**（§5b 终审一）：GET/confirm 五要素证据身份门禁 + 24h 新鲜期；剩余边界为 TTL=24h 为本地实现选定的默认值，可按运维需要调整常量。
5. gateway pool043 两个基线既有失败未修复（超出本计划范围）。
6. 终审修复轮复跑覆盖 §5 全部测试组 + 三包 lint + 三包 typecheck（web 未改动，仅回归测试）；全仓 build 未在终审轮重跑（终审轮不触及构建配置，结论沿用）。
7. **终审二部署规则**：配置哈希语义变更后，capability base_url 与实际调用端点不一致的历史资源，其未过期故障证据会被判 `configuration_changed`（一次性的正确拒绝，属端点语义真实的修正）；此类资源重新产生一次网关失败证据或更新凭证后即可正常恢复。
8. **终审三部署规则**：0077 为原地改写，checksum 见 §5b；仅允许在未应用过旧版 0077 的库上应用（生产满足），详见 §5b 部署规则四条。
9. ~~**审核 P2 ×13 / P3 清单未逐项闭环**~~ **已在 P2/P3 收尾轮闭环**（§5d）：F-P2-3/4/5/6/7/9/10/11/12/13 全部实施；F-P2-1/2/8 已在审核修复轮闭环；P3 可实施项落地 + 0078 CHECK 迁移 + 测试矩阵缺口补钉；剩余登记项（探针 run 清理策略、STALE 终态、公开响应字段偏差、待裁决口径、syncFlights/pool043 既有问题）逐条列明理由见 §5d。
10. **审核一轮 P1-3 的衍生语义**：caller 对端点歧义返回 `status=0 + CONFIGURATION_ERROR`，`Outcome.status` 文档语义为"0 表示传输层失败"，本处复用 0 表示"请求未发出"；如需精确分层，后续可在 contracts 增加 `LOCAL_CONFIGURATION` failureLayer 枚举值（涉及 contracts 包变更，本轮未扩权）。
11. **0078 部署规则**：纯加法 CHECK 约束迁移；应用前预检值域外既有行并 fail-closed；生产（未应用过 0076/0077 的库）可直接应用，down 仅删约束、无数据风险。
12. **F-P2-12 缓存语义变更**：sync 60s 缓存命中路径现在回填探针证据（携带 probe_evidence 字段）；永久性探针失败（AUTH_FAILED/PLAN_NOT_ENTITLED/404/配置错误）不再绕过 60s 缓存——管理员在失败后 60s 内重试同步会得到缓存结果，需等待缓存过期或修凭证/端点后重试。

## 7. 生产数据与 Key 声明

- 未使用、未读取、未存储任何生产 Kimi Key 或其他生产凭证。
- 未连接、未查询、未修改任何生产数据库；所有集成测试使用本地 Testcontainers PostgreSQL。
- 未 SSH/未以任何方式操作公网服务器；未修改生产容器。
- 探针证据设计与实现均不落 Key、Authorization、Prompt、上游响应正文（0076 迁移有列名黑名单断言）。

## 8. 候选获取与部署边界（2026-09-22 更新）

本节取代旧版指向 `5160002` / `ac1c6de` 的 bundle、补丁及部署说明。外部 I1 审核对象为 `ebc578b`，最终代码提交为 `3ac09ff`；后续收口提交仅包含文档和测试证据，不能遗漏 0078 或 worker 的修改。2026-09-22 用户授权本地 commit 并上传 GitHub 候选分支，未授权合并 main 或部署。

候选可从 `origin/codex/kimi-model-discovery-probe-fix-20260921` 获取。部署前由 Planning 锁定批准的完整 SHA，重新核对服务器版本及迁移状态；不得直接照搬旧版 bundle 命令。实际部署步骤、备份和回滚方案在 WP08 获得授权后依据服务器现状确定。

以下旧版命令仅保留为历史记录，**已作废，不得执行**：

```bash
# 1) 在任意检出 4a3d139 的环境中获取候选（不 push 的替代：bundle 或补丁，含 618e919..5160002 全部整改/终审/审核修复）
cd /Users/mac/.codex/worktrees/kimi-discovery-fix-20260921
git bundle create /tmp/kimi-fix-5160002.bundle 5160002 ^4a3d139346dfc21b37eab3783a16dd0aaa427242
# 或：git format-patch 4a3d139..5160002 --stdout > /tmp/kimi-fix-5160002.patch

# 2) 目标机器（基于 4a3d139 的部署源）应用候选
git fetch /tmp/kimi-fix-5160002.bundle codex/kimi-model-discovery-probe-fix-20260921:codex/kimi-model-discovery-probe-fix-20260921
git checkout codex/kimi-model-discovery-probe-fix-20260921   # fast-forward 自 4a3d139
# 补丁方式：git am /tmp/kimi-fix-ac1c6de.patch

# 3) 校验迁移 checksum（§5b 终审三）后构建与迁移（先代码后迁移，迁移为纯加法、可回滚）
shasum -a 256 packages/database/migrations/0076_provider_model_probe.js packages/database/migrations/0077_provider_model_probe_run_identity.js
corepack pnpm@11.11.0 install --frozen-lockfile
corepack pnpm@11.11.0 run build
corepack pnpm@11.11.0 run db:migrate          # 应用 0076 + 0077（无破坏性；0077 预检失败即人工甄别，绝不自动删除）
# 回滚：corepack pnpm@11.11.0 run db:rollback（有探针证据时 0076 拒绝回滚，属预期门禁）

# 4) 部署 control-api / gateway / web（沿用现有 deploy/scripts 与 compose 流程）
#    部署后不应自动创建 Kimi 资源（WP08 约束）。

# 5) 验证清单（生产验收前）：
#    - 对现有 Kimi Provider（capability_set.base_url=api.moonshot.cn/v1）执行模型检测：
#      探针应命中 api.kimi.com（Kimi 控制台出现对应调用），页面显示 4 个官方模型及逐模型状态；
#    - 换 Key 重新检测：旧探针证据失效（request_hash 变化）；
#    - DeepSeek/智谱同步无回归；仅 READY 模型可确认。
# 回滚：git checkout 4a3d139 重部署即可；0076 表为加法，可不回滚。
```

## 9. 实现方收口与 GitHub 候选上传（2026-09-22）

本节为实现方核对记录，不签发独立 Audit PASS。外部代码质量审核的 R2/R3 PASS 归属 Kimi Code，审核对象为 `ebc578b`（代码 `3ac09ff`）；本次不改变生产代码。当前记录不替代第一道功能与验收审计、业务验收、Gate 5 或 Planning Final Review。

### 9.1 本地补跑结果

| 检查 | 本次实跑结果与限制 |
|---|---|
| quality:architecture / quality:size | 通过，545 个生产文件 |
| quality:duplication | 通过，重复行 0.81%，阈值 5% |
| quality:licenses | FAIL：MPL-2.0；基线已有 @resvg/resvg-js，本候选未变更 lockfile、根 package.json 或 worker package.json。不在本轮修改许可证批准策略。 |
| quality:coverage 聚合 | FAIL：首段 domain 180 个测试通过，但 statements/lines 94.56% < 95% 全局阈值；其余子任务另行执行。 |
| quality:coverage:changed / pool029 / pool030 / pool040 | 均 exit 0 |
| quality:coverage:pool043 | FAIL：Node 段 294 passed、2 failed、5 skipped（另有登录 fixture suite 失败）；补跑 Gateway 79 passed、2 failed，为已登记 api_cost 断言；Web 93 passed、1 超时。Web 超时原因本轮未单独证实，不记为基线已确认或 PASS。 |
| quality:coverage:ratchet | FAIL：domain statements/lines 94.56% < 97.37%、functions 95.23% < 95.83%；pool029-web statements/lines 97.47% < 99.65%；pool030-gateway statements/lines 98.5% < 100%、branches 96.29% < 100%；pool043 三份 coverage-summary 缺失。不得把此结果写成 PASS 或 N/A。 |
| quality:mutation:pool021-r2 | exit 0，44/44 mutants Killed，得分 100%；更新证据为 packages/provider-adapters/reports/mutation/pool021-r2.json。范围仅 kimi-adapter.ts 指定行，不覆盖本候选新增的发现/端点/探针逻辑。 |
| 其他 mutation / 全部 quality:* | 未穷尽执行。现有 mutation 配置目标与候选改动文件无交集；广泛历史套件无法补足本候选增量变异证据。保留 Evidence Gap，提交 Planning 决定是否批准范围 N/A 或另设候选专用配置；本实现方未批准豁免。 |

以上失败说明仓库全量机械门禁未全绿，不等于否定外部审核所列特定 19 项复跑结果，也不能以外部 19/19 替代全量门禁。未修正阈值、删测试或绕过 hook。本轮停止扩展与 Kimi 无关的代码治理，后续验收以真实接入和调用流程为重点。

### 9.2 迁移锁

| 文件 | SHA-256 |
|---|---|
| 0076_provider_model_probe.js | cf99f269048e4cfdff1b6493d93c6700b1fd3350d98dbb591dc7ea9d37dd61fb |
| 0077_provider_model_probe_run_identity.js | 373125273893c3f6a737c8727107b5e88a91bf28a8ae36506494b7dcd2c0c654 |
| 0078_provider_model_probe_enum_checks.js | 27679407456cf8db51b2878bbaa50f7bead9eb288bb905008ff8eac45d0cc4e8 |

### 9.3 提交 Planning 的裁决与登记

裁决请求：Kimi API 模式对未知自定义 legacy base_url，选择 A 是维持当前按 API 地址解释的行为并明确修订计划 §7.2.6 的适用范围，优点是兼容现有自定义网关，代价是保留旧字段隐含模式语义；选择 B 是要求显式 endpoints.API，否则失败关闭，优点是与计划字面口径一致，代价是现有配置需迁移并可能暂时不可用。当前保持测试钉住的 A 行为，等待计划作者裁决，不视为本实现方已批准。

探针 run 保留期由 Planning 确定证据留存政策；probe finish 的 STALE 终态完善和 syncFlights 响应共享问题登记到后续维护任务；pool043 及迁移链断言问题由基础测试维护任务处理。§5d 实际列举为 pool015/pool025/provider-finance/system-settings-v1，而 handoff 列举 pool015/pool026：本轮未取得 pool026 的独立失败证据，保留名称不一致，需 Planning 核对原始用例，不宣称已验证 pool026。上述登记不扩大本次代码变更。

### 9.4 当前授权与后续动作

- 本次获准：在现有候选分支提交本地报告与测试证据，并推送 GitHub 同名候选分支。上传不是发布验收通过。
- 待安排：第一道阶段功能与验收审计、适用业务验收、Gate 5、Planning Final Review；本收口记录不替代上述角色签发。
- WP08 需另行明确批准：目标服务器、候选完整 SHA、备份与回滚点、0076–0078 数据库迁移、control-api/gateway/web/worker 构建及重启窗口；部署前复核服务器版本，保留现有资源和授权。
- WP09 需另行明确批准：使用指定 Kimi Code Key 执行会消耗少量额度的探针与真实验证，创建或确认测试资源/模型、采用确认的计价规则、启用测试路由和测试主体授权，执行 Chat Completions 与 Anthropic Messages 最小调用并核对用量记录。
- 截至本节编写，未合并 main、未部署、未触碰生产；远端上传结果以后续 git push 回执和远端 SHA 核验为准。

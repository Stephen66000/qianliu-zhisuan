const fs=require('fs'),vm=require('vm'),ts=require('/Users/mac/Projects/仟流智算/node_modules/typescript');
const root='/Users/mac/Projects/仟流智算/';const src=fs.readFileSync(root+'apps/web/src/components/dashboard/standard-home-model.ts','utf8');
function model(s){const box={exports:{},require:()=>({})};vm.runInNewContext(ts.transpileModule(s,{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2022}}).outputText,box);return box.exports;}
const baseline=model(src);const currency=model(src.replace('if (current[0]!.currency !== previous[0]!.currency) return null;','if (false) return null;'));const length=model(src.replace('if (current.length !== 1 || previous.length !== 1) return null;','if (false || previous.length !== 1) return null;'));
const result=[{mutant:'132',scenario:'current CNY 200 vs previous USD 100',expected:baseline.moneyChangePercent([{currency:'CNY',amount:'200'}],[{currency:'USD',amount:'100'}]),mutated:currency.moneyChangePercent([{currency:'CNY',amount:'200'}],[{currency:'USD',amount:'100'}])},{mutant:'127',scenario:'current CNY200+USD50 vs previous CNY100',expected:baseline.moneyChangePercent([{currency:'CNY',amount:'200'},{currency:'USD',amount:'50'}],[{currency:'CNY',amount:'100'}]),mutated:length.moneyChangePercent([{currency:'CNY',amount:'200'},{currency:'USD',amount:'50'}],[{currency:'CNY',amount:'100'}])}];console.log(JSON.stringify(result,null,2));

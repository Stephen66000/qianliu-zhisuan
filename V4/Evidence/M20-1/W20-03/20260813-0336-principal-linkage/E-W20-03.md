# E-W20-03｜导入成员衔接使用主体

| 字段 | 结果 |
| --- | --- |
| 项目／Stage／工作包 | 仟流智算 2.0／Stage 03／M20-1／W20-03 |
| 风险 | `HIGH` |
| Evidence 时间 | 2026-08-13 03:36（Asia/Shanghai） |
| 执行位置 | `/Users/mac/.codex/worktrees/708d/仟流智算` |
| 候选指纹 | 基线 HEAD `6fc1bec2a211c0d9c399400076cc56720f5c1815`；迁移头 `0049`；`pnpm-lock.yaml` SHA-256 `ade8a028cfbcb22e2fb4f7c5e82c223ec9cf75f8373c8790157405512515e3c6`；隔离 worktree `dirty` |
| 最终 Commit／Tree 锁 | `not_run`：未授权 commit，当前没有最终 Commit／Tree candidate lock；W20-10 仍需单独锁定 |
| AI Eval | `N/A`：本包没有生成式 Agent、Prompt 或模型行为变更 |
| 依赖 | W20-02 同候选完成 |
| 结论 | `W20-03=done` |

## 1. 输入与执行结果

- 输入为合成员工、部门、外部身份、主体、Key 和 Grant；未读取生产、客户或真实个人数据。
- `corepack pnpm@11.11.0 run quality`：预期完整质量门禁通过；实际 exit `0`。
- `corepack pnpm@11.11.0 run test:e2e`：预期新旧主体页面与代表性管理链不回归；实际 Web Playwright `29/29`，Gateway real Codex CLI 通过。
- 本文件只归档当前候选已执行结果，生成 Evidence 时未重复运行测试。

## 2. DoD 对照

| 冻结要求 | 实际结果／证据 | 状态 |
| --- | --- | --- |
| 按稳定身份匹配已有主体 | `w20-directory.test.ts` 覆盖员工编号大小写归一、显式本企业员工 Principal 绑定、外部身份读取；同名但不同稳定编号创建不同人员，不按姓名猜测 | `pass` |
| 新成员创建主体并初始化接入配置元数据 | Excel staging／apply 后断言 person、EMPLOYEE principal 和 `principal_access_config_state` 同时存在，新主体不预造 Key／Grant | `pass` |
| 同步部门关系及缺失部门状态 | 连接器和 Web 单测覆盖标准部门路径、缺失部门稳定 reason code 与“待归属”展示 | `pass` |
| 已有主体不重复创建 | 同文件重放、重复快照和同员工编号更新均不重复建 person／principal | `pass` |
| 重复同步不重置 Key／授权 | 集成测试先写入活动 Key／Grant，再执行更新、来源 inactive 和显式绑定，断言 Key／Grant 状态及额度保持不变 | `pass` |
| 冲突、离开通讯录、缺失部门状态明确 | 跨企业／错误显式 Principal 行进入 `CONFLICT/EXPLICIT_PRINCIPAL_INVALID` 且不阻断其他行；来源不再活跃进入 `SKIPPED/SOURCE_INACTIVE`；缺部门保留稳定状态 | `pass` |
| 导入后继续使用 1.0 接入能力 | 同候选的目录 E2E 覆盖导入成员可见，目录集成测试覆盖接入状态初始化；1.0 `WT-02/03` 覆盖 Key、模型额度与接入信息管理 | `pass（同候选组合证据）` |
| 一条脚本贯穿“真实厂商导入→新主体→配置真实 Key／额度／模型” | 无已批准厂商组织凭证，且测试不得创建真实上游 Key；上述三个合同已在同候选分别验证，不冒充真实外部全链 | `not_run` |

## 3. 失败、修复与复测

- 目录冲突、重复同步、来源 inactive 和缺部门均作为预期负向场景通过，没有遗留本包测试失败。
- E2E 首次缺 Chromium，安装项目锁定浏览器后全量重跑 `29/29`；未修改 W20-03 产品语义。

## 4. 残余风险与裁决

- 真实厂商组织到真实资源凭证的现场链未执行，原因是没有批准凭证且不得写外部系统；不以合成数据冒充现场验收。
- 稳定匹配、新建、接入元数据、部门状态、冲突待处理和既有 Key／授权不重置均已验证；未发现按姓名猜测绑定行为，满足 W20-03 DoD，裁决 `done`。


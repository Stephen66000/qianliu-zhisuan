# E-W20-02｜通讯录来源与导入

| 字段 | 结果 |
| --- | --- |
| 项目／Stage／工作包 | 仟流智算 2.0／Stage 03／M20-1／W20-02 |
| 风险 | `HIGH` |
| Evidence 时间 | 2026-08-13 03:36（Asia/Shanghai） |
| 执行位置 | `/Users/mac/.codex/worktrees/708d/仟流智算` |
| 候选指纹 | 基线 HEAD `6fc1bec2a211c0d9c399400076cc56720f5c1815`；迁移头 `0049`；`pnpm-lock.yaml` SHA-256 `ade8a028cfbcb22e2fb4f7c5e82c223ec9cf75f8373c8790157405512515e3c6`；隔离 worktree `dirty` |
| 最终 Commit／Tree 锁 | `not_run`：未授权 commit，当前没有最终 Commit／Tree candidate lock；不影响本包工程 DoD，W20-10 不得据此判 `done` |
| AI Eval | `N/A`：本包没有生成式 Agent、Prompt 或模型行为变更 |
| 结论 | `W20-02=done`；真实厂商组织现场验收按冻结计划保留 `not_run` |

## 1. 输入与执行结果

- 输入为合成企业、管理员、企微／飞书响应、Excel 模板和成员数据；未读取生产、客户或真实个人数据。
- `corepack pnpm@11.11.0 run quality`：预期 typecheck、lint、unit／integration、build、coverage、架构、大小、重复、生产依赖审计、许可证和 Evidence canary 全部通过；实际 exit `0`。
- `corepack pnpm@11.11.0 run test:e2e`：预期 Web 与 Gateway 代表性链通过；实际 Web Playwright `29/29`，Gateway real Codex CLI 通过。
- 本文件只归档当前候选已执行结果，生成 Evidence 时未重复运行测试。

## 2. DoD 对照

| 冻结要求 | 实际结果／证据 | 状态 |
| --- | --- | --- |
| 企业微信／飞书单向读取与统一格式 | `apps/worker/src/directory/connector.test.ts` 覆盖企微只读端点、飞书递归部门和分页成员、稳定外部 ID、去重、缺失部门及限流／401／403／500／网络／协议错误语义 | `pass` |
| 接口和 Excel 使用同一导入管道 | `apps/worker/src/__tests-integration__/directory-sync-worker.test.ts` 经统一 staging／apply 完成部分成功和重复快照；`apps/control-api/src/__tests-integration__/w20-directory.test.ts` 的 Excel 上传进入同一 Run／item／apply 结果模型 | `pass` |
| 模板、大小、重复行、非法行和部分失败 | `apps/control-api/src/directory/excel.test.ts` 覆盖固定模板、5 MiB、1,000／1,001 行边界、伪装文件、工作表／表头、公式、必填、非法部门和隐藏表；Control API 集成测试覆盖重复工号、错误行、正常行继续创建及行级 reason code | `pass` |
| 单次／重复／增量幂等 | 同一接口幂等键返回原 Run、冲突请求返回 409；同一 Excel 文件重放返回原 Run；后续同员工编号更新、稳定新编号增量创建，重复快照不重复建人 | `pass` |
| 失败精确到行且可修复重试 | Run item 保留 `row_number/status/reason_code`；可重试厂商错误保持 `QUEUED`，永久凭证错误为 `FAILED`；正常行不被错误行阻断 | `pass` |
| 凭证和个人信息不进入日志／Evidence | 集成测试断言 Secret 仅密文存储、不经 API 回显、不进入 operation log；连接器错误不回显上游响应；本 Evidence 只含合成标识 | `pass` |
| 页面主链 | Web 单测覆盖来源指纹、Secret 写后清空、`.xlsx` 限制、部分失败和缺部门状态；Playwright 覆盖真实页面下载模板、上传并显示处理结果 | `pass` |
| 企微／飞书真实内部组织现场验收 | 没有已批准只读凭证；冻结计划 §10 明确此情形先完成统一管道和 Excel、现场验收保持 `todo`，不得冒充通过 | `not_run` |

## 3. 失败、修复与复测

- 首次 E2E 环境缺少 Playwright Chromium，属于本机测试依赖而非产品缺陷；安装项目锁定的 Chromium 后重跑，最终 Web `29/29`。
- 本包最终门禁未留下测试失败；没有为现场凭证缺失伪造成功记录。

## 4. 残余风险与裁决

- 残余项仅为真实企微／飞书组织的现场验收，Owner 为接入 Owner；取得批准的只读凭证后执行，不操作客户数据。
- 两类连接器、Excel、统一管道、幂等、行级失败、重试与脱敏合同均已由当前候选自动测试覆盖，满足 W20-02 工程 DoD，裁决 `done`。


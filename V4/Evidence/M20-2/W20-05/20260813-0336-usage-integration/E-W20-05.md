# E-W20-05｜周期用量页面与 1.0 下钻集成

| 字段 | 结果 |
| --- | --- |
| 项目／Stage／工作包 | 仟流智算 2.0／Stage 03／M20-2／W20-05 |
| 风险 | `NORMAL` |
| Evidence 时间 | 2026-08-13 03:36（Asia/Shanghai） |
| 执行位置 | `/Users/mac/.codex/worktrees/708d/仟流智算` |
| 候选指纹 | 基线 HEAD `6fc1bec2a211c0d9c399400076cc56720f5c1815`；迁移头 `0049`；`pnpm-lock.yaml` SHA-256 `ade8a028cfbcb22e2fb4f7c5e82c223ec9cf75f8373c8790157405512515e3c6`；隔离 worktree `dirty` |
| 最终 Commit／Tree 锁 | `not_run`：未授权 commit，当前没有最终 Commit／Tree candidate lock；W20-10 仍需单独锁定 |
| AI Eval | `N/A`：本包没有生成式 Agent、Prompt 或模型行为变更 |
| 依赖 | W20-04 同候选完成 |
| 结论 | `W20-05=done` |

## 1. 输入与执行结果

- 输入为合成员工、项目和账本 Fixture；未读取生产或客户数据。
- `corepack pnpm@11.11.0 run quality`：预期完整质量门禁通过；实际 exit `0`。
- `corepack pnpm@11.11.0 run test:e2e`：预期完整 Web／Gateway 代表性链通过；实际 Web Playwright `29/29`，Gateway real Codex CLI 通过。
- 本文件只归档当前候选已执行结果，生成 Evidence 时未重复运行测试。

## 2. DoD 对照

| 冻结要求 | 实际结果／证据 | 状态 |
| --- | --- | --- |
| 首页员工消耗接入周期／单员工 | `Dashboard.test.tsx` 断言保留 1.0 月度总量，日／周／月周期及单员工选择传给服务端聚合，趋势由后端结果渲染 | `pass` |
| 用量概览接入员工／项目、周期和单主体 | `UsageOverviewPanel.test.tsx` 从 URL 恢复主体类型、周期、主体和 anchor，并传给后端；Playwright 从真实 `/usage` 页面验证员工／月状态 | `pass` |
| 排名进入同一主体状态 | 排名点击写回同一 `subject_id` URL 状态，随后明细链接使用该项目／员工口径 | `pass` |
| 1.0 筛选、URL、分页和证据下钻保留 | `Usage.test.tsx` 覆盖组合筛选 URL 恢复、清除、筛选变化重置页码及分页请求；概览下钻携带 subject、`settled_only` 和半开时间；POOL-012 E2E 覆盖复制型 URL、最终资源及清除 | `pass` |
| 指标、趋势、排名和请求明细同口径 | W20-04 守恒测试证明概览与明细同一 `detailQuery`；Web 测试证明排名和下钻复用相同 URL 状态，前端不重算账本 | `pass` |
| 搜索、分页、空态和加载态 | 主体选择器使用服务端 search／offset／limit；Control API 验证搜索分页并保留旧字段；Web 覆盖加载、错误、空数据三态 | `pass` |
| 移动端 | Playwright 在 `390×844` 重载真实用量页，验证主体／周期状态保持且页面无水平溢出 | `pass` |
| 大数据量 | 标准容量测试覆盖 100 员工＋100 项目及 1,000,000 ledger 的单主体聚合 P95；选择器使用服务端分页，不将全量主体装入前端 | `pass` |
| 实体移动设备与多浏览器人工视觉巡检 | 当前自动化运行环境为 Playwright Chromium；没有独立实体设备或 Safari／Firefox 人工巡检，冻结要求中的 390px 可用性已有自动断言 | `not_run` |

## 3. 失败、修复与复测

- 全量 E2E 曾发现“清除筛选”仍留下 `/usage?tab=details`，与 1.0 完整列表 URL `/usage` 不一致；最小修复为清空 search params。
- 修复后 `Usage.test.tsx` 相关单测、POOL-012 定向 E2E 及全量 Web Playwright `29/29` 均通过。
- 后续 E2E 的同名资源行定位歧义属于 W20-08 新增表格与测试定位冲突；收窄定位后全量复测通过，不改变 W20-05 产品行为。

## 4. 残余风险与裁决

- 未做实体设备／全浏览器矩阵人工巡检；当前 390px Chromium 自动化、服务端分页和标准容量已覆盖冻结计划要求的移动端及大数据量最低合同。
- 首页、概览、排名、URL、分页、搜索、空／加载态和 1.0 请求证据下钻均已在同一候选验证，且已修复实际发现的 URL 回归，裁决 `W20-05=done`。


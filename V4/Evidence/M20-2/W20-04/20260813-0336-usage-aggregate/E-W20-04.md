# E-W20-04｜周期用量聚合合同

| 字段 | 结果 |
| --- | --- |
| 项目／Stage／工作包 | 仟流智算 2.0／Stage 03／M20-2／W20-04 |
| 风险 | `NORMAL` |
| Evidence 时间 | 2026-08-13 03:36（Asia/Shanghai） |
| 执行位置 | `/Users/mac/.codex/worktrees/708d/仟流智算` |
| 候选指纹 | 基线 HEAD `6fc1bec2a211c0d9c399400076cc56720f5c1815`；迁移头 `0049`；`pnpm-lock.yaml` SHA-256 `ade8a028cfbcb22e2fb4f7c5e82c223ec9cf75f8373c8790157405512515e3c6`；隔离 worktree `dirty` |
| 最终 Commit／Tree 锁 | `not_run`：未授权 commit，当前没有最终 Commit／Tree candidate lock；W20-10 仍需单独锁定 |
| AI Eval | `N/A`：本包没有生成式 Agent、Prompt 或模型行为变更 |
| 结论 | `W20-04=done` |

## 1. 输入与执行结果

- 输入为可复现的合成员工、项目、企业时区、请求、Usage 和 Ledger 事实；未读取生产或客户账本。
- `corepack pnpm@11.11.0 run quality`：预期完整质量门禁通过；实际 exit `0`，其中相关 database、control-api、worker 和 web 测试通过。
- `corepack pnpm@11.11.0 run test:e2e`：预期现有请求事实链及新增概览入口通过；实际 Web Playwright `29/29`，Gateway real Codex CLI 通过。
- 本文件只归档当前候选已执行结果，生成 Evidence 时未重复运行测试。

## 2. DoD 对照

| 冻结要求 | 实际结果／证据 | 状态 |
| --- | --- | --- |
| 员工／项目、全体／单主体 | `usage-overview.integration.test.ts` 覆盖全体员工、单项目、项目归属快照及项目直发回退；Control API 拒绝主体类型错配并保持租户隔离 | `pass` |
| 今日／本周／本月及周一至周日 | Repository 集成测试覆盖 `TODAY/WEEK/MONTH`；上海周范围固定周一 00:00 至下周一 00:00，API 返回 7 个趋势点 | `pass` |
| 请求数、真实 Token、API 费用、套餐扣减统一 | API 和 Repository 对 requestCount、input/output/cache/reasoning、realTokens、apiCost、deductedQuota 做固定值断言 | `pass` |
| 缓存 Token 不重复计入真实 Token | 聚合 upsert 和概览测试明确断言 `realTokens=inputTokens+outputTokens`，缓存单列且不再相加 | `pass` |
| 跨日、跨月、时区、空结果和无权限／错类型 | 上海跨月、纽约 DST 回拨／跳时、23 小时自然日、空主体 24 小时零趋势、错类型不泄露及跨企业隔离均通过 | `pass` |
| 聚合与 1.0 请求明细逐项核对 | 员工和项目分别使用 `detailQuery` 回放现有 UsageRepository，请求数、Token、费用和扣减额度守恒；未结算事务被排除 | `pass` |
| 聚合缓存幂等与修正 | 小时／日桶幂等替换，不累计重放；归属修正清除旧项目维度；dirty 并发不丢失；缺水位、dirty 或过期数据回退 LIVE 而非伪装 0 | `pass` |
| 标准容量 | `w20-standard-capacity.integration.test.ts` 在 1,000,000 ledger 下校验单主体用量 P95 不超过冻结阈值 800 ms | `pass` |
| 与真实客户账本逐项对账 | 本轮未授权客户／生产数据操作；使用与真实 Schema、Repository 相同的合成 Fixture 验证 | `not_run` |

## 3. 失败、修复与复测

- 本包相关聚合、时区、守恒、缓存和 API 用例在最终 `quality` 全部通过，未留下包内失败。
- 对缺水位、dirty、过期和未结算事实采用显式回退／排除测试，没有通过补 0 掩盖数据缺口。

## 4. 残余风险与裁决

- 真实客户历史账本不在授权范围内，Owner 后续只能在目标环境授权下验证；当前不将合成账本称为生产结果。
- 冻结要求的口径、边界、时区、空态、隔离、明细守恒和容量均有自动化证据，未发现需要返回 W20-01 的基础事实歧义，裁决 `W20-04=done`。


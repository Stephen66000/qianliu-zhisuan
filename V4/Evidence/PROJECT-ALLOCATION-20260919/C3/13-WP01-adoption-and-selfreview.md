# WP01 C1/C2 采纳记录、WP01 静态自审 — 候选 C3

## A. C1（UNAUTHORIZED_DRAFT）处置

维持 v1.2 判定：整体拒绝，不修复不搬运。范式级参考（复合企业 FK、不可变触发器、部分唯一 current、零 UUID 哨兵、down 数据守卫）经 C2 两轮评审验证有效，作为 C3 设计输入记录于合同。

## B. C2（只读参考）逐文件采纳/拒绝

规则：禁止整体 cherry-pick/搬运/直接执行；采纳＝设计采纳，在新基线重新实现并全套重验。C2 经 R1/R2 评审淬炼的设计决策全部作为 C3 合同输入（见 10 号文档 §3/§9）；实现层按下表：

| C2 文件 | 处置 | C3 说明 |
| --- | --- | --- |
| migrations 0076/0077 | 拒绝搬运，设计采纳 | C3 按新迁移头重写，含 R1/R2 全部收紧（EXCLUDE、复合 FK 全覆盖、CHECK is_current、终态不可变、INSERT 触发、保守 down） |
| kysely-allocation-tables.ts | 设计采纳 | 重写（新增列与 R2 约束对应） |
| domain timeline/policy | 设计采纳 | 重写（边界扫描、冲突段合并、余量口径按 P2-1 重定义） |
| domain money/allocation/conservation | 设计采纳 | C3 在 WP03 重写（C2 未实施该部分，无既有实现可采） |
| 三个仓储 + common | 设计采纳 | 重写：同事务 dirty、原修订重放（R2-P1a）、员工级锁、OCC、裁剪 |
| 测试 | 用例设计采纳 | 期望值按合同独立重写；含 R1/R2 全部负例与恒真修复 |
| WP02 证据文档 | 拒绝作为证据 | 作为教训记录（R1/R2 发现的问题清单已并入 C3 合同） |

## C. WP01 静态自审（独立视角，2026-09-21）

按"产品口径冲突/真实阻断"清单核对：

1. 权限：P2-2 绑定现有实体，无新增；企业级入口读写均 operate 且负例明确——无冲突。
2. 预览口径：P2-1 公式与算例自洽（available 只减隐藏、remaining 再减当前意图；可见项目从 remaining 继续扣减且单列展示；全时间线校验兜底）——无歧义。
3. 时间：P3-1 已固定 +08:00；与经营账月份口径一致——无冲突。
4. 守恒/币种/精度：三桶分离、套餐 CNY 显式、未知缺口不冒充——与 v1.2 一致。
5. 变更识别：阶段一零 Gateway 改动满足 B02 最严格解释；新鲜度风险已列为 WP06 观测项——非阻断。
6. run 不可变/陈旧度推导/幂等重放：R1/R2 教训并入合同——无遗留阻断。
7. 0% 段/保留策略：P3-2/P3-3 已冻结——无冲突。
8. 与基线既有红（standard-home 等 8 例）无交集；阶梯扩链为基线既有任务。

结论：无产品口径冲突、无真实阻断 → 按授权连续执行 WP02–WP07/R01，不停顿。

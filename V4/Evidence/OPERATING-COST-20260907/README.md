# API费用隐藏与月度总Token入口修复

本地完成；未提交、推送或部署。线上仍为db78a30。

## 已确认原因

用户两次只读SQL输出确认：9月1932条已计费API记录，48.47156860 CNY；6条UNKNOWN_COST中5条已有历史处理关联。唯一未处理记录为FAILED、输入／输出／缓存均0、无响应提交。原资金余额缺口查询未排除此类失败审计行，导致INCOMPLETE_USAGE_COST；经营总览和采购复盘又因这个状态隐藏了全部已知费用。

## 改动

- 资金余额与月度费用缺口查询复用既有无消耗失败请求过滤。原请求、账本和历史处理记录保留，不更新生产数据。
- 已知月度API金额独立于余额完整性显示；真实有消耗但缺计价时，继续保留已计费金额及完整性标识，不把缺失费用填0。
- 员工／项目／部门汇总新增knownApiCost，保留apiCost=null的原完整金额语义，同时显示已计费小计；旧封账事实不重算。
- 月度总览第一张卡片为所选月份本月总Token，来自同月分析汇总，完整千分位显示。
- 与POOL20-057的8月历史订阅和续订入口在同一工作树；无新增迁移。

## 验证

本轮97项定向测试通过：数据库25，Web55，API17（含上轮新增的完整续订链）。新增复现覆盖零消耗失败不影响余额和费用、真实未计价请求保留已知费用、部门／员工小计、总览Token第一卡。原账户封账测试通过。Database／Control API／Web类型检查、增量Lint、架构、源码体积、diff检查及Web构建通过。构建保留原有大包提示。

日志database-final.log、web-final.log、api-final.log、build-final.log。前轮49项与本轮存在重叠，不相加声称146项。作者I0核查，不声称独立V1.4专项审核或生产业务验收完成。

提交说明：用户已授权本轮commit；上文未提交状态为验证时快照，实际提交以Git记录为准。未推送或部署。

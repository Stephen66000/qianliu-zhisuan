# I1 微型有界复审报告

**Decision：PASS。上一轮剩余 3 个 P2 全部 CLOSED。**

本轮仅验证 `saving_calculable=false`、`unexplained_baseline` 和稳定 `alertKey` 对应的三个非等价变异，并核对最终候选锁；没有重开其他范围。

## 1. 对象与合同

| 字段 | 实际值 |
| --- | --- |
| audit_id / round | RUNTIME-ADMIN-V14-I1-R3-20260908 / i1-reaudit-2 |
| reviewer | /root/i1_runtime_admin_review，未参与实现 |
| required / actual independence | I1 / I1 |
| contract / budget | [contract.md](./contract.md)、[budget.md](./budget.md) |
| state | V14-I1/state.yaml，已重读 state_version=4，CODE_AUDIT_IN_PROGRESS |
| candidate | V14-FIX2/candidate-lock.json，28 文件 |
| implementation / full_candidate hash | 5553837c01228f4e3ab1d9753e20497dcdf1d2ae082e02045057ae1a44eb42e1 |
| manifest hash | 2ceb5e67534f6daf16b0ad0a8ab0a2495df8e09d893ceba865e3f888fd734e61 |
| start lock | STABLE；[log](./lock-start.log)、[command](./lock-start.command.json) |
| end lock | STABLE；[log](./lock-end.log)、[command](./lock-end.command.json) |
| callback target | /root |

与上一轮候选逐项 SHA-256 对照，仅 `apps/control-api/src/__tests-integration__/w20-diagnostics-alerts.test.ts` 变化，无新增/删除文件，产品代码未变。旧测试从上一轮 sandbox 取回，剔除工具注入的 `@ts-nocheck` 后 SHA-256 精确匹配旧 manifest 的 `8e63f069...`；完整测试修复差异见 [test-change.diff](./test-change.diff)，核验过程见 [test-diff.log](./test-diff.log)。

新增 fixture 使 matched_policy_action 与 final_action 均为 ALLOW，同时设置 saving_calculable=false、not_calculable_reason=unexplained_baseline；最终按具体 decision.id 组成的稳定 key 找到该告警并断言 signal/resourceId。原断言未被删除或放宽。

## 2. 三项独立验证

| 原残余 | 精确故障注入 | 实际结果 | 结论 |
| --- | --- | --- | --- |
| R2-P2-01 | `eb("saving_calculable", "=", false)` → true | 基线通过；故障版在测试第 768 行因为目标告警缺失而断言失败 | CLOSED |
| R2-P2-02 | `unexplained_baseline` 条件值 → 空串 | 基线通过；故障版在同一精确目标断言处失败 | CLOSED |
| R2-P2-03 | dispatch alertKey 模板 → 空模板 | 基线通过；故障版无法按稳定 key 找到对应告警，精确断言失败 | CLOSED |

基线和三个故障版均使用真实 PG17 fixture，单独执行同一个自足八信号用例。三个故障版的登录和 GET /alerts 仍返回 HTTP 200；失败原因都是新增目标断言 `expected undefined to match object`，不是 SQL 语法、认证、容器或前序 fixture 错误。

定向变异结果为 **3/3 Killed，0 Survived**。原生产源文件未修改：审核 Vite 插件仅在内存中对指定文件进行唯一一次精确替换，逐个进程隔离执行；每次记录注入标记及断言失败位置。

完整证据：

- [exact-mutation-results.json](./exact-mutation-results.json)：基线 exit 0；三个故障版 exit 1，均有唯一注入标记和目标断言失败。
- [基线日志](./exact-baseline.log)、[saving 故障日志](./exact-saving.log)、[reason 故障日志](./exact-reason.log)、[key 故障日志](./exact-key.log)。
- [精确变异配置](./reviewer-exact-mutants.config.ts)、[串行运行器](./reviewer-exact-mutants.mjs)、各 `exact-*.command.json`：包含实际命令、起止时间、退出码。

真实性说明：先尝试复用 Stryker 的列范围选择，但实际生成 0 个变异，报告分数为 n/a/NaN；该次 `mutation.json`、`mutation.log` 仅作为失败的工具选择记录，**没有用于 PASS**。最终关闭证据来自上述逐一精确内存变异。未扩大变异范围，也未弱化测试或项目阈值。

## 3. 结论与回传

- `decision=PASS`，限定为本次微型复审合同。
- `closed_findings=[R2-P2-01,R2-P2-02,R2-P2-03]`。
- `remaining_findings=[]`；`regressed_findings=[]`；本轮无新增 blocker 或 Evidence Gap。
- 原 F-01～F-05 的关闭证据在上一轮报告中，本轮没有重开它们。
- 起止对象锁均 STABLE；产品代码、原测试、manifest、既有 Evidence 和状态账本均未由 Reviewer 改写。
- 执行约 4 分钟，低于 15 分钟预算；修复批次=0，budget_hit=false；token/cost 无精确计量，不作推算。
- Planning 可追加本轮 round_history 并对最终对象执行既定收口。此 PASS 不新增全仓门禁、正式完整功能 Audit 或生产/发布验收声明；此前报告中的其他声明边界继续保留。

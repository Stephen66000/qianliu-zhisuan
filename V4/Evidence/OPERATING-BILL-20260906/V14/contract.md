# 经营账单 V1.4 有界审核合同

用户于本任务明确同意 I1 独立审核；只审本次经营账单改版，发现问题逐项修复后复核，不过度开发、不过度治理。独立性为新审查会话、不继承实现聊天、不参与实现，同模型族，不能宣称 I2。风险 R3（账务计算、归属和迁移）；独立性按用户最新约定 I1，不要求 I2。

## 范围与依据

基线、完整新增/修改代码及测试清单与 SHA 见 candidate-r1.json；tracked diff 见 candidate-r1.diff，未跟踪新文件需阅读完整内容。必要上下游和项目门禁配置也在阅读范围。新增审核输出不属于产品候选。

规范原文目录：`/Users/mac/Documents/AI专区/仟流 规范与体系/编程规范/v1.4/`。依据代码质量审计模板、编码工程规范、功能验收规范和 SOP v1.4；项目门禁：`V3/仟流智算-质量门禁-v1.0.json`。用户指令优先；未运行的适用门禁如实列证据缺口，不推定通过或静默降阈值。

冻结需求：本轮用户确认的口径为月度总览清理；实际已付款采购联动厂商资金流水；员工/项目/部门一列一个数，保留总/输入/输出/缓存，厂商 Token 和金额分列，缓存是输入子集；同资源同月 199 元按 A60%/B40% 分为119.40/79.60，项目和员工为独立主体；员工指定部门，项目指定负责人并归负责人部门，待归属提示管理员补齐，历史请求不重写；Kimi/智谱月度 Token 与全历史峰值比例合表，新峰值重算往月；综合利用率两家均值；采购年度矩阵月份横排，下方月份为行、细项为列，包含 DeepSeek 余额/充值/实付/API 消耗/期末及两家利用率；趋势五指标（本月公司 Token、YTD 月均、YTD 月均环比、本月人均、人均环比），YTD 累计除以月份数，人均使用当月系统员工人数；标题/数字单行适配，过亿以万显示；前台30秒刷新，非 T+1；价值确认和对账导出留空，结账管理 UI 移除。详见上级 FINAL-IMPLEMENTATION.md 及 V2/V3/V4 历次口径，历史已被用户修改的建议不能作为当前要求。

已明确限制：未新增年付/跨月套餐服务期摊销；没有生产数据库金额复核与部署；历史未知事实不推测补齐；旧结账数据保留。审查应验证这些边界，不能将其说成已上线或已做生产验收。

## 执行和收敛

- 主会话负责冻结、执行测试、修复和记录；Reviewer 只读候选和证据，回传 Findings，不修改代码、测试或权威证据，不提交/推送/部署。
- 先独立核对功能与验收证据，回传同候选功能结论；主会话写入代码审核运行态后，Reviewer 复读 state.json 再完成七维代码矩阵与适用门禁判断。
- 审查开始/结束按 manifest 逐文件验 SHA；漂移停止并回传。代码冻结期间主会话只补充机械证据。
- 本合同最多初审一轮和针对已冻结 Findings 的复审一轮；总计60分钟，单轮30分钟。先完成全范围阅读再一次性冻结 Findings；只在发现新的明确代码/安全事实时补充 blocker，不无限扩张。达到预算如实回传，不自动续跑。
- 不启动全仓回归、全仓 Mutation 或新功能重构。复用现有同候选测试，必要时做局部复现/覆盖扫描；未满足的硬性门禁单列，不能伪称正式 PASS。
- callback 回主会话 `/root`；主会话记录每轮结论、对象和剩余项，复审只看修复及影响边界。当前 state.json 为握手与轮次记录。

# 三项生产问题统一修复

范围：仅处理此前生产只读验收发现的三项问题。用户确认三项一起部署，历史采用发生时归属、缺失记录由管理员批量确认；compass负责人已录入，继续使用现有字段与关系。

1. 趋势/套餐利用率：保留已记录Token和据此计算的参考指标，附不完整标记。UNKNOWN-only、未来月份继续保持缺失，不把未知填零。详见PARTIAL-USAGE-FIX.md。
2. 历史部门归属：在使用主体的“所属部门/项目负责人”面板增加“补齐历史归属”。选择主体、开始/结束日期、目标部门和确认依据；预览待补齐数量后显式确认。现有项目负责人可提供当前部门建议，但不会自动把当前关系套入历史。仅处理缺失归属的已结束且已有账本记录的请求，已有归属不变。
3. 零套餐费：当月资金账费用合计为零时直接返回零分摊；非零费用仍保留未知分母保护，员工/项目/部门/下钻一致。详见ZERO-SUBSCRIPTION-FIX.md。

历史确认复用request_attribution_snapshot的CORRECTION版本机制，保留旧快照、记录管理员、时间及确认依据；不修改ledger_line或ledger_transaction的原始用量和费用。预览指纹绑定确切记录集合，确认时变化则拒绝并要求重新预览；已结账的受影响月份拒绝补写。聚合刷新按批次去重，避免同一小时多请求触发重复冲突。每次支持不超过31天、10000条请求，超出时提示缩小范围。

不新增数据库迁移、依赖或负责人字段，不修改生产数据。发布后，历史待归属记录需要管理员在新入口完成预览与确认，代码不会替管理员猜测历史部门。

验证：数据库3文件20项、HTTP1文件5项、Web4文件22项通过，三包类型检查、变更Lint、规模与架构门禁及Web构建通过。覆盖旧快照保留、原始账本不变、调部门后历史不漂移、已有负责人建议、批量聚合、跨企业、失效预览、冻结账期和零费用。原始日志保留在本地/tmp/operating-three-fixes-*.log。未扩大覆盖率/Mutation治理，未冒充独立审核PASS。

业务候选和发布脚本将分开提交；统一发布基于当前线上a37de1a，数据库保持0065。此前单项修复发布指令不再使用。

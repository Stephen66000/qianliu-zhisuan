# 经营账单改版 · 本地交付记录

最新审核状态：I1独立功能复审通过，正式V1.4代码审核未通过。发现的3项代码问题已经修复并关闭；仍有覆盖率、ratchet及金额分配增量变异验证缺口，详见 [V1.4审核结论](./V14/FINAL-REVIEW.md)。以下开发验证结果不等于正式代码审核通过。

日期：2026-09-06。分支：`codex/operating-bill-redesign-0906`，基线：`4cea87f`。

状态：本地实现与验收完成；尚未提交、推送或上线。所有修改位于隔离工作树，未修改生产数据。

## 已完成

- 月度总览去掉标题解释、指标脚注、充值登记和旧快照 CSV 导入入口；零套餐费用正常显示。厂商使用主体显示数量；实付采购与厂商资金流水共用数据源。
- 员工、项目、部门账采用每列一个数的明细，拆开各厂商 Token、API 金额和订阅金额，保留输入、输出、缓存、活跃天数、请求次数。缓存是输入子集，现有计价规则区分缓存输入价格。
- 同月订阅费用按该资源各主体的真实 Token 占比分配，过滤和分页不改变分母。199 元按 60% / 40% 分为 119.40 / 79.60 元；三等份按分配余数处理，显示金额合计仍为 199 元。
- 使用主体支持员工部门、项目负责人。项目请求按负责人当时所在部门归属，历史快照不随后续修改重写；缺失归属提示管理员到使用主体补齐。
- Kimi、智谱月度 Token 与历史峰值利用率合并展示；新峰值重新计算历史比例。总览综合利用率使用两家指标的均值。
- 采购复盘提供 1—12 月费用矩阵及月度、年度总计，下方按月份展示 DeepSeek 余额、充值、实付、API 消耗和两家套餐利用率。
- AI 趋势展示公司本月 Token、YTD 月均、月均环比、本月人均及人均环比。YTD 月均为当年累计除以所选月份数；人均使用对应月份系统员工人数。
- 卡片标题和数字保持单行并适配字号；过亿 Token 使用“万”显示，保留精确值。报表在打开、回到前台及前台每 30 秒读取新数据，资金变更后失效相关查询。
- 对账与导出、价值确认保留空白；结账管理界面移除，历史结账数据和后端保护保留。

## 验收证据

| 检查 | 结果 | 记录 |
| --- | --- | --- |
| Web 相关测试 | 11 文件、68 项通过 | implementation-web-tests-final.log |
| 数据库集成测试 | 3 文件、23 项通过 | implementation-db-tests-final.log |
| HTTP 集成测试 | 2 文件、14 项通过 | implementation-http-tests-final.log |
| 类型、Lint、架构、源文件规模检查 | 通过 | typecheck-final.log、lint-final.log；门禁终端记录 |
| Web 生产构建 | 通过；现有大于 500 kB 的包体提示 | build-final.log |
| 浏览器联调 | 通过 | browser-run-final.log、browser/receipt.json |

浏览器验收启动隔离 PostgreSQL、真实 Control API 和 Web，完成登录、报表读取、30 秒刷新、部门与负责人关联、创建员工时部门必填、人均随人数更新和卡片溢出检查。截图为隔离测试数据，见 browser/overview.png、plans.png、procurement.png、departments.png。早期 failure 文件仅为已被最终通过记录取代的调试证据。

复验脚本：`scripts/verify-operating-reports.mts`。测试结束后停止临时服务与数据库容器。

## 发布边界与保留事项

- 发布前需要执行新增数据库迁移 `0065_principal_accounting_assignment.js`，再部署匹配版本；本轮没有执行生产迁移或部署。
- 未连接生产数据库复算截图中的 109.3 元；已验证代码读取数据库账务事实，没有硬编码该金额。
- 套餐费用来源沿用现有付款月规则，未新增年付／跨月套餐的服务期摊销算法。
- 历史人数、费用明细和归属缺少证据时保留未知，不推测补齐；历史已结账事实保持原状。
- 本地测试通过不能代替生产部署验收。当前交付可进入代码审查和后续发布流程。

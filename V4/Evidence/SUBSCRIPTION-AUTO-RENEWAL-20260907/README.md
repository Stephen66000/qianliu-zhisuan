# 订阅自动续记与取消

2026-09-07，按用户明确规则实现：已经登记的 Kimi／智谱订阅持续按原金额和周期续记，直到用户取消。不是厂商扣款接口。

- 入口：厂商 → 充值与订阅 → 选择资源 → 当前服务周期旁，显示状态、下次日期和金额，以及“取消自动续订”。
- 到期由现有 Worker 定时任务创建一笔 SYSTEM_RENEWAL 流水和下一服务周期；充值与订阅、本月采购使用同一笔记录。来源小字同行。页面每30秒更新。
- 取消和续记共用资源锁；取消不修改本期／历史。旧请求重放不恢复开关，新登记下一订阅才恢复。已自动记账的同周期同金额人工登记复用原流水，不重复收费记录。
- 按原登记日处理月末、季度、年度；停机后分批补漏；已关账、缺金额、已冲销不擅自补写。API充值保持人工登记。
- 加性迁移0066；已有订阅默认开启。产生自动流水或取消状态后不允许破坏性降级迁移。

## 验证与审核

数据库43项通过（其中续订集成8、日历7）；API完整资金路径10项通过；Worker5项通过。Web62项通过，合计120项；Web构建通过（保留已有大包体积提示）。详见validation.json与日志。四个包类型检查、增量Lint、架构及源码体积通过。

API旧测试按8位比例期望与基线已采用的分币守恒算法不一致，本次只将44.22222222／22.11111111／132.66666667修正为44.22／22.11／132.67，总额199断言保留；下游未知费用验证随后通过。

作者I0有界核查覆盖来源约束、租户隔离、并发取消、防重、日期锚点、关账保护、迁移回退和页面错误状态；不声称独立审核。没有操作生产页面或数据。尚未提交、推送、部署；上线需要0066及Worker ACTIVE配置。

此规则覆盖此前OPERATING-FEEDBACK记录中“只联动实付、不按到期自动记账”的旧理解；现在明确按用户登记的订阅约定自动记账，金额不猜补。

## 提交说明

用户于本次验证后明确授权commit。validation.json与candidate-final.json中的未提交状态为提交前快照，实际提交以Git记录为准；未推送或部署。当前核查是I0作者自查，未将其升级声明为完整V1.4专项审核。

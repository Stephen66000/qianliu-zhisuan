# WP08 GO/NO-GO 报告：本地部署候选验证（OpenSpec Phase 7.1～7.3）

- 变更：OpenSpec `provider-finance-initialization`，Phase 7 `Deployment Candidate Only`
- 计划：`V4/资金账本初始化与API期初余额录入开发计划-v1.2-20260921.md`（§9 WP08、§11）
- worktree：`/Users/mac/Projects/仟流智算-provider-finance-init-20260921`（隔离工作区）
- 报告归档：`V4/Evidence/PROVIDER-FINANCE-INITIALIZATION-20260921/V4-WP08-资金账本初始化-GO-NO-GO报告-20260922.md`
- 证据目录（**69 个文件**）：
  - `wp08-logs/`（10 个：7.1 构建/摘要/迁移备份/容器身份/路由接口 + 7.2 双端停写 + 7.3 静默演练）
  - `wp08-scripts/`（16 个：演练脚本、一次性 compose、夹具与上游 stub）
  - `wp08-fingerprint/fp`、`wp08-fingerprint/fp2`（43 个：源码/镜像内容指纹清单）
  - **排除的凭据类文件**：`secrets.json`、`quiescence-secrets.json`（含会话 cookie / apiKey）、`kek.txt`、
    `local.env`、`wp08-backup.dump`（二进制库转储，含会话行）——见 §六 合规声明。
- 分支：`codex/provider-finance-initialization-20260921`
- 候选 commit（冻结）：`c9bc9b93deb2c535d95346f7da4a9b141604441f`
  - tree：`f9bc02baf7f3260cce3f0edb706fa7e5287b1aef`
  - 候选源码树内容指纹（git 索引 mode+oid+path 全量摘要，1961 个受控文件）：
    `30da0d46dc216bf54bda50cd7c6ff0e306498cb692678343b3f2e532104f262c`
  - 版本：`VERSION=2.5.8`
- 迁移头：`0079_provider_finance_candidate_draft.js`（**WP08 零新增迁移、零 schema 变更**）
- 日期：2026-09-22

## 裁决

**WP08 = GO**（OpenSpec 7.1 / 7.2 / 7.3 勾选；**仍不得进入 WP09 / P 系列，不得真实部署或生产激活**）

| 任务 | 结论 | 关键结果 |
| --- | --- | --- |
| 7.1 构建不可变候选与部署回执 | **GO** | 4 镜像 `qianliu-candidate/*:c9bc9b93deb2` 构建于干净 HEAD；容器 image ID / revision 标签逐项吻合；迁移兼容性 + 备份可恢复性（`pg_restore` 退出码 0，逐表计数一致） |
| 7.2 双端停写演练（PFA-08） | **GO** | Control API 与 Worker 同时 `DARK`，写入口全部被拦（503）+ Worker tick 返回 `FINANCE_MODE_INACTIVE`；`strict_writes_enabled` 与既有资金事实全程不变；恢复 ACTIVE 前预检为只读 |
| 7.3 激活前静默演练（PFA-09） | **GO** | **断言 PASS=105 / FAIL=0**（`RESULT: GO`）；零上游、在途排空、租约 ≤60 分钟、剩余 ≥5 分钟门槛、候选 TTL 30 分钟、到期自动恢复、旧候选不得越期激活，全部实测 |
| G-2 文档漂移收口 | **已闭环** | `design.md` 状态行、§2 组件名、§8 旧记录关闭裁决三处回填；**未触碰历史报告与冻结记忆** |
| 生产部署 / 生产激活 | **明确排除** | 本 WP 全程未执行：真实部署、生产库迁移/备份、生产金额录入、生产 `strict_writes_enabled` 置位 |

---

## 一、7.1 构建不可变候选与部署回执

### 1.1 候选构建（`8.1-build-all.log`、`8.1-candidate-metadata.log`）

| 服务 | 镜像 | image ID | 大小 | 构建上下文 |
| --- | --- | --- | --- | --- |
| control-api | `qianliu-candidate/control-api:c9bc9b93deb2` | `2a2c4df520c0` | 702MB | 8.70MB |
| gateway | `qianliu-candidate/gateway:c9bc9b93deb2` | `4d8754d4838f` | 702MB | 1.43MB |
| worker | `qianliu-candidate/worker:c9bc9b93deb2` | `a05b6bd46f4c` | 732MB | 709.55kB |
| web | `qianliu-candidate/web:c9bc9b93deb2` | `9cf25cb9c0d1` | 51.5MB | 2.99MB |

镜像完整 digest：

```
control-api  sha256:2a2c4df520c079e73a01861c3d516c1e868bdae4345a5beef7384c116b2afd94
gateway      sha256:4d8754d4838f6abc98ff255166281f9811532daeadbee5d18143214ed8ca02cc
worker       sha256:a05b6bd46f4cdc2f85c396f0455cf1e92e485ddf607a9d4eea491dcadbfa97fb
web          sha256:9cf25cb9c0d198c62ec466219c78b8311ae7210300a5434063224321febe63ea
```

**构建输入哈希（sha256，节选）**：`package.json` `00b8246b…`、`pnpm-lock.yaml` `2a169b16…`、
`VERSION` `91159db7…`、`deploy/compose.yaml` `0be9a8b1…`、四个 Dockerfile 各一（`f26c6e68…` / `9ca2c998…` /
`01128911…` / `d6488565…`）。

**候选有效性前提**：构建时刻元数据明确记录 `WORKTREE=clean`（2026-09-21T21:52:24Z），即候选镜像构建于**干净
HEAD `c9bc9b9`**。本 WP 唯一的后续工作树改动是 `openspec/changes/.../design.md`（G-2 文档），
且四个 Dockerfile 均**不引用** `openspec/`，故不进入任何镜像层——候选镜像与其 `qianliu.candidate.index-sha256`
标签保持有效，**未被 G-2 文档改动污染**。

**候选回执标签**（四镜像一致）：

```json
{"org.opencontainers.image.revision":"c9bc9b93deb2c535d95346f7da4a9b141604441f",
 "org.opencontainers.image.version":"2.5.8",
 "qianliu.candidate.change":"provider-finance-initialization",
 "qianliu.candidate.index-sha256":"30da0d46dc216bf54bda50cd7c6ff0e306498cb692678343b3f2e532104f262c",
 "qianliu.candidate.source-tree":"f9bc02baf7f3260cce3f0edb706fa7e5287b1aef",
 "qianliu.candidate.workpackage":"WP08"}
```

### 1.2 源码 ↔ 镜像内容指纹（`8.1-image-source-fingerprint.log`、`wp08-fingerprint/`）

对每个服务，将「git 索引（mode+oid+path）」与「镜像内实际文件内容」分别汇总为清单并交叉比对
（`fp/expected-*.manifest` vs `fp/img-*.manifest`；`fp2/extra-*.txt` / `absent-*.fixed.txt`）：

- **`extra-*.txt`** 仅含构建期注入产物（如 `node_modules`、编译后的 `dist`、OCI 元数据），**无源码级差异**；
- **`absent-*.fixed.txt`** 剩余项经「传递构建产物」规则（`--fixed` 归一）后归零，即源码树中所有**应当进镜像**的
  受控文件均在镜像内匹配。

结论：镜像内容与候选源码树**逐文件一致**，无未记录注入、无静默漂移。`fp/` `fp2/` 全量清单随本报告归档。

### 1.3 迁移兼容性与备份可恢复性（`8.1-migration-backup.log`、`8.1-backup-toc.log`）

一次性 `Testcontainer postgres:17-alpine@sha256:742f40ea…`（与 `deploy/compose.yaml` 同 digest）内执行：

1. **回滚 0078/0079**：`✓ 已回滚迁移: 0079_provider_finance_candidate_draft`、`✓ … 0078 …`，
   且两者关键对象移除后探针**全为 `f` / 空**（回滚干净）；
2. **前进 0077 → 0078 → 0079**：两迁移重新 `✓` 应用，迁移台账末段含 `0078`、`0079`；
3. **备份**：`pg_dump --format=custom` → 479575 字节，`sha256=23806871930370d771b13599eb8e69012134f26a2a31794f955b3bff27adfdde`，
   TOC 条目 **826** 条，含全部 `provider_finance_*` 校验函数、`provider_finance_activation_attempt` /
   `provider_finance_activation_quiescence` 等表；
4. **恢复**：`pg_restore` 退出码 **0**、`stderr` 与错误文件均**空**；源库 ↔ 恢复库**逐表计数比对全 OK**
   （`kysely_migration` 80/80、`enterprise` 1/1、`provider` 2/2、`provider_resource` 2/2、
   `resource_purchase_record` 1/1、`provider_finance_runtime_state` 1/1，其余资金表 0/0）；
   恢复库迁移台账末段含 `0079`；
5. **清理**：一次性恢复库 `wp08_restore` 已删除。

### 1.4 容器身份与运行期一致性（`8.1-container-identity.log`）

- 6 个运行中容器：4 个候选镜像 + `postgres:17-alpine`（healthy）+ `redis:8-alpine`（healthy）；
- 逐容器核对 `Config.Image` == 候选标签，且 `revision` 标签 == `HEAD=c9bc9b9`（4/4 吻合）；
- 运行期抽样：容器内候选源码文件 hash == git 跟踪 hash（3 服务抽检一致）；
- **上游可达性**：容器内 `*_BASE_URL` 与库内全部 `provider_resource` 上游地址
  **统一指向容器私网 `http://stub-upstream:9299/v1`**，无任何真实厂商地址。

### 1.5 路由与公开接口一致性（`8.1-routes-interfaces.log`）

- **绑定**：全部发布端口位于回环（`127.0.0.1:8788/8787/8080/8081`），**无 `0.0.0.0`、无 `:::` 绑定**；
- `control-api`：`GET /health`→200；`GET /provider-finance/summary`→401（鉴权前缀生效，非 404）；
- `gateway`：`GET /health`→200；`GET /v1/models`（无 Key）→401 `invalid_principal_key`；
- `web`：`GET /`→200（SPA）；`GET /api/health`→200（命中 nginx 反代到 control-api）；`/some/spa/route`→200（fallback）；
- `caddy` 统一入口（`127.0.0.1:8081`）：`/health`→control-api、`/v1/models`→gateway、`/provider-finance/summary`→control-api，**分流正确**；
- `worker` 健康端点仅在容器私网，**不发布端口**；
- 上游 stub `GET /__stats` 可观测，证明其为唯一上游。

---

## 二、7.2 双端停写演练（PFA-08，`8.2-dual-dark.log`）

四阶段，`Control API` 与 `Worker` 的 `PROVIDER_FINANCE_MODE` 同步切换：

| 阶段 | 动作 | 结果 |
| --- | --- | --- |
| **A** | 基线 `ACTIVE` | 8 个写入口「PASSED(未拦)」；Worker tick `{"scanned":0,"created":0,"failures":[],"skippedQuiescentEnterprises":[]}` |
| **B** | 同时切 `DARK`（双端 `运行中=true`） | 写入口全部被拦；Worker tick `{"scanned":0,"created":0,"failures":[],"skipped":"FINANCE_MODE_INACTIVE"}`；`GET /provider-finance/activation-state`→200 且返回 `{"mode":"DARK","cutover_at":"2026-08-31T16:00:00.000Z",…}`（**保留只读可观测**） |
| **C** | 恢复前**重新预检（只读）** | 预检前后事实快照**逐行一致** ⇒ `★ 预检为只读` |
| **D** | 恢复 `ACTIVE` | 写入口恢复「PASSED(未拦)」；Worker tick 恢复；既有资金事实与 `strict_writes_enabled` **全程不变** |

**关键不变量（全程逐字节保持）**：`runtime_state=1 | strict_true=0`；
`events=0 ledger=2 ai_request=2 attempts=0 quiescence=1`；
`fact_fingerprint=29fa3e4668d37b4f34f4fecf8a501fc7`。

即：双端 `DARK` 只**拦写入**、不产生任何资金事实变更，`DARK` 期间保留只读状态观测，恢复前必须有只读预检。

---

## 三、7.3 激活前静默演练（PFA-09，`8.3-quiescence.log`）

本地合成企业 A/B/C/D/E/F + 企业池 P1～P4（直插 `admin_session`、锚定互不相同的 `created_at`，
绕过 `/auth/login` 的企业选择抖动）。**令牌均为合成假值，未触及任何真实企业或资金**。

执行方式：一次性本地容器栈（4 候选镜像 + `postgres` + `redis` + 上游 stub），`7.3` 全部为 HTTP/SQL 级真实调用；
「本地时钟注入」仅用于把静默租约/候选的 `started_at`/`created_at` 向过去平移（不修改任何业务事实）。

| 章节 | 覆盖点 | 断言 |
| --- | --- | --- |
| §0 | 基线清理 | 全部合成企业零有效租约（隔离前置） |
| §1 [A] | **租约上限 60 分钟** | PASS=5 / FAIL=0 |
| §2 [A] | **Gateway admission 零上游** | PASS=11 / FAIL=0 |
| §3 [A] | **在途请求继续排空** | PASS=14 / FAIL=0 |
| §4 [A] | Worker 共享门禁（静默企业被跳过） | PASS=3 / FAIL=0 |
| §5 [A] | **租约到期自动恢复** | PASS=7 / FAIL=0 |
| §6 [E] | **旧候选在租约过期后不得继续激活** | PASS=18 / FAIL=0 |
| §7 [E] | **激活需剩余 ≥5 分钟** | PASS=8 / FAIL=0 |
| §8 [C] | **候选 TTL 30 分钟**（过期候选不可激活） | PASS=10 / FAIL=0 |
| §9 [P3] | 正向路径：TTL 内 + 剩余 ≥5 分钟 → 激活成功 + 幂等重放 | PASS=19 / FAIL=0 |
| §10 [P3] | Worker 自动续订：静默期跳过 → 到期自动恢复 | PASS=10 / FAIL=0 |
| §11 | 收尾清理（零资金写入） | — |

**合计：断言 PASS=105 / FAIL=0，`# RESULT: GO`**（结束时刻 `2026-09-21T22:25:01Z`）。

### 3.1 逐项 PFA-09 要求 → 证据对应

| PFA-09 要求（逐字） | 实测证据 |
| --- | --- |
| 「Gateway admission **零上游**」 | §2：静默企业经 gateway 的 `chat` 请求被 `503 enterprise_maintenance` 拦截，`retry-after: 60`；上游 stub `__stats` 命中数增量为 **0**，`ai_request` / `upstream_attempt` / `usage_event` / `ledger_line` 增量均为 **0**；`GET /v1/models` **不受门禁**（保持可用） |
| 「Worker 跳过」 | §4：`listQuiescentEnterpriseIds` 恰好返回 `[ENT_A]`；Worker `runSubscriptionAutoRenewals` 返回 `skippedQuiescentEnterprises ⊆ {ENT_A}`；§10 展示真实续订 tick 在静默期**跳过**该企业、租约到期后**自动恢复**续订 |
| 「在途请求继续排空」 | §3：静默开启**之前**发起的慢模型调用在静默期内**正常完成**（不中断已提交上游请求）；静默期内**新**请求被拦截 |
| 「租约最长 60 分钟」 | §1：请求 >60 分钟窗口被 DB CHECK `provider_finance_quiescence_window_check`（`expires_at > started_at AND expires_at <= started_at + '01:00:00'`）拒绝 |
| 「激活需剩余 ≥5 分钟」 | §7：剩余 <5 分钟的租约下发起激活被拒（`ACTIVATION_NOT_QUIESCENT`→`activation_not_quiescent` 409） |
| 「候选 TTL 30 分钟」 | §8：`provider_finance_attempt_ttl_check`（`expires_at = created_at + '00:30:00'`）**固定** TTL、不可滑动；追溯 31 分钟的等价候选行不可激活（`candidate_expired` 409） |
| 「租约到期自动恢复」 | §5：租约越过 `expires_at` 后 `isEnterpriseQuiescent` 归假、admission 恢复、Worker 恢复续订 |
| 「**旧候选不可继续激活**」 | §6：租约 A 下生成的候选，在 A 的租约过期后即使候选本身仍在 30 分钟 TTL 内，激活仍被拒（`candidate_stale` / `activation_not_quiescent` 409）——候选的有效性**同时受**其自身 TTL **与**租约活性约束 |
| 「不得执行生产激活」 | 全程仅在本地一次性容器栈、本地合成企业上执行；激活仅发生在一次性企业 P3；**未触碰生产库、未置位生产 `strict_writes_enabled`** |

### 3.2 正向与幂等（§9、§10）

- **正向激活**（企业池 P3，TTL 内 + 剩余 ≥5 分钟）：`activate` 成功，`strict_writes_enabled=true`（本地一次性）；
  同 `idempotency_key` **重放**不重复写入、不重复扣费；
- **Worker 幂等**：重复 tick 不重复扣费（续订事件恰好 1 条，重复 tick 增量 0）。

### 3.3 不变量与残留效应披露

- 演练全程 **A/C/E 的 `strict_writes_enabled` 终态为 `false`**（零资金写入）；
- **P3 终态为 `true`**：这是本地一次性合成库上的一次性**正向激活**（不可逆）。属预期证据，**不涉及任何真实企业**。

---

## 四、G-2 文档漂移收口（WP06 披露项）

按授权在 `openspec/changes/provider-finance-initialization/design.md` 内回填三处漂移，**未修改任何历史报告或冻结记忆存档**：

| 编号 | WP06 披露 | 本次收口 |
| --- | --- | --- |
| **G-2a** | 状态行仍为 `DISCUSSION_DRAFT / IMPLEMENTATION_NOT_AUTHORIZED`（陈旧） | 改为 `LOCAL_IMPLEMENTATION_VERIFIED / DEPLOYMENT_AND_PRODUCTION_ACTIVATION_NOT_AUTHORIZED`，并附注：WP01～WP07 本地实现与验证完成、WP08 本地候选验证完成，但**真实部署、生产库迁移/备份、生产金额录入、生产激活仍未获授权** |
| **G-2b** | §2 组件图 `ProviderFinanceActivationPanel` 从不落地（全仓无此标识符） | 替换为**实际**组件：`FinanceInitializationPanel.tsx`（状态切换容器）+ `ProviderFinanceActivationWizard.tsx`（草稿/预检/确认向导）；服务端按「只读预检仓储 / 领域投影与哈希 / 激活协调器与写入原语 / 跨服务静默门禁」四层重绘 |
| **G-2c** | §8 旧购买关闭写作「独立控制表**或**不可变映射」二选一，未回填裁决 | 明确裁决 = **不新增独立控制表**，选定「在候选结果中建立不可变映射」：0079 的 `candidate_draft jsonb NOT NULL` + 不可篡改触发器；关闭结果承载于激活事务内 `operation_log` 审计行（`action='provider_finance.legacy_purchase.close'`、`target_type='resource_purchase_record'`、`change_summary={resolution,…}`，`resolution ∈ {MIGRATED, ALREADY_REPRESENTED, REJECTED_WITH_EVIDENCE}`） |

**验证**：`design.md` diff = `1 file changed, 47 insertions(+), 12 deletions(-)`；
`grep` 全仓确认 `ProviderFinanceActivationPanel` / `ProviderFinanceActivationService` 仅存于
`design.md`（命名说明段）、`tasks.md`（WP06 6.4 历史披露）、WP06 报告（历史披露）——
即**不再有任何现存设计承诺引用不存在的组件**；引用的
`FinanceInitializationPanel.tsx` / `ProviderFinanceActivationWizard.tsx` / `activation-routes.ts` /
`0079_provider_finance_candidate_draft.js` 均已确认**存在**。

---

## 五、WP08 执行边界与禁止项声明

| 项 | 状态 |
| --- | --- |
| 真实部署 | **未执行** |
| 生产库迁移 / 备份 | **未执行**（仅在一次性 Testcontainer 演练） |
| 生产金额录入 | **未执行** |
| 生产激活 / 生产 `strict_writes_enabled` 置位 | **未执行** |
| 外部厂商调用 | **未执行**（唯一上游为本地 stub，全程零真实出口） |
| 修改主工作区 `/Users/mac/Projects/仟流智算` | **未发生**（全程在隔离 worktree） |
| 写入 `.workbuddy/memory` | **未发生** |
| `--no-verify` / 修改 hook / 绕过安全门禁 | **未使用** |
| `git push` | **未执行**（仅本地提交） |
| 进入 WP09 / P 系列 | **未进入** |
| 修复 C-1 / 既有红灯 | **未触碰**（保留为独立立项事项） |

---

## 六、合成数据与安全合规声明

- **零真实数据**：演练企业/管理员/资源/订单/资金事件均为本地一次性库内合成；会话令牌为合成假值；
- **零真实厂商流量**：入口唯一上游为本地 `stub/server.mjs`，容器私网寻址，全程零命中真实厂商；
- **归档脱敏**：证据目录 `wp08-*` **已排除**全部凭据类文件（`secrets.json`、`quiescence-secrets.json`、
  `kek.txt`、`local.env`）与二进制库转储 `wp08-backup.dump`；对归档内全部文本证据执行
  JWT / 私钥 / `sk-` 类模式扫描，**零命中**；
- **口径与既有基线一致**：本次未新增/未修复既有红灯；C-1（迁移 `defaultTo("now()")` 常量默认值，41 列/16 迁移）
  与 `pool043` 等既有测试债保持独立立项，**不在本 WP 范围**。

---

## 七、结论与后续建议

1. **WP08 = GO**：7.1 不可变候选与部署回执、7.2 双端停写、7.3 激活前静默（PFA-09 八项要求）**全部本地实测通过**；
   OpenSpec `tasks.md` 7.1/7.2/7.3 勾选完成。
2. **仍严格受限**：本 GO **仅**覆盖「本地部署候选验证」，**不构成**任何真实部署或生产激活授权。
3. **后续（需单独授权）**：
   - P 系列（生产金额与证据确认 → `GO_CANDIDATE` 回执 → 生产静默与排空 → 生产激活 → 业务验收与流量恢复），
     **必须**在 `PRODUCTION_FINANCE_ACTIVATION_AUTHORIZED` 之后、由人工在有效租约与候选 TTL 内执行；
   - C-1 迁移默认值缺陷（41 列）应独立立项修复（加法迁移 `SET DEFAULT now()` + 回填评估）；
   - 本次候选镜像 `qianliu-candidate/*:c9bc9b93deb2` 为**本地不可变候选**，任何后续代码改动都会使其失效并需重新构建。

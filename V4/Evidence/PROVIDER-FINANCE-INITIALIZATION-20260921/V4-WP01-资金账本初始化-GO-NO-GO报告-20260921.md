# WP01 GO/NO-GO 报告：合同、候选与迁移

- 变更：OpenSpec `provider-finance-initialization`，Phase 0.2 + Phase 1（1.1～1.5）
- 计划：`V4/资金账本初始化与API期初余额录入开发计划-v1.2-20260921.md`
- worktree：`/Users/mac/Projects/仟流智算-provider-finance-init-20260921`
- 报告归档：`V4/Evidence/PROVIDER-FINANCE-INITIALIZATION-20260921/V4-WP01-资金账本初始化-GO-NO-GO报告-20260921.md`（worktree 内相对路径；2026-09-22 边界收口迁入）
- 分支：`codex/provider-finance-initialization-20260921`
- 提交：`aaacbab`（复核收口修复）；`45c54de`（WP01 主体）；`10059ee`（v1.2/OpenSpec 工件导入）
- 基线：`ca533e3d790270f2a33b15de73e3417ab8656b76`（Kimi 干净终态，代码 `ac1c6de`）
- 迁移头：`0078_provider_finance_activation.js`
- 日期：2026-09-22（复核收口后更新）

## 裁决

**WP01 = GO**

首轮报告被判 HOLD：F-1 系本提交引入的**客户端合同断链**（后端 PFH-06 已强制说明/证据，而日常充值/订阅 UI 会直接 400），
复核裁决明确不得推迟到 WP05。F-1 与既有的 F-2、F-3 均已在本轮最小收口并复验通过，HOLD 解除。
未扩大 UI 范围，未部署，未做任何生产操作。

## 一、复核裁决收口（本轮）

| 项 | 问题 | 处置 | 证据 |
| --- | --- | --- | --- |
| F-1 | 后端强制证据后，`ProviderFinancePanel` 日常表单既不提交 `evidence_ref`、`description` 亦可选 → 写入口 400 | **已修**。新增“说明”“证据引用”必填校验并如实提交；仅改现有日常表单，未开发初始化向导 | `ProviderFinancePanel.tsx`；web 测试改为断言完整 payload，并新增缺说明/缺证据的阻止提交负向用例 |
| F-2 | `provider-finance` 夹具硬编码 `MIGRATED_CARRYOVER` 周期已过期，被绑定守卫以 409 拒绝 | **已修**。周期与月份查询改为按当前上海自然月派生；**未改生产守卫语义** | 该用例现通过；推导值即当前窗口 `[本月上海零点, 次月上海零点)` |
| F-3 | `system-settings-v2` 断言 `migrateToLatest` 只返回单个迁移，任何新增迁移即误报 | **已修**。改为 `toContain("0072_admin_roles_security")`；迁移顺序与完整性仍由专用迁移测试负责 | `system-settings-v2.test.ts` 8/8 |

## 二、首轮交付物与证据（未变）

| 任务 | 交付 | 证据 |
| --- | --- | --- |
| 0.2 | 独立 worktree、分支、基线、迁移头、工具版本 | 本节头部；原脏工作区 `/Users/mac/Projects/仟流智算` 的**既有文件**未改动（报告不驻留该目录，见 §五 边界遵守声明） |
| 1.1 | 激活领域合同（严格 Zod、数组上限 500、拒绝未知字段） | `activation-contracts.ts` + 9 项测试；body 不接受 `enterprise_id`/`admin_id` |
| 1.2 | 稳定规范化与哈希 | `packages/domain/src/provider-finance-activation.ts` + 18 项测试（含顺序无关、金额等价） |
| 1.3 | 候选与激活幂等持久化 | 迁移 `0078` + 仓储 + 5 项集成测试 |
| 1.4 | 期初合同加固（PFH-01/06/07）+ 客户端合规收口 | `contracts.ts` 必填说明与证据；期初入口 409 双码；旧 CLI 失败关闭；F-1 修复 |
| 1.5 | 企业级静默租约 + 资源资金状态 | `provider_finance_activation_quiescence`、`provider_resource_finance_state`、Gateway 排除 PENDING |

### 迁移 `0078` 明细

sha256：`d6b9d8854eca6ff36231a3d6b7b848d94c12aa49134aedc0e56d6e00f10bf3fa`

- `provider_finance_activation_attempt`：TTL 为 `created_at + interval '30 minutes'` 表达式约束（不滑动）；
  状态 CHECK 白名单 `PREVIEWED|ACTIVATED|EXPIRED|REJECTED`（**无 `ACTIVATING`**）；
  触发器 `provider_finance_activation_attempt_guard` 阻止状态与不可变字段改写、阻止删除 `ACTIVATED`；
  `activation_idempotency_key` 局部唯一索引。
- `provider_finance_activation_quiescence`：窗口 CHECK `expires_at <= started_at + 60min`；审计写入 `operation_log`。
- `provider_resource_finance_state`：`PENDING|READY`、`required_currencies`、`ready_at/by`、`version`；
  触发器在 `provider_resource` 插入（`mode=API` 且企业已开严格写）时落 `PENDING` 行。
- `down()`：存在 `ACTIVATED` 候选或任何资源资金状态时拒绝回退。

### 裁决项落实

- ① 旧 `provider-finance-activate.ts` 已改为失败关闭（exit 2），不再直连 `activateStrictWrites`。
  新协调器（WP03）须在**同一外层事务**内调用本仓储；legacy→v1 固定锁序尚无调用点（WP03 落地）。
- ② PFH-07 采用独立 `provider_resource_finance_state`，未复用 `provider_resource.status`；新 API 资源默认 `PENDING`；Gateway 排除 `PENDING`。
- ③ 未复制项目归集 `0076` 草稿；本 WP 迁移号接续最终资金账本头。

## 三、最终验证结果（复核收口后复跑）

| 范围 | 结果 |
| --- | --- |
| web 全量 | ✅ 477/477（78 文件） |
| control-api `provider-finance.test.ts` | ✅ 11/11（含 F-2 修复后的迁移周期用例） |
| control-api `subscription-renewal-flow.test.ts` | ✅ 1/1 |
| control-api `system-settings-v2.test.ts` | ✅ 8/8（F-3） |
| domain | ✅ 198/198（12 文件） |
| database 迁移集成 | ✅ 5/5 |
| control-api 合同单元（`src/provider-finance`） | ✅ 23/23 |
| typecheck：domain / database / control-api / web | ✅ 四包 0 错误 |
| lint：web / control-api（`--max-warnings=0`） | ✅ 0 警告 |

工具：node v22.22.2、corepack pnpm@11.11.0、testcontainer `postgres:17-alpine@sha256:742f40ea…`（与 `deploy/compose.yaml` 同 digest）。

## 四、方法论提醒（保留）

在干净基线 `ca533e3` 上**整目录并发**跑 `src/__tests-integration__` 会得 9 项失败（pool015、pool025、pool026、pool043、
standard-home、system-settings v1/v2、w02、w03、w20），复跑后其中 5 个套件自行转绿，错误形态为
`Cannot read properties of undefined (reading 'split')` 与登录 401 —— 属 Docker/Argon2 并发负载下的**假红**。
门禁应按套件分片执行；本报告的结论均以「同一命令的 A/B 失败集差集」为依据，而非汇总计数。

## 五、边界遵守声明

- 未执行部署，未执行生产迁移/金额录入/激活，未做任何生产操作。
- 未使用 `--no-verify`；未修改 hook、门禁、守恒逻辑；未改生产守卫语义（F-2 仅改测试夹具）。
- 未扩大 UI 范围：只收口现有日常充值/订阅表单，未提前开发初始化向导。
- 未触碰原脏工作区 `/Users/mac/Projects/仟流智算` 的**既有文件**；未复制项目归集 `0076` 草稿；未推送远端。
- 边界违规与收口（2026-09-22）：WP01～WP04 四份 GO/NO-GO 报告曾被误写入原脏工作区根目录（四个未跟踪文件），违反「只允许在隔离 worktree 工作」。复核裁决判定 WP04 暂时 HOLD，仅做边界收口：四份报告已迁入本 worktree 的 `V4/Evidence/PROVIDER-FINANCE-INITIALIZATION-20260921/` 并保留原文件名，原目录该四项已移除、其余文件零改动。

## 六、下一步

WP01 = GO，可继续 **WP02（事务原语与假设投影）**。
需特别承接的约束：抽取 transaction-aware 资金写入原语时，余额投影必须复用
`provider-finance-balances.ts` 的既有聚合语义，不得另写一套冲销正负号公式。

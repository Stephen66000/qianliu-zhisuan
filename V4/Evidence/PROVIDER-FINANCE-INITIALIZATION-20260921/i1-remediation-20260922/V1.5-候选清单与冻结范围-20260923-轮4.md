# V1.5 候选清单与冻结范围——20260923 轮 4（fix batch 1 新代码候选 · 草案未提交）

> **文档状态**：实施方（WorkBuddy）依 `code-r1-fix-batch1-phase2.md` 第 3 条草拟的候选清单**草案**。
> 尚未提交、未生成锚提交、未生成新候选锁；待 Planning 复核后由 Planning 决定清单锚提交与 manifest 生成。
> 本文档不构成任何 PASS 结论；专项绿不得解读为全量绿；V1.5 代码/功能审计状态以独立 Reviewer 与账本为准。

## 一、候选对象

| 项 | 值 |
|---|---|
| 新代码 HEAD | `f4a04bb04f30c06f04fec323575877608356ce34` |
| 新 tree | `b22eb12fed4ee9912c525a08624702286de888b1` |
| 分支 | `codex/provider-finance-initialization-20260921`（本地，未推送） |
| 提交方式 | 正常 `git commit`（未用 `--no-verify`，hook 未拒绝） |
| 上一候选 | `2928405ceabfe6ee435a07954d630926b32efa49`（tree `322932c2b2dc6348875a4e57d9229c25ead745c1`），**保持可达**，其全部审计结论不被改写 |
| 本增量 | `2928405..f4a04bb` = **2 文件 / +17 −10**，diff 指纹 `ccd4052199b13392`（`post-check/delta-vs-2928405.txt`） |

### 增量内容（与两份合同逐字对应，无越权改动）

1. `packages/database/src/__tests-integration__/pool046-zhipu-window-alias-migration.test.ts`：
   - 同作用域重复 `const rolledBack` 重命名（C-P1-1）：L338→`rolledBackLedger`、L373→`billingRuleAfterRollback`（含 L376/377/398 引用）；
   - `replayed.results` 全量 `toEqual` 精确清单按修订授权在 0072 后补 `0073_credential_chat_probe` ～ `0079_provider_finance_candidate_draft` 七条 `"Success"`（C-P1-3，28→35 项；未用 slice/动态生成/`every(Success)`，既有 28 项未删未放宽）。
2. `packages/database/src/__tests-integration__/runtime-assurance-foundation.integration.test.ts`：
   - 同作用域重复 `const rolledBack` 重命名（C-P1-2）：L100→`rolledBackLedger`、L144→`regAfterRollback`（含引用）。

生产代码、迁移、fixture、断言语义、门禁、hook：零改动。

## 二、Phase 2 门禁实测（新 HEAD `f4a04bb`，执行器 `corepack pnpm@11.11.0`，本地 Testcontainers）

证据目录：`/private/tmp/pf-code-fix-batch1-phase2-20260923-151131-26658/`
（`SHA256SUMS` 自身 SHA-256 = `3f5b36ce8f35ea4d1c82e6fd0726d4a70ebe53ae2e3216ef077502274438db90`，38 文件；时间窗口 2026-09-23T15:11:31～15:33:55 ±0800）

| 门禁 | 命令 | 结果 | exit |
|---|---|---|---|
| 目标套件 1（pool046 完整） | `corepack pnpm@11.11.0 exec vitest run …pool046-zhipu-window-alias-migration.test.ts` | **PASS** 1/1 | 0 |
| 目标套件 2（runtime-assurance 完整） | 同法 `…runtime-assurance-foundation.integration.test.ts` | **PASS** 1/1 | 0 |
| typecheck | `corepack pnpm@11.11.0 run typecheck` | **PASS** | 0 |
| lint | `corepack pnpm@11.11.0 run lint` | **PASS** | 0 |
| build | `corepack pnpm@11.11.0 run build` | **PASS** | 0 |
| **正式全量 `pnpm test`** | `corepack pnpm@11.11.0 test` | **FAIL**（详见§三；Summary `2 fails, 8 passes`；15:13:01→15:29:30） | 1 |
| quality:coverage 链 | `corepack pnpm@11.11.0 run quality:coverage` | **FAIL**——链在 `pool043` 子步因已知 2 结算断言失败中断（与 R1 记录同型）；domain/changed/pool029/pool030/pool040 子步已跑完 | 1 |
| coverage ratchet（standalone 补跑） | `node scripts/check-coverage-ratchet.mjs` | **FAIL**（详见§三.3） | 1 |

## 三、正式全量失败清单（如实列红，未豁免、未代签）

### 3.1 已知断言红灯（7 项，与 R1 勘误口径一致）

| 组 | 用例 | 本轮表现 |
|---|---|---|
| 首页/用量口径（5） | standard-home 4 项（四指标聚合 / R01-F01 / V14-C2 F-B / V14-C4 G02b）+ pool042 POOL20-045 1 项 | 同因失败（FAILED 行 token 污染口径），Owner 未签收 |
| Gateway 结算（2） | pool043-operating-bill-settlement：中断冻结零用量证据 / UNKNOWN 占位结算 | 本轮首显字段为 `api_cost` 口径（`usage_quality==="UNKNOWN"` 行断言，stderr `:1047`），与 R1 勘误一致 |

### 3.2 W20 与 exception-center（如实登记）

- **W20-standard-capacity：本轮 2/2 通过**（该文件总用时约 59.99s，见 `full-test/stdout.log`；P95 达标）。通过不代表容量风险关闭（负载相关，波动性已在例外草案登记）。
- **exception-center：本轮为 1 个失败套件，内含 55 个因 `beforeAll` 失败而未执行的测试**。根因是迁移名单断言仍止于 0072，`ca533e3d` 基线文件相同；它不在既有 7 项断言例外草案内，必须单列，不能仅称“skipped”而淡化门禁失败。本轮不越界修复。

### 3.3 coverage / ratchet / 许可证

- domain 覆盖率本轮 97.98 / 94.61 / 97.61 / 97.98（stmts/branch/funcs/lines），超其防下降基线；
- ratchet FAIL 项与 R1 记录同型：`pool029-web-authorization`（stmts/lines 97.47<99.65，本轮已重新生成）、`pool030-gateway-timeout`（stmts/lines 98.5<100、branches 96.29<100，本轮已重新生成）、`pool043-gateway-model-identity` 报告缺失（因 pool043 子步中断）、`pool043-web-operating-bill-accounts` functions 89.33<90（上次遗留报告，本轮未重生成）；原始清单见 `coverage/ratchet-standalone-stderr.log`；
- 许可证（MPL-2.0）**本轮未重跑**（Phase 2 合同清单不含），既有 FAIL 登记不变，Owner 裁决待办。

### 3.4 无新失败声明

两目标套件之外，全量输出未出现**候选新引入的**失败文件；database 段 3 个失败文件分别为§3.1 首页/用量两个文件及§3.2 exception-center 一个失败套件，gateway 段 1 个失败文件为§3.1 的 pool043 结算。正式门禁仍为 FAIL。

## 四、复审方独立核验命令（示例）

```bash
git -C <worktree> rev-parse HEAD 'HEAD^{tree}'          # f4a04bb… / b22eb12f…
git -C <worktree> diff --stat 2928405..f4a04bb          # 2 files, +17 −10
shasum -a 256 -c /private/tmp/pf-code-fix-batch1-phase2-20260923-151131-26658/SHA256SUMS
corepack pnpm@11.11.0 exec vitest run --root . --config ../../vitest.config.ts \
  src/__tests-integration__/pool046-zhipu-window-alias-migration.test.ts   # cwd=packages/database
corepack pnpm@11.11.0 test                              # 预期 exit 1，红灯=§三清单
```

## 五、残余风险与授权边界（延续登记，不由实施方闭合）

1. 7 项断言红灯无 Owner 签收（例外草案仍待批）；exception-center 口径差异待裁定。
2. coverage ratchet 既有缺口、pool043 覆盖率报告缺失、许可证 MPL-2.0——均待 Owner/Planning 处置。
3. F-P2-5/6/11 维持 `DEPLOYMENT_BLOCKED`；P2/P3 disposition 未签收。
4. 新候选**不得自动沿用**旧功能/代码审计结论；V1.5 对测试语义变化（pool046 清单 +7、两套件从"加载失败"变"可运行"）的影响分类与必要复审由 Reviewer 决定。
5. **生产部署、生产迁移、真实金额录入、生产资金激活继续未授权。**

## 六、清单状态

本清单为**草案**，未提交。Planning 复核通过后：清单锚提交 → 生成新候选锁 → 由独立 Reviewer 按 V1.5 复审。原候选 `2928405` 的全部历史结论保持有效且不可改写。

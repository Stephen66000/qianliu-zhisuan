# V1.5 候选清单与冻结范围（第三轮，2026-09-23）

> **性质声明**：本文档由**实施方**出具，属 V1.5 开审材料（候选清单 + 冻结范围），不构成自我放行。
> 本轮为第三轮候选：响应第二轮 V1.5 有界复审 FAIL 的两个直接阻断项
> （coordinator 体量回归、变异门禁 61.10% < 70）。不改写第二轮冻结锚 `746e97d`，以新候选交付。

## 一、对象锁

| 项 | 值 |
|---|---|
| 候选 HEAD | `cc59b91ad2baab3bab7beb34b7efb22d5137dc7d` |
| 候选 tree | `891429eec4860ebcaa8b4070e013481ee702888b` |
| 分支 | `codex/provider-finance-initialization-20260921`（worktree `/Users/mac/Projects/仟流智算-provider-finance-init-20260921`，**未推送**） |
| 第二轮冻结锚（复审对象） | `746e97d8bae1debe4319ba990460d9a7b688a39e` |
| 工作区状态 | 本清单提交后 `git status` 应为零改动（冻结承诺见 §五） |

**冻结锚自证**：本提交（冻结锚）有且仅有本清单文件一个变更，父提交即候选 HEAD `cc59b91`，
因此本清单所列对象锁与其引用的树内容天然一致，不存在"清单写完又被后续提交改动"的窗口。

## 二、相对第二轮冻结锚的增量（`746e97d..cc59b91` = 6 文件）

diff 指纹：`git diff 746e97d..cc59b91 | sha256sum` → **`21c64f241c207fce`**

| 状态 | 文件 | 说明 |
|---|---|---|
| M | `packages/database/src/repositories/provider-finance-activation-coordinator.ts` | `FACT_WRITE_INVALID` 构造调用合并回一行（**纯换行合并，零语义变化**），恢复登记基线 569 逻辑行；未上调基线、未改 size 脚本 |
| A | `packages/domain/src/__tests__/provider-finance-activation-hardening.test.ts` | 新增定向加固测试 38 例（针对首轮 398 存活 + 35 无覆盖突变体密集区） |
| M | `packages/domain/vitest.provider-finance.mutation.config.ts` | 变异测试集 include +1；`mutate` 范围与 `break: 70` **未改动** |
| M | `V4/.../i1-remediation-20260922/I1-复审申请与整改闭环报告-20260922.md` | 追加 §十三（第三轮整改记录） |
| M | `V4/.../i1-remediation-20260922/r3-quantification/README.md` | 追加 §四（79.25% 复跑）与 §五（V1.5 对照更新） |
| A | `V4/.../i1-remediation-20260922/r3-quantification/pf-domain-mutation-round3.json` | 第三轮 Stryker 原始输出（首轮 FAIL 证据 `pf-domain-mutation.json` 保留未覆盖） |

## 三、两个直接阻断项的闭环

### 3.1 quality:size 回归（复审 [P1]）—— 已修复

- `provider-finance-activation-coordinator.ts` 570 → **569** 逻辑行（≤ 登记基线）。
- 复核命令：`node scripts/check-source-size.mjs`（期望 `passed: 581 files`，exit 0）。

### 3.2 变异门禁（复审 [P1]）—— 已达标

- 复核命令：`cd packages/domain && corepack pnpm@11.11.0 exec stryker run stryker.provider-finance.json`
- 结果：**killed 882 / survived 231 / no-cov 0 → 79.25% ≥ break 70**，exit 0。
- 独立重算：`882/(882+231) = 79.245%`（与 `pf-domain-mutation-round3.json` 一致，
  状态计数 `{'Killed': 882, 'Survived': 231}`）。
- 阈值与 mutate 范围相对首轮**零改动**；首轮 61.10% FAIL 证据保留在
  `pf-domain-mutation.json` 与 README §一（诚实登记，不掩盖历史）。

## 四、复审方独立核验命令（工作区内执行）

```bash
# 对象锁
git rev-parse HEAD 'HEAD^{tree}'
git diff --name-status 746e97d..cc59b91
git diff 746e97d..cc59b91 | sha256sum   # 期望 21c64f241c207fce...

# 阻断项 1：体量门禁
node scripts/check-source-size.mjs; echo "exit=$?"

# 阻断项 2：变异门禁（约 3 分钟）
cd packages/domain && corepack pnpm@11.11.0 exec stryker run stryker.provider-finance.json

# 语义零变化抽查（coordinator 仅一行合并）
git diff 746e97d..cc59b91 -- packages/database/src/repositories/provider-finance-activation-coordinator.ts

# 门禁复跑
corepack pnpm@11.11.0 -r run typecheck && corepack pnpm@11.11.0 -r run lint
node scripts/check-architecture.mjs
corepack pnpm@11.11.0 exec jscpd apps packages --min-lines 20 --min-tokens 100 --threshold 5 \
  --ignore '**/dist/**,**/coverage/**,**/reports/**,**/__tests__/**,**/__tests-integration__/**,**/*.test.ts,**/*.test.tsx' --reporters console

# 相关测试
cd packages/domain && corepack pnpm@11.11.0 exec vitest run --root . --config ../../vitest.config.ts   # 期望 301/301
cd packages/database && corepack pnpm@11.11.0 exec vitest run --root . --config ../../vitest.config.ts --maxWorkers=4 \
  src/__tests-integration__/provider-finance-activation-apply.integration.test.ts \
  src/__tests-integration__/provider-finance-activation-migration.integration.test.ts \
  src/__tests-integration__/provider-finance-activation-preview.integration.test.ts                     # 期望 23/23
```

## 五、冻结承诺

自本清单提交（冻结锚）起，分支 `codex/provider-finance-initialization-20260921` **不再追加提交**；
后续如需任何改动（含复审方反馈的整改），走**新候选 + 新清单**流程。本包继续**不推送、不合并、不部署、不生产激活**。

## 六、V1.5 前提责任划分（诚实登记）

| V1.5 开审前提 | 状态 | 责任方 |
|---|---|---|
| 候选清单与冻结范围（本文档） | **已交付**（第三轮） | 实施方 |
| 候选增量（`b23f2b0..cc59b91`）与质量门禁证据 | **已交付**（26 文件整改 + 6 文件第三轮，指纹见 §二；轮 2 归属与量化证据见报告 §十/§十三） | 实施方 |
| 正式 `pnpm test` 残留 8 项其他功能域失败的例外批准 | **未满足** | **Planning**（归属证据已备，实施方不自行豁免） |
| 原 I1 其余 P2/P3 的 Owner/缓解措施/复核日期/退出条件 | **未满足** | 计划方 / 审核方 |
| V1.5 状态账本 | **未满足** | 审核方 |
| `CODE_AUDIT_IN_PROGRESS` 握手 | **未满足** | 审核方 |
| 审计合同 | **未满足** | 审核方 |
| 预算 | **未满足** | 审核方 |
| 独立模型复审人指定（身份留痕） | **未满足** | 审核方 / 计划方 |

> 按 V1.5「阻断性 Evidence Gap / 必需门禁 FAIL 必须 FAIL」规则：上表审核方前提在复审时
> 仍未满足的，复审应 **FAIL**（而非有条件通过）。本轮实施方义务为 §一～§五全部内容；
> §六其余项不在实施方权限内，如实登记。

# V1.5 轮 5 候选清单：Gateway 零费用失败关闭小包

> 本地候选记录；不构成 V1.5 双审 PASS、全量测试绿色、部署或生产资金激活授权。本文档提交为冻结锚，父提交是下列代码 HEAD；旧轮 4 候选保留可达。无推送。

## 对象与范围

- 起点：轮 4 `d19aedf7d70ce24377ccf7a7bd65efde3c51f7fa`。
- 本轮代码 HEAD：`501b9bdf474ffbfe5e3a1acc240e6d9d78188985`，tree `6479f31b90e668af45726e5edd3f0bf82c9db3cf`；正常 commit、hook 未绕过。
- 相对轮 4：**恰好 8 个实现/测试文件，+108/−27**；无迁移/schema/依赖/门禁/全局经营过滤器改动。
- 语义：上游 caller 已执行但费用无证据的零 Token Attempt 写 `api_cost=NULL/UNKNOWN_COST`；真正上游前拒绝仍写确认零。资金 SQL、余额与 domain 投影对显式 UNKNOWN_COST，及状态 NULL/费用 NULL 的零 Token 行失败关闭；费用已知但状态 NULL/零 Token 的原排空反例不误报。合格确认零修复只抵消自身月度缺口，不能掩盖另一条未知费用。

## 定向验证（均在未提交的同一代码 diff 上执行，提交后代码字节未再变）

原始 Vitest JSON 在 `/private/tmp/pf-gateway-zero-cost-4wyLkT/`。`git diff --check`、`corepack pnpm@11.11.0 run typecheck`、`lint`、`build` 均 exit 0。

| 范围 | 结果 |
|---|---:|
| Gateway Attempt 结算单测 | 8/8 |
| Gateway pool043 真实 pipeline 集成（含原 2 个红灯） | 9/9 |
| domain hardening / 投影 | 40/40、46/46 |
| database 资金 activation apply / preview | 15/15、4/4 |
| database operating-feedback | 8/8 |

首次三文件 DB 定向运行中，operating-feedback 的旧断言仍把 `INCOMPLETE_USAGE_COST` 余额写作精确 `97.50`，实际安全结果为 `balance=NULL`；原始失败 JSON 保留。按已批准语义仅将该测试的余额与完整性预期收紧，保留账单/经营分析已知费用小计断言，随后该文件单独复跑 8/8。没有再改生产逻辑或扩文件。

## 未获关闭的门禁与风险

- 本轮**没有**重跑正式全量 `pnpm test`、百万行容量、全量覆盖率/变异门禁；旧轮 4 的非零结果不自动转绿。Gateway 两项仅取得候选绑定的定向通过，需在以后合适的门禁周期重核整体状态，不因本小包追加压力测试。
- 首页/用量 5 项、基线 exception-center 失败套件、pool029/pool030/pool043 覆盖率问题、MPL-2.0 许可证及 P2/P3 Owner 签收仍未解决。WP08 镜像演练仍未绑定当前 HEAD。
- 历史上可能被旧代码误标 `CONFIRMED_ZERO_NO_UPSTREAM` 的**存量行**没有自动迁移或批量改写；发布前必须基于证据评估。没有生产数据访问、真实金额录入或激活。
- 轮 4 的有界功能结论与代码质量 FAIL 绑定旧对象，不自动转移到本轮。后续只做与这 8 文件直接相关的有界安全/功能复核，不重启无关全量探索；正式双审与发布门禁分别裁决。

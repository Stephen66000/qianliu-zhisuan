# 2026-09-22 工作日志

## WP04 边界收口（授权边界违规整改）

- 复核裁决：WP04 **暂时 HOLD**（功能/测试结论保留）。事由：WP01～WP04 四份 GO/NO-GO 报告被写入原脏工作区
  `/Users/mac/Projects/仟流智算` 根目录（四个未跟踪文件），违反「只允许在隔离 worktree 工作，不得修改原脏工作区」。
- 收口动作（仅边界，零功能改动）：
  1. 四份报告迁入 worktree `V4/Evidence/PROVIDER-FINANCE-INITIALIZATION-20260921/`，保留原文件名；
     迁移前逐份记录 sha256、复制后校验逐字节一致（4/4）。
  2. 更正报告内因迁移失效的表述：WP01 §五 新增「边界违规与收口」；WP02/WP03/WP04 删除
     「本报告为该（原脏工作区）目录下的交付物」等失实句，改为 `V4/Evidence/...` 相对路径；
     WP04 裁决区追加边界收口记录（标注优先于原 GO 结论）。四份报告头部新增「报告归档：」行。
  3. 本地收口 commit `b9a1e84`（4 files changed, +692，仅四份报告）。
- 边界证据：worktree `git status --short` 干净；分支 `codex/provider-finance-initialization-20260921`；
  HEAD `b9a1e84627d378f6cb656d9d2679a3edb721e5a9`；迁移尾部 `0077→0078→0079` 三个文件均存在且被跟踪，
  本次 commit 未改动任何迁移。
- 原工作区非侵入性证明：四份报告路径已不出现于 status（未跟踪计数 0）；根目录已无该文件；
  `find -newermt 03:20:30` 无任何新建/改写文件；已修改(M)文件数仍为 26（与收口前一致）；
  分支仍为 `codex/wecom-activation-release-20260911`。原仓 stash 仅 1 条，为 2026-09-11 的 `wip-main-4`，非本次产生。
- 未越界：未修 C-1/`defaultTo("now()")` P0、未清理既有全量红、未进入 WP05、未部署、未做生产操作、
  未使用 `--no-verify`、未改 hook/安全门禁。
- 结论：**WP04 恢复 GO**（边界证据全部通过）；C-1 P0 与既有全量红仍需单独立项裁决。

## WP05 Web 初始化向导、结构化预检与不可逆激活确认（tasks 5.1～5.5）

- 基线 `b9a1e84`（WP04 边界收口后干净点）。提交 `0102309`（29 files, +3991/−40），报告提交 `94e7fc9`。
  **零后端改动**：变更面仅 `apps/web/**` + `V4/Evidence/**` + `openspec/.../tasks.md`，
  无 `apps/control-api`、无 `packages/*`、无迁移文件 ⇒ 迁移尾部仍 `0077→0078→0079`。
- 新增 Web 前端结构（全部在 `apps/web/src/`）：
  - `api/provider-finance-activation-types.ts`：HTTP 合同类型。**关键口径：HTTP 面 snake_case，
    域直通对象（缺口 / 回执 / 投影）是 camelCase**。`preview.gaps` 是**原样直通的 `ActivationGap[]`（camelCase）**，
    不是 snake_case 包装 —— 这里最容易写漂移。`quiescenceView` 不含 `started_by_admin_user_id`。
  - `api/provider-finance-activation.ts`：React Query 钩子，写操作一律 `retry: 0`（UI 绝不自动重试）。
  - `api/provider-finance.ts`：新增 `invalidateProviderFinanceCaches(client)` 集中失效
    资金 / 资源 / 经营账单（含 `operating-bill-` 前缀谓词）/ 经营分析 / 首页；日常入账与激活成功共用。
  - `components/resources/activation-draft-model.ts`（纯模型）、`ActivationDraftEditor.tsx`（5 区编辑器）、
    `activation-preflight-model.ts`（缺口分组 / 候选保持生命周期 / 错误码文案）、
    `ActivationQuiescencePanel.tsx`、`ActivationReceiptPanel.tsx`、`ProviderFinanceActivationWizard.tsx`、
    `FinanceInitializationPanel.tsx`（状态切换容器）、`use-now-ticker.ts`（仅显示用）。
- 关键设计口径：
  - **切换唯一依据是服务端 `activation-state.strict_writes_enabled`**，不以模式字符串推断：
    `false` ⇒ 初始化向导；`true` ⇒ 既有 `ProviderFinancePanel` + 不可变回执；`mode==="OFF"` ⇒ 不渲染。
    日常 `ProviderFinancePanel` **零改动**（避免初始化与日常语义互污）。
  - **金额「空值 ≠ 0」**：字段留空 ⇒ 本地报错且整行省略；填 `0` ⇒ 合法零值照常提交。
    整行皆空则整行省略（不报错）。请求体只含业务草稿，**绝不出现 `enterprise_id` / `admin_id`**。
  - `REJECTED_WITH_EVIDENCE` 不得用于关闭 `UNKNOWN_COST`（UI 明示警告，不留旁路入口）。
  - **静默门禁是两段式**：`unpaired_usage_lines` 属**落库门**、由候选自身修复行集豁免，
    不阻断首次预检 ⇒ 客户端 advisory 门禁不得拿它挡第一次预检。
  - 候选保持生命周期：过期 / 被取代 / 状态非 `PREVIEWED` / 哈希变化 / 水位漂移 ⇒ 立即清可激活态；
    但 `ACTIVATION_RETRY_REQUIRED` **不清**（服务端已回滚，同一幂等键人工重试）。
- 测试：新增 4 个文件 36 项（草稿模型 12、预检模型 8、向导交互 10、容器 6）。
  Web 全量回归 82 文件 / 513 用例全绿；`vite build` 通过；web 与 control-api 的
  typecheck/lint 均 0 错 0 警；架构门禁通过（568 生产源文件无运行时环）。
- `Resources.test.tsx` 两个资金标签页用例改为**显式声明已激活状态**（新增 `activatedActivationState(mode)` 辅助函数）——
  因为日常面板现在只在已激活时可达。`permissions.tsx` 的 `Access` 新增可选 `enterpriseId?`（纯加法）
  以免容器再订阅一次 `useAdminSession`。
- 页面证据 `V4/Evidence/PROVIDER-FINANCE-INITIALIZATION-20260921/page-evidence/`：
  5 张真实构建产物截图 + 可复现脚本（桩 API + CDP 截图）。取证工具链见 MEMORY.md「无头页面取证」。
- 未越界：未修 C-1 P0、未清既有基线红、未进入 WP06、未部署 / 未生产操作 / 未 `--no-verify` /
  未改 hook 与门禁 / 未写原脏工作区 / 未推送。
- 结论：**WP05 = GO**，STOP 等待 WP06 授权。

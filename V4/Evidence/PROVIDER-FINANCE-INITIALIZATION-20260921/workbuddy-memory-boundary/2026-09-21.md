# 2026-09-21

## 资金账本初始化 WP00（OpenSpec 与候选基线，只读审查）

- 角色：资金账本初始化实现代理（Codex 监督）。授权边界：仅独立本地 worktree 开发与测试；禁改脏工作区/部署/生产迁移/生产录入/生产激活/--no-verify/跳过守恒。
- 已完整阅读 v1.2 计划 + provider-finance-initialization 全部 OpenSpec 工件（proposal/design/tasks/3 delta specs）。
- 只读检查结论：
  - 当前分支 `codex/wecom-activation-release-20260911`，HEAD `2b33719d`（= origin/main `4a3d139` + 3 提交，仅 directory/excel 与 w20 测试差异，资金代码与 main 完全一致）。
  - 脏树 219 项（26 M + 193 ??）归属在途「项目分摊」功能：untracked `migrations/0076_project_allocation.js`、`kysely-allocation-tables.ts`、kysely.ts 增量等；另有三个 project-allocation 专用 worktree。已提交迁移头 = 0075。
  - 关键冲突点：① `provider-finance-cutover-repository.ts:259` 现用咨询锁键 `provider-finance-activation:${enterpriseId}`，与计划规定的 `qianliu:provider-finance-activation:v1:` 不同 → 新旧激活路径不互斥；② 迁移号 0076 冲突风险；③ 旧 CLI `provider-finance-activate.ts` 单月守恒直接激活，与 PFA-04 候选流程重叠，需封闭；④ PFH-07 资金未就绪字段复用/新增留待 WP01。
  - 建议：基线 origin/main `4a3d139346dfc21b37eab3783a16dd0aaa427242`，worktree `/Users/mac/Projects/仟流智算-provider-finance-init-20260921`，分支 `codex/provider-finance-initialization-20260921`。
  - WP00 结论 GO（附上述 4 个决策点），本轮未创建任何源文件/迁移/勾选，等待 Codex 下一阶段指令。
  - openspec/changes/provider-finance-initialization/ 目前整体 untracked，需在实施分支提交。
  - 环境：node v22.22.2（engines >=22.17 <23 ✓），pnpm 仅 corepack 11.11.0 可用（裸 pnpm 不在 PATH）。

## Codex 对 WP00 的回执（23:14）

- 升级顺序锁定：① Kimi 模型发现与权限探针修复验收 → ② 资金账本 → ③ 项目成员与 AI 用量归集。
- 已授权 tasks 0.2 + WP01：自 `4a3d139346dfc21b37eab3783a16dd0aaa427242` 创建 worktree `/Users/mac/Projects/仟流智算-provider-finance-init-20260921`，分支 `codex/provider-finance-initialization-20260921`，复制 v1.2/OpenSpec 工件；原脏工作区不动。
- 关键约束：`4a3d139` 仅为 development base；**WP07 前必须重基于 Kimi 验收后的精确提交并重跑迁移与回归**。
- 仍禁止：部署、生产迁移/金额/激活、--no-verify、改 hook/门禁。
- 当前状态：已确认收到，**未开始执行**，等待 Codex 补充「Kimi 模型发现与权限探针修复」的技术裁决。

## Codex 技术裁决（覆盖 4a3d139/0077 记录）

- 基线改锚为 Kimi 干净终态 `ca533e3d790270f2a33b15de73e3417ab8656b76`（代码 `ac1c6de`，迁移头 `0077`）。worktree 必须从 `ca533e3` 创建，首个迁移为 `0078_provider_finance_activation.js`。
- ① 新协调器固定顺序：先取 legacy 锁 `provider-finance-activation:${enterpriseId}`，再取 v1 锁；旧 `provider-finance-activate.ts` 必须委托候选/静默/幂等协调器或失败关闭，禁止直接调 `activateStrictWrites`。
- ② PFH-07 使用独立 `provider_resource_finance_state` 表（PENDING|READY、required_currencies、ready_at/by、version），不复用 `provider_resource.status`；新 API 资源默认 PENDING；Gateway 排除 PENDING。
- ③ 项目归集 `0076` 草稿无效且不得复制；第三项工作从最终资金账本迁移头继续。
- 授权：tasks 0.2 + WP01，通过后才勾选，输出 GO/NO-GO 后停止等 WP02。

## WP01 执行与验收（资金账本初始化：合同、候选/幂等、静默租约、资源资金状态）

- 产物提交 `45c54de`（worktree `/Users/mac/Projects/仟流智算-provider-finance-init-20260921`，分支 `codex/provider-finance-initialization-20260921`；前序 `10059ee` 为 v1.2/OpenSpec 工件导入）。原脏工作区未改动。
- 迁移头 `0078_provider_finance_activation.js`，sha256 `d6b9d8854eca6ff36231a3d6b7b848d94c12aa49134aedc0e56d6e00f10bf3fa`。三张控制表：`provider_finance_activation_attempt`（TTL 30 分钟表达式约束、状态白名单无 ACTIVATING、终态不可改/ACTIVATED 不可删触发器、幂等键局部唯一）、`provider_finance_activation_quiescence`（窗口 ≤60 分钟）、`provider_resource_finance_state`（PENDING|READY + 资源种子触发器）。`down()` 有激活事实或资源资金状态时拒绝回退。
- 领域纯函数 `packages/domain/src/provider-finance-activation.ts`（金额精度、上海自然日边界、稳定哈希、缺口/修复行排序、候选状态机、租约求值）；`listServableResources` LEFT JOIN 排除 PENDING；旧 CLI 失败关闭（exit 2）。
- 验证全绿：domain typecheck + 198 测试；database typecheck + 迁移集成 5 项；control-api typecheck + 合同单元 23 项；`subscription-renewal-flow` 与 `provider-finance` 集成通过。
- **重要基线事实（可复现）**：在 `ca533e3` 干净基线上，整目录跑 `apps/control-api/src/__tests-integration__` 已有 9 项失败/10 个失败文件（pool015、pool025、pool026、pool043、provider-finance 日期脆弱项、standard-home、system-settings v1/v2、w02、w03、w20）。该红集**不稳定**：复跑后 5 个套件自行转绿，说明是 Docker/Argon2 并发负载导致的假红，而非产品缺陷。因此整目录并发跑不适合作为门禁，应按套件分片。
- 已确认**与 WP01 无关的既有红项**（基线同样失败，保留未改，属测试债）：`provider-finance.test.ts`「历史迁移周期的新额度保存后取代无周期配置」为日期脆弱夹具（硬编码周期 2026-08-18→2026-09-18，运行时已非“当前周期”，被 `bind_current_subscription_period` 守卫以 409 拒绝）；`system-settings-v2.test.ts` 断言 `migrateToLatest` 仅返回 `0072_admin_roles_security`，任何新增迁移都会打破（0078 使其由 6 变 7）。
- **WP01 引入并已修复的真实回归**：PFH-06 强制说明+证据后，`subscription-renewal-flow.test.ts` 未提交 `evidence_ref` 导致 400（已补证据引用）。
- **未修复、需裁决的阻塞项**：`apps/web/src/components/resources/ProviderFinancePanel.tsx:124-139` 日常「充值／订阅」表单既不提交 `evidence_ref`，且 `description` 为可选；PFH-06 生效后该写入口将返回 400。现有 web 测试用 `objectContaining` 断言，**无法捕获**该问题。按 v1.2 计划，证据 UI 属 WP05（Web 初始化向导 / PFU-02）范围，故未擅自改造，留待 Codex 裁决（现修 or 随 WP05）。

## 复核裁决：WP01 转 HOLD → 收口后 GO（09-22）

- Codex 裁决：F-1 不能留到 WP05——后端 PFH-06 已强制说明/证据而日常 UI 直接 400，属**本提交引入的客户端合同断链**，必须在进入 WP02 前最小收口。禁止扩大 UI/部署/生产操作。
- 收口提交 `aaacbab`（`45c54de` 为 WP01 主体）：
  - **F-1** `ProviderFinancePanel.tsx` 日常 API 充值 + Coding Plan 购买/续费新增「说明」「证据引用」必填校验，如实提交 `description`/`evidence_ref`；只改现有日常表单，未开发向导。web 测试改为断言**完整 payload**（不再仅 `objectContaining`），并新增缺说明/缺证据的阻止提交负向用例。
  - **F-2** `provider-finance.test.ts` 的 `MIGRATED_CARRYOVER` 周期与月份查询改为按**当前上海自然月**派生（`[本月上海零点, 次月上海零点)`），不再硬编码；未改生产守卫语义。
  - **F-3** `system-settings-v2.test.ts` 改为 `toContain("0072_admin_roles_security")`，新增迁移不再误报。
- 复验全绿：web **477/477**（78 文件）；provider-finance **11/11**（含 F-2 用例）+ subscription-renewal-flow 1/1 + system-settings-v2 8/8；domain 198；迁移集成 5；合同单元 23；domain/database/control-api/web 四包 typecheck 与 lint（`--max-warnings=0`）全通过。
- 结论：WP01 = **GO**，可继续 WP02。报告 `V4-WP01-资金账本初始化-GO-NO-GO报告-20260921.md` 已按收口结果更新（含裁决表、最终验证表）。
- 复用经验已固化为技能 `~/.workbuddy/skills/regression-triage-baseline-diff/`：用 `git stash push -u` + JSON reporter 做基线/改动 A/B 失败集差集，区分「新增回归 / 既有失败 / 并发假红 / 日期脆弱夹具」。

## WP02 执行与验收（只读候选投影、完整事实水位、切换时点至候选水位守恒）（09-22）

- 授权边界：只做 WP02（tasks 2.1～2.4），不进入 WP03；禁部署、生产操作、--no-verify、改 hook/安全门禁。
- 产物提交 `6b019e9`（17 文件，+3009/-92），worktree 干净；原脏工作区未改。报告 `V4-WP02-资金账本初始化-GO-NO-GO报告-20260921.md`。
- 新增：
  - domain `provider-finance-balance-components.ts`——**余额公式唯一实现**（`addBalanceComponents`/`sumBalanceComponents`/`balanceAmount`/`balanceFormulaMatches`/`isBalanceNegative`）。
  - domain `provider-finance-activation-projection.ts`——纯投影：`conservationMonths` 上海自然月窗口、`projectActivationCandidate`（虚拟期初/充值、`draft:<key>` 虚拟周期、旧记录 MIGRATED/ALREADY_REPRESENTED/REJECTED 关闭）、`planUsageRepairs` + `usageRepairFieldDigests`（固定行集与逐行目标/非目标字段摘要，与「完整事实水位」是两个独立合同）；经营账单缺口以 SQL `countFinanceGaps` 为权威口径做增量抵消。
  - database `provider-finance-activation-facts.ts`——`loadActivationScope`（2.1）、`loadProjectionFacts`、`loadLedgerLines`（支持 `ledgerLineIds` 供 WP03 复验）、`buildFactWatermark` + `FACT_WATERMARK_SECTIONS` 十段（2.2）。
  - database `provider-finance-activation-preview.ts`——`buildCandidateProjection`（`REPEATABLE READ` + `SET TRANSACTION READ ONLY`，零写入）/`previewActivation`（只读事务提交后由独立短控制事务落候选，TTL 30 分钟）。
  - database `provider-finance-balance-facts.ts`——`loadBalanceFactTotals`/`toBalanceComponents`（各分量经 `money()` 归一，避免 `"1"` vs `"1.00000000"` 回归）。
- **去重**：`provider-finance-balances.ts` 重写为复用共享语义；`provider-finance-cutover-repository.ts` 改用 `balanceFormulaMatches`；两处自写 `.plus/.minus` 冲销链删除（满足「禁止第二套公式」）。
- 本轮修复：① 遗漏 `sortActivationGaps` 导入；② 新增 domain 测试自相矛盾断言（`2026-09-31`）已改为只接受真实自然日；③ `projectActivationCandidate` **圈复杂度 109 超 `complexity:30` 门禁** → 拆为 7 个纯函数子步骤（行为不变）。
- 既有债顺带消除：`provider-finance-ledger`/`provider-finance-cutover` 的迁移回滚断言硬编码 head=`0072`，WP01 的 `0078` 使其在基线即失败 → 抽 `migration-rollback.ts` 的 `rollbackTo(db,target)` 解耦迁移头。
- 验证全绿：domain 221/221（13 文件，含新增投影 23 项）+ domain/database lint 0 警告；database 资金与迁移集成 22/22（5 套件，含新增 preview 4 项；另 `migration.integration.test.ts` 7/7）；control-api `src/provider-finance` 单元 23/23；domain/database/control-api/web/worker 五包 typecheck 全 0 错误，web lint 0 警告。
- 结论：**WP02 = GO**。未进入 WP03。
- 遗留边界：锁序无调用点、`SERIALIZABLE`/候选复验/`40001|40P01` 映射、四字段修复写入原语、静默门禁与幂等键、Control API 路由与 Web 向导均属 WP03～WP05。

## WP03 执行与验收（事务原语、四字段修复、原子激活协调器、幂等终态）

- 授权边界：只做 WP03（tasks 3.1～3.4），不进入 WP04；禁部署、生产操作、`--no-verify`、改安全门禁。
- 产物提交 `5c71b78`（10 文件，+2328/−101），worktree 干净；迁移头仍 `0078_provider_finance_activation.js`（**未新增/修改迁移**）。报告 `V4-WP03-资金账本初始化-GO-NO-GO报告-20260921.md`。
- 新增：
  - database `provider-finance-activation-writes.ts`——7 个**纯 Tx** 原语（`insertFinanceEventTx`/`insertOpeningBalanceTx`/`insertRechargeTx`/`insertSubscriptionTx`/`insertCarryoverPeriodTx`/`closeLegacyPurchaseTx`/`writeActivationAuditTx`），自身不开事务；`provider-finance-events.ts` 与 `provider-finance-renewal.ts` 改为委托（公开合同不变）。
  - database `provider-finance-usage-repairs.ts`——`applyUsageRepairsTx`：按 `ledgerLineId` 排序 + `loadLedgerLines({ledgerLineIds, forUpdate})` 只锁候选固定行集，逐行前后复验 `nonTargetFieldsBeforeHash`，只写四列。
  - database `provider-finance-activation-coordinator.ts`——唯一 `SERIALIZABLE` 事务、12 步：二次确认 → 先 try legacy 后 try v1 `pg_try_advisory_xact_lock`（失败即 409，不等待）→ 幂等重放 → `FOR UPDATE` 锁候选校验（GO/TTL/哈希/未激活）→ 静默租约 ≥5 分钟 + 排空 → 锁内重算水位/候选哈希 → 写期初/充值/购买/跨切换周期 → 关闭旧记录 → 四字段修复 → **空草稿 + 真实事实**重跑完整窗口守恒 → 写 `strict_writes_enabled`/资源 READY/`PREVIEWED→ACTIVATED`/回执/审计 → 提交。不写 `ACTIVATING`；`mapActivationFailure` 在 `execute` 之外映射 `40001|40P01` → `ACTIVATION_RETRY_REQUIRED`，零重试。
  - repository 增补 `loadCandidateForUpdate`、`promoteResourceFinanceReady`。
- **本轮修复的真实缺陷 B-1**：`countNewRowsAfterPreview` 写成 `line.id <> ALL(sql.join(...))`——`sql.join` 只产出逗号列表（行表达式而非数组），PG 报 `42809`，会导致**任何带修复行的激活**在四字段修复一步整体失败。改 `ALL(ARRAY[...]::uuid[])` 后转绿。另有 3 项类型错误（`operation_log.result` 取值、`api_cost_status` 枚举收窄、`periodSource` 缺 `MIGRATED_PURCHASE`）与 2 处未用导入。
- **重要口径发现（需 WP04 裁决）**：`collectDrainReport` 把"切换时点后 `api_cost_status IS NULL` 的 API 行"计为未配对，而 `loadLedgerLines` 窗口是 `COALESCE(settled_at,created_at) >= 切换时点`；两者叠加使 `settled_at IS NULL` 的行只要在窗口内就必然被排空门禁拦下 ⇒ 经协调器可修复的行只能形如「切换前采集 + 切换后结算 + 未定价」，`settled_at` 在生产路径不可达（其可修复性由独立直测原语用例覆盖）。WP03 未改既有门禁，选择以夹具贴合并把歧义上报。
- 验证全绿：database 定向集成 **11/11**；资金集成回归 **43/43**（7 套件）；domain 资金单测 **41/41**；database 资金/账单单测 **83/83**；domain/database/control-api typecheck 0 错误、lint 0 警告。另注意 `packages/database/tsconfig.json` 的 `exclude` 跳过 `__tests-integration__`，集成测试须另用临时 tsconfig 做 `tsc --noEmit`（B-2～B-4 正是由此暴露）。
- **既有债（证伪非本次回归）**：`operating-snapshot-subscription-period-migration.integration.test.ts` 硬编码回滚 head=`0072`，与 WP02 已修的两处同类；三重证据（0078 来自 WP01、该测试只 import `kysely.js`/`migrator.js`、`git stash` 后同样失败）。未擅自修改，建议按其 `rollbackTo()` 口径收口。
- 结论：**WP03 = GO**，未进入 WP04。

## WP03 复核收口（同日，裁决 HOLD → 修复 → GO 恢复）

- 复核裁决：WP03 **暂时 HOLD**，不进入 WP04。理由：上一轮上报的「排空口径歧义」是**真阻断**——候选已冻结、
  且将在同一事务内确定性修复的 `ledger_line` 若仍被排空门禁一律计为未排空，四字段修复主路径在生产不可达，违反 PFH-04。
- 修复（**最小范围**，commit `8d568b3`，6 文件 +303/−50）：
  1. `collectDrainReport(enterpriseId, trx?, { excludeLedgerLineIds })`——**只**从 `unpairedUsageLines` 一个计数排除；
     `IN_PROGRESS` 请求／未结束 `upstream_attempt`／`PENDING` 交易**无豁免入口**；新增
     `exoneratedUsageLines` 使豁免数可审计；`<> ALL(ARRAY[...]::uuid[])` 显式包数组（同 B-1 的 42809 坑）。
  2. 排空门禁从租约门禁**拆出**为 `assertDrained`，并**移到 `reverifyCandidate` 之后**（步骤 7）：
     豁免集 = 候选存档 `usage_repair_baseline`，只有候选已 `FOR UPDATE` 锁定且 GO/TTL/候选哈希/完整事实水位
     全部验证通过（含 `assertSameBaseline` 逐行一致）后该集合才可信 ⇒ 非候选行与预检后新增行拿不到豁免。
     租约门禁仍留在候选锁定后第一时间。四字段修复（步骤 10）仍按主键 `FOR UPDATE` + 逐行目标字段/`nonTargetHash` 复验。
  3. 集成用例 **11 → 13**：新增「已冻结的切换后未结算可修复行可通过排空并在同事务修复（含"不传豁免集 → drained=false"
     的回归护栏；在途请求/未结束 Attempt 即使存在对应固定行也不豁免）」与「排空豁免只认候选冻结行：预检后出现的
     同形状行与新行都被挡下且不被修复」；静默门禁用例补「企业内非候选未结算行」「在途请求」两种缺口。
     夹具助手新增 `outputTokens`（零 Token 才能既不算经营账单缺口、又隔离排空门禁）。
  4. 用既有 `rollbackTo` 收口 `operating-snapshot-subscription-period-migration` 的硬编码回滚头
     （锚点改为目标迁移 `0064_...`）；已核查其余回滚套件都用 `migrator.migrateTo("<具体迁移>")` 钉住迁移头，不受新增迁移影响。
- 验证全绿：WP03 定向集成 **13/13**；资金集成回归 **46/46**（8 套件，含收口的迁移套件）；收口迁移套件单独 **1/1**；
  domain 资金单测 **41/41**；database 资金/账单单测 **83/83**；domain/database/control-api typecheck 0 错误、lint 0 警告。
- 报告已更新（`V4-WP03-…-GO-NO-GO报告-20260921.md` 增加 §三 复核收口、§七 边界声明改为"受授权的合同扩展"）；
  `tasks.md` 3.3 改为 13 步、3.4 用例数改 13、新增 3.5 收口条目。
- 结论：**WP03 = GO（HOLD 已解除）**，worktree 干净，未进入 WP04。

## WP04：Control API 激活接口 + 静默租约跨服务门禁（commit `a11af6e`，32 文件 +2302/−78）

- 交付（tasks 4.1～4.5）：
  - 新增 `apps/control-api/src/provider-finance/activation-routes.ts`：`activation-state`（`resources.view`，只回候选元数据与
    摘要计数，脱敏 `candidateMetadata`/`scopeSummary`/`drainView`/`quiescenceView`）、`activation-preview`（严格 schema，
    显式拒收 `enterprise_id`/`admin_id`/未知键）、`activate`（只收 4 个字段，权威企业与管理员只取自 `req.admin`，委派
    WP03 协调器）、静默租约 start/query/release（上限 60 分钟、解除须填原因）。新增 `leaseView()` 把仓储 camelCase
    归一化为 HTTP snake_case（3 个出口）。
  - 新增 `apps/gateway/src/admission/enterprise-maintenance.ts`：`createQuiescenceGate` 经 `chainGuards` 插到模型授权**之前**，
    有效租约直接 503 `enterprise_maintenance`（带 `retry-after`），**零 pipeline、零 `ai_request`**；`/v1/models` 不设门禁。
  - Worker：`operating-bill/runner.ts` 与 `provider-operating-sync/runner.ts` 跳过静默企业的自动续费/开账单/经营同步。
  - 新增迁移 `0079_provider_finance_candidate_draft.js`：候选草稿 `jsonb NOT NULL` + 历史行**哨兵空草稿**（必不匹配哈希，
    只能重新预检，绝不猜测继承）+ 并入既有不可篡改触发器；不写任何资金事实。
- 门禁结果：control-api 集成 **37/37 文件、295/295 用例**（连续 3 次全绿）；worker **3/3、10/10**；
  database WP04 定向（迁移 5/5、旧 CLI 失败关闭 1/1）；四包 typecheck 0 错误、lint 0 警告。
- **关键发现（既有系统性缺陷，P0，未修）**：迁移里 `defaultTo("now()")` 被 Kysely 渲染为**引号字面量**，PostgreSQL 在 DDL 时
  求值并存成**常量默认值**。实测 `enterprise.created_at` 的 `column_default` = `'<迁移执行时刻>'::timestamptz`，
  间隔 1.5s 插入 2 行仍 `count(distinct created_at)=1`。**共 41 列 / 16 个迁移受影响**，仓库内**无一处**
  使用 `defaultTo(sql\`now()\`)`。后果：所有 `ORDER BY created_at` 退化为按 id 排序。它叠加「单企业口径 `/auth/login`
  取 created_at 最早、同值按 id 排序」，使多企业夹具约 **50%** 概率把会话落到隔离企业 → 401。
  建议单独立项（加法迁移 `ALTER COLUMN ... SET DEFAULT now()` + 回填评估）。
- 既有测试债（基线一致，未修）：database 全量集成 **17 文件 / 20 用例**红（多数是 `*migration*` 套件的迁移头漂移断言）；
  gateway `pool043-operating-bill-settlement` **2 用例**红（零用量 `api_cost: null` vs `"0.00000000"`）。
  两者都用 `git stash -u` 在 `8d568b3` 上复现：失败清单**逐字一致** ⇒ 100% 既有。
- 本次在授权范围内做的**测试层**最小收口（不改断言、不动 schema、不动安全门禁）：
  1. 新增 `apps/control-api/src/__tests-integration__/migration-rollback.ts#rollbackTo`（与 `packages/database` 同名辅助
     语义一致；后者在测试目录、不属对外导出面，故另存一份），收口 `pool015`、`system-settings-v1`、`pool025` 的硬编码回滚头。
  2. 按仓库**既有先例**（`pool027` 已用显式时间戳并附同因注释、`system-settings-v2`、`pool043-operating-bill-accounts`）
     为 6 个多企业夹具显式锚定 `created_at`：`w02-auth-principal`、`w20-resource-insights`、`pool026-deployment-logs`、
     `w03-key-grant`、`w20-directory`、`standard-home-route.integration`。
- 归因方法（有效，已沉淀进 `regression-triage-baseline-diff` skill）：`git stash push -u -- apps packages` 回到纯基线 →
  跑同一命令比对**失败集合**（不是汇总计数）；对抖动套件**两侧各跑 3 次**比失败率（本次 baseline 2/3 失败、含 WP04 1/3 失败
  ⇒ 同为抖动）。
- 报告：`V4-WP04-资金账本初始化-GO-NO-GO报告-20260921.md`；`tasks.md` 勾选 4.1～4.5 并新增 4.6 门禁收口条目
  （明确标注「非 WP04 功能项，建议单独立项」）。
- 结论：**WP04 = GO**，worktree 干净，未进入 WP05。


# 厂商模块复盘修复：生产部署与业务验收

日期：2026-09-05（Asia/Shanghai）

结论：**PASS。最终候选已部署到 Mac Mini，技术健康及本轮厂商模块生产业务验收通过。** 本结论不代表产品版本整体完成。

## 1. 部署对象

| 项目 | 事实 |
| --- | --- |
| 主机 | `stephendeMac-mini.local`，arm64 |
| 原发布 | `/Users/stephen/releases/qianliu-0905-e2e77e1` |
| 原提交 | `e2e77e1d753e0667b105118e99b5d5d055fc1ec6` |
| 最终发布 | `/Users/stephen/releases/qianliu-provider-review-7b27014-20260905-190032` |
| 最终候选提交 | `7b2701442c2bd5e83709c6282d549d4a5a581cbe` |
| 最终候选 Tree | `fcd139c3851cfa29b63d18c0079977b12b272455` |
| GitHub 来源 | `refs/heads/codex/provider-module-review-20260905` |
| 数据库迁移头 | 发布前后均为 `0063_operating_snapshot_subscription_period` |
| 数据库变更 | 无迁移、无历史数据改写 |

发布前已确认原提交是候选祖先；候选对迁移目录、Compose、Caddyfile 和 PostgreSQL 初始化目录无差异。应用镜像在线构建完成后，业务服务才进入短暂停写、备份和切换。

生产验收期间依次发布：首轮 `405dc5f`、历史月口径修正 `acad686`、标题及小数展示修正 `7b27014`。每轮均从当时正在运行的精确提交向前发布，没有回退已上线修复。

## 2. 发布结果

发布脚本返回 `COMPLETE`：

```text
control=200
gateway=200
web=200
caddy=200
worker=healthy
database=0063_operating_snapshot_subscription_period
```

容器启动初期出现两次 `curl: (52) Empty reply from server`，随后在健康等待窗口内全部达到上述状态，未触发回滚。

最终发布前备份：

```text
/Users/stephen/backups/qianliu-zhisuan/pre-provider-review-7b27014-20260905-190032.dump
sha256=9ab3845d83da75273e78a03db9c10888707b18b6dff227db33331786a5b9fbdc
```

## 3. 生产页面验收

### 资源利用

- 页面登录及真实数据读取正常。
- 首轮表头显示“利用率（近三月均值）”，第二轮先修正为“利用率（近月均值）”；需求方随后要求表头只保留“利用率”，计算依据继续放在悬停说明中。
- 资源利用标题下的说明文字已删除。
- 订阅周期下“按登记事实展示，不推断月订阅”已删除。
- 已登记资源表的凭证指纹列已删除，状态、创建时间和操作列对齐。
- “周期已扣减”已删除；Kimi 周期真实 Token 为 `95,056,621`，智谱周期真实 Token 为 `237,747,535`。
- 2026-09 首轮利用率均显示 `—`。修正后以完整的 8 月作为分母，Kimi、DeepSeek、智谱分别显示 `3.7%`、`7.6%`、`45.3%`；需求方确认页面已经有数且算法含义正确。
- 最终表头只显示“利用率”，月份数量、基线月份和月均 Token 保留在悬停说明中。
- 本月真实 Token：DeepSeek `43,923,352`、Kimi `13,114,372`、智谱 `116,587,268`。

### 用量总览

- DeepSeek“已分配主体额度”为 `2,300,000,000`，原显示 0 的问题已关闭。
- Kimi 和智谱的主体额度分别为 `3,530,100,000`、`4,005,100,000`。
- 模型使用明细已删除“所属资源余额 / 剩余额度”列。
- 所有 Coding Plan 模型的本月花费显示“不涉及”；API 模型继续显示实际花费。
- 厂商总体使用情况和模型使用明细标题下的说明文字已删除。
- 模型 Token 与厂商汇总守恒：
  - DeepSeek：`25,137,793 + 2,409,698 + 16,375,861 = 43,923,352`；
  - Kimi：`13,113,980 + 392 = 13,114,372`；
  - 智谱：`17,004,486 + 27,066,625 + 72,516,157 = 116,587,268`。

### 额度窗口、供给与健康

- 厂商额度窗口标题下指定说明文字已删除。
- DeepSeek API 正确显示不适用；Kimi、智谱的滚动 5 小时和周窗口正常显示并可同步。
- 供给预测和资源健康与异常标题下的说明文字已删除。
- 三家资源健康均显示正常、允许请求、无需处置。
- 供给预测继续展示真实速度、预计耗尽、下一恢复、覆盖时长与可信度，本轮未修改其算法。
- 最终页面确认速度、覆盖时长、额度窗口值及近月平均 Token 最多显示两位小数；金额仍固定两位、利用率一位、Token 整数。接口和数据库原始精度未改变。

### 充值与订阅

- 页面可正常读取新账本：人民币实付 `600.00`、API 经营成本 `109.03`、当前 API 余额 `497.80`。
- 历史期初、两笔充值及历史费用封口记录正常展示。
- 本页面不属于本轮修改范围，只作为发布后账本读取未回归的补充观察。

截图中出现的“人民币、代币、编码计划”等中英文重复来自 Chrome 自动翻译，不属于应用返回的数据或本次缺陷。

## 4. 审核与交付边界

- CQA-01、CQA-P3-01、CQA-P3-02 已修复关闭。
- 利用率 1～3 个完整历史月口径、表头精简及厂商模块小数显示已完成 I1 复核并通过生产页面确认。
- 厂商功能候选和发布脚本均完成用户约定的 I1 有界复核。
- GitHub 草稿 PR #4 已关闭且未合并；本次通过专用分支发布，没有创建新的 PR。
- 远端 main 未合并本候选；产品版本整体仍处于继续开发状态。
- 正式发布、模块验收、Git 主线合并和产品版本完成是不同结论。

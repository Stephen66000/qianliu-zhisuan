# 厂商模块 P3 精度修复与 I1 复核

日期：2026-09-05

基线：`6e4fb4b37e99ba0290f19c13a011adf53a34d1c0`

分支：`codex/provider-module-review-20260905`

## 审核口径

用户已明确：只执行 I1，不再做 I2。本轮按此执行，不因缺少 I2 阻断推进。独立 Reviewer 不参与本轮实现，复核期间产品代码和测试保持冻结。

## 本轮修复

| Finding | 修复 |
| --- | --- |
| CQA-P3-01 | 一位百分比显示改用十进制字符串与 BigInt，按 half-up 四舍五入，避免二进制浮点的半位偏差 |
| CQA-P3-02 | 主表真实 Token 复用 `formatCount`，与悬停一样保留超安全整数的精确值 |

修改限定为 `ResourceUtilizationPanel.tsx`、对应组件测试和既有单场景 E2E 的百分比期望。新增格式化函数为组件私有，不改变其他页面的百分比格式，不新增依赖。后端三月均值、CQA-01 截止与事务快照、DeepSeek Grant 汇总均未变化。

E2E 不再复制生产端的 `Number * 100 / toFixed` 计算，而使用 Intl 作为独立显示期望；固定 90.0% 业务断言继续保留。半位及大整数回归使用组件测试中的字面预期值。

## 验证记录

新增 8 项边界回归，修改产品代码前 3 项失败、5 项通过；修改后整个组件测试文件 19/19 通过。

| 输入 | 预期 |
| --- | --- |
| `0.01150000` | `1.2%` |
| `0.02550000` | `2.6%` |
| `0.01149999` | `1.1%` |
| `0.00050000` | `0.1%` |
| `0.99950000` | `100.0%` |
| `1.99950000` | `200.0%` |
| `0.00000000` | `0.0%` |
| Token `9007199254740993` | 主表和悬停均为 `9,007,199,254,740,993` |

Web typecheck、受影响代码 ESLint、source-size（417 个生产源码文件）、architecture 和 `git diff --check` 均通过。Web build 通过，保留原有 bundle 大小提示。`provider-review.spec.ts` 单场景真实接口/浏览器验证 1/1 通过，使用本机专用 `_e2e` PostgreSQL 17 夹具；没有执行全仓 E2E。

## 冻结对象及 I1 结论

- 对象清单：`P3-整改代码对象.json`。
- 21 个代码/测试文件集合摘要：`cddfb2bf82a285dcafc965d4106beec9e1e6beb2b38d7573f01af92f0f0755e2`。
- Reviewer：`/root/provider_review`，独立会话，未参与实现。
- 复核范围：两项 P3 修复、边界测试和受影响的显示逻辑；不重开已关闭的 CQA-01。
- I1 回传：**PASS。CQA-P3-01、CQA-P3-02 均可关闭，无新增 Finding；CQA-01 维持关闭。**
- Reviewer 首尾逐文件核对 21 个 SHA 全部一致，相对 R2 仅预期的三个前端文件变化。
- Reviewer 只读复核十进制 half-up、超安全整数格式化及字面预期回归；未参与实现、未修改文件、未重复运行测试。

## 当前交付状态

本次厂商模块复盘所登记的 CQA-01 与两项 P3 均已关闭，本轮 I1 有界复核通过。已完成本地候选的目标测试、类型检查、Lint、架构/规模检查、构建和单场景浏览器验证，可进入提交准备。

本轮不等于正式环境实际数据验收。尚未提交、推送或部署。

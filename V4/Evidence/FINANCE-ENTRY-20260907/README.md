# 充值与订阅入口及完整续订流程修复

## 本地完成，尚未提交或部署

用户验收要求：8月份可查Kimi／智谱已登记订阅及金额；新增Coding Plan时明确选择自动续订，并跑通实际后台流程。

- 修复历史资源选择被API表单筛选状态重置。所有资源可切换；入口显示充值／订阅。
- 显示已登记订阅产品、周期、金额、状态；资金历史和月度采购汇总读取已有历史采购及无新流水的已登记周期费用。按原登记周期开始月归属，不伪造9月付款，不写入历史数据库。已迁移采购按原ID去重，已有真实采购关联的周期不再补一条展示。
- 新增auto_renew勾选值穿透UI、API、请求防重及数据库开关；支持其他厂商Coding Plan。未勾选不续记，勾选后按登记金额／周期继续。旧请求重放不改变取消状态。
- 自动续订使用现有Worker路径，不联系厂商扣款。取消真实生产订阅不作为测试手段。

## 证据

49项定向测试通过：数据库22（含8月双厂商历史、汇总、采购及迁移去重、完整后台状态流）；API17（含真实登录→登记false→到期不续→登记true→定时续记→历史与采购及金额汇总→防重→取消→停止）；Web10（含选择器、金额展示、勾选提交、取消及采购页面）。测试均使用隔离测试数据库或组件环境，不声称线上业务已验收。

API全链测试文件：apps/control-api/src/__tests-integration__/subscription-renewal-flow.test.ts。数据库测试：subscription-auto-renewal.integration.test.ts、operating-feedback.integration.test.ts。对应日志database.log、api-final.log、web.log。

Database／Control API／Web类型检查通过；增量Lint、架构、源码体积和Web构建通过。沿用0066，无新增迁移。本轮为作者I0核查，无完整V1.4专项审核声明。线上仍是db78a30。

提交说明：用户已授权本轮commit；上文未提交状态为验证时快照，实际提交以Git记录为准。未推送或部署。

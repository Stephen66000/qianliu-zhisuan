# E-W20-08｜逐资源利用、耗尽与闲置事实

| 字段 | 结果 |
| --- | --- |
| Stage／工作包／风险 | Stage 03／M20-4／W20-08／`NORMAL` |
| Evidence 时间 | 2026-08-13 03:41（Asia/Shanghai） |
| 候选指纹 | 基线 HEAD `6fc1bec2a211c0d9c399400076cc56720f5c1815`；迁移头 `0049`；锁文件 SHA-256 `ade8a028cfbcb22e2fb4f7c5e82c223ec9cf75f8373c8790157405512515e3c6`；隔离 dirty worktree |
| 最终 Commit／Tree 锁 | `not_run`：未授权 commit；W20-10 仍需唯一候选锁 |
| AI Eval | `N/A` |
| 结论 | `W20-08=done` |

## 1. 输入与真实执行

- 输入为 DeepSeek／Kimi／智谱形态的合成 API 与 Coding Plan 资源、额度窗口、账本和预测快照；未调用厂商写接口。
- 完整 `quality` exit `0`；完整 E2E Web `29/29`、Gateway real Codex CLI 通过。E2E 从真实资源页进入账本明细并保持资源筛选。

## 2. 冻结开发计划对照

| 要求 | 实际证据 | 状态 |
| --- | --- | --- |
| API／Coding Plan 分栏 | API 返回真实 Token、API 费用、月预算／余额／覆盖预测；Coding Plan 分开展示 5 小时和周窗口 | `pass` |
| 利用、耗尽、最近使用和连续无调用 | 服务端按资源返回 utilization、1h／24h／7d 速度、预测状态、lastUsedAt 和 noCallDays；页面只展示事实 | `pass` |
| 可下钻 | 资源名关联账本明细、额度窗口、供给预测和资源健康；E2E 验证账本 URL 及请求行 | `pass` |
| 未知不伪造 | 无分母显示“— / 未设置”；余额未知、速度不足、预测缺失／超过 15 分钟分别返回不可计算或 STALE，不输出精确日期 | `pass` |
| 不把余额比例当利用率、不凭 Token 少判闲置 | 利用率只采用 API 月预算或厂商原生 5h／周窗口；闲置为最近调用时间事实 | `pass` |
| 无自动副作用 | 新增目录／用量／资源范围扫描后台 scheduler 命中 `0`；资源事实为只读查询，未写配置或自动调度 | `pass` |

## 3. 失败恢复、残余风险与裁决

- E2E 曾因表内同名资源定位歧义失败；收窄到资源区及操作列后完整复验 `29/29`，未改业务口径。
- `not_run`：真实厂商余额／权益快照现场核对；无已批准测试资源，合成快照不冒充实时厂商结果。
- 冻结事实链、未知语义和 non-scope 均满足，裁决 `done`。


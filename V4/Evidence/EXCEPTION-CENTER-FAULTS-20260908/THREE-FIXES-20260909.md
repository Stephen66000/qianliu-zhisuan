# 三项问题最小修复交付

日期：2026-09-09。按用户最后确认的四条边界执行：仅修三个已确认问题，检查直接影响路径，不重构、不新增功能、不执行大规模变异或全仓审核。

## 修复结果

- F01：经营同步的每次失败以已有执行记录 ID 保留，不再被后续成功覆盖。后续成功记录将该失败转入历史，再次失败有自己的发生时间；重复查询不会重复入库。成功必须属于同一企业、资源，且发生在失败执行结束之后。
- F02：新失败归并前，在原事务锁内复用成功请求验证，识别旧失败与新失败之间的真实恢复。旧事件保留人工处理说明及客观恢复时间，新事件按新的发生月份显示；当前资源再次不健康不会抹去历史成功，其他模型或晚于本次失败的成功不会错误切分事件。
- F03：有同企业可靠触发请求的资源异常，以请求开始时间作为发生日期；观察时间仍保留事件记录时间。无关联或跨企业不可靠关联时回退到事件发生时间。

本轮仅修改四个仓储实现文件和一个现有合同测试文件：alert-event-background.ts、alert-event-repository.ts、alert-event-recovery.ts、alert-observation-writer.ts、exception-center-contract.integration.test.ts。未改前端、Worker、接口结构、依赖或迁移。

## 验证

- 三个主场景先在旧实现上真实失败，再在修复后通过。另补模型/时序负向与跨企业日期回退断言。
- 仓储合同：55 项通过；Control API 异常接口：14 项通过；Gateway 运行保障：5 项通过。
- RuntimeAssurance、AlertDetailDrawer、AlertsPanel、alert-presenters：4 文件、50 项通过。
- 合计124项直接相关回归通过。数据库 TypeScript 检查、五个修改文件的 ESLint、格式和 Git 差异空白检查通过。
- 直接回归首次有一条旧断言要求中间成功后仍只保留一个历史事件，与F02目标冲突。已替换为两条事件及各自16:05、16:07恢复时间的精确断言，未仅放宽数量判断。

使用现有 REPAIR-01/node.config.ts 与 web.config.ts，明确传入上述测试文件；未启用coverage或Mutation，也未启动新的独立全面审核。此前审核FAIL及失败原始证据不改写，本记录仅关闭上述三个问题并说明本次验证边界。

## 部署建议与边界

这三项修复及直接影响路径验证通过，建议进入本批变更的提交、推送和部署准备，不再追加全仓审核。当前尚未提交、推送或部署；Mac mini真实生产数据的更新后验收尚未执行。

本轮F01针对已持久化的经营同步执行记录；未改变额度窗口现态信号规则，也不能补造源系统从未保存的失败事实。本批此前已有0069迁移，本轮没有新增迁移；部署须使用匹配本批代码的既有发布流程，不能把生产已部署的0068误当最新候选已部署。

# 仟流智算 2.1｜Mac Mini 部署后复测记录

| 项目 | 内容 |
| --- | --- |
| 版本 | `v2.1` |
| Commit | `c2ff5fa447326c139c14074d14c0a64ab879466c` |
| 部署时间 | 2026-08-19 12:16（Asia/Shanghai） |
| 目标环境 | Mac Mini／`https://ic.qianliuai.com` |
| 数据库迁移头 | `0051_pool20_operating_sync_and_closing_confirmation` |
| 部署前备份 | `/Users/stephen/backups/qianliu-zhisuan/pre-v2.1-20260819-120200.dump` |
| 部署结果 | 服务健康、Web／Control API／Gateway 公网入口均 HTTP 200 |
| 测试方式 | 已登录生产页面只读走查、请求下钻、筛选与页面状态核对；不人为制造熔断或额度耗尽 |

## 1. 复测结论

| 问题 | 结论 | 生产证据 |
| --- | --- | --- |
| POOL20-025 经营余额与费用同步 | 通过 | `厂商资源` 显示 DeepSeek `FRESH`，余额 ¥32.93，数据时间 2026-08-19 12:10:33；费用明确标记 `NOT_SUPPORTED`，本地账本费用仍单独展示。Kimi／智谱无余额接口时显示 `STALE/NOT_SUPPORTED`，没有伪造余额。 |
| POOL20-026 结账事实确认 | 通过 | `经营账单 → 结账管理` 先列出 3 个资源的采购／套餐费用／API 费用／余额／服务周期／来源快照／请求范围；待确认事实和具体字段缺口明确显示，未确认时结账按钮禁用。 |
| POOL20-027 停用策略恢复 | 通过 | `额度规则 → 调度策略` 的 RETIRED 行显示“重新启用 / 复制为新版本”，没有原地恢复旧版本。 |
| POOL20-028 配置归档 | 通过 | `查看已归档配置` 可切换到归档模型，显示“已归档”和“取消归档”；历史列表默认隐藏归档项。 |
| POOL20-029 策略时间线与优先级解释 | 通过 | 调度策略显示创建时间、操作人、状态；页面明确“调度先于计价”，并说明 REJECT 不进入上游 Attempt／Usage／计量。 |
| POOL20-030 高峰拒绝提示 | 未制造真实事件 | 当前没有新的高峰 REJECT 事件；生产环境未人为制造拒绝。规则页面已存在硬熔断动作和恢复方式配置。需后续在约定窗口进行受控业务验证。 |
| POOL20-031 调度／计价独立展示 | 通过 | 请求详情展开后同时显示独立的“计价决策”和“调度决策”，含规则版本、模型／资源、倍率／单价、最终费用、动作和理由码。 |
| POOL20-032 调度输入技术详情折叠 | 通过 | 请求详情默认只显示调度摘要；原始 `dispatch_input` 位于“技术详情（调度输入快照）”折叠区，没有长 JSON 冲出页面。 |
| POOL20-033 Coding Plan 额度耗尽提示 | 未制造真实事件 | 当前 Kimi／智谱均有剩余额度，没有额度耗尽事件；生产环境未人为耗尽额度。需后续使用受控测试资源验证厂商额度耗尽提示和不自动切换。 |
| POOL20-034 首页 8 卡 | 通过 | 首页显示真实 Token、本月总支出、套餐支出、API 支出、活跃人数、厂商接入账号、本月充值、本月调度节省共 8 张卡；调度节省为 0 时显示原因。 |
| POOL20-035 调度节省三层口径 | 通过 | 首页显示已实现调度节省、潜在峰值节省及避免高峰扣减；当前无实际切换时已实现为 0，并显示“本月无可计算的实际切换”，潜在峰值缺关联时不伪造金额。 |

## 2. 现场补充观察

- Worker 已正常运行，部署后持续产生 Coding Plan 窗口同步、供给预测和经营同步日志。
- 智谱 5 小时窗口没有显示厂商重置时间，周额度显示厂商提供的重置时间；符合当前已知厂商事实口径。
- 当前生产请求明细已有新的 `v2.1.* · User-Agent 观测` 请求进入，说明新 Gateway 已承接真实请求。
- 现场复测未执行“复制策略”“归档写入”“结账确认”“策略发布”等生产写操作，避免污染真实账本和配置；这些动作已由本地自动化测试覆盖。

## 3. 待后续受控验证

1. 在可控工作日高峰窗口验证 POOL20-030：返回“高峰时段暂停使用”，且 Attempt／Usage／扣费均为 0。
2. 使用隔离测试额度验证 POOL20-033：额度耗尽提示厂商和下一重置时间，不自动切换厂商或 API。

当前结论：**v2.1 部署成功；11 项中 9 项生产页面复测通过，2 项因未制造高风险真实事件暂未测试，不判为失败。**

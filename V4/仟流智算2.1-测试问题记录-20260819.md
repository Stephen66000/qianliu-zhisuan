# 0819 仟流智算 2.1 测试问题记录

| 项目 | 内容 |
| --- | --- |
| 测试版本 | `v2.1` |
| 测试环境 | Mac Mini 已部署环境 `https://ic.qianliuai.com` |
| 测试日期 | 2026-08-19 |
| 测试来源 | Owner 生产页面人工走查＋Codex 页面复核 |
| 原始条目 | 13 条（原编号缺少第 5 条） |
| 独立根因 | 第一轮 9 项 `POOL20-036～044`；第二轮重开 5 项并新增 3 项 |

## 结论摘要

1. 归档需要确认缓冲，建议统一模型、路由和计价规则使用相同确认合同。
2. 调度策略需要拆分两个按钮，但不建议把 RETIRED 的 V2 原地改回 PUBLISHED；恢复使用应产生新的已发布版本，复制则产生 DRAFT。
3. 首页调度节省为 0 时说明过长，应压缩为一行。
4. DeepSeek 首页余额已经正常；Kimi／智谱套餐费用仍错误映射为“未录入／0”。
5. 使用主体需要在用、停用、归档三个独立范围。
6. Coding Plan 的业务利用率应按订阅周期累计，不应直接使用当前 5 小时／周窗口百分比；数据时间和订阅周期应分开。
7. 用量搜索当前确实只过滤候选，不会应用选择；应合并成可搜索下拉，或 Enter 自动应用唯一结果。
8. 经营账单的 10、11、13 是同一根因：缺少月度余额桥接。统一公式为 `期初余额 + 本月充值 - 期末余额`，再与套餐费用求本月总花费。
9. 左上角 Logo 应跳转首页看板。

## 现场数据核对

- 首页概览：8 卡已上线；调度节省 0 元时存在三行额外解释。
- 首页资源摘要：DeepSeek 显示余额 `32.93`、本期费用 `47.41`；Kimi／智谱余额／费用显示未录入，本月费用为 0。
- 资源登记：Kimi `273,971,222 / 300,000,000`，智谱 `111,533,326 / 300,000,000`；经营账单套餐利用分析仍显示缺少已用额度。
- 用量搜索：输入王涛后候选和数量变为 1，但主体仍为“全部员工”，Enter 无法应用。
- 经营账单：API 消耗、总投入、DeepSeek 月度成本均为 `—`；套餐费用为 `621.10`；综合利用率为 `0.00%`。
- 归档列表：勾选后可以看到 Kimi／DeepSeek 已归档项及“取消归档”，因此归档后不是只能后台查询。

## 第二轮验收与整改记录

第一轮修复候选部署后再次验收，结论为：`POOL20-036、037、040、044` 已通过，只进入固定回归；`POOL20-038、039、041、042、043` 因边界 Finding 重新打开；新增 `POOL20-045、046、047`。下一可用编号为 `POOL20-048`。

| 编号 | 第二轮状态 | 冻结 Finding／验收重点 |
| --- | --- | --- |
| `POOL20-036` | PASS／固定回归 | 三类归档确认合同保持一致，取消零写入。 |
| `POOL20-037` | PASS／固定回归 | 恢复原配置与复制为新版本继续分离，历史版本不可改写。 |
| `POOL20-038` | FIXED-LOCAL／待独立复审 | 已有实际切换但节省恰为 0 时不得误报“无可计算的实际切换”。 |
| `POOL20-039` | FIXED-LOCAL／待独立复审 | 同厂商多资源跨币种不得直接求和并按“元”展示。 |
| `POOL20-040` | PASS／固定回归 | 在用／停用／归档三类查询保持独立。 |
| `POOL20-041` | FIXED-LOCAL／待独立复审 | 缺订阅开始或结束日期时不得声称“订阅周期累计”。 |
| `POOL20-042` | FIXED-LOCAL／待独立复审 | Enter 精确匹配必须丢弃过期异步结果，不覆盖后续主体类型或查询。 |
| `POOL20-043` | FIXED-LOCAL／待独立复审 | 余额桥接统一命名 API 花费，账本计价标为核对证据；首页显示具体同源缺口。 |
| `POOL20-044` | PASS／固定回归 | Logo 与产品名保持同一 `/dashboard` 可访问链接。 |
| `POOL20-045` | FIXED-LOCAL／待独立复审 | 真实 Token 只计已结算输入＋输出；缓存／推理不重复，兼容 Kimi `cached_tokens`，补质量和跨月边界。 |
| `POOL20-046` | FIXED-LOCAL／待独立复审 | 经营账单补录期初余额、承接上月期末，历史补录不覆盖当前余额；首页不设入口。 |
| `POOL20-047` | FIXED-LOCAL／待独立复审 | 月度总览独立展示六项金额；已知项不因缺一项全部变横线，补录后自动重算。 |

## 第二轮实现边界

- 本轮任务名为“仟流智算｜2.1测试修复（第二轮）”，不是 2.2 功能开发。
- 以已部署第一轮候选 Commit `7305abb9d62f9e3d5702e08f3a285d7ceffc88ad`／Tree `0fd9a508e778db7f8a4ab185f1f5f9ae7e64a218` 为唯一基线，在 `codex/v2.1-test-fixes-r2` 隔离分支整改。
- 不移动或覆盖 `v2.1` Tag，不覆盖历史分支；未经 Owner 再次授权不 push、不部署、不建 PR。
- 只做上述失败 Finding 和新增三项的最小修复，不重做已通过项，不做无关重构或扩展治理。
- 优先复用现有快照、充值和经营账单合同；确需迁移时先证明现有结构无法安全承载，并从 `0053` 起使用全仓唯一编号。

## 第二轮本地候选结果（2026-08-20）

- 唯一基线保持 Commit `7305abb9d62f9e3d5702e08f3a285d7ceffc88ad`／Tree
  `0fd9a508e778db7f8a4ab185f1f5f9ae7e64a218`；分支为 `codex/v2.1-test-fixes-r2`，
  `v2.1` Tag 未移动。
- `POOL20-038` 使用实际切换数判定空态；已有切换但节省为 0 时继续展示同源事实。
- `POOL20-039` 首页厂商分组金额按资源币种 fail-closed，并返回具体资源／币种缺口；已知项不被整体抹除。
- `POOL20-041` 只有订阅起止日期完整且额度分母有效时才标记“订阅周期累计”；5 小时／周窗口保持独立。
- `POOL20-042` 精确匹配用请求代际与主体类型／搜索词共同校验，过期异步响应不再回写选择。
- `POOL20-043` 统一“API 花费／账本 API 计价（核对证据）”标签、具体缺口原因和六项独立经营事实。
- `POOL20-045` 兼容 Kimi 顶层 `cached_tokens`，使用稳定单选优先级；真实 Token 固定输入＋输出，
  暴露已结算／估算／未知质量及上海自然月半开边界。
- `POOL20-046` 新增加性迁移 `0053_operating_bill_opening_balance`。现有经营快照表示当前事实，
  历史期初补录若写入该表会污染当前余额，因此 0053 使用账期级追加事实、复合租户外键、不可变触发器，
  已有事实时禁止破坏性 down。自动承接上月已关闭期末优先；仅缺失时允许补录，重复提交幂等，
  跨币种、未来月份、已关闭账期和跨企业账期均拒绝。
- `POOL20-047` 月度总览分别展示期初余额、本月充值、期末余额、API 花费、套餐费用、本月总花费；
  缺期初时仍显示已知充值、期末和套餐费用，补录后统一重算。首页没有补录入口。
- `POOL20-036`、`037`、`040`、`044` 固定回归保持通过，没有重复开发。

### 本地验证

- Node `v22.17.1`、pnpm `11.11.0`；全 workspace typecheck、lint、build 通过，Web 只保留既有 chunk warning。
- 固定有界全量回归分段通过：167 个测试文件／1171 个测试；Web E2E Chromium 29/29。
- 受影响定向：Web 27、Provider Adapter 54、月度经营单测 17、Control API 实库 30、Database／迁移实库 22，均通过。
- Coverage 13 个 ratchet scope 无回退；POOL043 Web 为 statements `98.35%`、branches `85.44%`、
  functions `90.34%`、lines `98.35%`。
- POOL043-v22 专项 mutation：244/244 killed，survivor/no-coverage 均为 0，score `100%`。
- 架构无 runtime cycle；源码体量默认 400 行门禁通过；重复率 `0.68% < 5%`；生产依赖漏洞、许可证门禁通过。
- Sensitive canary 日志命中 0；PostgreSQL／Redis／Trace 仍不在该命令扫描范围。

### 交付边界

这是本地修复候选，不是生产验收。未调用真实 Provider，未读取生产／客户数据，未 push、未部署、未创建 PR。
候选仍待源任务在同一最终 Commit／Tree 上执行双路独立复审与 V1.4 代码审核；复审 Finding 未关闭前不得改为最终通过。

## Owner 最终确认

1. 归档确认正文冻结为：“归档后，该模型将从默认列表和新配置入口中隐藏。可通过‘查看已归档配置’恢复。是否继续？”
2. 调度策略拆成“恢复原配置／复制为新版本”；恢复原配置生成递增的新发布版本，历史 V2 保持 RETIRED。
3. 同意按本记录中的 API 花费、套餐周期利用率、页面标签和其余交互方向进入 2.1 测试修复；第二轮范围与边界以上述新增记录为准。

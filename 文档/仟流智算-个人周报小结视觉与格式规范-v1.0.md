# 仟流智算 · 个人周报小结视觉与格式规范

**—— 企业微信个人用量「一周小结」推送标准**

> **版本**：v1.0（正式定稿版）  
> **状态**：正式发布 / 研发与设计基线  
> **发布日期**：2026-09-12  
> **上位规范**：《仟流视觉法则 1.2》、《仟流 Web 产品视觉规范 1.0》  
> **适用场景**：每周一早 09:00，通过企业微信自建应用（应用名：仟流智算）向已激活员工定向推送个人专属“一周小结”长图。

---

## 🎯 01 / 产品定位与设计哲学

### 1.1 从“监控看板”到“陪伴手账”
在传统的企业级 IT 系统中，用量报表通常以冷峻、严肃、甚至带有压迫感的技术看板（如 Grafana 仪表盘）呈现。
仟流智算个人周报彻底打破这一范式：
- **人文温度**：员工打开企微收到的不是“审查清单”，而是一张犹如高级文具手账、私人信笺般的**白色浮动卡片**；
- **精神锚点**：卡片以专属**金句**开篇，提振员工每周开启协同工作的心智；
- **克制理性**：摒弃巨幅大数字轰炸，回归 14px 沉稳墨黑排版，让核心指标（请求次数、Token 消耗、日均水平、协同习惯）清晰、安静地呈现；
- **品牌归属**：左下角企业标识与右下角原生矢量 Logo 稳固锚定，建立企业级信任感。

---

## 📐 02 / 画布与卡片尺寸规范

| 维度 | 参数 | 像素值 | 说明 |
| :--- | :--- | :--- | :--- |
| **总画布（Canvas）** | 宽 × 高 | `540 × 800 px` | 手机端与企微聊天窗口自适应最佳比例 |
| **画布背景色** | 底色 | `#EEF2F6` | 极淡冷灰，模拟移动端原生背景，为卡片留出四周自然呼吸边距 |
| **白底卡片（Card）** | 宽 × 高 | `460 × 700 px` | 居中浮动于画布内，上下四周均匀留白（左右边距 40px，上下边距 50px） |
| **卡片圆角** | 拐角弧度 | `18 px` | 苹果级大圆角，圆润亲和 |
| **卡片边框** | 描边 | `1 px solid #E2E8F0` | 超细浅灰边界，界定卡片质感 |
| **弥散微投影** | Drop Shadow | 复合双层阴影 | `dy=12, stdDeviation=18, rgba(15, 27, 46, 0.06)` + `dy=2, stdDeviation=4, rgba(15, 27, 46, 0.03)` |

---

## 🏛 03 / 栅格系统与绝对对齐基准（核心数学法则）

为彻底消除版面视觉上的“左窄右宽”、“上窄下空”问题，本规范锁定两套空间几何基准：

```text
X = 94px (主左基线)              X = 118px (内容缩进线)                  X = 445px (右锚定线)
    │                                │                                      │
    ├─ 张三                          ├─ 一周小结 9.7-9.11                    │
    ├─ “ (外挂引号)                  ├─ 功不求疾，但求有恒 ”                │
    ├─ ──────────────────────────────┼──────────────────────────────────────┤ (浅线)
    │                                ├─ 总请求次数                          │
    │                                ├─ 75 次                               │
    ├─ ──────────────────────────────┼──────────────────────────────────────┤
    │                                ├─ 周消耗 Token 总量                   │
    │                                ├─ 32.9 万                             │
    ├─ ──────────────────────────────┼──────────────────────────────────────┤
    │                                ├─ 日均使用量                          │
    │                                ├─ 4.7 万 / 天                         │
    ├─ ──────────────────────────────┼──────────────────────────────────────┤
    │                                ├─ 最晚请求时间                        │
    │                                ├─ 周四深夜 23:15                      │
    ├─ ──────────────────────────────┼──────────────────────────────────────┤ (预留空行)
    │                                │                                      │
    ├─ 仟流智算                      │                        ┌────────────┐│
    ├─ Qianliu IC                    │                        │ 仟流 Logo  ││
    │                                │                        └────────────┘│
```

### 3.1 X 轴双垂直对齐法则
1. **主基准轴（`X = 94 px`）**：
   - 贯通卡片左侧的核心骨架：**顶部标题「姓名」、金句前双引号「`“`」、横线起点、底栏「仟流智算」、底栏「Qianliu IC」**全部在 `X = 94` 绝对左对齐；
2. **标点外挂缩进轴（`X = 118 px`）**：
   - 采用经典字体排版（Hanging Punctuation）规则：金句首字**「功」**位于 `X = 118`，使开头的双引号自然悬挂在行外空白处；
   - **中间所有数据指标**（标签如「总请求次数」、数值如「75 次」）**统一在 `X = 118` 严格左对齐**，与金句正文形成优美的阶梯式垂直律动；
3. **右侧锚定轴（`X = 445 px`）**：
   - 所有横线的最右端为 `X = 445`（横线全宽为 `351 px`）；
   - 右下角官方 Logo 的最右边界与 `X = 445` 严格对齐，形成右侧直角支撑。

### 3.2 Y 轴绝对等距居中法则（彻底消除失衡）
- **内容整体包围盒（Content Bounding Box）**：
  - 从「张三」文字顶部（`Y = 121`）到底部「Qianliu IC」/ Logo 底端（`Y = 679`），高度精确锁定为 **`558 px`**；
- **上下绝对等距**：
  - 卡片高度为 `700 px`；
  - **顶部空白留白 = `71 px`**；
  - **底部空白留白 = `71 px`**；
  - `顶部留白 === 底部留白`，实现像素级 1:1 绝对垂直居中，彻底消除任何“上面窄、下面宽”的局促感。
- **金句开阔呼吸距**：
  - 标题「张三 一周小结」底线至金句的垂直距离设置为 **`94 px`**（较常规间距翻倍），使金句具有仪式感与尊崇感；
- **指标行距翻倍**：
  - 指标标签基线到下方数值基线的垂直距离设置为 **`24 px`**（数据在标签下方清晰舒展，杜绝贴在一起）。

### 3.3 预留手账线与行距自适应机制
- **预留行数**：在最后一项指标下方，**仅预留 1 行手账线**（彻底移除多余的空行），保留记事本信笺韵味的同时保持卡片精炼紧凑；
- **自适应行距算法**：
  - 当展示 **4 项常规指标** 时：单项槽位高度（Slot Height）为 **`68 px`**；
  - 当展示 **5 项扩展指标**（如加入主力模型分布）时：槽位高度自动动态收缩为 **`56 px`**；
  - 无论指标数量为 4 项或 5 项，外层白卡尺寸、上下外边距（71px:71px）均保持绝对静止稳定！

---

## 🎨 04 / 色彩令牌系统（Color Tokens）

严格依循《仟流视觉法则 1.2》与《Web 视觉规范 1.0》要求锁定 Hex 色值：

| 语义角色 | 颜色名称 | Hex 色值 | 适用元素与设计依据 |
| :--- | :--- | :--- | :--- |
| **金句精神色** | 琥珀暖金 (Amber) | `#D97706` | 用于金句正文与双引号。褪去红橙的火气，呈现温润、典雅、高贵的真金质感。 |
| **品牌主张色** | 仟流蓝 (Brand Blue) | `#417EE0` | 用于顶栏「一周小结 9.7-9.11」，以及官方 Logo 底部右角括弧。 |
| **正文主字色** | 深墨黑 (Primary Text)| `#172033` | 用于姓名「张三」、全部统计数据（「75 次」等数值）、底栏「仟流智算」。 |
| **辅助说明色** | 次级灰 (Secondary) | `#7D8FA4` | 用于所有指标标签（「总请求次数」等）、底栏英文「Qianliu IC」。 |
| **信笺记事线** | 浅淡水印灰 (Ruled Line) | `#F2F5F8` | 1.1px 线宽，减淡 50% 的极淡灰色，若隐若现，仔细看清晰可辨。 |
| **表面表面色** | 纯白表面 (Surface) | `#FFFFFF` | 卡片主体背景。 |
| **外围衬底色** | 冷灰画布 (Canvas) | `#EEF2F6` | 页面外层大背景。 |

---

## 🔤 05 / 字体层级阶梯（Typography Scale）

采用系统默认优雅中文字体族降级链：`-apple-system, BlinkMacSystemFont, "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", sans-serif`。

| 层级 | 元素 | 字号 | 字重 | 颜色 | 尺寸比例关系 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **H1** | 员工姓名（如「张三」） | `26 px` | Bold (700) | `#172033` | 镇页大字 |
| **Sub 1** | 一周小结与周期（如「一周小结 9.7-9.11」） | `13 px` | Semi-bold (600) | `#417EE0` | **严格为姓名 26px 的 1/2（2:1 比例）** |
| **Quote** | 励志金句正文 | `26 px` | Bold (700) | `#D97706` | **与姓名 26px 完全等大**，首尾引号采用 Georgia 字体 |
| **Label** | 指标标签（如「总请求次数」） | `13 px` | Medium (500) | `#7D8FA4` | **与「一周小结」13px 完全等大** |
| **Value** | 统计具体数值（如「75 次」） | `14 px` | Bold (700) | `#172033` | **收敛 50%**，数字与汉字单位（13px）精致组合 |
| **Brand** | 底栏中文「仟流智算」 | `16 px` | Bold (700) | `#172033` | 品牌背书主字 |
| **Brand Sub** | 底栏英文「Qianliu IC」 | `12 px` | Medium (500) | `#7D8FA4` | 字母间距 0.8px，紧凑专业 |

---

## 🏷 06 / 核心指标与行业专业术语定义

### 6.1 Token 计量单位统一规范（“万”与“亿”自适应进位规则）
在《个人周报小结》与《全员用量周报》中，所有关于大模型 Token 消耗量的数值一律**杜绝使用无单位或超长千分位纯数字**（如 `328,500 Tokens`），统一推行**“万”与“亿”智能进位法则**：
1. **统一单位后缀**：
   - 消耗总量后缀统一为 **`万`** 或 **`亿`**；
   - 日均用量后缀统一为 **`万 /天`**（或 `亿 /天`）；
2. **精度与四舍五入规范**：
   - 数值除以相应基数后，**严格保留 1 位小数，最后一位四舍五入**（如 `328,500` $\rightarrow$ `32.9 万`；`46,928` $\rightarrow$ `4.7 万 /天`；`1,280,000` $\rightarrow$ `128.0 万`）；
3. **阈值分界阶梯**：
   - **用量 $< 100,000,000$（1 亿）**：以 **`万`**（÷ 10,000）作为基准计量单位；
   - **用量 $\ge 100,000,000$（1 亿）**：自动升格进位为 **`亿`**（÷ 100,000,000），防止图片版面因数字超长而挤压变形。

### 6.2 核心指标定义速查表

| 指标字段 | 官方推荐文案 | 计量单位 | 示例数值 | 业务口径定义 |
| :--- | :--- | :--- | :--- | :--- |
| `totalRequests` | **总请求次数** | `次` | `75 次` | 本统计周（周一 00:00 至周日 23:59）内，该员工通过仟流网关发起的有效模型交互/API请求总和。 |
| `totalTokens` | **周消耗 Token 总量** | `万` 或 `亿` | `32.9 万` | 本统计周内输入 Token 与输出 Token 的合计总消耗，按万/亿单位换算并保留 1 位小数。 |
| `dailyAvgTokens`| **日均使用量** | `万 /天` 或 `亿 /天` | `4.7 万 /天` | `总消耗 Token ÷ 7 天`，反映日常与 AI 协同深度，按万/亿换算保留 1 位小数。 |
| `latestTime` | **最晚请求时间** | 文本 | `周四深夜 23:15` | 本统计周内该员工发起请求的最晚物理时间点，体现“最深陪伴”时刻。 |
| `topModel` *(可选)*| **核心主力模型** | 文本 | `DeepSeek V3` | （5 指标版本支持）过去 7 天内该员工消耗 Token 最多的具体主力模型版本代号。 |

---

## 🖼 07 / Logo 资产与对齐规范

1. **Logo 来源**：提取自工程原生矢量资产 `apps/web/src/assets/qianliu-logo-primary.png`；
2. **透明化与去噪**：剔除了原生图片周围的偏白底色杂质，并经 Bounding Box 紧凑裁切至可见几何边缘（尺寸 `692 × 716`）；
3. **渲染尺寸**：卡片上等比渲染为 **`70 × 72 px`**（相当于原始小图标放大一倍）；
4. **定位锁死**：
   - **底边缘**：Logo 最下边缘与左侧「Qianliu IC」文字的底部基线严格平齐在 **`Y = 679 px`**；
   - **右边缘**：Logo 最右边缘与上方信笺划线最右端严格平齐在 **`X = 445 px`**。

---

## 💻 08 / 数据接口与代码渲染架构

### 8.1 数据结构定义（TypeScript Payload）

```typescript
export interface PersonalWeeklyReportData {
  userId: string;
  userName: string;              // 姓名，如 "张三"
  dateRange: string;             // 周期文案，如 "一周小结 9.7-9.11"
  quote: string;                 // 金句，如 "功不求疾，但求有恒"
  quoteColor?: string;           // 默认 "#D97706"
  metrics: Array<{
    label: string;               // 标签，如 "总请求次数"
    value: string;               // 数值，如 "75" 或 "32.9"
    unit?: string;               // 单位，如 "次"、"万"、"万 /天"
  }>;
}

/**
 * 格式化 Token 用量指标为“万”或“亿”
 * 保留 1 位小数，四舍五入
 */
export function formatTokenVolume(tokens: number, isDailyAvg: boolean = false): { value: string; unit: string } {
  if (tokens >= 100_000_000) {
    const val = (tokens / 100_000_000).toFixed(1);
    return { value: val, unit: isDailyAvg ? '亿 /天' : '亿' };
  }
  const val = (tokens / 10_000).toFixed(1);
  return { value: val, unit: isDailyAvg ? '万 /天' : '万' };
}
```

### 8.2 Worker 渲染与发送流程

```mermaid
flowchart TD
    A[Worker Cron: 每周一 09:00] --> B[从数据库聚合员工 7 天用量数据]
    B --> C[装配 PersonalWeeklyReportData]
    C --> D[buildFinalAlignedReportSvg 构造纯矢量 SVG]
    D --> E[@resvg/resvg-js 2倍高清转 PNG 缓冲区]
    E --> F[调用企微 API 上传临时素材: upload_media]
    F --> G[获取 media_id]
    G --> H[向员工发送应用消息: mpnews/image 消息]
```

---

## 📦 09 / 成果文件索引

| 文件相对路径 | 作用说明 |
| :--- | :--- |
| `personal_weekly_final_aligned.svg` | 定稿矢量 SVG 源代码，具备纯几何图形与动态坐标计算 |
| `personal_weekly_final_aligned.png` | 2倍超高清渲染图（1080 × 1600 px），供移动端真机查阅 |
| `preview.html` | 交互式原型多端预览器，已将此版设为第一页首选展示 |
| `scratch/generate_final_aligned.cjs` | 生产级渲染脚本原型，可直接移植入 `apps/worker` 服务 |

---

> **结语**：本规范自 2026 年 9 月 12 日起作为仟流智算企微自建应用个人周报的标准格式交付技术团队实施。后续如需调整文案或指标，均须在遵循本规范“双基线对齐”与“Y 轴等距居中”数学法则的前提下演进。

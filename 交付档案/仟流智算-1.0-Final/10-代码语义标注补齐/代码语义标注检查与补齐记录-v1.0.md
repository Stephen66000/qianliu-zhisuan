# 仟流智算 1.0 代码语义标注检查与补齐记录

## 结论

`PASS`。关键代码已经能够说明“这段做什么、为什么这样做、不能破坏什么”。本次只补高风险且缺少上下文的代码，不要求给显而易见的 CRUD、类型声明和页面布局逐行加注释。

## 检查口径

- 模块：职责、输入输出、副作用和明确不处理的范围；
- 核心函数：状态变化、幂等、失败、重试、回退和外部依赖；
- 复杂逻辑：说明 why、不变量、安全／并发／账本边界；
- 禁止：逐行复述、过期说明、用注释掩盖坏结构、正文或 Secret 入注释。

## 本次补齐

| 模块 | 补齐内容 |
| --- | --- |
| Chat／Messages／Responses 协议 | SSE 适配职责、正文不落库、`committed` 后禁止切换、Responses 为转换子集 |
| 最终模型授权 | Key、主体、模型、Route、Provider、资源、Grant、计费规则必须同时成立 |
| 计费与价格证据 | Attempt 开始时冻结规则；成本未知不能误算为零 |
| 账本结算与兼容写入口 | request 行锁、终态发布屏障、跨租户 fail-closed、迟到写入拒绝 |
| Provider 超时策略 | 资源覆盖与统一默认值的回落边界 |
| 运行保障／供给预测 Worker | 无长期内存状态、有界重试、算法权威在 Repository |

## 已具备充分标注、无需重复修改的关键模块

- `apps/gateway/src/pipeline/real-pipeline.ts`：端到端步骤、硬过滤、Attempt、Affinity、调度、额度、失败切换和结算已有完整业务注释；
- `packages/domain/src/resource-lifecycle.ts`：状态机、错误处置矩阵和恢复边界已写明；
- `packages/provider-adapters/src/openai-compatible-caller.ts`：协议转换、凭证回退和正文内存边界已写明；
- `apps/gateway/src/pipeline/attempt-usage-settlement.ts`：零消费、原子结算、重放和费用未知边界已写明；
- `packages/database/src/repositories/operating-bill-write-barrier.ts`：月锁、关账与跨月在途请求边界已写明。

## 复审证据

- TypeScript、ESLint、1006 项基础测试和 11 个工作区构建通过；
- 关键覆盖率 13 个 ratchet 范围无下降；
- 生产代码未发现 `@ts-ignore`、`@ts-expect-error`、`as any`、`Record<string, any>` 或无归属 `TODO/FIXME`。


# 工程核对工单：经营账单「价值体现」汇总口径三处修正

> 来源：客户购买理由研究报告（2026-09-18）附录 A2 复算发现。
> 影响：该页是面向客户展示"省了多少钱"的核心页面，汇总行算术与明细行不自洽，对外展示前必须修复。
> 涉及文件：`apps/web/src/components/operating-bill/ValueRealizationSection.tsx`（前端计算，约 20–70 行）。
> 页面快照依据：S1 截图（2026-09-18 22:22:12）：Kimi ¥199 / 162.26M Token / 官方基准 ¥12/M；智谱 ¥422.10 / 444.49M Token / 官方基准 ¥15/M。

---

## 问题 1：汇总「加权平均 API 单价」用了算术平均，不是加权平均

**现状**（ValueRealizationSection.tsx 第 65 行）：

```ts
const weightedDiscount = (weightedUnitCost / ((kimiOfficialRate + zhipuOfficialRate) / 2)) * 10;
```

分母 `(12 + 15) / 2 = 13.50 元/M` 是两个官方单价的**算术平均**，未按两家实际 Token 用量加权。

**页面值 vs 复算值**：

| 项目 | 页面显示 | 按明细复算 |
|---|---|---|
| 加权平均 API 单价 | 13.50 元/M | 8,614.49 ÷ 606.75 ≈ **14.20 元/M** |

**期望口径**：汇总行官方基准单价 = `总等效 API 成本 ÷ 总 Token`，即：

```ts
const blendedOfficialRate = totalOfficialCost / totalPlanTokensM; // ≈14.20
const weightedDiscount = (weightedUnitCost / blendedOfficialRate) * 10;
```

等价写法：`weightedDiscount = totalPlanPrice / totalOfficialCost * 10`。

## 问题 2：汇总「等效折扣」与明细行金额不自洽

**现状**：页面汇总显示 **0.8 折（−92.4%）**，由问题 1 的错误分母直接派生。

**按页面自身金额复算**：

```text
费用比 = 621.10 ÷ 8,614.49 ≈ 7.21%
折数   = 10 × 621.10 ÷ 8,614.49 ≈ 0.72 折
降幅   = 1 − 621.10 ÷ 8,614.49 ≈ 92.79%
```

**期望**：汇总折扣必须由汇总金额推导（0.72 折 / −92.8%），与明细行的 1,947.18、6,667.31、621.10 完全自洽；修掉问题 1 后此项自动归正。

## 问题 3（一并处理）：单价与兜底 Token 数为硬编码

第 26/27/37/38 行硬编码了套餐月费（199.0 / 422.1）与"官方 API 参考单价"（12.0 / 15.0），第 24/35 行在取不到数据时兜底为 `"50000000"` / `"92500000"`（50M / 92.5M 假数据）。

风险：厂商调价、套餐变更或数据缺失时，页面会输出**看起来精确实则虚构**的数字——这违反产品"空状态禁止伪造数据"的基本原则。

**期望**：
- 套餐月费从经营账单的当月套餐付款净额取数；
- 官方基准单价从额度规则模块的已发布价格版本取数，或标注为"参考基准（手工维护，更新于 × 日期）"；
- 数据缺失时该行显示"数据不足"而不是兜底假值。

---

## 验收标准

1. 汇总行每个数字都能从明细行复算得出（差异 < 0.01）；
2. 页面不再有硬编码的套餐价格、官方单价和兜底 Token 数；
3. 新增一条前端单测：给定 Kimi 162.26M/¥199/¥12 与智谱 444.49M/¥422.10/¥15，断言汇总折数 = 0.7 折（保留 1 位小数）、汇总降幅 = −92.8%；
4. 修复后重新截图归档到 `V4/Evidence/`，替换研究报告附录 A2 的"待核对"状态。

## 修复前的对外纪律

- 对外材料**只引用可复算的金额差额**（少购 1,863.30 元、API 等效差额 7,993.39 元），**不引用汇总折扣率与加权单价**；
- 截图用于演示时避开汇总行，或待修复后再截。
